{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [
    "specled_-xkn.1"
  ],
  "created": "2026-07-26T00:26:53Z",
  "description": "Advances: specled.coverage_capture\n\nFound by the specled_-dn4.9 audit (2026-07-26T00:25:30Z). specled.coverage_capture.envelope_meta's statement covers the tolerant READ of a missing :meta, but never states the present-but-non-map rejection rule that store_test.exs now asserts after dn4.9 item 14. The test is ahead of the requirement.\n\nFix: state the rejection rule in the requirement so the assertion has an owning clause. Purely additive, so append-only-safe.",
  "id": "specled_-npo",
  "labels": [
    "phase:merging"
  ],
  "parent": "specled_-k6s",
  "comments": [
    {
      "text": "Resolved by branch specled_-td7/self-identifying-descriptors, commit 3bbd7e7b, while implementing specled_-n5s (session a5f50a6d-689f-4616-80ad-806b0595aa28).\n\nn5s's fix needed an owning clause for write_v2/2's exception contract in specled.coverage_capture, and its new test asserts the present-but-non-map :meta rejection — leaving that rule unstated would have widened this ticket rather than left it alone. specled.coverage_capture.envelope_meta now states: write_v2/2 tolerates a missing :meta on the same terms as read_v2/1, and rejects a present-but-non-map :meta as malformed. Covered by scenario specled.coverage_capture.scenario.envelope_meta_tolerant_read.\n\nNot closing it here — that call belongs to whoever grades the PR. Verify against the branch and close if satisfied.",
      "timestamp": "2026-07-26T20:58:34Z"
    },
    {
      "text": "CORRECTION to my previous comment on this ticket — the earlier 'Resolved by' claim was OVERSTATED. Do not close npo on the specled_-td7 branch.\n\nThe cold verifier for specled_-n5s caught it: npo's mandate names a READ-path rule. The assertion npo says is unowned is store_test.exs 'read_v2 rejects envelopes with non-map :meta as invalid artifacts' (dn4.9 item 14), whose outcome is {:error, :invalid_artifact}. The clause I added to specled.coverage_capture.envelope_meta has write_v2/2 as its grammatical subject throughout, so it gives the WRITE-path assertion an owning clause and leaves read_v2/1's rejection rule exactly as unstated as npo found it.\n\nnpo is at most half-discharged by that branch. Remaining work, per the verifier's suggested remedy: one clause stating that read_v2/1 classifies a present-but-non-map :meta as {:error, :invalid_artifact}.\n\nSession: a5f50a6d-689f-4616-80ad-806b0595aa28\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/a5f50a6d-689f-4616-80ad-806b0595aa28.jsonl",
      "timestamp": "2026-07-27T00:10:19Z"
    },
    {
      "text": "ADMISSION VERIFICATION (specled_-k6s, 2026-07-27)\nCold fresh-context verifier run against the epic base tree (feat/specled_-td7-self-identifying-descriptors @ e81f9f98).\n\nVERDICT: partially_done   EFFORT: trivial\n\n== MANDATE AS ADMITTED (implement THIS, not the original description) ==\nSingle spec-prose edit, no code and no test change.\n\n1. `.spec/specs/coverage_capture.spec.md`, requirement `specled.coverage_capture.envelope_meta` (statement block, lines 552-565): append one clause with `read_v2/1` as the grammatical subject, e.g. after line 565 — \"`read_v2/1` shall classify an envelope whose `:meta` key is present but not a map as `{:error, :invalid_artifact}`, never as a well-formed envelope with a defaulted `meta: %{}`.\" The clause must be about the READ path and must name the `{:error, :invalid_artifact}` outcome; do NOT reword or remove the existing `write_v2/2` clause at 563-565 (it belongs to specled_-n5s, already landed on this branch).\n\n2. Recommended (makes the assertion scenario-visible, not just requirement-visible): add one `then` line to `specled.coverage_capture.scenario.envelope_meta_tolerant_read` (lines 940-953), e.g. \"an envelope whose present `:meta` is not a map reads back as `{:error, :invalid_artifact}`\", and add a matching `given` line (\"a v2 envelope whose `:meta` key holds a non-map term\").\n\nNo change needed to `test/specled_ex/coverage/store_test.exs` — the test at :307-323 already exists and already carries `@tag spec: \"specled.coverage_capture.envelope_meta\"`. No change needed to `lib/specled_ex/coverage/store.ex` — `classify_v2/1` already implements the rule. No change needed to the `tagged_tests` verification block at lines 1076-1080 — `specled.coverage_capture.envelope_meta` is already listed there with `execute: true`.\n\n== EVIDENCE ==\nThe requirement statement in this tree (`.spec/specs/coverage_capture.spec.md:551-565`) reads, in its final clause: \"`write_v2/2` shall tolerate a missing `:meta` on the same terms as `read_v2/1` — it is additive, so an envelope without it is well-formed — and shall reject a present-but-non-map `:meta` as malformed.\" (lines 563-565). Every verb in that clause takes `write_v2/2` as its grammatical subject; `read_v2/1` appears only as the object of a comparison (\"on the same terms as\"). The earlier `read_v2/1` sentence (line 554-555) states only the TOLERANT read: \"`read_v2/1` shall tolerate envelopes written without `:meta` (older artifacts default `meta: %{}`)\". No clause states the read-path REJECTION rule.\n\nThe unowned assertion npo names is still present and still unowned: `test/specled_ex/coverage/store_test.exs:307-323`:\n    @tag spec: \"specled.coverage_capture.envelope_meta\"\n    test \"read_v2 rejects envelopes with non-map :meta as invalid artifacts\", %{path: path} do\n      ... |\u003e Map.put(:meta, [:not, :a, :map])\n      assert {:error, :invalid_artifact} = Store.read_v2(path)\n    end\nIts outcome is `{:error, :invalid_artifact}` (read path), not an `ArgumentError` (write path).\n\nThe implementation the missing clause would own exists: `lib/specled_ex/coverage/store.ex:343-350`, `classify_v2/1`:\n      case Map.get(envelope, :meta, %{}) do\n        meta when is_map(meta) -\u003e {:ok, Map.put(envelope, :meta, meta)}\n        _ -\u003e {:error, :invalid_artifact}\n      end\n(the write-path counterpart is a separate raise at store.ex:334-336, `raise(ArgumentError, \"envelope :meta must be a map\")`, owned by the new `specled.coverage_capture.write_v2_argument_error_contract` at line 568).\n\nWhat td7 DID land is visible in `git diff main..HEAD -- .spec/specs/coverage_capture.spec.md` (commit 17b63ff6): +3 lines of write_v2 clause on envelope_meta, a whole new requirement `write_v2_argument_error_contract`, its scenario, and its addition to the `tagged_tests` verification block at line 1079. That confirms the 2026-07-27T00:10:19Z CORRECTION comment on the ticket (which retracts the 2026-07-26T20:58:34Z \"Resolved by\" claim) is accurate: npo is half-discharged, not discharged.\n\nScenario `specled.coverage_capture.scenario.envelope_meta_tolerant_read` (lines 940-953) likewise has no `then` clause for the read-path rejection; its four `then` lines cover missing-`:meta` read, `meta.boundary`, `meta.unmapped_modules`, and the write_v2 missing-`:meta` acceptance added by td7.\n\n== RISK NOTES ==\n- NO realization-hash impact. `.spec/realization_hashes.json` keys only code bindings (`api_boundary` entries such as `SpecLedEx.Coverage.Store.read_v2/1`); grep for \"coverage_capture\"/\"envelope_meta\" in that file returns zero hits. A statement-text edit touches no binding, so the realization-drift gate is unaffected. Corollary: if an implementer decides to also touch `lib/specled_ex/coverage/store.ex` (they should NOT — the behavior is already correct), `SpecLedEx.Coverage.Store.read_v2/1` IS a hashed api_boundary binding (spec file lines 134-135) and would require a baseline update.\n\n- REQUIREMENT-ID DRIFT HAZARD, ordering-relevant: `specled.coverage_capture.envelope_meta`'s statement was already amended on this branch by commit 17b63ff6 (specled_-n5s work). An implementer who diffs against `main` or pulls the requirement text from the npo ticket body will see the pre-td7 text and can silently clobber the `write_v2/2` clause at lines 563-565. Edit the tree's current text, append only.\n\n- No other ticket in the set {4f7, wby, z5t, dn4.12, 26g, xkn.1, xkn.2, 4pl, ako, ehv, ozm, pii, yds, chb, 14t, 4a6, asb, b23, vbo, zd8, zek} mentions `coverage_capture` or `envelope_meta` (scanned all 21 bodies+comments), so `.spec/specs/coverage_capture.spec.md` is uncontended.\n\n- Adjacent-file-only overlap, not a true conflict: specled_-26g and specled_-ehv both edit `test/specled_ex/coverage/store_test.exs` (26g: the `read_status/1` describe block's temp-dir setup around :330+; ehv: `System.tmp_dir!()` + `unique_integer` patterns repo-wide). npo requires zero edits to that file, so they cannot collide — but if a grader expands npo to add a redundant test there, it would.\n\n- Gate cost: the verification leg covering `envelope_meta` is `kind: tagged_tests, execute: true` (lines 1076-1080), shared with `boundary_noop_unarmed` and `write_v2_argument_error_contract`. Bare `mix spec.check` will re-run that leg; `--no-run-commands` suffices to prove the structural edit is well-formed.\n\n- No dependency on another ticket landing first; specled_-n5s (its prerequisite half) is already in this tree at 17b63ff6.\n\n- Do not close npo on the strength of the 2026-07-26T20:58:34Z comment — the 2026-07-27T00:10:19Z comment explicitly retracts it.\n",
      "timestamp": "2026-07-27T08:36:54Z"
    },
    {
      "text": "SCOPE CONTRACT (orchestrator, specled_-k6s — 2026-07-27)\n\nAuthoritative for mechanical scope checks. The ADMISSION VERIFICATION comment\nSUPERSEDES this ticket's description: npo is half-discharged by specled_-n5s\n(already in main), and the remaining work is one spec-prose edit plus one\noptional scenario line. Implement that, not the original description.\n\n## Allowed touches\n- `.spec/specs/coverage_capture.spec.md` — and nothing else.\n  - Item 1: APPEND one clause with `read_v2/1` as the grammatical subject to the\n    `specled.coverage_capture.envelope_meta` statement. Append only. Do NOT\n    reword or remove the existing `write_v2/2` clause — it belongs to\n    specled_-n5s and is already landed.\n  - Item 2 (recommended): add one `given` and one `then` line to\n    `specled.coverage_capture.scenario.envelope_meta_tolerant_read`.\n\n## Out of Scope (files)\n- `lib/specled_ex/coverage/store.ex` — `classify_v2/1` ALREADY implements the\n  rule correctly. Touching it would drag the api_boundary-hashed\n  `SpecLedEx.Coverage.Store.read_v2/1` binding into the diff. Do not touch it.\n- `test/specled_ex/coverage/store_test.exs` — the covering test already exists at\n  ~:307-323 and already carries `@tag spec: \"specled.coverage_capture.envelope_meta\"`.\n  Adding a redundant test here is a scope violation and would collide with\n  specled_-26g and specled_-ehv.\n- The `tagged_tests` verification block — `envelope_meta` is already listed with\n  `execute: true`. No change needed.\n- `CHANGELOG.md` (orchestrator consolidates once at the end), `VERSION`,\n  `.spec/realization_hashes.json`, `.spec/state.json`.\n- Every file not listed under Allowed touches. This ticket should be a ONE-FILE diff.\n\n## Out of Scope (intent)\n- No code change and no test change. If you believe one is needed, stop and say so\n  in the completion record rather than making it.\n- Edit the TREE's current requirement text, appending to it. Do not paste the\n  requirement text out of the ticket body or diff against `main` — both show the\n  pre-n5s wording and would silently clobber the landed `write_v2/2` clause.\n- Do not weaken spec prose or add a false `decisions:` link to quiet branch-guard.\n  Use a `Spec-Drift:` commit trailer if branch-guard fires.\n\n## Verification (change-scoped, run-until-green; host mix, no compose)\n1. `mix spec.check --no-run-commands` — sufficient to prove the structural edit\n   is well-formed.\n2. `mix compile --warnings-as-errors`\n3. `git diff --name-only` must list exactly one file.\n\nDo NOT run bare `mix spec.check` and do NOT run the full suite. The orchestrator\nowns whole-system proof at the merge gate and PR CI.",
      "timestamp": "2026-07-27T10:12:34Z"
    },
    {
      "text": "IMPLEMENTED\nSession: k6s-npo-impl-1\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-npo/grok-k6s-npo-impl-1/log\n\nCommit: 144350d233e7be7271f1d8fb474cbd4a16b1cb85\nFiles touched: .spec/specs/coverage_capture.spec.md\nTests added: none (prose-only; covering test already exists at store_test.exs ~307-323)\nVerification stubs flipped: none (envelope_meta already execute: true in tagged_tests)\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix spec.check --no-run-commands; git diff --name-only (exactly one file). Full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\nDeferred must requirements: none\n\nSummary: Appended a read_v2/1-subject clause to specled.coverage_capture.envelope_meta stating that a present-but-non-map :meta is classified as {:error, :invalid_artifact}, never defaulted to meta: %{}. Left the existing write_v2/2 clause intact (n5s). Added matching given/then lines to scenario envelope_meta_tolerant_read so the rule is scenario-visible. No code or test changes — classify_v2/1 and the tagged store_test already implement and assert this.",
      "timestamp": "2026-07-27T10:21:28Z"
    },
    {
      "text": "VERIFY: PASS\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nMechanical checks:\n- Files touched: 1 (.spec/specs/coverage_capture.spec.md) — commit 144350d2, base 2023373d (== origin/main)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0. lib/specled_ex/coverage/store.ex and test/specled_ex/coverage/store_test.exs are both untouched; .spec/realization_hashes.json, .spec/state.json, CHANGELOG.md, VERSION all untouched. Untracked .grok/sandbox.toml ignored per launcher-artifact instruction (it is why spec.check reports changed_files=2 against a 1-file tracked diff).\n- Diff shape: +9/-1 lines, all inside the envelope_meta statement block and the envelope_meta_tolerant_read scenario. The pre-existing write_v2/2 clause is byte-identical — the single removed line is only re-terminated to append the new sentence (\"...as malformed.\" -\u003e \"...as malformed. `read_v2/1`\"). The specified n5s clobber hazard did not occur.\n\nSemantic checks:\n- Spec stubs run: 1, passed: 1. No stubs were flipped by this ticket (envelope_meta was already `execute: true` in the tagged_tests block at coverage_capture.spec.md:1074-1081). I exercised that leg directly: `mix test test/specled_ex/coverage/store_test.exs --only spec:specled.coverage_capture.envelope_meta` -\u003e 4 tests, 0 failures.\n- Requirements coverage:\n  - specled.coverage_capture.envelope_meta (priority: must; the only requirement this diff touches): the appended read-path clause is covered by test/specled_ex/coverage/store_test.exs:308 \"read_v2 rejects envelopes with non-map :meta as invalid artifacts\", assertion at :323. Would fail if classify_v2/1 (lib/specled_ex/coverage/store.ex:341-353) lost its `meta when is_map(meta)` discrimination and fell back to `Map.get(envelope, :meta, %{})` unguarded — the envelope would then classify as `{:ok, %{meta: [:not, :a, :map]}}` and `assert {:error, :invalid_artifact} = Store.read_v2(path)` would raise MatchError. Also fails if the non-map branch were changed to return `{:error, :legacy_artifact, msg}` (3-tuple, no match) or to default `meta: %{}`.\n    Seams: none. Real Store.build_envelope/1, real :erlang.term_to_binary, real File I/O into a per-test System.tmp_dir!() directory (setup at store_test.exs:156-168, on_exit rm_rf). No Mock*/Memory*/fake-*/stub-*/injected-* seam anywhere in the path.\n  - Path proof, not just existence: read_v2/1 is `def read_v2(path) when is_binary(path)` at store.ex:229 and its `with` body calls `classify_v2(term)` at store.ex:239. classify_v2/1 is private and reachable only from read_v2/1 in this module, so the clause names the actual production path, not an adjacent one. `{:error, :invalid_artifact}` is in the read_v2/1 @spec at store.ex:227-228.\n- Intent-level scope concerns: none. Zero code change, zero test change, exactly the ADMISSION-named item 1 plus the recommended item 2.\n- Targeted verification: pass.\n  - `mix compile --warnings-as-errors` -\u003e clean\n  - `mix test test/specled_ex/coverage/store_test.exs` -\u003e 30 tests, 0 failures\n  - `mix test ... --only spec:specled.coverage_capture.envelope_meta` -\u003e 4 tests, 0 failures, 26 excluded\n  - `mix spec.check --no-run-commands` -\u003e `spec.check result=pass`, validate status=pass errors=0 warnings=0, branch findings=0, change_type=non_contract_or_meta\n  - Flipped stubs: none (none were required).\n- Corpus-scoped claims (step 7.5): 6 claims validated, 3 by execution, 0 falsehoods.\n  1. \"read_v2/1 shall classify an envelope whose :meta key is present but not a map as {:error, :invalid_artifact}\" — TRUE. store.ex:346-350: `case Map.get(envelope, :meta, %{}) do meta when is_map(meta) -\u003e {:ok, ...}; _ -\u003e {:error, :invalid_artifact} end`. Confirmed by execution.\n  2. \"never as a well-formed envelope with a defaulted `meta: %{}`\" — TRUE. The `{:ok, Map.put(envelope, :meta, meta)}` arm is guarded by is_map/1; a non-map value cannot reach it. No other clause of classify_v2/1 matches a map with `version: @v2_version`.\n  3. Identifier `read_v2/1` — TRUE, arity 1, store.ex:229.\n  4. Outcome atom `:invalid_artifact` — TRUE, present in both the implementation and the read_v2/1 @spec.\n  5. Scenario given \"a v2 envelope whose `:meta` key holds a non-map term\" — TRUE, matches store_test.exs:318 `|\u003e Map.put(:meta, [:not, :a, :map])`.\n  6. Scenario then \"an envelope whose present `:meta` is not a map reads back as {:error, :invalid_artifact}\" — TRUE, matches the assertion at store_test.exs:323. Confirmed by execution.\n  Pre-existing n5s clause survival re-checked as a distinct claim: intact (see Mechanical, diff shape).\n  No contradiction with the earlier tolerant-read sentence at :554-555 — missing `:meta` and present-but-non-map `:meta` are disjoint cases, handled on different branches of the same `case` at store.ex:346.\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope (step 11): none — no `AUDIT: BOUNCE` comment exists on this ticket.\n\nVerdict: PASS\n\nNotes (advisory, none change the verdict):\n- Pre-existing, not introduced here: two clauses of the envelope_meta statement have no test carrying `@tag spec: \"specled.coverage_capture.envelope_meta\"`. `meta.unhooked_modules` is separately owned by specled.coverage_capture.unhooked_degrade (:616) so it is covered under another id, but `meta.unattributed` has no other owning requirement and no envelope_meta-tagged assertion. That predates npo and is explicitly outside its Allowed touches; worth a future ticket, not a bounce here.\n- Pre-existing scenario looseness, now slightly extended: envelope_meta_tolerant_read has a single `when` (\"`Store.read_v2/1` reads that path\") but its given/then lists are a bag that also covers formatter flush and write_v2. The new given/then follow that existing style rather than tightening it. The ADMISSION comment recommended exactly this shape, so it is compliant.\n- The read-path clause is appended to the END of a statement that opens with build_envelope/1 and read_v2/1 and closes with write_v2/2, so the requirement now reads read -\u003e write -\u003e read. Purely editorial; append-only was the correct call given the clobber hazard.\n- envelope_meta shares a tagged_tests leg with boundary_noop_unarmed and write_v2_argument_error_contract; a bare `mix spec.check` at the merge gate will re-run all three. Not run here per the scope contract.",
      "timestamp": "2026-07-27T10:25:57Z"
    },
    {
      "text": "AUDIT: APPROVE\n\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nReasoning:\n(a) The appended clause is TRUE of production, traced independently. read_v2/1 (lib/specled_ex/coverage/store.ex:229) calls classify_v2(term) at :239; classify_v2/1 at :341-353 guards `meta when is_map(meta) -\u003e {:ok, ...}` with `_ -\u003e {:error, :invalid_artifact}`, and that return passes through read_v2/1 unwrapped. The negative half of the clause (\"never ... a defaulted meta: %{}\") also holds: the {:ok, Map.put(envelope, :meta, meta)} arm is is_map-guarded, so a non-map value cannot reach it. classify_v2/1 is private and called from exactly one site (:239), so the clause names the real production path.\nThe covering test at test/specled_ex/coverage/store_test.exs:307-323 is load-bearing, not orthogonal-pass: I checked the branch it actually reaches. build_envelope/1 (store.ex:137-149) always populates all eight @v2_required_fields, so `Enum.all?(@v2_required_fields, \u0026Map.has_key?/2)` at :342 succeeds and control reaches the :meta case rather than short-circuiting on the missing-required-field `else`. Delete the is_map guard and the test yields {:ok, %{meta: [:not, :a, :map]}} and the assert at :323 raises MatchError. The verifier’s \"Would fail if\" is falsifiable and correct. Real File I/O into a per-test tmp dir (setup :156-168), real term_to_binary, no mock/injected seam anywhere — the verifier’s \"Seams: none\" is accurate.\n\n(b) The n5s write_v2/2 clause survived byte-intact. The single -1 line in the diff is `and shall reject a present-but-non-map :meta as malformed.`, re-emitted verbatim with the new sentence appended after it on the same line. Tree text at .spec/specs/coverage_capture.spec.md:562-568 still carries write_v2/2 as grammatical subject of its own clause. The named clobber hazard did not occur.\n\n(c) Scoping was legitimate; approving on that basis. I enumerated all 41 `priority: must` requirements in .spec/specs/coverage_capture.spec.md myself. The diff is two hunks, both wholly inside (i) the envelope_meta statement block scalar and (ii) the given/then sequences of scenario.envelope_meta_tolerant_read, whose `covers:` names only specled.coverage_capture.envelope_meta. No requirement id, no `covers:` list, no verification block (envelope_meta’s tagged_tests leg at :1080-1085 is untouched), and no `realized_by:` block (:115-116, above both hunks) changed. The only three occurrences of the id in the file are :551, :957, :1084 — statement, scenario covers, verification covers — and only the first two were edited, in body only. .spec/realization_hashes.json keys module/function names with no line data and zero coverage_capture entries, so no hash can shift from a prose edit. No mechanism exists by which this diff reaches another `must`. The verifier was right to scope to one requirement, even though it should have said so explicitly.\n\nMechanical re-check: exactly one tracked file (.spec/specs/coverage_capture.spec.md), commit 144350d2 on base 2023373d. Zero files matching Out of Scope — lib/specled_ex/coverage/store.ex, test/specled_ex/coverage/store_test.exs, CHANGELOG.md, VERSION, .spec/realization_hashes.json, .spec/state.json all untouched. Out of Scope (intent) respected: zero code, zero test change; ADMISSION item 1 plus recommended item 2 exactly. Spec-Drift trailer present on the commit rather than a fabricated decisions: link, as the contract required. No seam disclosed, and none found.\n\nTwo corrections to the VERIFY comment, neither changing the verdict:\n1. Line counts are wrong. VERIFY says \"+9/-1\"; the actual diff is +6/-1 (git show --stat: 6 insertions, 1 deletion). The overstatement is in the harmless direction — every one of the 6 added lines is in scope and accounted for — but the count was not verifiable as written.\n2. .grok/sandbox.toml is untracked AND not gitignored (`git check-ignore` returns nothing). It is correctly excluded from the commit, so scope is clean, but it is not an ignored artifact; whoever cuts the PR should confirm it does not get swept in.\n\nAdvisory, pre-existing, not a bounce: the `meta.unattributed` clause of envelope_meta still has no test carrying `@tag spec: \"specled.coverage_capture.envelope_meta\"` (the four tagged tests in store_test.exs are at :128, :216, :286, :307; \"unattributed\" appears only in formatter.ex/formatter_test.exs). That predates npo and adding a test would have been a scope violation under this contract. Worth a follow-up ticket.",
      "timestamp": "2026-07-27T10:31:09Z"
    },
    {
      "text": "APPROVED. Merged into epic/specled_-k6s-0.11.0-fast-follow. Closing per orchestrator sign-off.",
      "timestamp": "2026-07-27T10:37:43Z"
    }
  ],
  "priority": 3,
  "status": "in_progress",
  "title": "envelope_meta requirement under-specifies its own test: present-but-non-map :meta rejection is asserted but unstated",
  "type": "task",
  "updated_at": "2026-07-27T10:37:43Z"
}
