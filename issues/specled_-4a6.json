{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-07-25T13:43:40Z",
  "description": "Pre-existing latent finding from specled_-dn4.8 cold verification (2026-07-25T13:34:35Z, Note 2): CrossField.validate/3's current_index-aware checks (R4 resolution, R6 deprecates exemption) have no production caller — Index.build calls DecisionParser.parse_file/2 with no current_index, and validate_all/2 is test-only. The cross_field_affects_resolve requirement is specified and tested but unwired in the live gate; the aggregate Verifier duplicates the logic instead. Options: wire current_index through Index.build's parse, or consolidate on the Verifier path and re-scope the requirement. Independent of the dn4 epic (pre-existing on main).",
  "id": "specled_-4a6",
  "labels": [
    "phase:in-ci",
    "slug:wire-crossfield-validate-3"
  ],
  "comments": [
    {
      "text": "CUT FROM specled_-k6s (2026-07-27) by user decision. NOT closed, NOT descoped - deferred to its own PR.\n\nCold verification says the mandate is still fully applicable (verdict: still_applicable, effort: medium). It was excluded from the single-PR fast-follow epic for two reasons the verifier surfaced:\n\n1. SELF-GATE RISK. Wiring CrossField into the live decision-parse path fires cross_field/affects_unresolved on 5 shipped repo.governance ADRs and flattens 9 missing_change_type warnings into hard errors (verifier.ex:1894-1898), which violates specled.decisions.change_type_optional. Landing it inside a 21-ticket PR means remediating specled own governance corpus mid-PR.\n2. ONLY HASH-MOVER. It is the only ticket in the candidate set that moves .spec/realization_hashes.json entries (CrossField.validate/3, DecisionParser.parse_file/4, Verifier.verify_decision/4). Cutting it lets the epic forbid every agent from touching the baseline outright instead of regenerating mid-flight.\n\nSEQUENCING FOR THE FOLLOW-UP PR: specled_-14t stays in specled_-k6s and lands first. Its ruling on the repo. affect-prefix carve-out determines whether 4a6 adds a carve-out or migrates the 5 ADRs - so do not start 4a6 until k6s has merged and 14t resolution is readable in the tree.\n\n== SCOPED REMAINING WORK (preserved from admission verification) ==\nPick and execute ONE of the two options. Both are blocked on the same two hazards that a naive wiring would trip; the implementer must handle them or `mix spec.check` breaks on specled's own 50 ADRs.\n\nOPTION A (wire it — preferred, keeps the requirement text as written):\nA1. lib/specled_ex/index.ex `build/2`: split decision parsing into two passes. Keep line 31 producing `decisions` with `current_index = nil`, build `base_index` (lines 33-42) as today, then replace the `\"decisions\"` value with `DecisionParser.validate_cross_fields(decisions, base_index, opts)` before `maybe_add_tag_data/3`. `CrossField.resolvable_ids/1` (cross_field.ex:288) reads `current_index[\"subjects\"]` / `[\"decisions\"]` and `subject[\"requirements\"]`, so `base_index` is already the right shape — no new struct needed.\nA2. HAZARD 1 — `repo.*` affects. 5 shipped ADRs (specled.decision.{explicit_subject_ownership, declarative_current_truth, file_backed_linked_strength, guided_reconciliation_loop, local_skill_scaffold}.md) carry `affects: [repo.governance]`. The Verifier accepts these via `String.starts_with?(affect, \"repo.\")` (verifier.ex:2100) but `CrossField.resolvable_ids/1` does not, so wiring as-is emits 5 `cross_field/affects_unresolved` ERRORS. Add the same `repo.`-prefix carve-out (and the `claim_ids` source) to `CrossField.apply_rule(:r4_affects_resolve, ...)` at cross_field.ex:145-172 so the two paths agree exactly, and state the carve-out in the `cross_field_affects_resolve` requirement text at .spec/specs/decision_governance.spec.md:101-110.\nA3. HAZARD 2 — warning severity is flattened to error. lib/specled_ex/verifier.ex:1894-1898 `add_decision_parse_error_findings/5` does `[finding(\"error\", \"decision_parse_error\", message, ...)]` for EVERY `parse_errors` string, and decision_parser.ex:74-76 encodes warnings as the plain string `\"warning #{code}: #{message}\"`. R7 (`cross_field/missing_change_type`, cross_field.ex:175-188) fires for 9 shipped ADRs that have no `change_type:` line (declarative_current_truth, evidence_based_attribution, explicit_subject_ownership, file_backed_linked_strength, file_touch_yields_to_realization, guided_reconciliation_loop, local_skill_scaffold, no_app_start, spec_review_html_viewer), so wiring as-is turns 9 warnings into 9 hard errors — directly violating .spec/specs/decision_governance.spec.md:57-64 (`specled.decisions.change_type_optional`: \"shall parse successfully and emit a `cross_field/missing_change_type` warning-level diagnostic rather than a parse error\"). Carry severity structurally (e.g. a separate `\"parse_warnings\"` key on the decision map, or a `{severity, message}` tuple) and emit `finding(\"warning\", \"decision_cross_field_warning\", ...)` for those; `finding/5` already takes severity as its first argument so `\"warning\"` is expressible.\nA4. Add an integration test (new or in an index-level test) proving `Index.build/2` output carries a `cross_field/affects_unresolved` error for an unresolvable non-deprecates affect and does NOT for a `deprecates` one — i.e. R4/R6 exercised through the live path, not only through direct `CrossField.validate/3` calls.\nA5. Either delete `validate_cross_fields/3` (decision_parser.ex:58-62) or make A1 its single caller; do not leave it dead.\n\nOPTION B (re-scope): rewrite .spec/specs/decision_governance.spec.md:101-110 so `cross_field_affects_resolve` names the live enforcer (`SpecLedEx.Verifier.verify_decision/4` / `valid_decision_affect?/4`) rather than \"the CrossField validator\", add `SpecLedEx.Verifier.verify_decision/4`-backed tagged tests to the covers list, and delete the unreachable `current_index` plumbing (decision_parser.ex:58-62 and the `parse_file/4` `current_index` parameter) so no dead gate remains. Note this makes `CrossField` a partially-vestigial module and needs an ADR in .spec/decisions/ recording the consolidation.\n\n== RISK NOTES ==\nREALIZATION-HASH DRIFT: yes, near-certain. .spec/realization_hashes.json carries api_boundary entries for \"SpecLedEx.DecisionParser.CrossField.validate/3\" (line 351) and \"SpecLedEx.DecisionParser.parse_file/4\" (line 355), plus an implementation entry for \"SpecLedEx.Verifier.verify_decision/4\" (line 651) — all three are bindings this work edits, and per prior experience api_boundary hashes are line-position-sensitive, so even non-semantic edits above those functions in decision_parser.ex / cross_field.ex / verifier.ex will drift them. The baseline must be regenerated as part of the ticket, and in a 22-ticket single-PR landing the regeneration MUST be done once at the end by the orchestrator, not per-ticket, or the hash file will conflict repeatedly.\n\nHARD ORDERING DEPENDENCY on specled_-14t: 14t is \"Document or remove the unverified 'repo.' affect-prefix exemption in Verifier.valid_decision_affect?/4\" — the exact function (lib/specled_ex/verifier.ex:2095-2101) and the exact `String.starts_with?(affect, \"repo.\")` clause that step A2 above must mirror into CrossField. If 14t REMOVES the exemption, A2's carve-out must not be added and instead the 5 `repo.governance` ADRs must be migrated first; if 14t DOCUMENTS it, A2 must copy it. Land 14t first, or merge the two tickets. Do not implement 4a6 blind to 14t's outcome.\n\nSOFT CONFLICT with specled_-vbo: also edits lib/specled_ex/verifier.ex, but in the workflow_file verification region (~L706 per its description), not the decision-finding region (L1811-1930, L2095-2101). Different hunks; textual merge should be clean, but both will contend for the verifier.ex realization hash.\n\nSPEC-REQUIREMENT CONTENTION: this ticket edits .spec/specs/decision_governance.spec.md requirement `specled.decisions.cross_field_affects_resolve` (:101-110) and interacts with `specled.decisions.change_type_optional` (:57-64). specled_-14t also targets ADR-affect resolution semantics and may want to touch `specled.decisions.reference_validation` (:38-45) in the same file. Reserve decision_governance.spec.md for the 4a6+14t pair.\n\nSELF-GATE RISK: this is the rare ticket that can turn specled's own `mix spec.check` red on merge. Both hazards (5 `repo.governance` errors, 9 missing-`change_type` warnings-flattened-to-errors) fire on the repo's OWN 50 ADRs in .spec/decisions/. Run `mix spec.check --no-run-commands` after wiring and BEFORE assuming the ticket is done; a green pre-wiring gate says nothing.\n\nADR SURFACE: .spec/decisions/specled.decision.deprecates_affect_reference_carveout.md:19-25 and :44 explicitly reason about the CrossField-vs-live-verifier gap (\"something stricter than both CrossField and the live verifier now enforce\"; \"The current-truth requirement matches CrossField and verifier\"). Whichever option is chosen, that ADR's prose must be re-read and likely amended — and ADRs are append-only under CrossField R5 (cross_field.ex:193-255), so amend by superseding, not by editing `affects`/`change_type`/`reverses_what` in place.\n",
      "timestamp": "2026-07-27T08:37:26Z"
    },
    {
      "text": "Confirmed again by the 2026-08-12 five-persona critical review (SPEC-H1), independently traced twice: Index.build calls DecisionParser.parse_file/2 (decision_parser.ex:9-11), which passes current_index = nil; maybe_run_cross_field(decision, nil, _opts) returns the decision untouched (:64). validate_cross_fields/3 has no caller in lib/, and no mix task references DecisionParser or CrossField at all. All six cross_field_* must requirements are enforced only by their own unit tests. NEW EVIDENCE: release 0.15.0 shipped three narrows-scope ADRs with no reverses_what — a combination specled.decisions.cross_field_reverses_what says 'shall emit an error' — and spec.check returned result=pass. They were corrected by hand in commit e71ac7bc, but the gate did not catch them. This is the first tree where the dead validator demonstrably let a real governance violation through.",
      "timestamp": "2026-08-12T21:22:07Z"
    },
    {
      "text": "THIRD INDEPENDENT CONFIRMATION — first from a downstream adopter, plus a correction to step A1 and a live violation in this repo's own corpus.\n\nSession: f15267ae-52b5-4442-b6e4-a556b2b100c6\nLog: /Users/mz/.claude-personal/projects/-Users-mz-dev-mz-drovr/f15267ae-52b5-4442-b6e4-a556b2b100c6.jsonl\nFiled from: drovr (github.com/iautom8things/drovr), ticket drv-3t8, 2026-08-14.\n\n== A1 IS WRONG AS WRITTEN — base_index is NOT the right shape ==\n\nA1 says: \"CrossField.resolvable_ids/1 reads current_index[\"subjects\"] / [\"decisions\"] and subject[\"requirements\"], so base_index is already the right shape — no new struct needed.\"\n\nIt is not. Parser.parse_file stores a %SpecLedEx.Schema.Meta{} STRUCT under subject[\"meta\"] on the success path (parser.ex:57-59, via Schema.validate_block(\"spec-meta\", meta)), while resolvable_ids/1 reads the id with a STRING key through get_in_nested(subject, [\"meta\", \"id\"]) (cross_field.ex:288-298). Map.get(%Meta{}, \"id\") is nil, so EVERY subject id resolves to nil and every affects entry naming a subject or requirement looks unresolved.\n\nMeasured on this tree (HEAD c228bff7, `mix run --no-start`, Index.build then CrossField.validate against the index as-is):\n\n  subject meta is_struct=true\n  string-key id lookup =\u003e nil\n  decisions=36 subjects=33\n  %{\"cross_field/affects_unresolved\" =\u003e 36,\n    \"cross_field/missing_change_type\" =\u003e 9,\n    \"cross_field/reverses_what_missing\" =\u003e 1}\n\nSo naive wiring emits affects_unresolved on all 36 ADRs, not the 5 repo.governance ones HAZARD 1 predicts. This is a THIRD hazard, independent of the other two, and it is the one that makes the wiring look catastrophically broken on first run. Note the inversion it creates: an INVALID spec-meta falls to parser.ex:62-63, which stores the raw string-keyed map, so ids from BROKEN specs resolve while ids from valid ones do not.\n\nWhichever option is chosen, either normalize subject meta at the call site before handing the index to CrossField, or make resolvable_ids/1 struct-aware. drovr's local workaround normalizes at the call site (Map.from_struct + Atom.to_string on keys) and pins the behavior in both directions — a resolve test and a reject test — because a normalizer that declares everything resolvable passes a resolve-only assertion.\n\n== THIS REPO'S OWN CORPUS HAS A LIVE R2 VIOLATION RIGHT NOW ==\n\n.spec/decisions/specled.decision.parser_resilient_errors_split.md carries change_type: narrows-scope (a weakening type) with no reverses_what. specled.decisions.cross_field_reverses_what says the validator \"shall emit an error\" for exactly that. On the same tree, `mix spec.check --no-run-commands` reports `status=pass errors=0 warnings=0`, exit 0.\n\nThat is now the second such escape in this repo after the three 0.15.0 narrows-scope ADRs noted on 2026-08-12, and this one is still in the tree rather than hand-corrected. Add it to the remediation list for whichever option lands.\n\n== DOWNSTREAM EVIDENCE (drovr, pinned at bb64e69) ==\n\ndrovr hit the same gap from the outside and had to build its own gate. Sequence:\n\n1. A five-persona critical review of drovr's drv-6pr found a malformed ADR by hand (finding F17) — an ADR that R2 classifies as a hard error, while `mix spec.check --no-run-commands` reported errors=0 warnings=0 on it.\n2. drv-3t8 confirmed the cause: on the pinned tree, Index.build calls DecisionParser.parse_file/2 (index.ex:31), that arity passes current_index=nil (decision_parser.ex:9-11), and maybe_run_cross_field(decision, nil, _) returns the decision untouched (:64). validate_cross_fields/3 has zero callers in lib/. verifier.ex contains no cross_field reference at all. Same as reported here on 2026-08-12; independently re-traced.\n3. Live repro, run by a cold verifier on drovr's tree: a NEW ADR with change_type: weakens and no reverses_what → `mix spec.check --no-run-commands` says `validate status=pass errors=0 warnings=0`, `spec.check result=pass`. Same file switched to change_type: supersedes with no replaces → also pass.\n4. Contrast confirming only R4 is duplicated by the Verifier: the same ADR with an unresolvable affects id → spec.check FAILS with `[ERROR] decision_unknown_affect`. So of the classes drovr cared about, affects-resolve was already covered and R1/R2 were genuinely blind.\n\ndrovr's fix (test/spec/adr_cross_field_test.exs, commit ed54e2f) runs CrossField over its ADR corpus from its own test suite, so `mix test` and therefore `make gates` enforce it. Every enforced rule is driven by a fixture ADR written to a tmp dir and read back through DecisionParser.parse_file/2; a cold verifier mutation-proved each of R1 (both branches), R2, R3, R4 and R7 red-lights exactly its own test. That is a workaround, not a fix — every adopter needs the same ~250 lines, and each one will independently trip the struct-meta trap above.\n\n== SUGGESTED PRIORITY ==\n\nRaising P3 -\u003e P2. Three independent traces, two demonstrated escapes in this repo, one demonstrated escape plus a full workaround ticket in a downstream adopter. Six must requirements remain enforced only by their own unit tests. Not P1 only because the hazards are real and the remediation is genuinely multi-step, exactly as scoped above.",
      "timestamp": "2026-08-14T15:01:17Z"
    },
    {
      "text": "IMPLEMENTED\\nSession: f7145a10-323f-4aae-a8f3-1c77e224d1a1\\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/f7145a10-323f-4aae-a8f3-1c77e224d1a1.jsonl\\n\\nCommit: f7faf654 (branch specled_-4a6/wire-crossfield, worktree /Users/mz/src/specled_ex-wire-crossfield, base 2521671a)\\nFiles touched: lib/specled_ex/index.ex, lib/specled_ex/decision_parser.ex, lib/specled_ex/decision_parser/cross_field.ex, lib/specled_ex/verifier.ex, lib/specled_ex/append_only.ex, .spec/specs/decision_governance.spec.md, .spec/specs/append_only.spec.md, .spec/specs/index_state.spec.md, .spec/specs/verification.spec.md, .spec/decisions/specled.decision.live_cross_field_gate.md (new), .spec/decisions/specled.decision.adr_reverses_what_backfill.md (new), .spec/decisions/specled.decision.{amplification_scoped_dedupe,doc_identifier_lint_spec_corpus,parser_resilient_errors_split,resolution_path_provenance}.md, test/specled_ex/decision_parser/live_cross_field_gate_test.exs (new), test/specled_ex/append_only_test.exs, test/specled_ex/index_state_test.exs\\nTests added: SpecLedEx.DecisionParser.LiveCrossFieldGateTest (4 tests: resolvable affects produce no finding; weakening ADR with no reverses_what fails the gate; unresolvable non-deprecates affect fails the gate; change_type-less ADR warns without failing a strict gate) + AppendOnlyTest \"backfilling an absent reverses_what is exempt; rewording an existing one is not\"\\nVerification stubs flipped: none — every stub covering the touched requirements was already execute: true\\nVerification tier: change-scoped — test/specled_ex/{append_only,decision_parser,index_state,index_state_tag,verifier,branch_check,state_json_fixture}_test.exs, test/specled_ex/{decision_parser,branch_check,review,validator}/, test/docs_identifier_lint_test.exs (480 tests, 0 failures); plus mix compile --warnings-as-errors, mix format --check-formatted, mix deps.unlock --check-unused, make xref (0 edges), and bash ./scripts/check_specs.sh =\u003e spec.check result=pass. Full-suite proof deferred to PR CI.\\nFinal-tier runs: 1\\n\\nSummary: Option A of the charter. Index.build/2 parses ADRs in two passes and runs CrossField.validate/3 over each one against the index built from the same tree, so the six cross_field_* must requirements now gate mix spec.check instead of only their own unit tests. Cross-field severity is carried structurally — errors into parse_errors, warnings into a new parse_warnings list the verifier reports as decision_cross_field_warning at info — because spec.check validates with strict: true, where a warning is fatal and would break every workspace holding a change_type-less legacy ADR that specled.decisions.change_type_optional exists to protect. New requirement specled.decisions.cross_field_live_gate plus ADR specled.decision.live_cross_field_gate record the wiring and the severity mapping.\\n\\nDEVIATIONS AND FINDINGS BEYOND THE CHARTER (all confirmed by execution, not inspection):\\n\\n1. The charter's hazard A2 was already resolved. specled_-14t landed the repo. carve-out in both the requirement text and CrossField R4 (reject_repo_namespace/1), so no carve-out work was needed here.\\n\\n2. NEW: CrossField had never met a real index. resolvable_ids/1 read subject[\"meta\"][\"id\"] with string keys, but Index.build/2 emits schema structs with atom keys, so ZERO subject ids resolved. Wiring as-is produced 55 cross_field/affects_unresolved errors against correct ADRs — an order of magnitude past the 5 the charter predicted, and none of them repo.*. Every R4 fixture was a hand-built plain map, which is exactly why nothing caught it. Fixed with a key-agnostic accessor; the resolvable set now also includes scenario ids, matching what Verifier.valid_decision_affect?/4 accepts (charter step A2's claim_ids clause).\\n\\n3. NEW: 4 shipped ADRs (amplification_scoped_dedupe, doc_identifier_lint_spec_corpus, parser_resilient_errors_split, resolution_path_provenance) declare a weakening-set change_type with no reverses_what — the same class as the 0.15.0 escape, 4 more instances. They could not be repaired: append_only.ex:496 compared reverses_what verbatim and emitted append_only/adr_affects_widened at error for any difference, so the only legal remedy was superseding 4 records whose decisions are correct, and every adopter would hit the same wall on upgrade. Per user decision (asked mid-implementation, options: backfill carve-out / supersede all 4 / drop R2 from the live gate) the carve-out was chosen: new requirement specled.append_only.adr_reverses_what_backfill + ADR specled.decision.adr_reverses_what_backfill make blank -\u003e non-blank exempt while change_type and affects stay compared verbatim and any edit to an existing value is still drift. Real justification prose was then backfilled into all 4.\\n\\n4. Charter step A5 (do not leave validate_cross_fields/3 dead): Index.build/2 is now its single caller. parse_file/4 remains the production parse entry point via parse_file/2.\\n\\n5. Realization hashes did NOT drift — the charter predicted near-certain drift on CrossField.validate/3, DecisionParser.parse_file/4, and Verifier.verify_decision/4. mix spec.check --no-run-commands --verbose reports no branch_guard_realization_drift finding at any severity, so .spec/realization_hashes.json is untouched and needs no regeneration.\\n\\n6. Two branch_guard_missing_subject_update errors were downgraded via a Spec-Drift: branch_guard_missing_subject_update=info commit trailer: verifier.ex impacts specled.prose_guard and index_state_test.exs impacts specled.package / specled.status, none of whose contracts this change moves. The three subjects whose contracts DID move were updated properly instead: specled.decisions (new requirement), specled.index_state (summary gained decision_parse_warnings; two-pass parse), specled.verify.decision_governance (the new finding channel and its severity).\\n\\n7. Advisory append_only/statement_rewritten warnings remain for the three amended requirements. They are the intended signal for a reviewed in-place clarification, not a gate failure.\\n\\nNo must requirement, scenario, or success criterion was deferred; no follow-up tickets were filed.",
      "timestamp": "2026-08-14T17:34:12Z"
    },
    {
      "text": "VERIFY: PASS\\nSession: f7145a10-323f-4aae-a8f3-1c77e224d1a1\\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/f7145a10-323f-4aae-a8f3-1c77e224d1a1.jsonl\\n\\nCold verification of f7faf654 against base 2521671a, worktree /Users/mz/src/specled_ex-wire-crossfield.\\n\\nMechanical checks:\\n- Files touched: 18\\n- Files outside the ticket's expected touch set (index.ex, decision_parser.ex, cross_field.ex,\\n  verifier.ex, decision_governance.spec.md, tests): 10 — all judged JUSTIFIED.\\n  - lib/specled_ex/append_only.ex, .spec/specs/append_only.spec.md,\\n    .spec/decisions/specled.decision.adr_reverses_what_backfill.md (new): traceable to the\\n    charter's SELF-GATE RISK note plus the mid-implementation user decision in the IMPLEMENTED\\n    comment. Activating R2 makes 4 shipped ADRs hard errors; repairing them tripped\\n    append_only/adr_affects_widened, which compared reverses_what verbatim. Both facts confirmed\\n    by execution (see claims c and d). The charter made \"specled's own corpus goes red on merge\"\\n    this ticket's problem, so the remedy is in scope.\\n  - .spec/decisions/specled.decision.live_cross_field_gate.md (new): required by the charter's\\n    ADR SURFACE note.\\n  - 4 backfilled ADRs (amplification_scoped_dedupe, doc_identifier_lint_spec_corpus,\\n    parser_resilient_errors_split, resolution_path_provenance): the corpus repair the activation\\n    forces. Frontmatter change is reverses_what only; change_type and affects untouched in all 4.\\n  - .spec/specs/index_state.spec.md, .spec/specs/verification.spec.md: branch-guard-required\\n    subject updates for contracts this change genuinely moves (summary gained\\n    decision_parse_warnings; the verifier gained a finding channel).\\n- Files matching Out-of-Scope: 0. Notably .spec/realization_hashes.json is untouched, which the\\n  charter required (no per-ticket regeneration).\\n- No mix.exs / mix.lock change: no new external deps.\\n- No verification stub flipped execute: false -\u003e true; no test disabled.\\n\\nSemantic checks:\\n- Spec stubs run: 6, passed: 6. Every execute: true stub whose covers list intersects the\\n  advanced requirements: append_only tagged_tests + source_file(lib/specled_ex/append_only.ex);\\n  decision_governance tagged_tests + source_file(lib/specled_ex/decision_parser/cross_field.ex);\\n  index_state tagged_tests; verification tagged_tests. All executed by the armed wrapper.\\n\\n- Requirements coverage:\\n  - specled.decisions.cross_field_live_gate: covered by\\n    test/specled_ex/decision_parser/live_cross_field_gate_test.exs — the whole file, not one test.\\n    PROVEN BY MUTATION: I replaced Index.build/2's second pass with a pass-through\\n    (validated = decisions), and 3 of the 4 tests failed (lines 137, 168, 199), e.g.\\n    `assert [\"cross_field/reverses_what_missing: \" \u003c\u003e _] = decision[\"parse_errors\"]` went to `[]`.\\n    Tree restored to HEAD (git status clean). The live path is genuinely proven — no hand-built\\n    index anywhere in the file. Seams: SpecLedEx.FixtureCase temp-dir workspace;\\n    `Index.build(root, test_tags: false)` disables the tag scanner (harmless — the cross-field\\n    pass runs before maybe_add_tag_data/3); SpecLedEx.validate/3 called directly rather than\\n    through the mix task, but with the same strict: true / run_commands: false the task uses.\\n  - specled.decisions.cross_field_reverses_what: live_cross_field_gate_test.exs:137. Would fail if\\n    R2's result stopped reaching parse_errors or the verifier stopped promoting parse_errors to\\n    error findings — the test asserts both the parse_errors entry and\\n    report[\"status\"] == \"fail\" with a decision_parse_error finding. This is the literal 0.15.0\\n    escape, reproduced end to end. Seams: as above.\\n  - specled.decisions.cross_field_affects_resolve: live_cross_field_gate_test.exs:168 (dangling\\n    affect -\u003e error) and :92 (subject id + requirement id + scenario id + repo.* all resolve, no\\n    finding). Would fail if the key-agnostic accessor regressed or the resolvable set narrowed.\\n    Seams: as above.\\n  - specled.decisions.change_type_optional: live_cross_field_gate_test.exs:199. Asserts\\n    parse_errors == [], parse_warnings has the entry, summary counts it, report status == \"pass\",\\n    and explicitly `warning[\"severity\"] == \"info\"`. \"Strict gate still passes\" IS asserted —\\n    findings_for/2 passes strict: true and the test asserts status == \"pass\". Flipping the\\n    verifier back to \"warning\" fails it via verifier.ex:98\\n    (`fail? = errors \u003e 0 or (strict? and warnings \u003e 0)`). Seams: as above.\\n  - specled.append_only.adr_reverses_what_backfill: test/specled_ex/append_only_test.exs:672. One\\n    test proves BOTH halves — d1 (blank -\u003e non-blank) produces no finding and d2 (reworded)\\n    produces exactly one at :error, asserted as `assert [finding] = drift`, so deleting the\\n    guard's blank check or dropping reverses_what from comparison entirely both fail. I\\n    additionally confirmed the narrowness by direct execution beyond what the test asserts (see\\n    claim d). Seams: SpecLedEx.AppendOnlyFixtures hand-built state maps — the detector never sees\\n    real .spec files or a git diff here. That is the pre-existing pattern for the whole file, and\\n    I re-ran the same matrix independently against AppendOnly.analyze/3 to confirm.\\n  - specled.append_only.adr_affects_widened (amended): existing tests in append_only_test.exs plus\\n    the new one. My independent probe confirms change_type and affects still fire at :error.\\n  - specled.index.subject_and_decision_index (amended): test/specled_ex/index_state_test.exs:77\\n    asserts the exact summary map, so a missing or misnamed decision_parse_warnings key fails on\\n    map equality. The two-pass clause is proven by the mutation test above.\\n  - specled.verify.decision_governance (amended): live_cross_field_gate_test.exs:137 (parse_errors\\n    -\u003e decision_parse_error at error) and :199 (parse_warnings -\u003e decision_cross_field_warning at\\n    info). Both channels and both severities are asserted literally.\\n\\n- Intent-level scope concerns: none. No new deps. No test disabled. No stub un-flipped. Three\\n  `must` statements were amended in place; I judge each:\\n  - specled.append_only.adr_affects_widened — amended to name the carve-out. This IS a narrowing\\n    of a detector, but it is authorized by an accepted ADR with change_type: adds-exception, the\\n    carve-out's narrowness is proven by execution, and the ADR's Consequences section states the\\n    residual risk honestly (\"an author who omits reverses_what ... may add it later\"). Honest\\n    description of new behavior, not a weakening to make failing code pass.\\n  - specled.index.subject_and_decision_index — adds obligations (two-pass, new counter). Honest.\\n  - specled.verify.decision_governance — adds obligations (new finding channel + severities).\\n    Honest.\\n  No requirement text was softened to let failing code through; specled.decisions.change_type_optional\\n  in particular was left untouched.\\n\\n- Targeted verification: PASS.\\n  - mix test on the 3 diff-touched test files: 2 properties, 58 tests, 0 failures.\\n  - bash ./scripts/check_specs.sh: validate status=pass errors=0 warnings=0;\\n    branch findings=5 (error=0 warning=3 info=2); `spec.check result=pass`.\\n  - mix spec.check --no-run-commands --verbose: result=pass, 2 info findings only.\\n  - Mutation test on lib/specled_ex/index.ex (reverted; git status clean afterward).\\n\\n- Whitelist adherence: change-scoped + diff-touched tests + contract adds only; bare\\n  mix spec.check: not run; compile/format/credo/deps re-establishment: not run.\\n\\n- Claim validation (step 10) — all six CONFIRMED BY EXECUTION:\\n  a. CONFIRMED. `git show 2521671a:lib/specled_ex/index.ex` line 31 is\\n     `DecisionParser.parse_file(\u00261, root)` (arity 2 -\u003e current_index = nil), and\\n     `git grep validate_cross_fields 2521671a -- lib/ test/` returns only the definition at\\n     decision_parser.ex:58. No production caller, no test caller.\\n  b. CONFIRMED, and the \"55\" is exact. Against the live index, base resolvable_ids/1 resolved\\n     0 of 34 subject ids (459 requirement ids and 65 decision ids did resolve). Running the real\\n     HEAD validator over all 65 shipped ADRs with subject ids made unresolvable yields exactly 55\\n     cross_field/affects_unresolved findings across 55 ADRs; with the fixed accessor it yields 0.\\n     One correction to the implementer's framing, immaterial to the fix: the live index's subjects\\n     are string-keyed plain maps whose *values* are schema structs, so the base accessor died on\\n     the second hop (`Map.get(%Schema.Meta{}, \"id\")`), not on the subject map itself.\\n  c. CONFIRMED. Stripping the backfilled reverses_what from each of the 4 ADRs in memory and\\n     re-running CrossField.validate/3 against the live index yields\\n     [\"cross_field/reverses_what_missing\"] for all 4; with the backfill, [] for all 4.\\n  d. CONFIRMED, and narrower than the test alone proves. Direct AppendOnly.analyze/3 matrix:\\n     absent -\u003e non-blank: no finding. blank(\"   \") -\u003e non-blank: no finding.\\n     non-blank -\u003e reworded: error. non-blank -\u003e DELETED: error. non-blank -\u003e whitespace: error.\\n     affects widened: error. change_type moved: error.\\n  e. CONFIRMED. `bash ./scripts/check_specs.sh` run by me: `spec.check result=pass`.\\n  f. CONFIRMED, and absent rather than suppressed. --verbose shows only 2 info findings, neither\\n     realization-related. Mechanical explanation: all three predicted bindings\\n     (CrossField.validate/3, DecisionParser.parse_file/4, Verifier.verify_decision/4) are\\n     api_boundary entries, and ApiBoundary.hash/2 hashes the resolved function *head* only —\\n     \"stable under whitespace, variable renames, and line shifts\" (api_boundary.ex:51-63). None of\\n     the three heads changed; only bodies did. The charter's \"near-certain drift\" prediction rested\\n     on a false premise about line-position sensitivity. The Spec-Drift trailer on the commit\\n     downgrades only branch_guard_missing_subject_update, not any drift code.\\n\\n- info-vs-warning deviation (step 11): JUSTIFIED AND DOCUMENTED.\\n  The premise is real: spec.check.ex:117 passes `strict: true`, and verifier.ex:98 is\\n  `fail? = errors \u003e 0 or (strict? and warnings \u003e 0)`. Emitting the missing-change_type diagnostic\\n  at warning severity would fail mix spec.check for every workspace holding a change_type-less\\n  legacy ADR — precisely the outcome specled.decisions.change_type_optional exists to prevent.\\n  The deviation does not gut the requirement: all six error-severity cross-field rules stay fatal\\n  on the live path (verified — the reverses_what and dangling-affect tests both drive\\n  report[\"status\"] == \"fail\"), which is the 0.15.0 escape class the ticket exists to close. Only\\n  R7, the one rule whose own requirement mandates non-fatality, lands at info.\\n  It is documented in three places, not buried: the new requirement text states the severity and\\n  the reason inline; the ADR carries it as a named Negative consequence; verifier.ex:1918-1926\\n  carries the rationale at the emitter. The stated escape hatch is real — I confirmed\\n  apply_severity_overrides/2 (verifier.ex:2863) is generic over finding codes and that\\n  spec.check.ex:100,121 feeds it from config.verification.severities, so an adopter can raise\\n  decision_cross_field_warning to error.\\n\\n- Full-suite verification: not run by verifier (by design).\\n\\nVerdict: PASS\\n\\nNotes (advisory only — none change the verdict):\\n1. TAG PLACEMENT. Only 1 of the 4 tests in live_cross_field_gate_test.exs carries\\n   `@tag spec: \"specled.decisions.cross_field_live_gate\"`, and it is the negative-assertion one.\\n   Under the mutation, `mix test --only spec:specled.decisions.cross_field_live_gate` still passes\\n   while the file as a whole fails 3/4. The suite and spec.check both catch the regression (the\\n   sibling requirements share one tagged_tests stub), so this is not a gap — but if that stub is\\n   ever split per-requirement, cross_field_live_gate would be left tag-covered by an assertion\\n   that cannot fail. Cheap fix: add the tag to the three positive tests.\\n2. TERMINOLOGY TENSION. specled.decisions.change_type_optional still reads \"warning-level\\n   diagnostic\", while the verifier now surfaces it at info. The ADR reconciles this (\"the\\n   diagnostic remains warning-level inside CrossField, which is what that requirement\\n   constrains\") and CrossField genuinely still returns %{severity: :warning}, so the code matches\\n   both texts. Still, a reader comparing the two requirements side by side will see a conflict.\\n   Worth a one-line clarification in change_type_optional in a later pass.\\n3. SPEC-DRIFT TRAILER. The commit downgrades two branch_guard_missing_subject_update errors to\\n   info (verifier.ex -\u003e specled.prose_guard; index_state_test.exs -\u003e specled.package,\\n   specled.status). I checked the diff hunks against those three subjects and agree none of their\\n   contracts moved: the verifier change is a new decision-finding channel, unrelated to prose\\n   linting, and the test change is a single expected-map key. Legitimate use of the trailer.\\n4. append_only_test.exs's module-level `# covers:` comment does not list\\n   specled.append_only.adr_reverses_what_backfill even though append_only.ex's does. spec.check's\\n   tag-consistency check is satisfied by the per-test `@tag spec:`, so nothing is red; noting it\\n   only for symmetry.",
      "timestamp": "2026-08-14T17:34:13Z"
    },
    {
      "text": "TEST_CRITIC: FIXED (iteration 1)\\nSession: f7145a10-323f-4aae-a8f3-1c77e224d1a1\\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/f7145a10-323f-4aae-a8f3-1c77e224d1a1.jsonl\\n\\nCritic session: elixir-test-critic spawned read-only against 2521671a..f7faf654 (test/ only).\\nBlocking findings: 5 (resolved: 5, waived: 0)\\nFiles reviewed: test/specled_ex/decision_parser/live_cross_field_gate_test.exs, test/specled_ex/append_only_test.exs, test/specled_ex/index_state_test.exs\\nFix commit: c6d8ba5f\\n\\nEvery blocking finding was the same species — an assertion that could not fail for the regression it appeared to guard:\\n\\nB1. The reverses_what carve-out tested only absent -\u003e prose. The requirement names deletion explicitly (\"including deleting it\") and nothing covered it: writing the guard with `or` instead of `and` would exempt DELETING justification from an accepted ADR — wider than the hole this ticket closes — and the suite stayed green. Replaced the two-ADR test with a five-case matrix (absent, blank \"   \", reworded, deleted, blanked), asserting the exact set of drifted ids. Building it surfaced a fixture bug: adr/1 reads options with Keyword.get, so `base ++ [id: id]` silently gave all five ADRs the base id. Per-case keys now precede the base.\\n\\nB2. live_cross_field_gate_test.exs's dangling-affect test asserted only report[\"status\"] == \"fail\". The verifier's own decision_unknown_affect check (verifier.ex:1951) already rejects that ADR and predates this branch, so the assertion passed with R4 completely unwired. Now pins the cross_field/affects_unresolved finding itself.\\n\\nB3. specled.decisions.cross_field_live_gate was tagged only on the negative-assertion test, whose three assertions all pass in a tree where Index.build/2 never calls validate_cross_fields/3. The cold verifier independently reached the same conclusion by mutation (it reverted the second pass and watched 3 of 4 tests fail — but not the tagged one). Tagged on the three tests that drive the live path.\\n\\nB4. summary[\"decision_parse_warnings\"] was asserted only where it equals 1 with zero errors, plus a vacuous == 0 in a zero-decision fixture; summing parse_errors into the warnings counter would pass both. Now asserted at 0 in the error-bearing tests, which simultaneously gives R7 its negative half on the live path (a well-formed ADR produces no warning).\\n\\nB5. push_parse_warning/2 uses Map.update/4 for decisions parsed before \"parse_warnings\" existed (base-tree views read by BranchCheck). Nothing fed it a legacy-shaped map, so switching to Map.update!/3 would raise KeyError at runtime with every test green. Added a direct unit test on validate_cross_fields/3 — the one place a hand-built input is the right tool, since the legacy shape is precisely what the live path no longer produces.\\n\\nAlso adopted three advisories: findings_for/2 renamed to verify_strict/3 with the underlying argument order (it returned a report, not findings); the drift message is now asserted to name reverses_what and carry its fix block (specled.append_only.fix_block_discipline); and the documented severities escape hatch is tested — raising decision_cross_field_warning to error must fail the gate, which was previously an untested promise in both the requirement text and the ADR.\\n\\nNot adopted, with reasons:\\n- A3 (R1/R3 have no live-gate test; R5 is reachable if a caller passes :prior_decisions to Index.build/2): R1/R3 keep their unit coverage and neither changed in this ticket. The R5 reachability observation is real but latent — no caller supplies the option — and closing it means deciding whether index build should ever run append-only checks. Out of scope for a wiring ticket; not filed, since no requirement demands it and nothing triggers it today.\\n- A6 (multi-diagnostic ordering when one ADR violates two rules): CrossField.validate/3 already ends in Enum.reverse/1 and both push helpers prepend, so order is deterministic; no requirement constrains it.\\n- A9 (file lives under test/specled_ex/decision_parser/ though the code under test is Index.build/2 plus the verifier): fair, but the gate spans three modules and no location mirrors all of them.\\n\\nRe-verification after the fix: 247 change-scoped tests green (append_only, decision_parser + subtree, index_state, verifier, branch_check + subtree, docs_identifier_lint), mix format --check-formatted clean, and bash ./scripts/check_specs.sh =\u003e validate status=pass errors=0 warnings=0, spec.check result=pass.\\nFinal-tier runs: 1 (post-fix)",
      "timestamp": "2026-08-14T17:34:13Z"
    },
    {
      "text": "VERIFY: PASS (delta — f7faf654..c6d8ba5f)\\nSession: f7145a10-323f-4aae-a8f3-1c77e224d1a1\\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/f7145a10-323f-4aae-a8f3-1c77e224d1a1.jsonl\\n\\nDelta scope: test-only, confirmed. Two files, no production code:\\n  test/specled_ex/append_only_test.exs (+40/-33)\\n  test/specled_ex/decision_parser/live_cross_field_gate_test.exs (+91/-11)\\nNothing in the delta touches lib/. No undisclosed production change.\\n\\nMutation results (all run at the stage worktree, tree restored between each):\\n- B1: append_only.ex maybe_drift_on_reverses_what/3 `and` -\u003e `or` — FAILED as required.\\n  left [\"d4_prose_deleted\",\"d5_prose_blanked\"] vs right [\"d3_prose_reworded\",\"d4_prose_deleted\",\"d5_prose_blanked\"].\\n  Matrix covers blank(\"   \")-\u003eprose, prose-\u003edeleted, prose-\u003eblanked: confirmed, all three present.\\n- B2: Index.build/2 second pass -\u003e `validated = decisions` — the affects test FAILED, but not on\\n  the new finding assertion. It aborts earlier at line 197, the pre-existing\\n  `assert [\"cross_field/affects_unresolved: \" \u003c\u003e message] = ...parse_errors` which predates the\\n  delta. Verified the orchestrator's sub-question separately by script: under the mutation\\n  report[\"status\"] is still \"fail\" (sole error: decision_unknown_affect, nowhere.subject) and\\n  parse_errors is []. So the status assertion alone was indeed non-falsifying and the new\\n  finding assertion is real strengthening — but the pre-delta TEST already failed under this\\n  mutation. See Notes.\\n- B3: `mix test --only spec:specled.decisions.cross_field_live_gate` under the same pass-through\\n  mutation — FAILED (1 of 3 selected: \"an adopter can raise the cross-field warning to a gate\\n  failure\"). Before the fix that run was green. Requirement now falsifiably tag-covered.\\n- B4: summary warnings counter — tested both readings.\\n  Replace (warnings := sum of parse_errors): fails, but the PRE-delta suite already caught it via\\n  the existing `== 1` assertion. Additive (warnings := warnings + errors, the reading the finding\\n  describes): pre-delta suite GREEN, post-delta FAILS at line 163\\n  `assert index[\"summary\"][\"decision_parse_warnings\"] == 0`. Genuine strengthening.\\n- B5: decision_parser.ex push_parse_warning/2 Map.update/4 -\u003e Map.update!/3 — FAILED with\\n  `:erlang.map_get(\"parse_warnings\", ...)` KeyError inside Map.update!/3, in exactly one test:\\n  the new legacy-shape test at line 285. Confirms it is the only guard on that path.\\nTree restored to HEAD after mutations: yes. `git status --porcelain` empty, HEAD c6d8ba5f.\\n\\nNew-test quality (steps 3-4):\\n- Matrix two-sidedness: proven both directions. Wrongly-exempted case (B1 `or`) fails; wrongly-\\n  flagged case (carve-out removed entirely -\u003e `if false`) also fails, left listing all five ids.\\n- Distinct ids: that second mutation printed d1_absent_to_prose, d2_blank_to_prose,\\n  d3_prose_reworded, d4_prose_deleted, d5_prose_blanked as five separate findings. The rows do\\n  not collapse — the `[id: id, reverses_what: rw] ++ base` ordering fix holds against\\n  adr/1's Keyword.get.\\n- Tag honesty: six tests now carry specled.decisions.cross_field_live_gate. Four fail under the\\n  pass-through mutation (the three retagged live-path tests plus the new escape-hatch test), so\\n  they genuinely prove the wiring. Two pass under it: line 92 (the original negative-assertion\\n  test — legitimate, it is the no-false-positive half) and line 285 (the legacy-shape unit test,\\n  which calls validate_cross_fields directly and never reaches Index.build). The line-285 tag is\\n  the weakest of the six, but it pins the requirement's \"warning-severity results shall be\\n  threaded into a separate parse_warnings list\" clause and the B3 defect — the tag existing ONLY\\n  on a non-falsifying test — is closed. Note, not Gap.\\n\\nRe-run:\\n- mix test on both changed files: 2 properties, 53 tests, 0 failures.\\n- bash ./scripts/check_specs.sh: validate status=pass errors=0 warnings=0;\\n  branch findings=5 (error=0 warning=3 info=2); spec.check result=pass. The three warnings are\\n  append_only/statement_rewritten on specled.append_only.adr_affects_widened,\\n  specled.index.subject_and_decision_index, specled.verify.decision_governance — all from\\n  f7faf654, outside this delta.\\n\\nVerdict: PASS\\n\\nNotes (advisory only, nothing blocking):\\n1. B1's stated mechanism is wrong about which case `or` exempts. With\\n   `blank_field?(prior) or not blank_field?(current)`, deletion still drifts — the deleted value\\n   is blank, so `not blank_field?(current)` is false and both disjuncts are false. What `or`\\n   silently exempts is REWORDING (prose -\u003e different prose), which the failure output confirms.\\n   The hole is real and the matrix catches it; only the label \"exempts DELETING justification\"\\n   is inaccurate.\\n2. B2 overstates the pre-fix gap. The finding says the test \"asserted only report[\"status\"] ==\\n   \"fail\"\". It also asserted the parse_errors content, which the pass-through mutation breaks.\\n   True statement: the report-level assertion alone was non-falsifying, and now it is not.\\n3. B4's premise holds only under the additive reading of \"summing parse_errors into the warnings\\n   counter\". Under the substitution reading the pre-delta suite already caught it.\\n4. `mix test --only spec:\u003cid\u003e` selects only the three BARE-string-tagged tests; ExUnit's --only\\n   filter does not match the list-form `@tag spec: [...]` added by this commit. specled's own\\n   tagged_tests verification is unaffected — it runs a merged `mix test` and attributes per test\\n   through TaggedTests.Formatter, whose spec_ids/1 does List.wrap. So the retagging does reach\\n   the spec gate; it is only the ad-hoc --only invocation that misses it. Worth knowing before\\n   anyone uses --only as a proxy for requirement coverage.\\n5. The new legacy-shape test calls validate_cross_fields/2 (third arg defaulted), not /3 as the\\n   comment and the finding text say. Cosmetic.",
      "timestamp": "2026-08-14T17:34:14Z"
    },
    {
      "text": "CORRECTION to the TEST_CRITIC record (from delta verification, both confirmed by execution)\\n\\nSession: f7145a10-323f-4aae-a8f3-1c77e224d1a1\\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/f7145a10-323f-4aae-a8f3-1c77e224d1a1.jsonl\\n\\nTwo claims in the TEST_CRITIC comment and in commit c6d8ba5f's message are wrong on mechanism. The fixes are unaffected; the descriptions are not.\\n\\n1. B1. I wrote that writing maybe_drift_on_reverses_what/3 with `or` instead of `and` \"would exempt DELETING justification from an accepted ADR\". It does not. With `blank_field?(prior) or not blank_field?(current)`, a deletion has a non-blank prior and a blank current, so both disjuncts are false and it still drifts. What `or` actually exempts is REWORDING — prose to different prose — which is the case the mutation's failure output names (d3_prose_reworded goes missing from the drifted set). The hole is real and the matrix catches it; only my label for it was wrong. The deletion and blanked rows remain worth having: the requirement names deletion explicitly, and they guard other plausible mutations of the same guard.\\n\\n2. B2. I wrote that the dangling-affect test \"asserted only report status\". It also asserted the parse_errors content, and that assertion does fail under the pass-through mutation — so the pre-fix test was not fully non-falsifying, and under mutation it aborts at the parse_errors line before ever reaching the new finding assertion. The accurate claim is narrower: the report-level `status == \"fail\"` assertion alone proved nothing, because the verifier's own decision_unknown_affect check fires independently on that ADR. That is now fixed, and it is real strengthening, but smaller than I described.\\n\\nAlso worth recording from the delta run, since it sharpens B4: the warnings-counter finding has two readings, and only one was a real gap. Replacing the counter with a sum of parse_errors was already caught pre-delta by the `== 1` assertion. The ADDITIVE reading (warnings + errors) passed the pre-delta suite and fails post-delta. The strengthening is genuine, against the second reading only.",
      "timestamp": "2026-08-14T17:34:14Z"
    },
    {
      "text": "AUDIT: APPROVE\\nSession: f7145a10-323f-4aae-a8f3-1c77e224d1a1\\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/f7145a10-323f-4aae-a8f3-1c77e224d1a1.jsonl\\n\\nReasoning: Every advanced `must` has a test I read that fails if the wiring is removed, because\\nparse_file/2 seeds `\"parse_errors\" =\u003e []` and `\"parse_warnings\" =\u003e []` (decision_parser.ex:90-95)\\nand only index.ex:50's second pass can fill them — cross_field_reverses_what at\\nlive_cross_field_gate_test.exs:160, cross_field_affects_resolve at :197 (pins the finding, not just\\nreport status) and :125 (subject + requirement + scenario + repo.* all resolve, load-bearing on the\\natom-key accessor since parser.ex:59 stores %Schema.Meta{} structs), change_type_optional at\\n:241/:251, cross_field_live_gate at :259 (severities escape hatch), index.subject_and_decision_index\\nat index_state_test.exs:77 (exact map equality), verify.decision_governance on both channels at :169\\nand :248, adr_reverses_what_backfill at append_only_test.exs:672 (five-case matrix asserting the\\nexact drifted id set — two-sided). No disconnected seam: spec.check.ex:110-123 does\\n`SpecLedEx.index(root, index_opts)` then `SpecLedEx.validate(index, root, strict: true,\\nseverities: ...)`, which is exactly the pair the tests drive; `test_tags: false` cannot bypass the\\ngate because index.ex:50 runs before maybe_add_tag_data/3. Mechanical counts re-checked\\nindependently and correct: 18 files touched, 10 outside the expected set, realization_hashes.json\\nand state.json untouched (state.json is not tracked here), the four backfilled ADRs changed\\n`reverses_what` only, no dep change, no stub un-flipped, no test disabled.\\n\\nScope expansion judgment (step 6): Legitimate. The charter's SELF-GATE RISK note made \"specled's own\\ncorpus goes red on merge\" this ticket's problem, and both hazards it named turned out to be\\nunderstated (55 spurious affects_unresolved, not 5; the change_type flattening plus a fourth class\\nthe charter missed). The ticket could technically have landed by superseding the four ADRs instead,\\nso the expansion was not strictly forced — but that path adds four ceremony records and hands every\\nadopter the same wall on upgrade, the alternatives were put to the user mid-implementation, and the\\nchosen remedy arrived with a requirement, an ADR, and a falsifiable test. Not creep.\\n\\nPolicy-change audit (step 7): The carve-out holds and is not abusable as feared. `reverses_what`\\nplays no role in authorization anywhere in lib/ — authorizing_decision/3 (append_only.ex:746-775)\\nreads only `status`, `affects`, and `change_type`, and the only other readers are the state\\nserializer (specled_ex.ex:235) and the two comparators. The code matches the spec text exactly:\\nmaybe_drift_on_reverses_what/3 (append_only.ex:528) exempts only `blank_field?(prior) and not\\nblank_field?(current)`; deleted, blanked, and reworded all fall through to drift_on/4, which the\\nmatrix pins. The ADR's claim that the live gate bounds the omit-then-backfill window traces: R2\\n(cross_field.ex:104-125) now runs on the live path and errors at introduction for any weakening-set\\nADR with a blank `reverses_what:`, and blank -\u003e non-blank is the only exempt transition, so an ADR\\ncannot land blank and be quietly filled later without the gate having been red. End-to-end evidence\\nbeyond the unit test: the branch guard runs against base 2521671a, where all four ADRs had no\\n`reverses_what:`, and check_specs.sh reports error=0.\\n\\ninfo-vs-warning contradiction (step 8): Acceptable, not blocking. The two texts constrain different\\nartifacts and both are literally true of the code: change_type_optional governs the\\n`cross_field/missing_change_type` diagnostic, which CrossField still emits at `severity: :warning`\\ninto `parse_warnings` and not into `parse_errors` — its operative clause (\"rather than a parse\\nerror\") is satisfied. cross_field_live_gate governs the verifier finding `decision_cross_field_warning`,\\na different code at a different layer, and states the `info` default and its reason inline. The\\ncharter's \"warning\" prescription was self-defeating on its own terms: verifier.ex:98 is\\n`errors \u003e 0 or (strict? and warnings \u003e 0)` and spec.check.ex:117 passes `strict: true`, so warning\\nseverity would fail the gate for exactly the workspaces change_type_optional exists to protect. A\\nclarifying clause in change_type_optional would help a side-by-side reader; that is a Note.\\n\\nSpec-Drift trailer (step 10): Legitimate. specled.prose_guard claims verifier.ex in its surface, but\\nevery verifier hunk in this diff is in the decision-finding region (1583-1594, 1697-1703, 1829-1840,\\n1915-1930) and none of its prose-lint requirements move. specled.package and specled.status attach\\nvia index_state_test.exs's @moduletag (index_state_test.exs:4-13), not surface, and the single\\nchanged line there adds one summary key; package.index_and_state (\"index ADRs, write derived state\")\\nand status.decision_index (\"summarize indexed ADRs\") are both unmoved. The three subjects whose\\ncontracts did move — specled.decisions, specled.index_state, specled.verify — were updated, plus\\nspecled.append_only.\\n\\nNotes (advisory, no verdict change; both new, neither caught by the prior graders):\\n1. push_parse_warning/2's comment (decision_parser.ex:139-140) justifies Map.update/4 with\\n   \"base-tree views read by BranchCheck\". That is wrong: validate_cross_fields/3 has exactly one\\n   caller in lib/ (index.ex:50), every decision it receives came from parse_file, which seeds\\n   \"parse_warnings\" =\u003e [], and BranchCheck never calls DecisionParser at all. The defensive branch\\n   and its test at live_cross_field_gate_test.exs:290 guard a shape production cannot produce. The\\n   verifier flagged that test's tag as the weakest of the six but not the comment. Harmless — four\\n   other tests carry cross_field_live_gate falsifiably — so a comment fix, not a gap.\\n2. Latent policy split on the same transition: CrossField R5 (cross_field.ex:210) still compares\\n   `reverses_what` verbatim and would fire cross_field/adr_field_drift on exactly the backfill\\n   AppendOnly now exempts. R5 is dead today (nothing supplies :prior_decisions), but Index.build\\n   forwards opts straight through at index.ex:50, so it activates the moment any caller passes the\\n   option. specled.decisions.cross_field_adr_append_only and\\n   specled.append_only.adr_reverses_what_backfill are not literally contradictory — the latter is\\n   scoped to append_only/adr_affects_widened — but they encode opposite policy for the same edit,\\n   and specled.decision.adr_reverses_what_backfill says nothing about R5. Worth a follow-up ticket\\n   before anyone wires prior_decisions.\\n3. resolvable_ids/1 also backs R1's `replaces:` check, so a scenario id now satisfies `replaces:`.\\n   Pre-existing in kind (requirement ids already did) and the requirement text says only \"resolve in\\n   the current index\", which is true. Cosmetic laxity.",
      "timestamp": "2026-08-14T17:34:14Z"
    },
    {
      "text": "APPROVED per auditor sign-off. Branch specled_-4a6/wire-crossfield at 63a32404 (worktree /Users/mz/src/specled_ex-wire-crossfield, base 2521671a) is ready for a PR to main; held in_progress for the ship arc (/open-pr -\u003e CI -\u003e merge -\u003e deploy). /deliver closes it.\n\nSession: f7145a10-323f-4aae-a8f3-1c77e224d1a1\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/f7145a10-323f-4aae-a8f3-1c77e224d1a1.jsonl\n\nPipeline: IMPLEMENTED (f7faf654) -\u003e VERIFY PASS -\u003e TEST_CRITIC FIXED iteration 1 (c6d8ba5f) -\u003e VERIFY PASS (delta) -\u003e AUDIT APPROVE. No bounces.\n\nPost-audit change: 63a32404, comment-only. The auditor's Note 1 was right — push_parse_warning/2's comment claimed the tolerant Map.update/4 exists for \"base-tree views read by BranchCheck\", and BranchCheck never calls DecisionParser at all. Corrected to the real reason (validate_cross_fields/3 is public API, so an out-of-repo caller can supply its own decision map). Immaterial by the audit's own materiality rule: no behavior and no gate verdict depends on it. Re-ran the two affected test files (77 tests, 0 failures), mix format --check-formatted, and bash ./scripts/check_specs.sh =\u003e validate status=pass errors=0 warnings=0, spec.check result=pass.\n\nAuditor Note 2 filed as https://github.com/iautom8things/specled_ex/blob/beadwork/issues/specled_-eta.json — CrossField R5 compares reverses_what verbatim and encodes the opposite policy to the carve-out this ticket added. Dead today (nothing supplies :prior_decisions) but Index.build/2 now forwards opts straight through, so it activates the moment any caller passes the option. Standalone, not parented: it must not block this PR, and it needs deciding before anyone wires prior_decisions.\n\nAuditor Note 3 (a scenario id now satisfies R1's `replaces:` check) left as-is: pre-existing in kind — requirement ids already did — and the requirement text says only \"resolve in the current index\", which remains true.\n\nNot filed as tickets, deliberately: the terminology tension in change_type_optional (\"warning-level diagnostic\" vs the info-severity verifier finding) is reconciled by specled.decision.live_cross_field_gate and both texts are literally true of the code; and the R1/R3 live-gate coverage gap, which no requirement demands and nothing triggers.\n\nMerge gates at 63a32404: mix compile --warnings-as-errors clean, mix format --check-formatted clean, mix deps.unlock --check-unused clean, make xref at 0 compile-connected edges, bash ./scripts/check_specs.sh =\u003e spec.check result=pass. Full suite deferred to PR CI — no tier in this pipeline ran it, by design.",
      "timestamp": "2026-08-14T17:34:57Z"
    },
    {
      "text": "PR opened: https://github.com/iautom8things/specled_ex/pull/39 (release 0.17.0, branch feat/specled_-4a6-wire-crossfield at 466ad3e9). Babysitting CI.\n\nTier was a user decision: minor rather than patch. The commit histogram is all fix/test/docs with no BREAKING marker, which reads as patch, but activating a previously-dead gate can turn an adopter's mix spec.check red on upgrade — and repo precedent is 0.16.0, itself a fix(...)-typed release that took a minor bump for the adopter-visible Earmark/MDEx swap.\n\nCritical review ran fresh on 2026-08-17 against 63a32404 (5 of 6 personas landed; the performance reviewer and the consolidator were lost to an account weekly limit, and simplicity's measured L2 covered the performance persona's primary target). Reports:\n/Users/mz/vaults/rmbrs-vault/code_review/critical/specled_ex/2026-08-17/specled_-4a6-wire-crossfield/\n\nRaw: 0 critical, 6 high, 8 medium, 11 low. Two consensus findings, both fixed on-branch in bb13bcfe:\n- to_string/1 on unvalidated ADR frontmatter aborted the whole gate (security H1 + idioms H1, both reproduced end to end). Wiring CrossField live is what made it reachable, and blank_field?/2 added a second instance. Fixed at four sites, plus a FIFTH the reviewers missed that only the new integration test found: Verifier.add_decision_affects_findings/6 interpolated a raw affect value.\n- The R5 vs backfill policy split (security M1 + spec H1 + testing H1, proved by a probe driving Index.build with prior_decisions). Already filed as specled_-eta; the ADR now states it instead of implying the opposite.\n\nThe spec audit found three FALSE claims in this branch's own governance prose, all mine, all corrected in bb13bcfe:\n1. parser_resilient_errors_split's backfilled reverses_what claimed a statement narrowing that never happened — parser.spec.md still asserts all three failure modes and git log -L shows it was only ever widened. The real reversal (scenario coverage 2-\u003e1) was in the ADR body I wrote the backfill from. A false justification is worse than the absent one it replaced: a blank field is visibly missing, false prose reads as verified.\n2. cross_field_live_gate and its ADR claimed R1-R6 gate spec.check. R5 needs prior_decisions that Index.build never supplies, and there is no R6 rule at all — it is a branch inside R4.\n3. The ADR claimed the two affect-resolution paths agree; CrossField also resolves decision ids, which the verifier does not.\n\nAlso corrected: change_type_optional now says what warning-level means at the operator boundary (the ticket's own hazard note read that phrase and derived finding(\"warning\", ...) from it, so a careful reader had already mis-implemented it); index_state no longer pins 'two passes' as a must on a stable requirement.\n\nThe spec reviewer's sequencing point drove doing all of this pre-merge: affects is compared verbatim and only reverses_what got a carve-out, so correcting either post-merge costs an adr_affects_widened error or a supersession. This branch had one commit of freedom left on its own governance records.\n\nVerification at 466ad3e9: mix test --include integration --cover = 1393 tests 0 failures (CI parity), make smoke green, make xref 0 edges, format/deps clean, scripts/check_specs.sh =\u003e spec.check result=pass.\n\nCAVEAT recorded in the PR body: the four implementation commits carry cold verification + audit sign-off, but bb13bcfe (~300 insertions across 5 lib files) was verified only by me — the grader agents hit the account weekly limit (resets 2026-08-18 22:00 ET) before they could re-read it. User chose to open now and let CI plus review bots cover that delta rather than park green work for a day.",
      "timestamp": "2026-08-17T10:11:32Z"
    },
    {
      "text": "CI green on the first run, no fix commits needed. All three checks passed on https://github.com/iautom8things/specled_ex/pull/39 at 466ad3e9:\n- Check Specs — pass (2m27s)\n- Test (Elixir 1.19.5 / OTP 28.3.1) — pass (2m4s)\n- Test (Elixir 1.18.4 / OTP 26.2.5) — pass (2m7s)\n\nThe OTP-26 leg is the one that could not be reproduced locally (it exercises the classic coverage path, since :code.coverage_support/0 is false below OTP 27). It passed, so the local pre-flight's blind spot is now covered.\n\nmergeable=MERGEABLE, mergeStateStatus=CLEAN, no conflicts.\n\nNOTE for the record: zero reviews and zero comments after a 4-minute bounded wait, and PRs #1 and #2 also carry zero reviews — this repo has no automatic review bot configured. That matters because the decision to open now rather than wait for the grader limit to reset rested partly on review-bot coverage substituting for the unavailable cold read of bb13bcfe. That premise was wrong, so the ~300-insertion review-fix commit still has no independent read. Surfaced to the user rather than merged on a premise now known to be false.",
      "timestamp": "2026-08-17T10:19:01Z"
    }
  ],
  "priority": 2,
  "status": "in_progress",
  "title": "Wire CrossField.validate/3 into the live decision-parse path (R4/R6 exercised only in tests)",
  "type": "task",
  "updated_at": "2026-08-17T10:19:01Z"
}
