{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-07-26T23:50:46Z",
  "description": "Advances: specled.verification\n\nDESCOPED OUT OF specled_-ymn, 2026-07-26, by orchestrator decision during epic specled_-233. This is the F2/CF2 item from the specled_-hyt pre-ship critical review (L severity, consensus idioms+simplicity, recorded on ymn as 'worth folding in'). It was implemented in ymn iteration 1 and then deliberately REVERTED in iteration 3. Recording it here so the reduction is explicit rather than silent.\n\nTHE ITEM. The cross-VM temp-name rationale is re-narrated at three sites in lib/specled_ex/verifier.ex — :2174 (the streaming attribution sidecar), :2304-2305 (the command temp script), :2404-2405 (the forensic capture log). The ask was to trim each to a one-line pointer at SpecLedEx.TempName so the moduledoc is the single narrative. A fourth narration at lib/specled_ex/compiler/tracer.ex:339 is LOAD-BEARING and must be kept: it explains why the tracer cannot call TempName at all (Mix prunes undeclared deps from a host project's code path), in a module where that constraint is invisible.\n\nWHY IT WAS REVERTED, not abandoned as wrong. Editing verifier.ex drags specled.prose_guard into the branch-guard impacted set, because verifier.ex sits on prose_guard's surface (prose_guard.spec.md:19) even though prose guarding has nothing to do with temp names. prose_guard binds no code in that file, so no realization-attestation downgrade is available — unlike specled.decisions, which downgrades to INFO via SpecLedEx.Verifier.verify_decision/4. That left exactly two ways to green mix spec.check:\n  (a) co-change prose_guard.spec.md and/or add the cross_vm_temp_names_reach ADR to its decisions: list — which asserts that a temp-naming decision governs prose guarding. That is FALSE, and it is the same over-claim ymn iteration 1 was bounced for. Rejected.\n  (b) revert verifier.ex and drop the comment trim. Chosen.\nSo an L-severity comment-tidying item was traded away rather than buying it with a false spec-graph assertion. That is the right trade, but it is a real scope reduction and the item is genuinely still undone.\n\nDOING IT LATER IS CHEAP BUT NOT FREE. Whoever picks this up hits the same wall: any verifier.ex edit re-opens branch_guard_missing_subject_update against specled.prose_guard. Options, none of which ymn was scoped to take:\n  - Bundle it with work that legitimately advances specled.prose_guard, so the co-change is honest.\n  - Fix the root cause instead: prose_guard's surface list claims a 3800-line general-purpose module because SpecLedEx.Verifier happens to host prose-finding emission. If prose emission moved to its own module, or if surface membership could be narrowed below whole-file granularity, this class of false coupling disappears for every future ticket. That is the more valuable ticket and is worth considering before doing the cosmetic trim.\n  - Accept the co-change as mechanically required bookkeeping with no semantic claim, if the project decides a decisions: link is not an assertion of governance. That is a policy call, not an implementation one.\n\nSTATE AT REVERT. ymn iteration 1's version of the trim was reviewed and found ACCEPTABLE by cold audit pass 1, which overturned a verifier Gap claiming the trim lost rationale: the exit-127 command-script story survives as accepted normative text at .spec/specs/verification.spec.md:84-92 (specled.verify.command_temp_names_cross_vm_unique, priority: must), and the sidecar cover_not_executed story at .spec/specs/tagged_tests_verification.spec.md:279-288. Only the shared-capture-dir forensic-log rationale is thinner in spec text — specled.verify.command_output_capture_dir says only 'a uniquely named file'. If this trim is redone, consider strengthening that one requirement in the same pass so the pointer target is complete.\n\nNot urgent. Cosmetic. No behavioral or verification consequence.",
  "id": "specled_-chb",
  "labels": [
    "phase:merging"
  ],
  "parent": "specled_-k6s",
  "comments": [
    {
      "text": "ADMISSION VERIFICATION (specled_-k6s, 2026-07-27)\nCold fresh-context verifier run against the epic base tree (feat/specled_-td7-self-identifying-descriptors @ e81f9f98).\n\nVERDICT: still_applicable   EFFORT: trivial\nMandate confirmed still applicable as written; no narrowing needed.\n\n== SCOPED REMAINING WORK ==\n1. lib/specled_ex/verifier.ex:2172-2174 — replace the 3-line comment above `artifact_path = …` in `run_merged_command/5` with a one-line pointer, e.g. `# Cross-VM-unique name — see SpecLedEx.TempName.`\n2. lib/specled_ex/verifier.ex:2306-2308 — same trim for the comment above `tmp_out = …` in `run_command/4`.\n3. lib/specled_ex/verifier.ex:2424-2426 — same trim for the comment above `path = Path.join(dir, …)` in `preserve_failed_output/4`.\n4. DO NOT touch lib/specled_ex/compiler/tracer.ex:335-341 — that narration is load-bearing (explains why the tracer cannot call TempName at all) and the ticket explicitly exempts it.\n5. OPTIONAL, recommended in the same pass (makes the pointer target complete for site 3): in .spec/specs/verification.spec.md, requirement `specled.verify.command_output_capture_dir` (statement at :138-149), replace \"a uniquely named file in that directory\" with a clause requiring cross-VM uniqueness via `SpecLedEx.TempName.cross_vm_suffix/0`, mirroring the wording already accepted at :87-91 for `specled.verify.command_temp_names_cross_vm_unique`. If you do this, verification.spec.md IS co-changed, which additionally removes any branch-guard question for specled.verification.\n6. No new tests are needed — nothing in the repo asserts on these comment strings (`grep -rn \"Cross-VM-unique name\"` outside verifier.ex returns nothing).\n7. Ensure at least one commit in the range keeps the `Spec-Drift: branch_guard_missing_subject_update=info` trailer already used by every commit on this branch, so the prose_guard INFO stays INFO.\n\n== EXPECTED TOUCHES ==\nlib/specled_ex/verifier.ex, .spec/specs/verification.spec.md\n\n== RISK NOTES ==\nREALIZATION HASH: no risk. .spec/realization_hashes.json contains only the `api_boundary` tier (172 entries; `python3` walk of the file shows no `implementation` key), and the only SpecLedEx.Verifier entries are `SpecLedEx.Verifier.verify/3` and `SpecLedEx.Verifier.verify_decision/4`. All three comments sit inside private functions (`run_merged_command/5`, `run_command/4`, `preserve_failed_output/4`) that are not bound, and lib/specled_ex/realization/api_boundary.ex:8-9 documents the api_boundary hash as stable under \"line-number shifts of the function's source position — including edits above it\". Deleting 6 comment lines cannot move a baselined hash.\n\nBRANCH GUARD: editing verifier.ex pulls specled.prose_guard (prose_guard.spec.md:19 surface) and specled.decisions into the impacted set. On this branch both already resolve to INFO — decisions via realization attestation of `SpecLedEx.Verifier.verify_decision/4`, prose_guard via the `Spec-Drift: branch_guard_missing_subject_update=info` trailer present on all 9 commits. The ticket's claim that this is a hard blocker requiring a false decisions: link is WRONG in this tree; do not let an implementer add specled.decision.cross_vm_temp_names_reach to prose_guard.spec.md's decisions: list to \"fix\" it. Since verifier.ex is ALREADY modified by td7 in this PR, the marginal branch-guard cost of this ticket is exactly zero.\n\nSPEC REQUIREMENT OVERLAP: item 5 edits `specled.verify.command_output_capture_dir` (verification.spec.md:137-149). specled_-4f7 is scoped to the same requirement's arming path (its own header: \"the forensics behavior it feeds is specled.verify.command_output_capture_dir\") but 4f7's remaining work per its 2026-07-27 comment is confined to .claude/settings.json distribution, not the requirement text — low collision risk, but sequence 4f7 and chb rather than running them in parallel if item 5 is taken. specled_-zek also advances specled.verification and will edit verification.spec.md; specled_-ehv edits .spec/decisions/specled.decision.cross_vm_temp_names*.md on the identical cross-VM topic and its description explicitly cross-references chb — land ehv's ADR wording decision BEFORE chb's pointer trim if you want the pointer to aim at final text (soft ordering preference, not a hard dependency).\n\nSAME-FILE, DIFFERENT-REGION: specled_-14t edits lib/specled_ex/verifier.ex at `valid_decision_affect?/4` (:2095, plus call sites :1723 and :1921); specled_-4a6 may consolidate verifier.ex decision-parse logic. Neither overlaps lines 2172/2306/2424, but all three land in one PR so expect merge-order churn on verifier.ex line numbers — apply chb's trim by anchor text, not by line number.\n\nGATES: `mix spec.check --no-run-commands` currently passes on this tree (verdict `spec.check result=pass`, errors=0). This change is comment-only and cannot affect test outcomes.\n",
      "timestamp": "2026-07-27T08:37:06Z"
    },
    {
      "text": "SCOPE CONTRACT (orchestrator, specled_-k6s — 2026-07-27)\n\nAuthoritative for mechanical scope checks. Supplements, does not replace, the\nADMISSION VERIFICATION comment's mandate (items 1-7), which is what you implement.\n\nSEQUENCING NOTE: specled_-ehv has already landed its wording decision in\n`.spec/decisions/specled.decision.cross_vm_temp_names_reach.md`. Read that ADR\non your base BEFORE trimming, so your one-line pointers aim at final text.\n\n## Allowed touches\n- `lib/specled_ex/verifier.ex` — the three comment blocks ONLY (above\n  `artifact_path = …` in `run_merged_command/5`, above `tmp_out = …` in\n  `run_command/4`, and above `path = Path.join(dir, …)` in\n  `preserve_failed_output/4`). Apply by ANCHOR TEXT, not by line number —\n  other tickets in this PR shift this file's line numbers.\n- `.spec/specs/verification.spec.md` — item 5 only: the\n  `specled.verify.command_output_capture_dir` statement. Spec co-change is\n  AUTHORIZED and is recommended here; it also settles the branch-guard question\n  for `specled.verification`.\n\n## Out of Scope (files)\n- `lib/specled_ex/compiler/tracer.ex` (~:335-341) — item 4 is explicit: that\n  narration is LOAD-BEARING (it explains why the tracer cannot call TempName at\n  all) and the ticket exempts it. Do not touch it.\n- `.spec/specs/prose_guard.spec.md` — the ticket body claims a `decisions:` link\n  is needed there. THE ADMISSION PASS PROVED THAT WRONG in this tree. Do NOT add\n  `specled.decision.cross_vm_temp_names_reach` to prose_guard's `decisions:` list.\n  A false spec-graph assertion to quiet a guard is an automatic bounce.\n- `.spec/decisions/specled.decision.cross_vm_temp_names_reach.md` — specled_-ehv\n  owns it. Point AT it; do not edit it.\n- `CHANGELOG.md` (orchestrator consolidates once at the end), `VERSION`,\n  `.spec/realization_hashes.json`, `.spec/state.json`.\n- Every file not listed under Allowed touches.\n\n## Out of Scope (intent)\n- No behavior change whatsoever. This is a comment trim plus one optional\n  requirement-wording tightening. No new tests are needed — nothing in the repo\n  asserts on these comment strings.\n- Do not restructure, rename, or refactor the three private functions.\n- `.spec/specs/verification.spec.md` is append-only-checked: item 5 TIGHTENS the\n  statement (adds a cross-VM-uniqueness clause mirroring the accepted wording at\n  `specled.verify.command_temp_names_cross_vm_unique`). Do not delete or weaken it.\n- No new dependencies.\n\n## Verification (change-scoped, run-until-green; host mix, no compose)\n1. `mix compile --warnings-as-errors`\n2. `mix format --check-formatted`\n3. `mix spec.check --no-run-commands` — expect `spec.check result=pass`.\n4. `git diff --stat -- .spec/realization_hashes.json` must be EMPTY.\n5. Your commit MUST carry the `Spec-Drift: branch_guard_missing_subject_update=info`\n   trailer, as every commit on this branch does, so the prose_guard finding stays INFO.\n\nDo NOT run bare `mix spec.check` and do NOT run the full suite. The orchestrator\nowns whole-system proof at the merge gate and PR CI.",
      "timestamp": "2026-07-27T10:20:26Z"
    },
    {
      "text": "IMPLEMENTED\nSession: k6s-chb-impl-1\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-chb/grok-k6s-chb-impl-1/log\n\nCommit: 1c4b996ce90226f0db1128355ab910c5cbe55148\nFiles touched: lib/specled_ex/verifier.ex, .spec/specs/verification.spec.md\nTests added: none (comment-only + requirement wording; nothing asserts on these comment strings)\nVerification stubs flipped: none\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix format --check-formatted; mix spec.check --no-run-commands (result=pass); realization_hashes.json empty diff; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\nEpic lessons checked against diff: stale-spec (pointer aims at TempName moduledoc + ADR already landed by ehv; capture_dir statement matches code via cross_vm_suffix); untested-clause N/A (no branch flag); format gate green; completion on correct ticket.\n\nSummary: Trimmed the three re-narrated cross-VM rationale comments in Verifier (run_merged_command, run_command, preserve_failed_output) to one-line pointers at SpecLedEx.TempName. Tightened specled.verify.command_output_capture_dir to require cross-VM uniqueness via TempName.cross_vm_suffix/0 so the forensic-log pointer target is complete. Tracer exception narration and prose_guard decisions list left untouched; Spec-Drift trailer retained.",
      "timestamp": "2026-07-27T12:09:32Z"
    },
    {
      "text": "VERIFY: PASS\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nTicket: specled_-chb. Base: 9af2ed86 (git merge-base HEAD epic/specled_-k6s-0.11.0-fast-follow). Head: 1c4b996c.\n\nMechanical checks:\n- Files touched: 2 (lib/specled_ex/verifier.ex, .spec/specs/verification.spec.md)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0. Verified absent from `git diff --name-only 9af2ed86..HEAD`: lib/specled_ex/compiler/tracer.ex, .spec/specs/prose_guard.spec.md, .spec/decisions/specled.decision.cross_vm_temp_names_reach.md, CHANGELOG.md, VERSION, .spec/realization_hashes.json, .spec/state.json. In particular prose_guard.spec.md is untouched — no false `decisions:` link was added to quiet the branch guard.\n\nSemantic checks:\n\n- Spec stubs run: 1, passed: 1. The `tagged_tests` leg that `SpecLedEx.TaggedTests.build_command/2` resolves for `specled.verify.command_output_capture_dir` is `mix test --include integration \u003cformatter flags\u003e test/specled_ex/verifier_test.exs` (tag_map entries at verifier_test.exs:1763, :1974, :2275 all resolve to that one file). Executed: 58 tests, 0 failures, 50.2s. The other tagged_tests legs (tag_findings_test.exs, spec_tasks_test.exs, spec_check_test.exs, verification_strength_test.exs) were NOT run — targeted scope, and none of their files changed in this diff.\n\n- Comment-only claim (step 5): CONFIRMED by execution, two independent ways.\n  1. `git diff 9af2ed86..HEAD -- lib/specled_ex/verifier.ex | grep -E '^[+-]' | grep -vE '^(\\+\\+\\+|---)' | grep -vE '^[+-][[:space:]]*#'` → EMPTY. Every added and removed line in the file is a comment line.\n  2. Comment-stripped equivalence: `git show 9af2ed86:lib/specled_ex/verifier.ex | grep -vE '^\\s*#'` vs the same filter on HEAD → `diff` reports IDENTICAL. No expression, guard, function head, arity, clause order, or blank-line structure changed.\n  Diff shape: 3 hunks, 9 comment lines removed, 3 added (stat `12 +++---------`). The three anchors are exactly the mandated ones — above `artifact_path = …` in `run_merged_command/5` (verifier.ex:2172), above `tmp_out = …` in `run_command/4` (:2304), above `path = Path.join(dir, …)` in `preserve_failed_output/4` (:2420). Applied by anchor text; the surrounding call expressions are byte-identical to base.\n\n- Realization hash: `git diff --stat 9af2ed86..HEAD -- .spec/realization_hashes.json` is EMPTY (y). `mix spec.check --no-run-commands --verbose` reports NO realization-drift finding (y = no drift). Independent confirmation that the transitive `implementation`-tier concern does not bite here: the baseline file contains ONLY the `api_boundary` tier (`\"implementation\"` key absent from the JSON; `\"api_boundary\"` present), and its only Verifier entries are `SpecLedEx.Verifier.verify/3` and `SpecLedEx.Verifier.verify_decision/4`. Positive live evidence rather than inference: the branch-guard output attests `SpecLedEx.Verifier.verify_decision/4` as CLEAN on this tree —\n  `[INFO] branch_guard_missing_subject_update lib/specled_ex/verifier.ex :: … impacts subject specled.decisions; spec.md not co-changed, but realization attested binding(s) SpecLedEx.Verifier.verify_decision/4 as clean — informational only.`\n  i.e. the hash was recomputed against the edited file this run and still matched. Claim verified by execution, not accepted on the implementer's framing.\n\n- Requirements coverage — all 22 `must` requirements in specled.verification, individually. Shared seam note: this diff changes ZERO executable bytes in lib/specled_ex/verifier.ex (proven above), so for requirements 1-13 and 15-22 the only seam this diff could act through is the SPEC TEXT, and it edits exactly one requirement's statement (#14). Each is still enumerated on its own terms below.\n  - specled.verify.meta_required: covered by test/specled_ex/verifier_test.exs:42 (moduletag) + test/specled_ex/verification_strength_test.exs:12. Would fail if `verify/3` stopped emitting a finding for a subject whose spec-meta omits a required field. Seams: none touched by this diff.\n  - specled.verify.reference_checks: covered by test/specled_ex/verifier_test.exs:43. Would fail if a dangling `covers:`/`decisions:` reference produced no finding. Seams: none.\n  - specled.verify.coverage_warnings: covered by test/specled_ex/verifier_test.exs:39. Would fail if an uncovered requirement stopped producing a coverage warning. Seams: none.\n  - specled.verify.malformed_entries_nonfatal: covered by test/specled_ex/verifier_test.exs:41. Would fail if a malformed requirement/verification entry raised instead of degrading to a finding. Seams: none.\n  - specled.verify.decision_governance: covered by test/specled_ex/verifier_test.exs:40; implementation binding `SpecLedEx.Verifier.verify_decision/4`. Would fail if a weakening decision stopped being gated. Seams: `verify_decision/4` is realization-bound and lives in the edited file — verified clean above (attested, hash unmoved).\n  - specled.verify.strength_semantics: covered by test/specled_ex/verification_strength_test.exs:14. Would fail if `meets_minimum?/2` accepted a level below the configured floor. Seams: none.\n  - specled.verify.command_execution_resilience: covered by test/specled_ex/verifier_test.exs:35. Would fail if a spawn/exec error crashed verification instead of degrading to a finding. Seams: none.\n  - specled.verify.command_temp_names_cross_vm_unique: covered by test/specled_ex/verifier_test.exs:976-977 — `@tag spec: \"specled.verify.command_temp_names_cross_vm_unique\"`, test \"command temp script names embed the OS pid and random entropy\". Would fail if `run_command/4`'s `tmp_out` name dropped `SpecLedEx.TempName.cross_vm_suffix/0` back to `System.unique_integer/1`. Seams: `SpecLedEx.TempName.cross_vm_suffix/0`; the trimmed comment at verifier.ex:2304 sits directly above this exact expression, which is unchanged. RAN, passed.\n  - specled.verify.command_output_via_tempfile: covered by test/specled_ex/verifier_test.exs:37. Would fail if output were read from a pipe at EOF rather than a temp file + exit status. Seams: none. RAN, passed.\n  - specled.verify.command_timeout_enforced: covered by test/specled_ex/verifier_test.exs:1088. Would fail if a timed-out command were recorded as an ordinary non-zero failure. Seams: none. RAN, passed.\n  - specled.verify.command_timeout_distinct_finding: covered by test/specled_ex/verifier_test.exs:1043 and :1346. Would fail if the timeout emitted `verification_command_failed`, or dropped the budget / config key / `--command-timeout-ms` hint from the message. Seams: none. RAN, passed.\n  - specled.verify.command_timeout_cli_precedence: covered by test/mix/tasks/spec_check_test.exs:250 and test/specled_ex/mix/tasks/spec_validate_test.exs:165. Would fail if `--command-timeout-ms` stopped overriding `.spec/config.yml`. Seams: none — its files are unchanged by this diff; NOT run (targeted scope).\n  - specled.verify.command_exit_code_recorded: covered by test/specled_ex/verifier_test.exs:36. Would fail if the exact spawned exit code were not recorded or a non-zero code did not mark the verification failed. Seams: none. RAN, passed.\n  - specled.verify.command_output_capture_dir: covered by test/specled_ex/verifier_test.exs:1974 (\"a failing command's full output is captured to SPECLED_COMMAND_OUTPUT_DIR\"), :2275 (\"passing commands write no capture and an unwritable dir leaves the result unchanged\"), and :1763 (shared-fate timeout capture records `timed_out`). Would fail if a failing/timed-out command wrote no capture file, if a passing command wrote one, if an unwritable dir altered the result or failed silently, or if the capture were written after temp-file cleanup. Seams: `SPECLED_COMMAND_OUTPUT_DIR` env var; `SpecLedEx.TempName.cross_vm_suffix/0`; `preserve_failed_output/4` ordering ahead of the `File.rm` block. THIS is the requirement whose statement the diff tightens. RAN, passed.\n  - specled.verify.command_capture_run_provenance: covered by test/specled_ex/verifier_test.exs:2019, :2074, :2117. Would fail if the capture omitted HEAD / the dirty list, or exceeded the bound without stating the omitted count. Seams: `@provenance_dirty_limit 50` (verifier.ex:2467, unchanged). RAN, passed.\n  - specled.verify.command_findings_echo_exunit_seed: covered by test/specled_ex/verifier_test.exs:1863. Would fail if a failure/timeout finding for a run whose output carried `Running ExUnit with seed: N` omitted the seed or the reproduce hint. Seams: none. RAN, passed.\n  - specled.verify.requirement_without_test_tag: covered by test/specled_ex/tag_findings_test.exs:15 and :52. Would fail if a `must` covered by `tagged_tests` with no backing `@tag spec:` produced no finding. Seams: none — tag_findings_test.exs and lib/specled_ex/tag_findings.ex are unchanged; NOT run (targeted scope).\n  - specled.verify.verification_cover_untagged: covered by test/specled_ex/tag_findings_test.exs:89. Would fail if a `covers:` id with no tagged test stopped being flagged. Seams: none; NOT run (targeted scope).\n  - specled.verify.tag_scan_parse_error: covered by test/specled_ex/tag_findings_test.exs:131. Would fail if an unparsable test file raised instead of producing a scan-error finding. Seams: none; NOT run (targeted scope).\n  - specled.verify.tag_findings_respect_enforcement: covered by test/specled_ex/tag_findings_test.exs:181. Would fail if tag findings ignored the configured enforcement level. Seams: none; NOT run (targeted scope).\n  - specled.verify.tag_findings_suppressed_when_disabled: covered by test/specled_ex/tag_findings_test.exs:218. Would fail if tag findings still emitted with enforcement disabled. Seams: none; NOT run (targeted scope).\n  - specled.verify.finding_severity_overrides: covered by test/mix/tasks/spec_tasks_test.exs:833. Would fail if a configured severity override did not change a finding's emitted severity. Seams: none; NOT run (targeted scope).\n\n- Intent-level scope concerns: none.\n  - No behavior change: proven by the comment-strip byte-identity above, not asserted.\n  - No new tests needed: VERIFIED by repo-wide grep rather than assumed. `grep -rn \"Cross-VM-unique name\" --include='*.ex' --include='*.exs' --include='*.md' .` (deps excluded) returns exactly three hits, all three being the new pointer comments themselves at verifier.ex:2172, :2304, :2420. Nothing in test/ or docs asserts on these comment strings.\n  - No new deps: mix.exs unchanged (not in the changed-file list); `mix deps.unlock --check-unused` not needed since neither mix.exs nor mix.lock is touched.\n  - append_only / must_downgraded: the verification.spec.md edit TIGHTENS in place. Requirement id `specled.verify.command_output_capture_dir` retained, `priority: must` retained, `stability: evolving` retained (both are diff CONTEXT lines, not changed lines). No requirement deleted, no `must` downgraded, no clause dropped — every original obligation (persist output/target/exit code/timeout state; create dir; before cleanup; passing writes nothing; best-effort with one-line stderr warning; unset/blank → no capture) survives verbatim, with the uniqueness clause strengthened from \"a uniquely named file\" to \"a cross-VM-unique file … (OS pid plus random entropy, via the shared SpecLedEx.TempName.cross_vm_suffix/0 helper …)\". `validate status=pass errors=0 warnings=0` confirms the append_only/must_downgraded `:error` checks accept it.\n  - The three private functions are not restructured, renamed, or refactored.\n\n- Targeted verification: PASS.\n  - `mix compile --warnings-as-errors` → exit 0.\n  - `mix format --check-formatted` → exit 0.\n  - `mix spec.check --no-run-commands` → `validate status=pass errors=0 warnings=0`; `branch base=origin/main changed_files=23 findings=11 (error=0 warning=0 info=11)`; verdict line `spec.check result=pass`.\n  - `git diff --stat 9af2ed86..HEAD -- .spec/realization_hashes.json` → empty.\n  - `mix test --include integration test/specled_ex/verifier_test.exs` → 58 tests, 0 failures.\n  - Bare `mix spec.check` NOT run and full suite NOT run, per the scope contract.\n  - Spec-Drift trailer present: YES. `git log -1 --format=full 1c4b996c` shows `Spec-Drift: branch_guard_missing_subject_update=info` in the commit message body. Effect confirmed downstream: the prose_guard finding is INFO, not an error —\n    `[INFO] branch_guard_missing_subject_update lib/specled_ex/verifier.ex :: Changed file lib/specled_ex/verifier.ex impacts subject specs that were not updated: specled.prose_guard`\n    with `error=0 warning=0` overall. specled.verification itself raises no branch-guard finding at all, because verification.spec.md IS co-changed — item 5 settled that as predicted.\n\n- Pointer-target completeness (step 7): COMPLETE at all three sites. Checked by reading the target, not by trusting the pointer.\n  `SpecLedEx.TempName`'s moduledoc (lib/specled_ex/temp_name.ex:2-14) carries the shared hazard in full: `System.unique_integer/1` is VM-local; \"Nested or parallel specled runs share tmp locations, so a VM-local counter lets one run's cleanup delete, truncate, overwrite, or misparse another run's in-flight temp file\", and it ENUMERATES the surfaces by name — \"command scripts, streaming attribution artifacts, review/base-view/spec-diff parse inputs, command-output captures, and write-rename siblings\" — which covers all three trimmed sites explicitly. It also carries the construction rationale the comments never had: \"The OS pid separates concurrently live VMs; the random suffix covers pid reuse against stale same-name leftovers.\" `cross_vm_suffix/0` (temp_name.ex:16-20) is `\"#{System.pid()}_#{Base.encode16(:crypto.strong_rand_bytes(6), case: :lower)}\"`, i.e. literally OS pid + 6 bytes CSPRNG.\n  Per-site accounting of what the trim dropped and where it now lives:\n  - Site 1, streaming attribution sidecar (verifier.ex:2172). Dropped consequence: \"a completed run then mass-reports cover_not_executed.\" Survives as accepted normative spec text at .spec/specs/tagged_tests_verification.spec.md:312-321, `specled.tagged_tests.attribution_artifact_name_cross_vm_unique` (priority: must, stability: stable): \"A VM-local counter would let a nested or parallel specled run sharing tmp_dir delete or truncate this run's in-flight artifact, silently degrading a completed merged run to shared fate with mass `cover_not_executed` warnings.\" Nothing lost.\n  - Site 2, command temp script (verifier.ex:2304). Dropped consequence: \"exit 127 blamed on an innocent subject.\" Survives at .spec/specs/verification.spec.md:85-92, `specled.verify.command_temp_names_cross_vm_unique` (must/stable): \"…so one specled run's cleanup can never delete another run's in-flight temp scripts and misattribute the resulting failure to an innocent subject.\" The literal exit code 127 is illustrative and also survives in the test's own comment at verifier_test.exs:974-975. Nothing load-bearing lost.\n  - Site 3, forensic capture log (verifier.ex:2420). Dropped consequence: \"concurrent specled runs may share a capture dir, and a per-VM-only unique name could overwrite another run's forensic log.\" This was the ONE genuinely thin target the ticket flagged, and item 5 fixed it in the same pass: the tightened statement now says \"…so CI can upload the capture as an artifact and concurrent runs that share a capture dir cannot overwrite each other's forensic logs.\" Byte-for-byte the dropped rationale, promoted from comment to normative `must`. This is the specific risk of the ticket and it is closed rather than deferred.\n  Is the tightened `command_output_capture_dir` statement TRUE of the code? TRACED, yes. `preserve_failed_output/4` (verifier.ex:2414-2449): reads `SPECLED_COMMAND_OUTPUT_DIR` (:2415); gates on `is_binary(dir) and dir != \"\" and (result.timed_out or result.exit_code != 0)` — so passing commands write nothing and unset/blank is a no-op (:2417); `File.mkdir_p!(dir)` creates the directory (:2418); `path = Path.join(dir, \"specled_cmd_#{SpecLedEx.TempName.cross_vm_suffix()}.log\")` (:2421) — the capture dir DOES use `cross_vm_suffix/0`, which is pid + CSPRNG entropy, exactly as the new wording claims; the file records command target, exit_code, timed_out, timeout_ms, provenance, and full output (:2423-2431); best-effort via `rescue e in File.Error -\u003e` emitting one stderr line and returning `:ok` (:2437-2448). Ordering: the call at verifier.ex:2379 precedes the cleanup block at :2382-2385 (`File.rm(tmp_out)` / `tmp_script` / `tmp_target` / `tmp_pgid`), so \"before temp-file cleanup\" holds. Every clause of the statement is true of the code.\n  ADR alignment: .spec/decisions/specled.decision.cross_vm_temp_names_reach.md (ehv's landed version, status accepted, 2026-07-26) names `SPECLED_COMMAND_OUTPUT_DIR` captures among the enumerated surfaces (:43-45, :95-97) and states in Consequences (:107-108) exactly the structure this ticket implements: \"`SpecLedEx.TempName` moduledoc carries the shared rationale for the enumerated surfaces; each call site names the helper.\" The trim is not merely permitted by the ADR — it is what the ADR prescribes. The ADR's tracer carve-out (:84-90) also confirms item 4's exemption; tracer.ex is untouched.\n\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope, if any: none\n\nVerdict: PASS\n\nNotes (advisory only, no action required):\n1. The pointers say \"see SpecLedEx.TempName\", which lands a reader in the moduledoc — the SHARED hazard plus the pid/entropy rationale. The subject-specific consequences (mass `cover_not_executed`; innocent-subject misattribution) live one hop further out in spec text, not reachable by following the comment alone. I do not score this a Gap: the ADR at :107-108 explicitly designates the moduledoc as the single narrative and the call site as a bare helper reference, so this is the prescribed shape rather than information loss. If a future pass wants the last hop closed, appending the covering requirement id to each pointer (e.g. `— see SpecLedEx.TempName; specled.verify.command_temp_names_cross_vm_unique`) would do it at zero risk. Out of scope here.\n2. The ticket body's premise that this trim requires a false `decisions:` link on prose_guard.spec.md is confirmed WRONG on this tree, as the admission pass predicted: prose_guard's finding is INFO under the Spec-Drift trailer with no spec-graph edit, and the implementer correctly did not add one. Worth folding back into the ticket's historical framing if anyone reads it later.\n3. The root-cause ticket the description floats — prose_guard's surface claiming the whole 3800-line verifier.ex because prose emission happens to live there — remains unaddressed and is still the more valuable follow-up. Not this ticket's job.",
      "timestamp": "2026-07-27T12:17:11Z"
    },
    {
      "text": "AUDIT: APPROVE (auto — selective audit)\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nReasoning: VERIFY reported PASS with full stub and requirement coverage, zero\nscope violations, passing targeted verification, complete seam disclosure, and\nno seam substituting production wiring. TEST_CRITIC: n/a (no Elixir test files\nin diff). No auditor was spawned per policy.\n\nGate detail: all 22 `must` requirements on specled.verification accounted for\nindividually. The two risks specific to this ticket were both closed by execution rather\nthan inference. First, the realization-hash concern — `lib/specled_ex/verifier.ex` is on\na hashed surface and `verify_decision/4` is bound at the TRANSITIVE implementation tier\n— was settled by an empty `realization_hashes.json` diff, no drift finding under\n`--verbose`, and branch-guard attesting `verify_decision/4` clean. Second, the\ncomment-only claim was confirmed two independent ways, so no executable byte moved.\n\nThe pointer-target check mattered most and passed: this ticket's whole risk is that\nreplacing re-narrated rationale with a pointer LOSES information if the target is\nincomplete. The verifier read `SpecLedEx.TempName` and the ehv-amended ADR at all three\nsites and confirmed a reader following the pointer finds what the comments used to\ncarry. `prose_guard.spec.md` is untouched — no false `decisions:` link was added to\nquiet the branch guard, which the ticket body wrongly suggested was necessary.",
      "timestamp": "2026-07-27T12:17:56Z"
    },
    {
      "text": "APPROVED. Merged into epic/specled_-k6s-0.11.0-fast-follow at 378de55a (confirmed present on the epic branch). Realization-hash baseline diff EMPTY after merge, re-checked at the epic level. Epic smoke green: 1276 tests, 0 failures. Closing per orchestrator sign-off.",
      "timestamp": "2026-07-27T12:24:31Z"
    }
  ],
  "priority": 4,
  "status": "in_progress",
  "title": "Trim the re-narrated cross-VM rationale at the three verifier.ex temp-name call sites to one-line pointers",
  "type": "task",
  "updated_at": "2026-07-27T12:24:31Z"
}
