{
  "assignee": "Manuel Zubieta",
  "blocked_by": [
    "specled_-4pl",
    "specled_-ehv"
  ],
  "blocks": [
    "specled_-xkn.2"
  ],
  "created": "2026-07-26T22:30:50Z",
  "description": "Advances: specled.package\n\nFrom the specled_-vk0 test-quality gate (epic specled_-233, 2026-07-26). Both are FALSE NEGATIVES in the stale-allow-marker sweep that specled_-vk0 ADDED — limitations of new work in that PR, not inherited debt. Neither was blocking: they weaken a new supplementary check, they do not cause a false pass of the main doc-identifier lint, and the sweep does catch both rot directions on well-formed markers (proved by data-level mutations against the live ADR corpus).\n\nBoth confirmed by probe in `test/docs_identifier_lint_test.exs`.\n\n## HOLE 1 — a `\u003e` in the reason text defeats the comment strip\n\n`@allow_marker_comment_pattern` (:168) uses `[^\u003e]*` to match the marker comment, so the first `\u003e` inside the reason text terminates the match early and the marker is never stripped. Confirmed by probe with this marker:\n\n    \u003c!-- spec-lint:allow-code=branch_guard_fake see A \u003e B --\u003e\n\nThe marker is left unstripped, so the in-marker token itself counts as a token \"outside the marker\", and rot direction (b) — \"the marker's token no longer appears on its line\" — cannot fire. A stale marker whose reason contains a `\u003e` is permanently exempt and invisible to the sweep.\n\n## HOLE 2 — grammar asymmetry between the two marker patterns\n\n`@allow_marker_pattern` (:167) does NOT require the `\u003c!-- --\u003e` wrapper, so the MAIN lint honours a bare marker of the form `spec-lint:allow-code=\u003ctoken\u003e` sitting in plain prose. But only WRAPPED markers are stripped by `@allow_marker_comment_pattern` (:168).\n\nNet effect: a bare unwrapped marker grants a real exemption in the enforcement path while being structurally invisible to the staleness sweep — the exact \"permanent unearned exemption\" shape that specled_-vk0's stale-marker item was filed to close. Confirmed by probe.\n\n## Fix\n\nMake the two patterns share one grammar. Either require the wrapper in both (and reject bare markers in the main lint), or strip on the marker token itself rather than on the surrounding HTML comment. Then add a regression test per hole: a marker whose reason contains `\u003e` must still be strippable, and a bare unwrapped marker must be either rejected by the main lint or visible to the sweep.\n\nVerify each by falsification before committing — remove the fix and confirm the new test goes RED. (Epic specled_-233 lesson: run the falsifiability gate against the specific clause you strengthened, not a convenient one.)\n\n## Also from the same review — smaller items, same file\n\n- `:426` (the stale-marker corpus sweep) has NO corpus-size guard, unlike its siblings at `:233` (asserts `\u003e 50` files) and `:254` (asserts `\u003e 8` files), which exist precisely so a repo reorg or cwd change cannot silently no-op the lint. `decision_files()` has no equivalent, so an empty decision-file glob would pass silently. `:266` partly covers this by asserting `.spec/` appears, but not `.spec/decisions/` specifically.\n\n- `:360`'s failure message says \"dead in @known_codes\" when the likeliest real cause is a stale `@implementation_code_files` (a new lib file emitting codes that was never added to the list). The implied action reads as \"delete the entry\", which is wrong. Self-correcting — deleting a real code trips the docs scan at `:243` — but the message misleads. Measured blast radius: dropping `append_only.ex`, `evidence/store.ex`, `overlap.ex`, or `spec.check.ex` from the list reports 0 differences, because their codes are all mirrored in `branch_check.ex`; only a rename-induced ENOENT catches those four.\n\n- `:510`'s message is imprecise for a marker naming an unguarded bare code (e.g. `allow-code=detector_unavailable`): it reports \"that token does not appear outside the marker on the same line\" even when it plainly does, because `guarded_tokens/1` only matches the five guarded families. The verdict (remove the unnecessary marker) is right; only the stated reason is wrong. No such marker exists today.\n\n- `:269` pins ONE live spec file by name (`.spec/specs/prose_guard.spec.md:83`) as its atom-form-severity example. Two others carry the same shape (`branch_guard.spec.md:338`, `spec_drift_trailer.spec.md:80`), so asserting any-of-`.spec/**` would survive a reword of that one scenario's `given:` clause. Failure would be loud and the path is visible in ExUnit's printed expression, so it misleads no one — it just breaks more often than necessary.\n\n- `fixture_file/1` (:556) writes into a FIXED shared directory with a VM-local-counter filename. This is permitted for test-only paths under `specled.decision.cross_vm_temp_names`, and the filename is not fixed, so it is NOT a significant flake risk. Residual sliver: two concurrent VMs can emit the same `System.unique_integer` value, and VM B's `on_exit` `File.rm` could delete VM A's fixture between write and read. The window is microseconds and the content is identical at the single call site. Folding `SpecLedEx.TempName.cross_vm_suffix/0` into the DIRECTORY name would close it entirely.",
  "id": "specled_-ozm",
  "labels": [
    "phase:implementing"
  ],
  "parent": "specled_-k6s",
  "comments": [
    {
      "text": "ADMISSION VERIFICATION (specled_-k6s, 2026-07-27)\nCold fresh-context verifier run against the epic base tree (feat/specled_-td7-self-identifying-descriptors @ e81f9f98).\n\nVERDICT: still_applicable   EFFORT: medium\nMandate confirmed still applicable as written; no narrowing needed.\n\n== SCOPED REMAINING WORK ==\nALL of it. Single file: test/docs_identifier_lint_test.exs.\n\n1. Unify the two marker grammars (:167-168) so they cannot drift. Recommended end state — derive both from one wrapper-required grammar:\n   - `@allow_marker_comment_pattern` must use a lazy any-char body instead of `[^\u003e]*`, e.g. `~r{\u003c!--\\s*spec-lint:allow-code=[a-z_/]+.*?--\u003e}` so a reason containing `\u003e` still strips.\n   - `@allow_marker_pattern` must require the `\u003c!-- ... --\u003e` wrapper and capture the token, e.g. `~r{\u003c!--\\s*spec-lint:allow-code=([a-z_/]+).*?--\u003e}`, so `allowed_codes/1` (:469) no longer honours a bare unwrapped marker. (The alternative sanctioned by the ticket — strip on the marker token itself rather than the surrounding comment — is acceptable, but then the sweep must still see bare markers.)\n   Keep both single-line-scoped: the corpus is streamed line-by-line via `File.stream!/1` (:238, :485), so `.*?` cannot span lines in practice, but do not add the `s` modifier.\n\n2. Add two REGRESSION tests, each falsified before commit (revert the fix, confirm RED):\n   a. `stale_allow_marker_violations([{\"\u003c!-- spec-lint:allow-code=branch_guard_totally_made_up see A \u003e B --\u003e\", @decision_file, 1}])` must equal the `...but that token does not appear outside the marker on the same line` violation. Today it returns `[]`.\n   b. Bare-marker test: `unknown_tokens(\"a `branch_guard_totally_made_up` code spec-lint:allow-code=branch_guard_totally_made_up no wrapper\", @decision_file)` must equal `[\"branch_guard_totally_made_up\"]` (rejected by the main lint). Today it returns `[]`. If instead the \"visible to the sweep\" arm is chosen, assert the sweep reports it.\n\n3. Corpus-size guard on the sweep test (:422-429): add `files = decision_files()`, `assert length(files) \u003e N, \"decision corpus collapsed to ... — the sweep is a no-op\"` (there are 12+ files under .spec/decisions/ today; pick a conservative N and state it), and optionally `assert Enum.all?(files, \u0026String.starts_with?(\u00261, \".spec/decisions/\"))`.\n\n4. Reword the mirror failure message (:360-363) so the \"dead in @known_codes\" side also names the likelier cause: a stale `@implementation_code_files` (:184-195) missing a new lib file that emits the code. The implied action must not read as \"delete the entry\".\n\n5. Split the :510 branch: when `code` is not matched by any `@token_patterns` family (i.e. `Regex.match?(@guarded_code_shape, code)` is false, :181), emit a distinct message (\"allow-marker names \u003ccode\u003e, which is not in a guarded family — the marker is unnecessary\") rather than the false \"does not appear outside the marker\" claim.\n\n6. Broaden :269: replace the single-file pin `atom_severity_offenders([\".spec/specs/prose_guard.spec.md\"]) != []` with an any-of assertion over `Path.wildcard(\".spec/specs/*.spec.md\")` (or the three known-shape files), so a reword of one scenario's `given:` clause does not break the test.\n\n7. `fixture_file/1` (:556-563): fold `SpecLedEx.TempName.cross_vm_suffix/0` into the DIRECTORY name, e.g. `Path.join(System.tmp_dir!(), \"specled_docs_identifier_lint_test_#{SpecLedEx.TempName.cross_vm_suffix()}\")`, and reword the ADR reference to the current `specled.decision.cross_vm_temp_names_reach` (the ticket cites the superseded `specled.decision.cross_vm_temp_names`).\n\nOptional spec touch: .spec/specs/package.spec.md:113 says only \"carries an explicit `spec-lint:allow-code=\u003ctoken\u003e` marker on the same line\" — it does not mandate the `\u003c!-- --\u003e` wrapper. If option (1a) is taken, tighten that clause to name the wrapped form, matching the moduledoc at :67-72.\n\n== EXPECTED TOUCHES ==\ntest/docs_identifier_lint_test.exs, .spec/specs/package.spec.md\n\n== RISK NOTES ==\nREALIZATION HASH: none. `.spec/realization_hashes.json` contains exactly one top-level key, `api_boundary`, and `grep -c \"specled.package\"` over it returns 0. No realized_by binding names test/docs_identifier_lint_test.exs. Editing this file cannot trip the realization-drift gate.\n\nGATE COUPLING: .spec/specs/package.spec.md:120-127 verifies `specled.package.doc_identifier_integrity` via `kind: tagged_tests, execute: true`. Every test in this file carries `@tag spec: \"specled.package.doc_identifier_integrity\"`. So a regex mistake here fails bare `mix spec.check`, not just `mix test`. The `--no-run-commands` form will NOT catch it — full `mix spec.check` (or at minimum `mix test test/docs_identifier_lint_test.exs`) is required before this ticket is called done.\n\nHARD CONFLICT — specled_-4pl edits the SAME FILE at overlapping regions and must be sequenced with this one (same-file, same-attribute-block): its mandate names moduledoc :44 (\"Deferred to specled_-vk0\"), the `@token_patterns` comment :155-156, `@known_code_list` :139 (dead indirection to delete), `@implementation_code_files` :184-195, and the mirror test at :357-364 — which is literally ozm's smaller item #4 (the \"dead in @known_codes\" message). Decide ONE owner for the :357-364 message rewording or the two tickets will produce competing edits. 4pl also edits .spec/decisions/specled.decision.doc_identifier_lint_spec_corpus.md, which is one of the four live marker-bearing ADRs this ticket's sweep reads (lines :22, :24, :47) — if 4pl rewrites those lines, re-run the sweep after both land.\n\nCONFLICT — specled_-ehv is a repo-wide sweep of the 30 test files that combine `System.tmp_dir!()` with `unique_integer`; test/docs_identifier_lint_test.exs:556-563 is one of them. ozm item #7 and ehv would edit the same 8 lines. Prefer letting ehv own the mechanical sweep and dropping item #7 from ozm, or vice versa — but not both.\n\nSOFT — specled_-26g is the same temp-name defect class in a different file (evidence Store.read_status sidecar tests); no textual overlap, but it should adopt the same `cross_vm_suffix/0`-in-directory-name shape for consistency.\n\nSOFT — specled_-yds edits test/docs_concepts_content_lint_test.exs (different file) but cites test/docs_identifier_lint_test.exs:298 as precedent and advances the same subject (specled.package), so both land under the same tagged_tests verification entry. specled_-z5t also advances specled.package but edits skills/spec-led-bootstrap/references/adoption-phases.md — note that skills/** IS inside this lint's `finding_code_corpus/0` (:216), so a z5t edit that introduces a finding-code-shaped token would be caught by this test.\n\nDEPENDENCY: none blocking. This ticket is self-contained and needs no other ticket to land first; only the 4pl/ehv ordering decisions above.\n\nFALSIFIABILITY MANDATE: the ticket explicitly requires proving each new test RED by reverting the specific clause strengthened (epic specled_-233 lesson). An implementer that only reports \"tests pass\" has not satisfied the ticket.\n",
      "timestamp": "2026-07-27T08:37:05Z"
    },
    {
      "text": "SCOPE CONTRACT (orchestrator, specled_-k6s — 2026-07-27)\n\nAuthoritative for mechanical scope checks. Supplements, does not replace, the\nADMISSION VERIFICATION comment's mandate (items 1-7), which is what you implement.\n\n## LINE NUMBERS HAVE MOVED — match by content\nspecled_-4pl has ALREADY LANDED on the epic branch and edited this same file\n(`test/docs_identifier_lint_test.exs`): it inlined `@known_code_list` into\n`@known_codes` and reworded two comments. Every line number the admission comment cites\nhas shifted by roughly one. Your stage is cut from the current epic head, so you have\n4pl's version. Re-read the file and anchor on CONTENT, never on the cited line numbers.\n\nspecled_-ehv has also landed. Its amended\n`.spec/decisions/specled.decision.cross_vm_temp_names_reach.md` is the current ADR —\nitem 7 is already aligned with it (fold `SpecLedEx.TempName.cross_vm_suffix/0` into the\ndirectory name, and reference `cross_vm_temp_names_reach`, not the superseded\n`cross_vm_temp_names`).\n\n## Allowed touches\n- `test/docs_identifier_lint_test.exs`\n- `.spec/specs/package.spec.md` — the OPTIONAL item only: if you take option (1a) and\n  require the `\u003c!-- --\u003e` wrapper, tighten the clause that currently says only \"carries\n  an explicit `spec-lint:allow-code=\u003ctoken\u003e` marker on the same line\" so it names the\n  wrapped form. Spec co-change is AUTHORIZED for this. It is append-only-checked:\n  tighten in place, never delete or downgrade.\n\n## Out of Scope (files)\n- `lib/**` — no production change. The lint lives entirely in the test file.\n- `.spec/decisions/specled.decision.doc_identifier_lint_spec_corpus.md` — specled_-4pl\n  owns it and has landed. Do not touch it, and do NOT disturb its two\n  `\u003c!-- spec-lint:allow-code=... --\u003e` markers, which your own sweep depends on.\n- `CHANGELOG.md` (orchestrator consolidates once at the end), `VERSION`,\n  `.spec/realization_hashes.json`, `.spec/state.json`.\n- Every file not listed under Allowed touches.\n\n## Out of Scope (intent)\n- Keep both marker grammars SINGLE-LINE-SCOPED. The corpus is streamed line-by-line via\n  `File.stream!/1`, so `.*?` cannot span lines in practice — but do NOT add the `s`\n  modifier, which would change that.\n- Do not implement specled_-4pl's FINDING 2 (union → per-emitting-site) or guard\n  `@implementation_code_files` staleness. Both were explicitly descoped there and are\n  not yours. Item 4 only REWORDS the mirror failure message to name the likelier cause;\n  it does not change the mirror's logic.\n- No new dependencies.\n\n## FALSIFIABILITY BAR — this ticket is one of the five the epic gates on\nThis ticket fixes two evasion holes in a lint. A fix whose regression tests cannot fail\nis the same defect one level up. For BOTH item-2 tests:\n1. Write the test, confirm it is GREEN with your fix in place.\n2. REVERT the grammar fix, confirm the test goes RED, and paste the failure.\n3. Restore the fix, confirm green.\nRecord both mutations and their reverts in the COMMIT MESSAGE. \"Tests pass\" is not\nevidence here — today both cases return `[]`, which is exactly why the holes survived.\n\nItem 3's corpus-size guard must also be shown to work: state the N you chose, the actual\nfile count, and why N is conservative.\n\n## Verification (change-scoped, run-until-green; host mix, no compose)\n1. `mix compile --warnings-as-errors`\n2. `mix test test/docs_identifier_lint_test.exs` — every test must stay green, including\n   the pre-existing 16.\n3. `mix format --check-formatted`\n4. `mix spec.check --no-run-commands`\n\nDo NOT run bare `mix spec.check` and do NOT run the full suite. The orchestrator owns\nwhole-system proof at the merge gate and PR CI.",
      "timestamp": "2026-07-27T11:33:33Z"
    },
    {
      "text": "IMPLEMENTED\nSession: k6s-ozm-impl-1\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-ozm/grok-k6s-ozm-impl-1/log\n\nCommit: 6db6ba2c6591a91fd0af60ec0935ec4d83e638d9\nFiles touched: test/docs_identifier_lint_test.exs, .spec/specs/package.spec.md\nTests added: SpecLedEx.DocsIdentifierLintTest.\"an allow-marker whose reason contains \u003e is still stripped for the stale sweep\"; SpecLedEx.DocsIdentifierLintTest.\"a bare unwrapped allow-marker does not exempt a fabricated code\"\nVerification stubs flipped: none (existing execute:true tagged_tests for specled.package.doc_identifier_integrity)\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/docs_identifier_lint_test.exs (18 tests); mix format --check-formatted; mix spec.check --no-run-commands; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\n\nSummary: Closed both stale-allow-marker sweep evasion holes by unifying exemption and strip on one HTML-comment-wrapped grammar (`.*?` to `--\u003e`, no `s` modifier). Bare unwrapped markers no longer exempt; reasons containing `\u003e` remain strippable. Added falsified regression tests for both holes, a decision-corpus size guard (N=8 vs live 51), mirror-message reword, unguarded-family marker message split, any-of atom-severity pin, and cross_vm_suffix fixture dirs. package.spec.md tightened to name the wrapped marker form.\n\nEpic lessons checked against diff:\n- [stale-spec] no new ADR; fixture comment names the remedy shipped (cross_vm_suffix in dir) and current ADR id\n- [stale-spec] grammar rules are satisfiable by the live marker corpus (all 4 live markers are wrapped)\n- [untested-clause] both holes tested with inputs where old dual-grammar and new unified grammar DISAGREE (falsified RED under revert)\n- [other] mix format --check-formatted green\n- [other] completion posted to specled_-ozm\n\nFalsifiability evidence (recorded in commit message): both new tests returned [] (pass vacuously) under the pre-ozm dual grammar and went RED after the assertions expected the fixed behaviour.",
      "timestamp": "2026-07-27T11:41:46Z"
    },
    {
      "text": "VERIFY: PASS\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nBase used: 1ef97ad1 (git merge-base HEAD epic/specled_-k6s-0.11.0-fast-follow). Head 6db6ba2c.\n\nMechanical checks:\n- Files touched: 2 (test/docs_identifier_lint_test.exs, .spec/specs/package.spec.md)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0\n  - lib/** : 0 files in diff.\n  - .spec/decisions/specled.decision.doc_identifier_lint_spec_corpus.md: `git diff 1ef97ad1..HEAD -- .spec/decisions/` is EMPTY. Its two live markers (:22, :24) plus the literal template at :47 verified undisturbed and still parsed correctly (see falsifiability, live-marker table).\n  - `git diff --stat 1ef97ad1..HEAD -- .spec/realization_hashes.json` is EMPTY.\n  - CHANGELOG.md / VERSION / .spec/state.json: not in diff.\n\nSemantic checks:\n- Spec stubs run: 1, passed: 1\n  `mix test --only \"spec:specled.package.doc_identifier_integrity\"` -\u003e 1277 tests, 0 failures, 1262 excluded (the tagged_tests leg covering the subject).\n- Requirements coverage (all 6 `must` requirements in specled.package):\n  - specled.package.doc_identifier_integrity: covered by test/docs_identifier_lint_test.exs:480 (hole 1) and :493 (hole 2), plus the corpus sweep at :435-455 and the pre-existing marker tests at :455-472. Would fail if the strip grammar reverts to a first-`\u003e`-terminating class (:480 goes RED, proved below) or if the exemption grammar again honours a bare unwrapped marker (:493 goes RED, proved below). Seams: `@allow_marker_pattern` (:173) is the single grammar for BOTH `allowed_codes/1` (:520) and the strip at :545 — one seam, not two, which is the fix; `@guarded_code_shape` (:186) is a second seam now consumed by the item-5 branch at :560; `@token_patterns` (:168-180) still owns what counts as a token outside the marker; `decision_files/0` (:213) is the corpus seam, now size-pinned at :442.\n  - specled.package.mix_tasks: not reachable by this diff because it is realized by `lib/mix/tasks/*.ex` and verified by a `kind: command` leg that loads all 9 tasks; `git diff --name-only` contains no file under `lib/`, so no task module, name, or registration changed.\n  - specled.package.index_and_state: not reachable by this diff because indexing and `.spec/state.json` writing live entirely in `lib/`; no `lib/` file is in the diff and `.spec/state.json` is not in the diff.\n  - specled.package.declarative_governance: not reachable by this diff because it governs `.spec` content shape and ADR usage; `git diff 1ef97ad1..HEAD -- .spec/decisions/` is EMPTY (no ADR added, removed, or edited), and the single `.spec/specs/` edit is an in-place statement tightening, not a policy relocation.\n  - specled.package.adoption_guide: not reachable by this diff because it is realized by `docs/adoption.md`, which is not in the diff (`git diff --name-only` = 2 files, neither under `docs/`).\n  - specled.package.concepts_guide: not reachable by this diff because it is realized by `docs/concepts.md`, which is not in the diff (same evidence).\n- Intent-level scope concerns: none.\n  - specled_-4pl FINDING 2 (union -\u003e per-emitting-site) NOT implemented: `implementation_guarded_codes/0` (:585) still folds all `@implementation_code_files` into one MapSet and compares the union against `@known_codes`. Unchanged by this diff.\n  - `@implementation_code_files` staleness guard NOT implemented: the list (:189-200) is unchanged and no new assertion pins it.\n  - Item 4 rewords the mirror failure message ONLY: the diff touches the message string at :369-376; the assertion `implementation_codes == @known_codes` (:369) and `implementation_guarded_codes/0` are byte-identical to base. Mirror logic unchanged.\n  - No new deps (mix.exs / mix.lock not in diff).\n- Targeted verification: PASS.\n  - `mix compile --warnings-as-errors` -\u003e exit 0\n  - `mix test test/docs_identifier_lint_test.exs` -\u003e 18 tests, 0 failures (16 pre-existing + 2 new, all green)\n  - `mix format --check-formatted` -\u003e exit 0\n  - `mix spec.check --no-run-commands` -\u003e `spec.check result=pass`, validate errors=0 warnings=0, branch findings=5 (all info, all `branch_guard_missing_subject_update`, none naming this ticket's files)\n  - Bare `mix spec.check` and the full suite deliberately NOT run.\n\n- Falsifiability re-proof (step 7.5), performed independently of the implementer's evidence:\n\n  (a) REVERT-RED. I restored the pre-ozm dual grammar in place (`@allow_marker_pattern ~r{spec-lint:allow-code=([a-z_/]+)}` + `@allow_marker_comment_pattern ~r{\u003c!--\\s*spec-lint:allow-code=[^\u003e]*--\u003e}`, strip site back to the comment pattern) and re-ran the file. EXACTLY the two new tests went RED; the 16 pre-existing stayed green:\n\n    1) test an allow-marker whose reason contains \u003e is still stripped for the stale sweep\n       test/docs_identifier_lint_test.exs:481\n       left:  []\n       right: [\".spec/decisions/specled.decision.example.md:1: allow-marker names \\\"branch_guard_totally_made_up\\\" but that token does not appear outside the marker on the same line\"]\n\n    2) test a bare unwrapped allow-marker does not exempt a fabricated code\n       test/docs_identifier_lint_test.exs:494\n       left:  []\n       right: [\"branch_guard_totally_made_up\"]\n\n       18 tests, 2 failures\n\n    Fix restored via `git checkout --`; re-ran: 18 tests, 0 failures.\n\n  (b) RIGHT-REASON. Confirmed non-vacuous. In both failures the pre-fix value is `left: []` and the asserted value is a NON-EMPTY list pinning the fixed behaviour. Neither test asserts `== []`, so neither can pass under the broken grammar. This is the specific vacuity trap the ticket names, and both tests avoid it.\n\n  (c) ADVERSARIAL — my own shapes. I built an independent probe by copying the module under a new name into the scratchpad and printing `allowed_codes/1`, the stripped line, `unknown_tokens/2` and the sweep verdict for 25 marker shapes, then re-ran the SAME probe against the reverted pre-ozm grammar for an old-vs-new diff. Selected results under the NEW grammar:\n\n    - `--\u003e` inside reason text (`... see --\u003e then more --\u003e`): non-greedy stops at the FIRST `--\u003e`, matching HTML comment semantics. Exempts the code, sweep correctly reports it stale. With the token also present outside: no violation. CORRECT.\n    - Two markers on one line, both stale: BOTH reported. First legit + second stale: only the second reported. CORRECT (scan and replace are global).\n    - Marker split across lines: opener-only half grants NO exemption and the main lint reports the token as unknown (fails closed); closer-only half is inert. A string with a real embedded `\\n` between `\u003c!--` and `--\u003e` does NOT match — `.` excludes newline, so the grammar cannot span lines even if handed a multi-line chunk. CORRECT.\n    - Whitespace: `\u003c!--spec-lint:allow-code=X--\u003e` (no spaces) and `\u003c!--\\t  spec-lint:allow-code=X   r   --\u003e` (tabs, wide padding) both parse. CORRECT.\n    - Casing: `SPEC-LINT:ALLOW-CODE=` and `Spec-Lint:Allow-Code=` grant NO exemption and the token is reported unknown. Fails closed; same as pre-fix. CORRECT.\n    - Item-5 split confirmed: `allow-code=branch_guard_totally_made_up` (guarded family, unknown code) -\u003e \"does not appear outside the marker\"; `allow-code=detector_unavailable` (NOT in a guarded family) -\u003e \"which is not in a guarded family — the marker is unnecessary\", and it stays that message even when the token DOES appear outside, which is the right verdict since `guarded_tokens/1` can never match it. Real implementation code still -\u003e \"remove the marker\". Three branches, three distinct correct messages.\n    - Unterminated marker (no `--\u003e`), and `\u003c!-- --\u003e spec-lint:allow-code=X \u003c!-- --\u003e` (wrapper present but not wrapping): NO exemption, token reported unknown. Fails closed.\n    - Malformed tokens (`branch_guard_made_up2`, `branch_guard_made_upX`): the `\\b` after `[a-z_/]+` makes the whole marker unmatchable, so NO exemption is granted and the token is reported. Fails closed. Pre-fix these were silently exempted on a TRUNCATED capture, so this is a tightening. The digit case is additionally backstopped by the pre-existing invariant test \"guarded implementation codes do not contain digits\" (:379).\n    - The 4pl ADR's literal template line `\u003c!-- spec-lint:allow-code=\u003ctoken\u003e reason --\u003e` matches nothing (`\u003c` is outside `[a-z_/]`), exempts nothing, and produces no violation — identical to pre-fix. The ADR's documentation line is not disturbed.\n\n    Old-vs-new diff across all 25 shapes: every behavioural change moves in the TIGHTENING direction (previously-exempt shapes now fail closed). No shape produced a wrong verdict. No new hole found.\n\n  (d) `s` MODIFIER: absent. `Regex.opts(@allow_marker_pattern)` prints `[]` (no modifiers at all); source is `\"\u003c!--\\\\s*spec-lint:allow-code=([a-z_/]+)\\\\b.*?--\u003e\"`. The old `@allow_marker_comment_pattern` is gone entirely, so there is no second pattern to check. Streaming semantics preserved.\n\n  (e) CORPUS-SIZE GUARD: N=8 at :442; live `decision_files()` returns 51 files (I ran it), and all 51 start with `.spec/decisions/` so the companion assertion at :445 holds. N=8 is conservative — it will not false-fail (it tolerates an 84% shrink) and it matches the sibling severity-corpus guard's N. It WOULD fire on collapse: `length([]) \u003e 8` is false, so an empty or near-empty glob raises with \"decision corpus collapsed to 0 files — the sweep is a no-op\". Advisory: the in-code comment says \"dozen+ files today\"; the true count is 51 — true but understated (see Notes).\n\n  (f) FOUR LIVE MARKERS: yes, all four still honoured. I enumerated every marker-bearing line in the live corpus under the NEW grammar and printed the exemption set and unknown-token set per line:\n      .spec/decisions/specled.decision.doc_identifier_lint_spec_corpus.md:22 exempts=[\"branch_guard_test_only_change\"] unknown=[]\n      .spec/decisions/specled.decision.doc_identifier_lint_spec_corpus.md:24 exempts=[\"branch_guard_missing_subject_update_attested\"] unknown=[]\n      .spec/decisions/specled.decision.doc_identifier_lint_spec_corpus.md:47 exempts=[] unknown=[]   (literal template line — correctly inert)\n      .spec/decisions/specled.decision.file_touch_yields_to_realization.md:108 exempts=[\"branch_guard_missing_subject_update_attested\"] unknown=[]\n      .spec/decisions/specled.decision.finding_code_budget.md:47 exempts=[\"branch_guard_test_only_change\"] unknown=[]\n    Full-corpus sweep over all 51 decision files: violations == []. The wrapper requirement rejects no real marker.\n\n  TREE LEFT CLEAN: yes. All temporary mutations (grammar revert; three spec.md mutations described below) were reverted with `git checkout --`. Final `git status --short` shows only the untracked `.grok/sandbox.toml` I was told to ignore, and `git diff --stat` against HEAD is empty. Probe copies live only in the session scratchpad, never in the repo.\n\n- Spec-edit append-only check (step 10): PASS by inspection AND by mutation.\n  The `.spec/specs/package.spec.md` diff is ONE line: the clause \"carries an explicit `spec-lint:allow-code=\u003ctoken\u003e` marker on the same line\" becomes \"carries an explicit `\u003c!-- spec-lint:allow-code=\u003ctoken\u003e reason --\u003e` marker on the same line\". The requirement `id`, `priority: must` and `stability: stable` are byte-identical; no requirement, scenario, or verification entry is deleted; nothing else in the file changed. This TIGHTENS the clause in place and matches the new grammar and the moduledoc.\n  I also mutated the file three times to test the gate: (1) `priority: must` -\u003e `should` — spec.check still passed; (2) statement \"shall\" -\u003e \"may\" — spec.check still passed; (3) deleting the whole `specled.package.declarative_governance` requirement — spec.check FAILED (`result=fail tier=validate`). So a deletion IS mechanically caught. See Notes for the (1)/(2) observation. All three reverted.\n\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope, if any: none. specled_-ozm carries three comments (ADMISSION VERIFICATION, SCOPE CONTRACT, IMPLEMENTED); there is no `AUDIT: BOUNCE` comment, so no file outside the Allowed globs is in scope.\n\nVerdict: PASS\n\nNotes (advisory only, nothing blocking):\n1. PRE-EXISTING INFRA, NOT THIS DIFF — the `append_only` leg appears not to fire for spec statement/priority changes under `mix spec.check --no-run-commands` against `origin/main`. Mutating `priority: must` -\u003e `should`, and separately the modal verb `shall` -\u003e `may`, on `specled.package.doc_identifier_integrity` each produced `spec.check result=pass` with zero `append_only/*` findings (not even `append_only/no_baseline`). `BranchCheck.contract_findings/4` does call `AppendOnly.analyze` and `append_only_applicable?` is true here (real git repo, base != \"HEAD\"), so the likely cause is `fetch_prior_state/2` having no committed baseline — `.spec/state.json` is gitignored per CLAUDE.md. Requirement DELETION is still caught, but via the validate leg (dangling `covers:` reference), not via `append_only/requirement_deleted`. This is exactly the \"gate that cannot fail\" class the epic cares about; worth a separate ticket. It does NOT affect this verdict — ozm's spec edit deletes and downgrades nothing, which I confirmed by reading the one-line diff.\n2. The comment at :440-441 says the guard is conservative \"against the live `.spec/decisions/` tree (dozen+ files today)\". The real count is 51. \"Dozen+\" is true but understates by 4x; a future reader calibrating N off that comment would under-tighten. One-word fix, not worth a bounce.\n3. Behaviour change worth knowing at review time: because the capture class is `[a-z_/]+` followed by `\\b`, a marker naming a malformed token (digits, uppercase) is now unmatchable rather than partially matched, so it grants no exemption and the token is reported as an unknown code. This fails closed and is backstopped by the existing no-digits invariant test, but an author who writes such a marker will get \"unknown finding code\" rather than a marker-specific message.\n4. Pre-existing and unchanged by this diff (verified identical under both grammars): if a token appears on the same line only inside an ADJACENT plain HTML comment, the sweep counts that as \"appears outside the marker\" and reports no staleness. That is consistent with the stated contract rather than an evasion of it, and it is not something ozm introduced.\n",
      "timestamp": "2026-07-27T11:53:44Z"
    },
    {
      "text": "TEST_CRITIC: FAIL\n\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nFindings:\n\nBLOCKING\n\n- (d) New cond branch has zero coverage. `test/docs_identifier_lint_test.exs:560-564` adds\n  `not Regex.match?(@guarded_code_shape, code) -\u003e \"...which is not in a guarded family — the\n  marker is unnecessary\"`. I confirmed the branch is REACHABLE: the capture group `([a-z_/]+)`\n  happily yields `detector_unavailable` from\n  `\u003c!-- spec-lint:allow-code=detector_unavailable budgeted --\u003e`, and that token fails\n  `@guarded_code_shape`, so the branch fires. But nothing exercises it:\n  * No test in the file feeds a non-guarded code to a marker — all seven marker literals in\n    the test bodies (lines 397, 411, 430, 459, 468, 481, 496) name `branch_guard_*` or\n    `overlap/*`.\n  * The live corpus cannot reach it either: all four real markers under `.spec/decisions/`\n    (`finding_code_budget.md:47`, `doc_identifier_lint_spec_corpus.md:22,24`,\n    `file_touch_yields_to_realization.md:108`) name `branch_guard_*`.\n  The whole point of splitting this branch is that the old fall-through emitted a MISLEADING\n  message (\"does not appear outside the marker on the same line\") for a token that may well\n  appear outside the marker — and nothing pins the corrected message. This file's own standard\n  is stated at line 291-293 (\"Each states the regression it catches\"); every other cond branch\n  has a controlled-input test (:456 for the known-code branch, :467 and :480 for the\n  outside-marker branch). A correct version adds a sibling to those, e.g.:\n\n      test \"an allow-marker naming a non-guarded code is reported as unnecessary\" do\n        line = \"`detector_unavailable` \u003c!-- spec-lint:allow-code=detector_unavailable budgeted --\u003e\"\n\n        assert stale_allow_marker_violations([{line, @decision_file, 1}]) == [\n                 ~s|#{@decision_file}:1: allow-marker names \"detector_unavailable\", which is not in a guarded family — the marker is unnecessary|\n               ]\n      end\n\n  Without it the reworded message is unverified prose and the branch can be deleted or\n  reordered with no test noticing.\n\n- (e) Fixture cleanup regressed while the dir was uniquified. `test/docs_identifier_lint_test.exs:616-628`\n  correctly puts `cross_vm_suffix/0` in the DIRECTORY name (mandate satisfied — line 620), but\n  `on_exit(fn -\u003e File.rm(path) end)` at :626 still removes only the FILE. Before this change the\n  leaked dir was a single fixed path; now it is a fresh dir per VM run, so the leak is unbounded.\n  Empirically confirmed on this machine: 10+ zero-entry `specled_docs_identifier_lint_test_\u003cpid\u003e_\u003chex\u003e`\n  dirs in `$TMPDIR` after a handful of runs, alongside the one legacy fixed-name dir. Both sibling\n  cross-VM roots in this repo use the right pattern — `test/specled_ex/realization/hash_store_test.exs:13`\n  and `test/specled_ex/compiler/tracer_bootstrap_test.exs:26` both `on_exit(fn -\u003e File.rm_rf!(root) end)`.\n  Correct version: `on_exit(fn -\u003e File.rm_rf!(dir) end)` (the dir is now VM-unique, so rm_rf is safe —\n  that is precisely what the suffix buys).\n\nNON-BLOCKING\n\n- (b) Corpus guard N is not decoration but its inline justification is wrong. `:442`\n  `assert length(files) \u003e 8` does fire on the failure mode it names: an empty glob from a cwd\n  change or reorg gives `0 \u003e 8` -\u003e false. But the comment at :441 says \"N=8 is conservative\n  against the live `.spec/decisions/` tree (dozen+ files today)\" and the live tree is 51 files,\n  not a dozen. Relative to its siblings the guard is loose: the finding-code guard is `\u003e 50`\n  against 99 files (:238) and the severity guard is `\u003e 8` against 12 (:259); this one is `\u003e 8`\n  against 51, so an 80% collapse (51 -\u003e 9) sails through. Either raise N toward the sibling\n  ratio (~`\u003e 30`) or correct \"dozen+\" to the real count so the next author is not misled into\n  thinking 8 is near the floor.\n\n- (b, minor) `:445` `assert Enum.all?(files, \u0026String.starts_with?(\u00261, \".spec/decisions/\"))` is a\n  tautology against `decision_files/0`'s own `Path.wildcard(\".spec/decisions/**/*.md\")` literal\n  three lines away — it cannot fail without an edit to that same function. Harmless, low value.\n  Ordering is at least correct: the length guard runs first, so it is not vacuous-on-empty.\n\nVERIFIED CLEAN\n\n- (a) Both regression tests are non-vacuous. I replayed each input against the BASE grammars\n  (`@allow_marker_pattern` = `~r{spec-lint:allow-code=([a-z_/]+)}`, `@allow_marker_comment_pattern`\n  = `~r{\u003c!--\\s*spec-lint:allow-code=[^\u003e]*--\u003e}`) and the HEAD unified grammar, reusing the file's\n  own `guarded_tokens`/`allowed_codes` logic:\n  * Hole 1 (`:480`): on base, `[^\u003e]*` stops at the `\u003e` in \"see A \u003e B\" so the comment pattern does\n    not match, the strip is a no-op, `tokens_outside_marker` still contains\n    `branch_guard_totally_made_up`, and the function returns `[]` — the test's non-empty\n    expectation FAILS on base. On HEAD the strip yields `\"\"` and the violation fires.\n  * Hole 2 (`:493`): on base `allowed_codes` captures `branch_guard_totally_made_up` from the\n    bare unwrapped text, so `unknown_tokens` returns `[]` — again the test's non-empty\n    expectation FAILS on base. On HEAD it returns `[\"branch_guard_totally_made_up\"]`.\n  Both pin a non-empty expectation, so neither can pass vacuously.\n\n- Unified grammar does not over-span. Checked the non-greedy `.*?--\u003e` against a line carrying TWO\n  markers with an unmarked code between them: the strip leaves ` `branch_guard_unmarked_two` `\n  intact (identical to base), so the per-token safety property at `:420` is not undermined. Two\n  markers on one line still yield both captures.\n\n- Grammar fails closed on digit-bearing codes: `([a-z_/]+)\\b` no longer matches\n  `allow-code=branch_guard_foo2` at all (base silently captured the truncated `branch_guard_foo`).\n  Fail-closed is the right direction and `:380` already forbids digit-bearing codes.\n\n- (c) The any-of at `:278` did NOT weaken the assertion. It is backed by three distinct live\n  files, not propped by one: `.spec/specs/branch_guard.spec.md:339`,\n  `.spec/specs/prose_guard.spec.md:83`, `.spec/specs/spec_drift_trailer.spec.md:80`. Because the\n  assertion is `!= []`, an empty glob (cwd change, reorg) FAILS rather than passing silently, and\n  it still fails if all three files lose the atom-severity shape. The property it exists to prove\n  — that excluding `.spec/**` from `severity_corpus/0` is load-bearing rather than vacuous — is\n  exactly an any-of claim; the old single-path pin over-asserted.\n\n- (f) No pre-existing assertion was weakened or deleted. Base had 16 tests / 23 assert-refute\n  lines; HEAD has 18 / 27. All 23 base assertions survive; the deltas are +2 new tests, +2 corpus\n  guards, one generalized path (item c), and two message-only rewords (:372-376, :560). The\n  `package.spec.md` must was TIGHTENED in step (bare `spec-lint:allow-code=\u003ctoken\u003e` -\u003e\n  `\u003c!-- spec-lint:allow-code=\u003ctoken\u003e reason --\u003e`), so spec and code now agree on the wrapper.\n\n- (g) Hygiene clean: `async: true` (:2), zero `Process.sleep`, zero `spawn`/`start_link`/\n  `Task.async`, no shared-ETS or `Application.put_env`. The only shared-tmp interaction is the\n  fixture dir covered in (e).\n\n- Suite green as committed: `mix test test/docs_identifier_lint_test.exs` -\u003e 18 tests, 0 failures.",
      "timestamp": "2026-07-27T11:59:22Z"
    },
    {
      "text": "AUDIT: BOUNCE → implementer\n\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nReasoning: The critic's blocking (d) holds on independent read — I enumerated every marker\nliteral in the file (:397, :411, :430, :459, :468, :481, :496; all `branch_guard_*` or\n`overlap/marked_one`, all of which satisfy `@guarded_code_shape` at :186) and every live\nmarker in the corpus (`finding_code_budget.md:47`, `doc_identifier_lint_spec_corpus.md:22,24`,\n`file_touch_yields_to_realization.md:108`; all `branch_guard_*`), so neither the controlled\ninputs nor the :436 corpus sweep can reach the new branch at :560-564. Its message is\nunverified prose against the file's own stated standard at :291-293. On Q — the branch EARNS\nITS KEEP and the remedy is to ADD a test, not to remove it: (i) it is mandate item 5, not\nimplementer invention; (ii) the old fall-through is not merely vague but FALSE — for\n`allow-code=detector_unavailable`, `guarded_tokens/1` (:578) can never yield a non-guarded\ntoken, so `tokens_outside_marker` is always missing it and :566 fires \"does not appear outside\nthe marker on the same line\" even when it plainly does, which is exactly the overclaim class\nthis epic hunts; (iii) it is corpus-plausible rather than corpus-impossible — `package.spec.md:113`\nitself names `detector_unavailable` and `spec_requirement_too_short` as unguarded bare codes\nthat \"remain author-enforced\", which is precisely the prose that would send an ADR author\nreaching for a marker; (iv) the only alternative fix (widening `tokens_outside_marker`) would\nbe wrong, since it would yield \"marker necessary\" for a code the main lint never flags. The\nbranch is message-only — both old and new emit a violation — which is the strongest reason to\npin it, since a message-only branch rots with nothing failing.\n\nIndependent confirmations requested (all read/trace only, no suite runs):\n- SCOPE CLEAN. `git diff --name-only 1ef97ad1..HEAD` = exactly `test/docs_identifier_lint_test.exs`\n  and `.spec/specs/package.spec.md`. No `lib/**`. `git diff 1ef97ad1..HEAD -- .spec/decisions/`\n  is EMPTY; 4pl's ADR and its two markers (:22, :24) plus the literal template (:47) are byte-\n  identical.\n- SPEC EDIT TIGHTENS. One-line statement change on `specled.package.doc_identifier_integrity`:\n  bare `` `spec-lint:allow-code=\u003ctoken\u003e` `` -\u003e `` `\u003c!-- spec-lint:allow-code=\u003ctoken\u003e reason --\u003e` ``.\n  `priority: must` and `stability: stable` byte-identical; no requirement, scenario, or\n  verification entry removed. Nothing for `requirement_deleted` / `must_downgraded` to catch.\n- LIVE MARKERS STILL ACCEPTED. All four are wrapped, carry `branch_guard_[a-z_]+` tokens, contain\n  no `\u003e` and no embedded `--\u003e` in the reason, so `\u003c!--\\s*spec-lint:allow-code=([a-z_/]+)\\b.*?--\u003e`\n  matches each with the right capture. The template at `doc_identifier_lint_spec_corpus.md:47`\n  matches nothing (`\u003c` is outside `[a-z_/]`) and stays inert, as before. No real marker broken.\n- ITEM 6 DID NOT WEAKEN THE PROPERTY. The property under test at :270 is that excluding `.spec/**`\n  from `severity_corpus/0` is load-bearing — an any-of claim by nature; the old single-file pin\n  over-asserted a file identity incidental to it. Three live witnesses back it, all inside\n  ```yaml spec-scenarios fences that `fence_lang/1` resolves to \"yaml\":\n  `branch_guard.spec.md:339`, `prose_guard.spec.md:83`, `spec_drift_trailer.spec.md:80`. Because\n  the assertion is `!= []`, an empty glob FAILS rather than passing silently.\n- ITEM 3 FIRES BUT IS MIS-JUSTIFIED. `length([]) \u003e 8` is false, so a collapsed or cwd-shifted glob\n  does raise. N=8 is conservative against false-failure. But the inline comment at :440-441 says\n  \"dozen+ files today\" and the live tree is 51 — a false statement in a test comment, and it\n  makes the guard an outlier against its own siblings (`\u003e 50` of 99 at :238, `\u003e 8` of 12 at :259;\n  this one is 8 of 51). A 51 -\u003e 9 collapse sails through.\n- DESCOPED WORK STAYED DESCOPED. 4pl FINDING 2 not implemented — `implementation_guarded_codes/0`\n  (:588) still folds the union and is absent from the diff. No `@implementation_code_files`\n  staleness guard — the list (:189-200) is unchanged and unpinned. Item 4's diff touches only the\n  message string at :369-376; the `implementation_codes == @known_codes` assertion is unchanged.\n- ITEM 2 TESTS ARE NON-VACUOUS (traced statically, corroborating verifier and critic). On base,\n  `[^\u003e]*` cannot cross the `\u003e` in \"see A \u003e B\" so the strip no-ops and :480 returns `[]` against a\n  non-empty expectation; and base `@allow_marker_pattern` captures from bare prose so :493 returns\n  `[]` against a non-empty expectation. Both RED on base.\n\nBounce reason: implementer must (1) add a controlled-input test for the :560-564 branch — a\nsibling to :467, feeding a marker whose code is not in a guarded family (e.g.\n`` `detector_unavailable` \u003c!-- spec-lint:allow-code=detector_unavailable budgeted --\u003e ``) and\npinning the exact \"which is not in a guarded family — the marker is unnecessary\" string. KEEP the\nbranch; do not delete it. Removing it would re-introduce the false message that mandate item 5\nwas filed to correct, and would be a mandate violation besides. Include the branch-removal\nfalsification (delete the branch, confirm the new test goes RED on the old wrong message), since\nthis is a message-only change and \"tests pass\" proves nothing about it.\n(2) Fix the fixture-dir leak at :626. `on_exit(fn -\u003e File.rm(path) end)` removes only the file\nwhile :617-621 now mints a fresh `specled_docs_identifier_lint_test_\u003cpid\u003e_\u003chex\u003e` dir per call —\n`cross_vm_suffix/0` (lib/specled_ex/temp_name.ex:18-20) draws fresh random bytes on every\ninvocation, so what used to leak one fixed empty dir now leaks a new empty dir per run. Both\nsibling cross-VM roots do it right: `test/specled_ex/realization/hash_store_test.exs:13` and\n`test/specled_ex/compiler/tracer_bootstrap_test.exs:26` both `on_exit(fn -\u003e File.rm_rf!(root) end)`.\nUse `File.rm_rf!(dir)` — the VM-unique suffix is exactly what makes that safe.\n(3) Non-blocking, fold in while here: correct \"dozen+ files today\" at :441 to the real count (51),\nor raise N toward the sibling ratio. Leaving a wrong number in the justification will mislead the\nnext author into thinking 8 is near the floor.",
      "timestamp": "2026-07-27T12:08:35Z"
    }
  ],
  "priority": 3,
  "status": "in_progress",
  "title": "Stale-allow-marker sweep has two evasion holes: '\u003e' in reason text defeats the comment strip, and bare unwrapped markers escape the sweep entirely",
  "type": "bug",
  "updated_at": "2026-07-27T12:15:39Z"
}
