{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [
    "specled_-b23",
    "specled_-zd8"
  ],
  "created": "2026-07-25T19:05:21Z",
  "description": "specled_-dn4.9 re-derived item 1 after dn4.5: formatter.ex and coverage_triangulation.ex still need one shared module-to-source/repo-relative resolver, but doing it inside SpecLedEx.Coverage added public API and tripped api-boundary attestation. Revisit with an internal module/file explicitly scoped for this helper or another design that does not widen public API. Evidence: full mix test failed guard_ergonomics_attestation_test until the public helper was removed; both private copies were kept non-loading via :code.is_loaded/1.",
  "id": "specled_-dn4.12",
  "labels": [
    "phase:verifying"
  ],
  "parent": "specled_-k6s",
  "comments": [
    {
      "text": "SCOPE ADDITION from the specled_-dn4.9 audit (2026-07-26T00:25:30Z): source_to_binary/1 is now duplicated in BOTH lib/specled_ex/coverage/formatter.ex and lib/specled_ex/coverage_triangulation.ex, growing the very duplication this ticket is chartered to remove. Fold it into this ticket's scope alongside repo_relative_path/1 and repo_relative_source_path/1 — that is now three private copies of shared resolver logic across the two modules.",
      "timestamp": "2026-07-26T00:26:20Z"
    },
    {
      "text": "Note from specled_-ygi: that ticket added a fourth byte-identical copy of the repo-relative source helper (now: Coverage.Formatter.repo_relative_path/1, CoverageTriangulation.repo_relative_source_path/1, Mix.Tasks.Spec.Triangle.repo_relative_source/1, Review.CoverageClosure.repo_relative_source/1). All four must stay in lockstep for the path-identity joins to hold; nothing enforces it. When consolidating the coverage source resolver, fold all four into the shared home (verifier suggested SpecLedEx.Coverage.Paths).",
      "timestamp": "2026-07-26T01:20:48Z"
    },
    {
      "text": "ADMISSION VERIFICATION (specled_-k6s, 2026-07-27)\nCold fresh-context verifier run against the epic base tree (feat/specled_-td7-self-identifying-descriptors @ e81f9f98).\n\nVERDICT: still_applicable   EFFORT: medium\nMandate confirmed still applicable as written; no narrowing needed.\n\n== SCOPED REMAINING WORK ==\nFull mandate remains. Nothing has landed.\n\n1. Create `lib/specled_ex/coverage/paths.ex`, module `SpecLedEx.Coverage.Paths`, with `@moduledoc false` (precedent: lib/specled_ex/coverage/arming.ex:2 — an unlisted, undocumented internal module that carries an `implementation` binding but is absent from coverage_capture.spec.md's `surface:` list, so a new internal module needs no spec surface entry). It must NOT be added to any `realized_by:` block. Suggested API (exact names free, but keep both return shapes so no call site changes behavior):\n   - `def to_binary(source)` — charlist -\u003e `List.to_string/1`, binary -\u003e itself (replaces both `source_to_binary/1` copies).\n   - `def repo_relative(path) :: String.t() | nil` — the `File.cwd!() |\u003e Path.expand()` / `Path.expand(path, root)` / `String.starts_with?(absolute, root \u003c\u003e \"/\")` body; non-binary -\u003e nil.\n   - `def repo_relative_list(path) :: [String.t()]` — `case repo_relative(path) do nil -\u003e []; p -\u003e [p] end` (the list shape copies 3 and 4 need).\n   - `def module_source(module, load?)` — `load?: false` uses `:code.is_loaded/1` (current formatter/triangulation semantics), `load?: true` uses `Code.ensure_loaded/1` (current closure/triangle semantics). Do NOT silently unify the loading semantics: coverage_closure.ex:136 and spec.triangle.ex:452 still call `Code.ensure_loaded`, and specled_-b23 item 2 is explicitly about the `Code.ensure_loaded`-fails posture of `binding_present?`. If you do unify, that is a behavior change requiring the `specled.triangulation.v1_file_level_path_identity` statement to be extended and coordinated with specled_-b23.\n\n2. Delete and re-point, preserving the exact current return shapes:\n   - lib/specled_ex/coverage/formatter.ex:274-286 (delete `source_to_binary/1`, `repo_relative_path/1`); rewrite `source_file/1` (:260-272) to call `Paths.module_source(module, false)`. Keep returning `String.t() | nil`.\n   - lib/specled_ex/coverage_triangulation.ex:449-461 (delete both helpers); rewrite `module_source_file/1` (:434-446) to `Paths.module_source(mod, false)`; rewrite the direct call at :388 to `Paths.repo_relative(r.file)`. Keep `String.t() | nil`.\n   - lib/specled_ex/review/coverage_closure.ex:135-165 — collapse `mfa_source_file/1` + `repo_relative_source/1` onto `Paths.module_source(mod, true)` wrapped by `Paths.repo_relative_list/1`. Must still return `[]` (never nil, never an absolute path) for out-of-repo or unloadable modules — `closure_files` and `binding_present?` at :137-141 depend on it. Reduce the 5-line duplicated comment at :151-155 to a one-line pointer at the new home.\n   - lib/mix/tasks/spec.triangle.ex:451-481 — identical treatment; comment at :468-471 likewise reduced.\n\n3. Hard constraint: do NOT add any public function to `SpecLedEx.Coverage`, `SpecLedEx.Coverage.Formatter`, `SpecLedEx.CoverageTriangulation`, `SpecLedEx.Review.CoverageClosure`, or `Mix.Tasks.Spec.Triangle`. All five are api_boundary-baselined (bare module or MFA) in .spec/realization_hashes.json; a new export changes `hash_module_head_union/2` for the bare-module ones and drifts the baseline — that is exactly the failure dn4.5 hit. Removing PRIVATE functions from those modules does not change the head-union hash, so this refactor needs no `.spec/realization_hashes.json` edit.\n\n4. Add `test/specled_ex/coverage/paths_test.exs` pinning: charlist and binary inputs; absolute in-repo path -\u003e repo-relative; out-of-repo absolute -\u003e `nil` / `[]`; already-relative path -\u003e unchanged; non-binary -\u003e `nil` / `[]`; unloadable module -\u003e `nil` / `[]` under both `load?` modes. This is the mechanical lockstep enforcement the second ticket comment asks for.\n\n== EXPECTED TOUCHES ==\nlib/specled_ex/coverage/paths.ex, lib/specled_ex/coverage/formatter.ex, lib/specled_ex/coverage_triangulation.ex, lib/specled_ex/review/coverage_closure.ex, lib/mix/tasks/spec.triangle.ex, test/specled_ex/coverage/paths_test.exs, CHANGELOG.md\n\n== RISK NOTES ==\nREALIZATION HASH: no `.spec/realization_hashes.json` change is expected, and any diff there is a red flag. Verified mechanism: lib/specled_ex/realization/api_boundary.ex:159-160 -\u003e lib/specled_ex/realization/canonical.ex:176-186 hashes only `discover_module_exports/2` (public exports), so deleting private helpers from formatter.ex / coverage_triangulation.ex / coverage_closure.ex / spec.triangle.ex leaves `\"SpecLedEx.Coverage.Formatter\"` (:219), `\"SpecLedEx.CoverageTriangulation.*\"` (:331-350), `\"SpecLedEx.Review.CoverageClosure.build_v2/2\"`, `\"Mix.Tasks.Spec.Triangle.run/1\"` unchanged. Adding even one public function to any of them drifts the baseline AND fails test/integration/guard_ergonomics_attestation_test.exs:57-58.\n\nMISLEADING SPEC PROSE — read this before trusting the tree: .spec/specs/branch_guard.spec.md:11 asserts \"A bare-module entry hashes the whole file, so an edit anywhere in `coverage.ex` ... legitimately drifts this subject's realization hash\". That contradicts `hash_module_head_union/2` (public-export union, not whole file) at canonical.ex:176-186. An implementer who believes that sentence will conclude this refactor is impossible. The sentence is out of scope for dn4.12 but is a live false-claim candidate for the td7 sweep.\n\nFILE-TOUCH GUARD: this edits four files that ARE on subject surfaces — formatter.ex on `specled.coverage_capture` (.spec/specs/coverage_capture.spec.md:97) and coverage_triangulation.ex / review/coverage_closure.ex / mix/tasks/spec.triangle.ex on `specled.triangulation` (.spec/specs/triangulation.spec.md:32-34) — with no spec-requirement change. Expect `branch_guard` missing-subject-update findings on `specled.coverage_capture` and `specled.triangulation`; clear them with Spec-Drift info trailers (repo convention, per specled_-ygi's landed record), not by weakening a requirement. The new paths.ex maps to no subject (arming.ex precedent) and should be silent.\n\nORDERING: no ticket must land first. But specled_-b23 item 2 renegotiates the `Code.ensure_loaded`-failure posture of `binding_present?` in the very producers this ticket rewrites (spec.triangle.ex / coverage_closure.ex `mfa_source_file/1`) and edits `specled.triangulation.v1_file_level_path_identity` (.spec/specs/triangulation.spec.md:244-256) plus test/mix/tasks/spec_triangle_test.exs. If both land in one PR, land dn4.12 FIRST as a pure no-behavior-change refactor, then let b23 add its aggregate-envelope test and requirement wording on top; otherwise b23's new test will be written against a helper that dn4.12 then moves.\n\nspecled_-zd8 edits lib/specled_ex/coverage/formatter.ex `flush/1` (starts at :288) and `record_inventory/2` — `flush/1` begins two lines after `repo_relative_path/1` ends at :286, so the two diffs are textually adjacent in the same hunk. Sequence them, do not parallelize.\n\nspecled_-npo edits `specled.coverage_capture.envelope_meta` in .spec/specs/coverage_capture.spec.md; dn4.12 should need no spec-file edit at all, so the collision is only if dn4.12's Spec-Drift handling is done by touching that spec instead of a trailer.\n\nGATE: verify with `mix spec.check --no-run-commands` plus targeted `mix test test/specled_ex/coverage/formatter_test.exs test/specled_ex/coverage_triangulation_test.exs test/specled_ex/review/coverage_closure_test.exs test/mix/tasks/spec_triangle_test.exs test/integration/guard_ergonomics_attestation_test.exs`. The attestation test is the specific canary for the public-API constraint. Per my prior experience with this repo, tagged_tests verification legs are timing-flaky under load — a single red run there is not proof of regression.\n",
      "timestamp": "2026-07-27T08:37:03Z"
    },
    {
      "text": "SCOPE CONTRACT (orchestrator, specled_-k6s — 2026-07-27)\n\nAuthoritative for mechanical scope checks. Supplements, does not replace, the\nADMISSION VERIFICATION comment's mandate (items 1-4), which is what you implement.\n\n## Allowed touches\n- `lib/specled_ex/coverage/paths.ex` (new, `@moduledoc false`, NOT added to any\n  `realized_by:` block and NOT added to any spec `surface:` list — arming.ex precedent)\n- `lib/specled_ex/coverage/formatter.ex`\n- `lib/specled_ex/coverage_triangulation.ex`\n- `lib/specled_ex/review/coverage_closure.ex`\n- `lib/mix/tasks/spec.triangle.ex`\n- `test/specled_ex/coverage/paths_test.exs` (new)\n- `.spec/specs/coverage_capture.spec.md`, `.spec/specs/triangulation.spec.md` —\n  spec co-change is AUTHORIZED, and takes precedence over Out of Scope, ONLY if\n  the gate cannot be cleared by a `Spec-Drift:` trailer. Expected posture is a\n  trailer, not a spec edit; justify any spec edit in the completion record.\n\n## Out of Scope (files)\n- `CHANGELOG.md` — the ADMISSION comment lists it under expected touches, but the\n  ORCHESTRATOR OVERRIDES THAT: the 0.11.0 section is consolidated once at the end\n  of the epic. Do not append. This is the one place the admission comment is stale.\n- `VERSION`, `.spec/state.json`.\n- `.spec/realization_hashes.json` — MUST NOT CHANGE. Any diff here means you added\n  a public export. Stop, revert the export, and report; never regenerate the baseline.\n- `.spec/specs/branch_guard.spec.md` — its line 11 sentence about bare-module\n  hashing is a known false claim and is explicitly OUT OF SCOPE for this ticket.\n  Do not believe it and do not fix it.\n- Every file not listed under Allowed touches.\n\n## Out of Scope (intent)\n- HARD CONSTRAINT: add NO public function to `SpecLedEx.Coverage`,\n  `SpecLedEx.Coverage.Formatter`, `SpecLedEx.CoverageTriangulation`,\n  `SpecLedEx.Review.CoverageClosure`, or `Mix.Tasks.Spec.Triangle`. All five are\n  api_boundary-baselined. Removing PRIVATE functions is safe; adding a public one\n  drifts the baseline. This is the ticket's defining constraint.\n- Do NOT unify the `:code.is_loaded/1` vs `Code.ensure_loaded/1` semantics. Keep\n  `load?: false` and `load?: true` distinct. Unifying is a behavior change owned\n  by specled_-b23.\n- This is a pure no-behavior-change refactor. Every rewritten call site must\n  preserve its exact current return shape (`String.t() | nil` for the two\n  `module_source` sites, `[]`-never-nil for the closure/triangle sites).\n- No new dependencies; `mix.exs` / `mix.lock` unchanged.\n- Do not weaken spec prose or add a false `decisions:` link to quiet branch-guard.\n\n## Verification (change-scoped, run-until-green; host mix, no compose)\n1. `mix compile --warnings-as-errors`\n2. `mix test test/specled_ex/coverage/paths_test.exs test/specled_ex/coverage/formatter_test.exs test/specled_ex/coverage_triangulation_test.exs test/specled_ex/review/coverage_closure_test.exs test/mix/tasks/spec_triangle_test.exs test/integration/guard_ergonomics_attestation_test.exs`\n   — `guard_ergonomics_attestation_test.exs` is the canary for the public-API constraint.\n3. `git diff --stat -- .spec/realization_hashes.json` must be EMPTY.\n4. `mix spec.check --no-run-commands`\n\nDo NOT run bare `mix spec.check` and do NOT run the full suite. The orchestrator\nowns whole-system proof at the merge gate and PR CI. Note: tagged_tests\nverification legs are timing-flaky under load; re-run a single red leg serially\nbefore treating it as real.",
      "timestamp": "2026-07-27T10:12:23Z"
    },
    {
      "text": "IMPLEMENTED\nSession: k6s-dn4.12-impl-1\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-dn4.12/grok-k6s-dn4.12-impl-1/log\n\nCommit: 26cd483d46884bd4e1e748ca56dc27dc9e40907c\nFiles touched: lib/specled_ex/coverage/paths.ex, lib/specled_ex/coverage/formatter.ex, lib/specled_ex/coverage_triangulation.ex, lib/specled_ex/review/coverage_closure.ex, lib/mix/tasks/spec.triangle.ex, test/specled_ex/coverage/paths_test.exs\nTests added: SpecLedEx.Coverage.PathsTest (to_binary/1 charlist+binary; repo_relative/1 + repo_relative_list/1 in-repo absolute, out-of-repo absolute, already-relative, non-binary; module_source/2 loaded module under both load? modes; unloadable module under both load? modes with list wrap)\nVerification stubs flipped: none\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/coverage/paths_test.exs test/specled_ex/coverage/formatter_test.exs test/specled_ex/coverage_triangulation_test.exs test/specled_ex/review/coverage_closure_test.exs test/mix/tasks/spec_triangle_test.exs test/integration/guard_ergonomics_attestation_test.exs (119 tests, 0 failures); git diff --stat -- .spec/realization_hashes.json EMPTY; mix spec.check --no-run-commands (result=pass, error_findings=0 via Spec-Drift trailers); full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 2\nDeferred must requirements: none\n\nSummary: Extracted SpecLedEx.Coverage.Paths (@moduledoc false, not on any surface/realized_by) with to_binary/1, repo_relative/1, repo_relative_list/1, and module_source/2 (load?: false keeps :code.is_loaded; load?: true keeps Code.ensure_loaded). Deleted the four private helper copies in formatter, coverage_triangulation, coverage_closure, and spec.triangle and repointed call sites preserving String.t()|nil vs [] return shapes. No public export added to any api_boundary-baselined module; realization_hashes.json untouched. Branch-guard findings cleared with Spec-Drift: unmapped_change/missing_subject_update/missing_decision_update=info (pure refactor, no requirement/policy co-change).",
      "timestamp": "2026-07-27T10:22:13Z"
    },
    {
      "text": "VERIFY: FAIL\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nMechanical checks:\n- Files touched: 3 (.spec/decisions/specled.decision.cross_vm_temp_names_reach.md,\n  test/specled_ex/compiler/tracer_test.exs, test/specled_ex/realization/hash_store_test.exs)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0\n  (.spec/realization_hashes.json, CHANGELOG.md, VERSION, mix.exs, mix.lock, the\n  superseded ADR, and the other ~28 test roots are all untouched. ADR frontmatter\n  `affects:` (lines 5-13) is unchanged — prose-only edit, so\n  specled.append_only.adr_affects_widened does not fire. Untracked .grok/sandbox.toml\n  ignored per instructions.)\n\nSemantic checks:\n- Spec stubs run: 0, passed: 0 (no .spec/specs/*.spec.md file was touched; no stub was\n  flipped to execute: true — implementer's claim of \"none\" confirmed by name-only diff)\n- Requirements coverage:\n  - specled.verify.command_temp_names_cross_vm_unique (the only specled.verification\n    `must` this deliverable governs): covered by test/specled_ex/verifier_test.exs:976-1001.\n    Would fail if SpecLedEx.TempName.cross_vm_suffix/0 stopped emitting `\u003cos-pid\u003e_\u003c12 hex\u003e`\n    or the verifier stopped using it — the `$0`-echoed script basename is matched against\n    ~r/^specled_cmd_#{System.pid()}_[0-9a-f]{12}\\.target\\.sh$/ at :1000.\n    Seams: none — real Verifier.verify/3 with run_commands: true against a real shell and\n    a real tmp root; no Mock/stub/fake.\n  - The remaining specled.verification `must` requirements are not reachable by this\n    diff's code paths: it changes zero lib/ files, so their coverage is byte-identical\n    to base. Not re-derived.\n  - Coverage this diff DID move (compiler_tracer, not in Advances: but in blast radius):\n    - specled.compiler_tracer.merge_on_flush: covered by tracer_test.exs:373-389\n      (`assert litter == []` at :388). The filter was narrowed from\n      `String.contains?(\u00261, \".tmp.\")` to `String.starts_with?(\u00261, \"xref_mfa.etf.tmp.#{System.pid()}_\")`.\n      Would fail if the tracer left its write-rename sibling behind after a successful\n      flush. Seams: none — real Tracer.trace/2 in this VM against the real\n      _build/\u003cenv\u003e/.spec manifest.\n    - specled.compiler_tracer.atomic_tmp_name_includes_pid: covered by tracer_test.exs:391-415\n      (`assert Enum.any?` at :411, unchanged). Would fail if tracer.ex:342 dropped the\n      `System.pid()` segment. Seams: none.\n      The narrowing above does not open a hole: a regression that both leaks litter AND\n      drops the pid from the name is caught by this second test.\n- Intent-level scope concerns: none. No dependency change (mix.exs/mix.lock untouched);\n  no opportunistic refactors; no spec prose weakened; no false `decisions:` link added.\n  The `Spec-Drift: branch_guard_missing_subject_update=info` trailer is the sanctioned\n  mechanism (lib/specled_ex/branch_check/trailer.ex:43-48) and is exactly what the SCOPE\n  CONTRACT directed. Verified it is doing real work, not decoration:\n  branch_guard_missing_subject_update defaults to :error (branch_check.ex:17), and the\n  two findings it downgrades are both test-file-only.\n- Targeted verification:\n  - mix compile --warnings-as-errors -\u003e exit 0\n  - mix test test/specled_ex/realization/hash_store_test.exs test/specled_ex/compiler/tracer_test.exs\n    -\u003e 1 property, 44 tests, 0 failures (seed 118002)\n  - mix spec.check --no-run-commands -\u003e exit 0; `spec.check result=pass`;\n    validate errors=0 warnings=0; branch findings=2 (error=0 warning=0 info=2)\n  - mix format --check-formatted -\u003e exit 1  **FAILS** (see Gap 1)\n  Flipped stubs: none.\n- Corpus-scoped claims (doc-shaped diff — the ADR is the actual deliverable):\n  8 claims validated whole-file, 5 by execution/grep against the implementation:\n  - \"SpecLedEx.TempName.cross_vm_suffix/0 is public\" -\u003e TRUE, lib/specled_ex/temp_name.ex:18\n    (def + @doc + @spec), returns `\u003cos-pid\u003e_\u003c12 hex\u003e`.\n  - \"already used from test/specled_ex/compiler/tracer_bootstrap_test.exs\" -\u003e TRUE, :20.\n  - \"HashStore ... shall use cross_vm_suffix/0 and therefore leave no fixed\n    .spec/realization_hashes.json.tmp collision point\" -\u003e TRUE,\n    lib/specled_ex/realization/hash_store.ex:213.\n  - \"the tracer may inline the scheme, but the inlined suffix must include the OS pid\"\n    -\u003e TRUE, lib/specled_ex/compiler/tracer.ex:342\n    (`\"#{path}.tmp.#{System.pid()}_#{System.unique_integer([:positive])}\"`).\n  - \"SpecLedEx.Evidence.Git.temp_path/2 ... allocates under the repo-local\n    .git/specled-tmp ... mixes 8 bytes of CSPRNG hex with the counter\" -\u003e TRUE,\n    lib/specled_ex/evidence/git.ex:187-195 + :201 (random_hex/1 = strong_rand_bytes).\n  - \"blanket conversion ... would drag their owning subjects into branch-guard's\n    impacted set\" -\u003e TRUE, demonstrated live by the two info findings this very run emitted\n    for the two converted files.\n  - \"a mix spec.check merged run executes the host project's tests inside a specled run\"\n    -\u003e TRUE, tagged_tests verifications run with execute: true.\n  - \"Those shall use SpecLedEx.TempName.cross_vm_suffix/0\" (ADR :61, applied to the\n    glob-delete-in-a-shared-dir class named at :60) -\u003e **FALSE as shipped** (Gap 2).\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope (per step 11): none. Every touched file is already on the SCOPE\n  CONTRACT's Allowed list (2026-07-27T10:11:43Z); the authorized-but-optional\n  .spec/specs/verification.spec.md co-change was not needed and was correctly not taken.\n\nVerdict: FAIL\n\nGaps:\n- MERGE GATE BROKEN — `mix format --check-formatted` exits 1 on\n  test/specled_ex/compiler/tracer_test.exs. CLAUDE.md lists it as a required merge gate,\n  and this commit introduced the violation: the base blob at 2023373d formats clean\n  (verified by extracting `git show 2023373d:test/specled_ex/compiler/tracer_test.exs`\n  and running the formatter on it -\u003e exit 0). Both edited hunks wrap `Enum.filter/2`\n  across three lines where the formatter collapses to one:\n    :384-386 and :420-422 -\u003e\n    `|\u003e Enum.filter(\u0026String.starts_with?(\u00261, \"xref_mfa.etf.tmp.#{System.pid()}_\"))`\n  (~87 cols, inside the default line_length of 98; .formatter.exs sets no override).\n  This is not a style preference — it is a deterministic gate failure that will stop the\n  orchestrator's merge gate and PR CI. Fix is `mix format`. Note the SCOPE CONTRACT's\n  three-command Verification list omits format, which is plausibly why it was missed;\n  worth adding to the contract template.\n- DELIVERABLE SELF-CONTRADICTION — ADR :59-64 vs. the same commit's tracer_test.exs fix.\n  The new normative sentence reads: \"That permission does **not** extend to tests that\n  assert on directory contents (`File.ls!` over a shared or tmp root, **or a cleanup\n  sweep that deletes by glob in a shared dir**). Those shall use\n  `SpecLedEx.TempName.cross_vm_suffix/0`.\"\n  The repo contains exactly one member of that second class — tracer_test.exs:417-423\n  (`File.rm(Path.join(Path.dirname(@manifest_path), \u00261))`, confirmed by sweeping\n  `File.rm(Path.join` across test/: one hit). This commit changed that exact sweep, and\n  it does NOT use cross_vm_suffix/0 — it filters on raw `System.pid()`.\n  Worse, the prescription is *unsatisfiable* for that class, not merely unfollowed:\n  cross_vm_suffix/0 mints fresh `:crypto.strong_rand_bytes(6)` on every call, so it can\n  only NAME a path you allocate — it can never MATCH names a different writer produced.\n  A reader who follows ADR :61 literally for a shared-dir sweep writes a filter containing\n  a fresh random suffix, which matches nothing: `assert litter == []` becomes silently\n  vacuous and the cleanup becomes a no-op. That is a worse outcome than the status quo,\n  and it is precisely the \"an unexplained sentence invites a reader to conclude something\n  that is not true\" failure this ticket was opened to remove — reintroduced one paragraph\n  below the sentence it replaced.\n  The ADR also never documents the remedy the commit actually shipped (pid-scoping the\n  filter), so the durable decision does not describe its own implementation.\n  Smallest fix — split the prescription by which half of the class you are in, e.g.:\n    \"Those shall scope by cross-VM identity: a test that ALLOCATES its own root shall\n     name it with `SpecLedEx.TempName.cross_vm_suffix/0`; a test that sweeps a genuinely\n     shared directory it cannot uniquify (e.g. `_build/\u003cenv\u003e/.spec`) shall filter on the\n     writer's `System.pid()` prefix instead, because a random suffix cannot match names\n     another writer produced.\"\n  This is one clause and stays inside the Allowed-touches prose region. In fairness to\n  the implementer: the offending sentence was transcribed near-verbatim from the\n  2026-07-27T08:37:06Z ADMISSION VERIFICATION mandate, which carries the same tension\n  between its option-1 wording and its own option-2b pid-filtering instruction. The\n  mandate is where the defect originates; it still ships in this deliverable.\n\nNotes:\n- Not a gap: narrowing the `litter` filter from `contains?(\".tmp.\")` to the pid-prefixed\n  form is a real reduction in what that one assertion can catch, but the paired\n  `atomic_tmp_name_includes_pid` test (:391-415, unchanged) independently pins the name\n  shape, so no regression escapes both. The audit called this leg \"optional\"; taking it\n  was the right call given the shared `_build/\u003cenv\u003e/.spec` root.\n- hash_store_test.exs:10 conversion is clean and does exactly what the mandate's option 2a\n  asked. It makes `tmp_siblings/1` (:20-25) and the `refute Enum.any?` at :274 and :413\n  cross-VM-safe by construction and removes the `File.ls!` raise-on-collision path flagged\n  in the 2026-07-27T00:18:20Z comment.\n- Count drift, commit message only (no shipped file affected): the message says \"Leave the\n  other ~28 unique_integer test roots alone.\" Measured now on this tree, 31 test files\n  still combine `System.tmp_dir!()` with `unique_integer`. Cosmetic; mentioned only so a\n  later reader does not treat \"~28\" as a baseline.\n- .spec/realization_hashes.json is untouched and shows no hash diff, as required.",
      "timestamp": "2026-07-27T10:27:33Z"
    },
    {
      "text": "TEST_CRITIC: FAIL\n\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nFindings:\n\n- (a) CASE COVERAGE: PASS. All six enumerated cases are present and asserted in test/specled_ex/coverage/paths_test.exs: charlist (:8) and binary (:12) to_binary/1; absolute in-repo -\u003e repo-relative (:21-22); out-of-repo -\u003e nil/[] (:28-29); already-relative unchanged (:35-36); non-binary -\u003e nil/[] (:40-43); unloadable module -\u003e nil/[] under both load? modes (:64-66). `mix test test/specled_ex/coverage/paths_test.exs` = 8 tests, 0 failures.\n\n- (c) FAIL, BLOCKING: the load?: false vs load?: true distinction is NOT pinned. Mutation-verified, not inferred. I built two mutants of module_source/2 in scratchpad — one ignoring load? and always calling Code.ensure_loaded, one ignoring load? and always calling :code.is_loaded — and ran the shipped assertions (paths_test.exs:47-67) verbatim against both with the subject module preloaded exactly as it is in the real run. Both mutants PASS all six module_source/2 assertions. The file therefore cannot go RED if the two branches are unified in either direction. Root cause: both tests pick inputs where the two modes agree by construction — an already-loaded module (:52-53, Paths is loaded because the test module references it) and a nonexistent module (:64-65, both branches fall through to nil). This matters in production: lib/specled_ex/coverage/formatter.ex:260 and lib/specled_ex/coverage_triangulation.ex:436 deliberately pass false (must not force-load during coverage collection), while lib/specled_ex/review/coverage_closure.ex:137 and lib/mix/tasks/spec.triangle.ex:453 pass true. Four call sites split on this flag and no test defends the split.\n  Correct version: assert the only discriminating input — an in-repo module that is loadable but not currently loaded. I confirmed this state is constructible and gives opposite answers:\n      mod = SpecLedEx.Review.CoverageClosure\n      :code.purge(mod); :code.delete(mod); :code.purge(mod)\n      refute match?({_k, _p}, :code.is_loaded(mod))\n      assert Paths.module_source(mod, false) == nil\n      refute match?({_k, _p}, :code.is_loaded(mod))   # load?: false must not load\n      assert Paths.module_source(mod, true) == \"lib/specled_ex/review/coverage_closure.ex\"\n  Observed: nil / \"lib/specled_ex/review/coverage_closure.ex\". That kills both mutants. The purge/delete mutates VM-global code-server state, so it must live in an async: false module (same rationale already documented at test/specled_ex/realization/binding_test.exs:2). The non-loading assertion is the load-bearing half — it pins that load?: false has no side effect, which is exactly why the formatter passes false.\n\n- (b) SECOND SURVIVING MUTANT (warning): the `root \u003c\u003e \"/\"` boundary guard in lib/specled_ex/coverage/paths.ex:20 is untested. I ran a mutant that drops it to `String.starts_with?(absolute, root)` against all eight shipped repo_relative/1 + repo_relative_list/1 assertions — all eight survive. The guard is deliberate (a sibling directory whose name prefixes the repo root must not resolve): with root .../specled_ex-coverage-source-resolver, input .../specled_ex-coverage-source-resolver-sibling/lib/foo.ex returns nil on the real impl and leaks the full absolute path on the mutant. Add: `assert Paths.repo_relative(root \u003c\u003e \"-sibling/lib/foo.ex\") == nil` (root derived via File.cwd!() as at :18). The existing out-of-repo case at :26 uses /tmp/..., which is far outside the root and exercises no boundary.\n\n- (b) minor: :55 and :66 wrap module_source/2 output in repo_relative_list/1 and re-assert what :22, :29 and :43 already cover. Harmless, but not load-bearing — they are composition restatements, not new constraints.\n\n- (d) CWD: no functional break. :18-19 correctly derives root from File.cwd!() rather than hardcoding, and :33 uses a relative path that resolves identically under any cwd. :50 hardcodes the expected repo-relative string, which is safe only because ExUnit runs from the project root — verified passing in the stage worktree. However the module is `async: true` and reads the VM-global cwd (via Paths), and it does not carry the async-safety note this repo requires for exactly this hazard; compare test/specled_ex/review/coverage_closure_test.exs:22-26, which documents that the suite's only File.cd!/1 (test/specled_ex/realization/binding_test.exs:56) lives in an async: false module and ExUnit never overlaps async and sync modules. The reasoning still holds, so this is a convention gap, not a race — but it should be stated, since the load?-discriminating test above will force this module to async: false anyway.\n\n- (e) HYGIENE: clean. No Process.sleep, no spawned or unmanaged processes, no start_supervised needed, no tmp-file writes. The \"/tmp/not-in-this-repo/source.ex\" at :26 is a pure string input never touched on disk, so there is no shared-tmp collision. async: true (ETC-CORE-001), one describe per public function (ETC-CORE-002), and the self-guarding preconditions at :61-62 are genuinely good — they prove the fixture module really is unloadable instead of trusting the name to be unique.\n\nVerdict: the file is a correct and reasonably thorough pin for to_binary/1, repo_relative/1 and repo_relative_list/1, but it does not do the one job the ticket names as key. As shipped, the four call sites could be silently unified on load? and this suite stays green.",
      "timestamp": "2026-07-27T10:27:58Z"
    },
    {
      "text": "RETRACTION of the immediately preceding comment on this ticket (posted by verifier session\nb19669f0-1a1a-42a6-b6f8-39da90177a9c). That comment's body is NOT this ticket's verification —\na shell redirect hit zsh noclobber, so the paste read a sibling verifier's scratchpad file and\nposted a DIFFERENT ticket's result (the \"VERIFY: FAIL\" about\nspecled.decision.cross_vm_temp_names_reach / tracer_test.exs / hash_store_test.exs) under\nspecled_-dn4.12. Ignore that comment entirely for this ticket. Its verdict, file counts, and\nrequirement lines describe another ticket's diff and say nothing about dn4.12. The correct\nresult for specled_-dn4.12 is the comment immediately following this one.",
      "timestamp": "2026-07-27T10:28:38Z"
    },
    {
      "text": "VERIFY: PASS\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\n(This is the authoritative dn4.12 result. The \"VERIFY: FAIL\" comment two above is a mis-paste\nof another ticket's verification — see the retraction comment directly above.)\n\nMechanical checks:\n- Files touched: 6 (lib/specled_ex/coverage/paths.ex NEW, lib/specled_ex/coverage/formatter.ex,\n  lib/specled_ex/coverage_triangulation.ex, lib/specled_ex/review/coverage_closure.ex,\n  lib/mix/tasks/spec.triangle.ex, test/specled_ex/coverage/paths_test.exs NEW)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0\n  - .spec/realization_hashes.json: `git diff 2023373d..HEAD --` EMPTY (confirmed)\n  - CHANGELOG.md, VERSION, .spec/state.json, .spec/specs/branch_guard.spec.md: untouched\n  - mix.exs / mix.lock: EMPTY diff — no new deps\n  - No spec file touched at all; branch-guard cleared by a Spec-Drift trailer, which is the\n    contract's expected posture (spec co-change not exercised, so no justification owed)\n  - (.grok/sandbox.toml untracked launcher artifact ignored per instruction)\n\nDefining constraint (public API) — verified mechanically, not on trust:\n- Diffed the `def `/`defmacro ` head set of all five api_boundary-baselined modules at\n  2023373d vs HEAD: coverage.ex, coverage/formatter.ex, coverage_triangulation.ex,\n  review/coverage_closure.ex, mix/tasks/spec.triangle.ex — ALL FIVE IDENTICAL. Only private\n  (`defp`) heads were removed, which is exactly what leaves hash_module_head_union/2 unchanged.\n- SpecLedEx.Coverage.Paths carries `@moduledoc false` (paths.ex:2) and `@doc false` on each\n  export; grep of .spec/ returns zero hits for \"Coverage.Paths\"/\"coverage/paths\" — absent from\n  every `realized_by:` block, every spec `surface:` list, and .spec/realization_hashes.json.\n- Canary green: test/integration/guard_ergonomics_attestation_test.exs, 2 tests 0 failures\n  (also re-run serially with --max-cases 1: 2 tests, 0 failures).\n\nNo-behavior-change claim — verified clause-by-clause against the deleted bodies:\n- formatter.ex `source_file/1` -\u003e `Paths.module_source(module, false)`. Old\n  `with {_kind,_} \u003c- :code.is_loaded/1` + `source when is_list or is_binary` + `else _ -\u003e nil`\n  + `rescue _ -\u003e nil` maps 1:1 onto `ensure_module/2`'s false-clause + the `case` + the inner\n  `rescue _ -\u003e nil`. Returns `String.t() | nil`. PRESERVED.\n- coverage_triangulation.ex `module_source_file/1` -\u003e `Paths.module_source(mod, false)`, with the\n  `is_atom` guard retained on the wrapper. Direct call at :388 -\u003e `Paths.repo_relative(r.file)`\n  (old had the same `is_binary` clause + catch-all nil, and likewise no rescue).\n  Returns `String.t() | nil`. PRESERVED.\n- review/coverage_closure.ex + mix/tasks/spec.triangle.ex `mfa_source_file/1` -\u003e\n  `Paths.repo_relative_list(Paths.module_source(mod, true))`. Traced every old exit:\n  ensure_loaded fail -\u003e [] ; source neither list nor binary -\u003e [] ; out-of-repo absolute -\u003e [] ;\n  in-repo -\u003e [rel] ; raise inside -\u003e old `rescue _ -\u003e []`, new `module_source`'s `rescue _ -\u003e nil`\n  then list-wrapped -\u003e []. Never nil, never an absolute path. PRESERVED.\n- Loading semantics NOT unified: `ensure_module(module, false)` = `:code.is_loaded/1`,\n  `ensure_module(module, true)` = `Code.ensure_loaded/1` — two distinct clauses (paths.ex:52-58).\n- All four duplicate copies are actually gone: repo-wide grep for\n  `String.starts_with?(absolute, root` returns exactly ONE hit (paths.ex:20). Zero dangling\n  references anywhere in the repo (code, specs, hashes) to the deleted names\n  repo_relative_path / repo_relative_source_path / repo_relative_source / source_to_binary.\n\nSemantic checks:\n- Spec stubs run: 0, passed: 0 (no `.spec/` change; no stub flipped — matches the completion record)\n- Requirements coverage (this ticket has no `Advances:`; covering the must requirement the\n  rewritten return shapes underwrite, plus the ticket's own item-4 enumeration):\n  - specled.triangulation.v1_file_level_path_identity: covered by\n    test/specled_ex/review/coverage_closure_test.exs:254 and :300, and\n    test/mix/tasks/spec_triangle_test.exs:468 and :544 (all `@tag spec:`-tagged, all green here).\n    Would fail if a producer emitted an absolute path, or if an out-of-repo compile source\n    produced a closure file instead of none, or if the `[]`-never-nil shape collapsed to nil and\n    broke the `binding_present?` join. Seams: none — real module compile sources.\n  - Paths internal contract (ticket item 4 enumeration): every enumerated case has a covering\n    assertion in test/specled_ex/coverage/paths_test.exs — charlist :7, binary :11,\n    absolute in-repo -\u003e repo-relative :17-22, out-of-repo absolute -\u003e nil/[] :25-30,\n    already-relative unchanged :33-38, non-binary -\u003e nil/[] :41-46, unloadable module -\u003e nil/[]\n    under BOTH load? modes :57-66 (plus a loaded-module case at :49-55 beyond the enumeration).\n    No enumerated case is missing. Seams: none.\n- Intent-level scope concerns: none\n- Targeted verification: PASS.\n  `mix compile --warnings-as-errors` — clean, no output.\n  `mix test \u003cthe 6 targeted files\u003e` — 119 tests, 0 failures (independently reproduces the\n  implementer's count).\n  `mix spec.check --no-run-commands` — `validate status=pass errors=0 warnings=0`;\n  `findings=10 (error=0 warning=0 info=10)`; verdict line `spec.check result=pass`.\n  `git diff 2023373d..HEAD -- .spec/realization_hashes.json` — EMPTY.\n  Flipped stubs: none.\n- Corpus-scoped claims (doc-shaped diffs, per step 7.5): n/a — code diff\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope, if any: none — this ticket carries no `AUDIT: BOUNCE -\u003e implementer` comment\n\nVerdict: PASS\n\nNotes (advisory — none of these change the verdict):\n1. Guard hardening at the new boundary. `Paths.module_source/2` has `when is_atom(module) and\n   is_boolean(load?)`; formatter's old `source_file/1` had NO guard and returned nil for a\n   non-atom (badarg from `:code.is_loaded/1`, swallowed by its `rescue`), and closure/triangle's\n   old `mfa_source_file/1` returned [] for a non-atom `mod` (badarg from `Code.ensure_loaded/1`,\n   likewise swallowed). The new head raises FunctionClauseError instead — a `rescue` in a `def`\n   body does not catch a head-clause mismatch. I checked reachability on all four call sites\n   rather than assuming: formatter.ex:232-234 `build_file_map` feeds boundary-table module atoms;\n   coverage_triangulation.ex:424-433 `module_source_index` feeds `line_index` keys (module atoms);\n   coverage_closure.ex:118 and spec.triangle.ex:406 feed `closure.owned_mfas ++ closure.shared_mfas`\n   MFA tuples. No non-atom reaches any of them, so this is unreachable in practice — Note, not Gap.\n2. No test separates `load?: false` from `load?: true`. paths_test uses `Paths` itself (already\n   loaded, so both modes resolve) and a nonexistent module (neither mode resolves). A module\n   compiled but not yet loaded is the only input that distinguishes them, and nothing pins it.\n   Not a Gap: item 4 does not enumerate that case, and the distinction is mechanically visible in\n   `ensure_module/2`'s two clauses. Worth a pin if specled_-b23 renegotiates the ensure_loaded posture.\n3. The cited \"arming.ex precedent\" is inaccurate as stated in the ADMISSION comment, which says\n   arming.ex \"is absent from coverage_capture.spec.md's `surface:` list\" — it is NOT absent; it is\n   listed at .spec/specs/coverage_capture.spec.md:94. (It IS correctly absent from\n   realization_hashes.json, so the \"no realization binding\" half holds.) This does not change the\n   verdict: the SCOPE CONTRACT independently and explicitly directs that paths.ex be added to no\n   surface list, the implementer complied, and spec.check surfaces paths.ex only as an info-level\n   `uncovered_policy_files` entry. Flagging it because the precedent is likely to be cited again by\n   a later ticket, where it could be load-bearing.\n4. The 5-line rationale comment deleted from coverage_closure.ex/spec.triangle.ex (\"a source\n   outside the repo root resolves to no file rather than an absolute path no record can equal\")\n   was dropped rather than relocated; paths.ex's module comment covers the lockstep motive but not\n   that rationale. That comment's phrasing \"Call sites that previously inlined this must stay in\n   lockstep\" is also stale on arrival — after this change nothing inlines it, which is the point.",
      "timestamp": "2026-07-27T10:28:38Z"
    },
    {
      "text": "Correction to my retraction above: it says \"the immediately preceding comment\", but another agent's comment landed in between while I was writing. The comment being retracted is identified unambiguously by its content — the \"VERIFY: FAIL\" whose Mechanical checks list .spec/decisions/specled.decision.cross_vm_temp_names_reach.md, test/specled_ex/compiler/tracer_test.exs, and test/specled_ex/realization/hash_store_test.exs. That is another ticket's verification, mis-pasted here; it is not a dn4.12 result. The authoritative dn4.12 result is the VERIFY: PASS below the retraction. Session: b19669f0-1a1a-42a6-b6f8-39da90177a9c",
      "timestamp": "2026-07-27T10:28:55Z"
    },
    {
      "text": "ORCHESTRATOR NOTE — comment trail correction (not a pipeline marker)\n\nThe `VERIFY: FAIL` comment at 2026-07-27T10:27:33Z on THIS ticket is VOID. Its\nMechanical-checks list names `.spec/decisions/specled.decision.cross_vm_temp_names_reach.md`,\n`test/specled_ex/compiler/tracer_test.exs` and `test/specled_ex/realization/hash_store_test.exs`\n— that is specled_-ehv's diff, not dn4.12's. It was mis-posted here and the posting\nagent retracted it at 10:28:38Z and 10:28:55Z.\n\nAuthoritative grading record for specled_-dn4.12 is:\n- `VERIFY: PASS` (2026-07-27T10:28:38Z)\n- `TEST_CRITIC: FAIL` (2026-07-27T10:27:58Z)\n\nOrchestrator disposition: the Selective Audit Gate is NOT met — the test-critic\nrecord is FAIL, and its finding is load-bearing rather than stylistic. Routing to\naudit. Anyone reconstructing this ticket's phase from comments should read the\n10:28:38Z PASS, not the 10:27:33Z stray.",
      "timestamp": "2026-07-27T10:31:03Z"
    },
    {
      "text": "AUDIT: BOUNCE → implementer\n\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\n(The `VERIFY: FAIL` at 10:27:33Z is void — mis-posted from specled_-ehv. Graded against\nthe `VERIFY: PASS` at 10:28:38Z and the `TEST_CRITIC: FAIL` at 10:27:58Z.)\n\nReasoning: The test-critic's claim holds, and it is readable off the source without\nrunning anything — paths_test.exs pins zero discrimination between the two `load?`\nmodes. At :52-53 the subject is `Paths` itself, which is necessarily loaded (the\nassertion is itself a call into `Paths`), so `:code.is_loaded(Paths)` returns\n`{:file, _}` and `Code.ensure_loaded(Paths)` returns `{:module, Paths}` — both\n`ensure_module/2` clauses at paths.ex:54-60 return `true`. At :58-67 the test does not\nmerely happen to pick an agreeing input, it *asserts* the agreement: `refute\nCode.ensure_loaded?(nonexistent)` at :61 and `assert :code.is_loaded(nonexistent) ==\nfalse` at :62, so both clauses return `false`. Collapse paths.ex:54-60 to a single\nclause that ignores `load?` in either direction and all 8 tests stay green. Meanwhile\nthe split is real and load-bearing in production: formatter.ex:260 and\ncoverage_triangulation.ex:436 pass `false` precisely so coverage collection does not\nforce-load, while coverage_closure.ex:137 and spec.triangle.ex:453 pass `true`. This\nrefactor is what makes the pin mandatory rather than nice-to-have: before it, unifying\nthe semantics required editing four helpers in four files; after it, one line at\npaths.ex:54-60 silently retargets all four call sites — and \"keep `load?: false` and\n`load?: true` distinct\" is a named hard constraint in the SCOPE CONTRACT's Out of Scope\n(intent), owned by specled_-b23. Consolidation that concentrates a behavioral fork into\none editable line and ships no test defending the fork is the ticket's defining\ninvariant left unguarded.\n\nEverything else in the ticket checks out, re-derived independently rather than taken\nfrom the VERIFY counts:\n\n- PUBLIC API — clean. Diffed the public `def` head-name set of all five\n  api_boundary-baselined modules at 2023373d vs 26cd483d\n  (coverage.ex, coverage/formatter.ex, coverage_triangulation.ex,\n  review/coverage_closure.ex, mix/tasks/spec.triangle.ex): byte-identical in every\n  case, counts unchanged (14/6/13/1/1). coverage.ex is untouched entirely. Only `defp`\n  heads were removed. `git diff 2023373d..26cd483d -- .spec/realization_hashes.json`\n  is a zero-line diff. `grep -rn \"Coverage.Paths\\|coverage/paths\" .spec/` returns\n  nothing — paths.ex is on no `surface:` list, in no `realized_by:` block, and in no\n  hash baseline.\n\n- SCOPE — clean. 6 files changed, every one on the Allowed list. CHANGELOG.md is NOT\n  touched (the SCOPE CONTRACT's override of the ADMISSION comment was respected), and\n  neither are VERSION, .spec/state.json, .spec/specs/branch_guard.spec.md, mix.exs, or\n  mix.lock. No spec file edited; branch-guard cleared by the `Spec-Drift:` trailer,\n  which the contract names as the expected posture. No opportunistic refactors in the\n  hunks.\n\n- NO-BEHAVIOR-CHANGE — holds, traced clause-by-clause against the deleted bodies.\n  formatter's old `source_file/1` `with {_kind, _loaded_path} \u003c- :code.is_loaded(module)`\n  is exactly `ensure_module(module, false)`'s `match?`; the old `is_list or is_binary`\n  source guard survives as the `case` clause at paths.ex:41; every other path returns\n  nil. Same for `module_source_file/1` (which already carried its own `when is_atom`\n  guard, so no guard change there) and for the direct call at\n  coverage_triangulation.ex:388, where old `repo_relative_source_path/1` and new\n  `repo_relative/1` are the same body with the same `is_binary` guard + catch-all-nil.\n  For the closure/triangle sites, `repo_relative_list(module_source(mod, true))` still\n  yields `[]`-never-nil-never-absolute: nil maps to `[]` at paths.ex:30-33, and the\n  double application of `repo_relative` (once inside `module_source`, once via\n  `repo_relative_list`) is idempotent because the first application already returned a\n  repo-relative path — the identity case is itself pinned at paths_test.exs:32-37. The\n  `must` those shapes underwrite, specled.triangulation.v1_file_level_path_identity\n  (.spec/specs/triangulation.spec.md:243-255), is genuinely defended at the production\n  level: coverage_closure_test.exs:253-256 and :299-303 and spec_triangle_test.exs:466-475\n  and :543-550 drive the real `CoverageClosure.build_v2` / `mix spec.triangle` producers\n  through the rewritten `mfa_source_file/1`, and assert both halves of the requirement —\n  repo-relative join success (`execution_reach: 1/1`, `refute message_contains?(messages,\n  File.cwd!())`) and out-of-repo source contributing no closure file while keeping\n  `binding_present?`. Those are load-bearing, not happy-path decoration.\n\n- SEAMS — no disconnected production path. The verifier disclosed \"Seams: none\" and\n  that is accurate: there is no mock, stub, or fake anywhere in this diff, and all five\n  new call sites are direct static calls (grepped: formatter.ex:260,\n  coverage_triangulation.ex:388 and :436, coverage_closure.ex:137, spec.triangle.ex:453).\n  No INTEGRATION_REGRESSION_LATENT condition — the wiring is connected; the defect is a\n  non-discriminating test, not an unreached path.\n\n- VERIFIER NOTES 1, 3, 4 correctly classified. Note 1 (the new `when is_atom(module)`\n  guard converts an old rescued-to-nil/[] badarg into an uncaught FunctionClauseError,\n  since a `rescue` in a `def` body does not catch a head-clause mismatch) is real but\n  unreachable — I re-walked the four feeders and agree: formatter.ex:233 `build_file_map`\n  maps over boundary-table module atoms, coverage_triangulation.ex:436 already guarded\n  `is_atom`, and the closure/triangle sites destructure `{mod, _fun, _arity}` from\n  closure MFAs. Same disposition for the narrower point that the old `rescue _ -\u003e []` at\n  the closure/triangle call sites is gone entirely, so a raise from `File.cwd!()` inside\n  `repo_relative_list/1` now escapes where it previously became `[]`. Note-level, not a\n  Gap. Note 3's correction stands and is worth carrying forward: arming.ex IS listed at\n  .spec/specs/coverage_capture.spec.md:94, so the ADMISSION comment's \"absent from the\n  surface list\" precedent is wrong as stated — only its \"no realization binding\" half\n  holds. That does not affect this diff (the SCOPE CONTRACT independently directed the\n  no-surface posture and the implementer complied), but a later ticket citing the\n  precedent could be misled.\n\nVerifier miss, for the record: this exact gap is written down in the PASS comment's\nNote 2 and then dismissed — \"Not a Gap: item 4 does not enumerate that case, and the\ndistinction is mechanically visible in `ensure_module/2`'s two clauses.\" Source\nvisibility is not falsifiability; a distinction no assertion can break is untested by\ndefinition, and the SCOPE CONTRACT's Out of Scope (intent) elevates the `load?` split to\na hard constraint independently of item 4's enumeration. That is a non-falsifiable\ncoverage claim accepted at the ticket's own defining behavioral constraint. Routing to\nthe implementer because the fix is a test, not a re-grade.\n\nBounce reason: Add a test to test/specled_ex/coverage/paths_test.exs that discriminates\n`module_source(mod, false)` from `module_source(mod, true)` — the only input that can is\nan in-repo module that is loadable but not currently loaded. The test-critic already\nconfirmed that state is constructible and gives opposite answers:\n\n    mod = SpecLedEx.Review.CoverageClosure\n    :code.purge(mod); :code.delete(mod); :code.purge(mod)\n    refute match?({_k, _p}, :code.is_loaded(mod))\n    assert Paths.module_source(mod, false) == nil\n    assert Paths.module_source(mod, true) == \"lib/specled_ex/review/coverage_closure.ex\"\n\nRequirements on the fix: (1) it must go RED if paths.ex:54-60 is collapsed to a single\n`ensure_module/2` clause in EITHER direction — state that falsifiability explicitly in\nthe completion record rather than asserting coverage; (2) pick a purge target that is\nsafe to unload mid-suite and restore it, and mark the test `async: false` or move it to\na non-async module — purging a module out from under concurrently running async tests is\nexactly the kind of load-sensitive flake this repo has been bitten by; do not purge\n`Paths` itself. (3) Keep the fix inside the Allowed touches: paths_test.exs only. No\nproduction change is needed — paths.ex is correct as written, it is only unguarded. Do\nnot touch CHANGELOG.md, .spec/realization_hashes.json, or any spec file. Re-run the\nSCOPE CONTRACT's four-step verification, and note that `mix format --check-formatted`\nis not in that list but is a CLAUDE.md merge gate — run it before reporting.",
      "timestamp": "2026-07-27T10:35:42Z"
    },
    {
      "text": "IMPLEMENTED\nSession: k6s-dn4.12-impl-2\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-dn4.12/grok-k6s-dn4.12-impl-2/log\n\nCommit: 5069e23eda00511ecd62e2032df515c4e6101484\nFiles touched: test/specled_ex/coverage/paths_test.exs\nTests added: SpecLedEx.Coverage.PathsTest / \"loadable-but-not-loaded module discriminates load?: false from load?: true\"\nVerification stubs flipped: none\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/coverage/paths_test.exs test/specled_ex/coverage/formatter_test.exs test/specled_ex/coverage_triangulation_test.exs test/specled_ex/review/coverage_closure_test.exs test/mix/tasks/spec_triangle_test.exs test/integration/guard_ergonomics_attestation_test.exs (120 tests, 0 failures); git diff --stat -- .spec/realization_hashes.json EMPTY; mix spec.check --no-run-commands (result=pass); mix format --check-formatted green; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\nAudit-expanded scope: none (paths_test.exs only, as instructed by AUDIT: BOUNCE → implementer 2026-07-27T10:35:42Z)\n\nSummary: Closed the audit gap by adding a falsifying test for the load?: false vs load?: true fork in Paths.module_source/2. The case purges SpecLedEx.Review.CoverageClosure (safe mid-suite target; not Paths), asserts non-forcing load? false returns nil while force-loading load? true returns the repo-relative source path, restores the module on_exit, and runs under async: false. Falsifiability confirmed locally: collapsing ensure_module/2 to always :code.is_loaded makes load?: true return nil (RED); collapsing to always Code.ensure_loaded makes load?: false return the path (RED). No production, spec, CHANGELOG, or realization_hashes change.",
      "timestamp": "2026-07-27T10:45:23Z"
    },
    {
      "text": "TEST_CRITIC: FAIL\n\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nFindings:\n\n- ITERATION-1 BLOCKER IS CLOSED. I built both collapse mutants against\n  lib/specled_ex/coverage/paths.ex ensure_module/2 and ran\n  `mix test test/specled_ex/coverage/paths_test.exs` on each. Both go RED, and\n  both fail on the NEW test (paths_test.exs:71), not on anything else:\n    * Mutant A - `defp ensure_module(module, _load?), do: match?({_kind, _p}, :code.is_loaded(module))`\n      -\u003e RED at paths_test.exs:95, `Paths.module_source(mod, true)` left: nil,\n      right: \"lib/specled_ex/review/coverage_closure.ex\". 9 tests, 1 failure.\n    * Mutant B - `defp ensure_module(module, _load?), do: match?({:module, ^module}, Code.ensure_loaded(module))`\n      -\u003e RED at paths_test.exs:90, `Paths.module_source(mod, false)` left:\n      \"lib/specled_ex/review/coverage_closure.ex\", right: nil. 9 tests, 1 failure.\n  Unmutated baseline: 9 tests, 0 failures. The discrimination is real. Both\n  mutants reverted; `git diff` is empty and `git status --porcelain` shows only\n  the pre-existing untracked .grok/sandbox.toml.\n\n- BLOCKING (new, introduced by iteration 2) - the purge destroys real coverage\n  data. test/specled_ex/coverage/paths_test.exs:79-81 purges and deletes\n  SpecLedEx.Review.CoverageClosure, a cover-tracked lib/ module (581 lines).\n  Measured, not theorised:\n    * `mix test --cover test/specled_ex/review/coverage_closure_test.exs`\n      -\u003e `83.69% | SpecLedEx.Review.CoverageClosure`\n    * `mix test --cover test/specled_ex/review/coverage_closure_test.exs \\\n       test/specled_ex/coverage/paths_test.exs`\n      -\u003e `[warning] Module SpecLedEx.Review.CoverageClosure: Failed to collect\n         coverage information. Has it been reloaded or unloaded?`\n      and the module has NO row in the percentage table at all - it is silently\n      dropped from the tally, not merely reduced.\n  Local runtime OTP 27, `code:coverage_support() = true`, i.e. the native\n  coverage path. Counters live in the loaded module instance, so `:code.delete`\n  + `:code.purge` annihilates them and the subsequent `Code.ensure_loaded/1`\n  installs a fresh zeroed instance.\n  Why this is blocking rather than cosmetic:\n    1. CI runs `mix test --include integration --cover` (.github/workflows/specs.yml:131)\n       against the `test_coverage: [summary: [threshold: 83]]` gate in mix.exs:10,\n       and the repo's own comment at specs.yml:124-126 records measured true\n       coverage as 83.05% - 0.05 points of headroom. Silently removing a\n       ~84%-covered 581-line module from the denominator moves that number in a\n       way nobody predicted, on a gate with essentially no margin. I did not run\n       the full suite (per instructions) so I cannot say which side of 83 it\n       lands on - which is itself the problem: the repo's coverage figure now\n       depends on whether this test ran.\n    2. Worse, it corrupts specled's own product data. SpecLedEx.Coverage.Snapshot\n       native mode reads `:code.get_coverage(:line, Module)` (lib/specled_ex/coverage/snapshot.ex:144);\n       purging the module wipes exactly those counters. CoverageClosure carries\n       `# covers: specled.spec_review.coverage_tab_v2_envelope_data_layer`, so\n       the per_test.coverdata that feeds closure/triangulation reach for that\n       binding can lose its lines. A test in the coverage subsystem must not\n       destroy the coverage subsystem's evidence.\n  `async: false` does not help here and is not the relevant axis: ExUnit never\n  overlaps async and sync modules, so no concurrent test file is at risk (see\n  answer to (b)/(c) below) - but the `:cover` / native-coverage coordinator is a\n  BEAM-global observer that ExUnit does not schedule at all, so no ExUnit-level\n  isolation flag can contain a code-server mutation.\n\n- VERIFIED FIX. Do not purge any lib/ module. Purge an ephemeral module that was\n  never in the app's ebin and therefore was never cover-compiled, compiled with a\n  repo-internal `:file` so `repo_relative/1` still resolves. I ran this exact\n  shape against the unmutated Paths and it discriminates identically:\n      tmp = Path.join(System.tmp_dir!(), \"purge-#{System.unique_integer([:positive])}\")\n      File.mkdir_p!(tmp)\n      mod = :\"Elixir.PathsTestPurgeTarget#{System.unique_integer([:positive])}\"\n      src = Path.join(File.cwd!(), \"test/test_support/paths_test_purge_target.ex\")\n      [{^mod, bin}] = Code.compile_string(\"defmodule #{inspect(mod)} do def noop, do: :ok end\", src)\n      File.write!(Path.join(tmp, \"#{mod}.beam\"), bin)\n      Code.prepend_path(tmp)\n      on_exit(fn -\u003e Code.delete_path(tmp); File.rm_rf!(tmp) end)\n      :code.purge(mod); :code.delete(mod); :code.purge(mod)\n  Observed: `:code.is_loaded(mod)` -\u003e false after purge;\n  `Paths.module_source(mod, false)` -\u003e nil;\n  `Paths.module_source(mod, true)` -\u003e \"test/test_support/paths_test_purge_target.ex\".\n  Same two-mutant kill, zero blast radius on cover, no production module touched.\n  Prefer `@tag :tmp_dir` over `System.tmp_dir!` so ExUnit owns the directory.\n\n- (b) purge hygiene, remaining points. Restoration itself is correct: `on_exit`\n  at :83-85 reloads, and the happy path additionally reloads via the\n  `module_source(mod, true)` call at :95, so the module is live again before the\n  test returns even if an assertion aborts mid-test. on_exit is registered after\n  the purge (:79-81) rather than before, which is a narrow window but harmless\n  since nothing between them can raise. `async: false` IS sufficient against\n  concurrent test FILES regardless of `--max-cases`: ExUnit.Runner drains all\n  async modules before taking any sync module, and sync modules run one at a\n  time. The repo already relies on this invariant and documents it at\n  test/specled_ex/review/coverage_closure_test.exs:22-26.\n\n- (c) target choice, correctness half. coverage_closure_test.exs is\n  `async: true` (:27), so it always completes before PathsTest starts, and\n  review_test.exs (async: false, references CoverageClosure at :372) sees a\n  reloaded module whichever order the sync phase picks. So there is no\n  test-outcome flake from the target choice. The target choice is nonetheless\n  wrong for the coverage reason above - it is the coverage tooling running the\n  suite, not a sibling test, that the purge damages.\n\n- (d) The six enumerated cases all still carry assertions and are undisturbed:\n  charlist and binary `to_binary/1` (:9, :13); absolute in-repo -\u003e repo-relative\n  (:19-25); out-of-repo -\u003e nil/[] (:27-32); already-relative unchanged (:34-39);\n  non-binary -\u003e nil/[] (:41-46); unloadable -\u003e nil/[] under both modes (:60-69).\n  9 tests total, consistent with 6 + the both-modes-agree test + the new\n  discriminator. Note the both-modes-agree test at :53-58 survives BOTH mutants -\n  it is the one iteration 1 flagged as actively anti-discriminating. Harmless now\n  that :71 carries the load, but it still cannot fail for the reason it appears\n  to test; consider deleting or renaming it to \"resolves for an already-loaded\n  module\" so it stops reading as load?-coverage.\n\n- (e) cwd. The `repo_relative` tests derive `root` from `File.cwd!()` themselves,\n  so they are cwd-agnostic. The `module_source(Paths, _)` assertions at :53-58\n  and the new test's `expected` DO assume cwd equals the repo root that compiled\n  the beams. That holds under `mix test` and the suite's only `File.cd!/1` lives\n  in another async: false module, so it cannot overlap. Latent coupling, not a\n  defect.\n\n- (f) No `Process.sleep`, no unmanaged processes, no spawned tasks, no shared\n  tmp paths. Clean on all three.\n\nVerdict: the load?-discrimination gap is genuinely closed and the mutant\nevidence is solid - the only thing standing between this and a PASS is the purge\ntarget. Swap SpecLedEx.Review.CoverageClosure for an ephemeral compiled-in-test\nmodule as shown and I expect this to pass on iteration 3.",
      "timestamp": "2026-07-27T12:33:00Z"
    },
    {
      "text": "VERIFY: FAIL\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nTicket: specled_-dn4.12, ITERATION 2 (26cd483d + 5069e23e, HEAD 5069e23e, base 2023373d).\nThe 10:27:33Z `VERIFY: FAIL` on this ticket is void (specled_-ehv's diff); graded fresh.\n\nThe bounce gap IS closed — I proved it with both mutants myself. The FAIL is for a\nseparate, newly-introduced defect in the *same* new test: its `:code.purge`/`:code.delete`\npermanently de-instruments a cover-compiled production module for the rest of any\n`--cover` run, which is how this repo's CI invokes the suite.\n\nMechanical checks:\n- Files touched: 6 (lib/specled_ex/coverage/paths.ex NEW, lib/specled_ex/coverage/formatter.ex,\n  lib/specled_ex/coverage_triangulation.ex, lib/specled_ex/review/coverage_closure.ex,\n  lib/mix/tasks/spec.triangle.ex, test/specled_ex/coverage/paths_test.exs NEW)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0\n  CHANGELOG.md, VERSION, .spec/state.json, .spec/specs/branch_guard.spec.md, mix.exs,\n  mix.lock all untouched (the orchestrator's CHANGELOG override was respected). No spec\n  file edited at all; branch-guard cleared by `Spec-Drift:` trailers, the contract's\n  expected posture. Untracked `.grok/sandbox.toml` ignored per instruction.\n- Public-export delta per constrained module (step 5) — re-derived, not taken on trust.\n  Extracted the `def`/`defmacro`/`defdelegate` head multiset from `git show 2023373d:\u003cf\u003e`\n  vs `git show HEAD:\u003cf\u003e` and diffed:\n  - SpecLedEx.Coverage (coverage.ex): 14 -\u003e 14, IDENTICAL (file untouched entirely)\n  - SpecLedEx.Coverage.Formatter: 6 -\u003e 6, IDENTICAL\n  - SpecLedEx.CoverageTriangulation: 13 -\u003e 13, IDENTICAL\n  - SpecLedEx.Review.CoverageClosure: 1 -\u003e 1, IDENTICAL\n  - Mix.Tasks.Spec.Triangle: 1 -\u003e 1, IDENTICAL\n  Only `defp` heads were removed, which is what leaves `hash_module_head_union/2` unchanged.\n- realization_hashes.json diff empty: y (`git diff --stat 2023373d..HEAD -- .spec/realization_hashes.json` produced no output)\n- SpecLedEx.Coverage.Paths: `@moduledoc false` at paths.ex:2, `@doc false` on all four\n  exports; `grep -rn \"Coverage\\.Paths\\|coverage/paths\" .spec/` returns ZERO hits — absent\n  from every `realized_by:` block, every spec `surface:` list, and the hash baseline.\n  `mix spec.check` surfaces it only as an info-level `uncovered_policy_files` entry.\n\nBounce-gap closure:\n- load?: false vs true discrimination pinned: **CLOSED**. Built both collapse mutants myself\n  against lib/specled_ex/coverage/paths.ex:54-60 and ran the shipped suite:\n  - Mutant A — collapse to always `Code.ensure_loaded` (ignore `load?`):\n    RED. 9 tests, 1 failure. `paths_test.exs:90` — `assert Paths.module_source(mod, false) == nil`\n    got `\"lib/specled_ex/review/coverage_closure.ex\"`.\n  - Mutant B — collapse to always `:code.is_loaded` (ignore `load?`):\n    RED. 9 tests, 1 failure. `paths_test.exs:95` — `assert Paths.module_source(mod, true) == expected`\n    got `nil`.\n  Each mutant is killed by a *different* assertion of the new test, and in both runs the\n  other 8 tests stayed green — which independently reconfirms the test-critic's iteration-1\n  finding that the pre-existing cases are non-discriminating, and that the new case is the\n  only thing carrying the load. Reverted via `git checkout --`; both `ensure_module/2`\n  clauses restored; re-ran `mix test test/specled_ex/coverage/paths_test.exs` = 9 tests,\n  0 failures. `git status --porcelain` clean (only untracked `.grok/sandbox.toml`).\n- Purge hygiene, the three checks named in the brief: all PASS.\n  - `async: false` — yes, paths_test.exs:3, with a comment stating the reason.\n  - restores the module — yes, `on_exit(fn -\u003e _ = Code.ensure_loaded(mod) end)` at :83-85;\n    additionally the test body itself force-loads it at :95 before finishing.\n  - not depended on by a concurrent test — confirmed. All four test modules referencing\n    `SpecLedEx.Review.CoverageClosure` (review_test.exs, review/coverage_closure_test.exs,\n    coverage/mfa_lines_test.exs, coverage/paths_test.exs) are `async: false`, so no async\n    module can overlap the purge window.\n- Purge hygiene, a FOURTH leak the three checks do not cover: **OPEN — see Gap 1.**\n\nSemantic checks:\n- Spec stubs run: 0, passed: 0 (no `.spec/` file touched; no stub flipped — matches the\n  completion record, confirmed by name-only diff)\n- Requirements coverage. This ticket has no `Advances:`; the two file-surface subjects are\n  `specled.coverage_capture` (41 `must`) and `specled.triangulation` (16 `must`) = 57, all\n  accounted individually below. The diff's only production edits are four source-path\n  seams, so reachability is decided by whether a requirement's realization transits one:\n    S1 = `Formatter.source_file/1` (formatter.ex:260) -\u003e `build_file_map/1` (:233) -\u003e `state.file_map` (:171, :209)\n    S2 = `CoverageTriangulation.module_source_file/1` (:435) -\u003e `module_source_index/1` (:424) -\u003e `per_test_requirement_reach/3` (:284)\n    S3 = `Paths.repo_relative(r.file)` (coverage_triangulation.ex:388) inside `group_records/2` (:380),\n         shared by `group_records_by_test/1` (findings/3 :117, execution_reach_map/2 :158, per_requirement_reach/2 :200)\n         and `group_records_with_lines/1` (:283)\n    S4 = `mfa_source_file/1` in CoverageClosure (:136) and Mix.Tasks.Spec.Triangle (:452) -\u003e `closure_files` (:118 / :406)\n  Every \"not reachable\" line below names the function that realizes the requirement and\n  states that it is not one of S1-S4 and does not call them — checkable by grep.\n\n  ON-SEAM (specled.coverage_capture, S1):\n  - path_identity: covered by formatter_test.exs:396-445 (tagged, multi-req) — asserts\n    `refute Path.type(record.file) == :absolute` and `record.file == Path.relative_to_cwd(__ENV__.file)`;\n    also formatter_test.exs:185-188 asserts `state.file_map`'s value equals\n    `Path.relative_to_cwd(source)`. Would fail if `Paths.repo_relative/1` dropped the\n    `root \u003c\u003e \"/\"` prefix test or returned the absolute path. Seams: none — real Formatter\n    GenServer, real modules, stub only for `snapshot_fn` (the spec'd DI seam).\n  - unmapped_modules_meta: covered by formatter_test.exs:440 (`assert envelope.meta.unmapped_modules == [unmapped]`).\n    Would fail if `Paths.module_source/2` returned a path instead of nil for an unloadable\n    module, since the module would then map to a file and drop out of the list. Seams: none.\n  - formatter_auditor: covered by formatter_test.exs:196, :396-445, :649, :747. The\n    seam-relevant half is \"boundary payload and remainder compaction reuse the same\n    suite-start module-to-source map\"; :443-445 asserts the unmapped module's line is\n    absent from `meta.unattributed`. Would fail if S1 resolved unmapped modules. Seams: none.\n  - envelope_meta: covered by formatter_test.exs:396-445 (meta.unattributed / meta.unmapped_modules\n    shapes) plus store_test.exs:128, :216, :286, :307 for the container contract. Would fail\n    if S1 emitted absolute paths into `meta.unattributed`'s `{file, lines}` tuples. Seams: none.\n  - per_test_v2_envelope: covered by formatter_test.exs:436-441 (payload record `file`,\n    envelope `files` union) and the module-level manifest at spec_cover_test_test.exs:15.\n    Would fail if S1 changed record `file` identity, breaking the payload/files union.\n    Seams: none for the formatter leg.\n  - boundary_row_exclusive: covered by formatter_test.exs:343ff. Transits S1 only via\n    \"compacted via the formatter's file map\"; the requirement's own subject (exclusive\n    derivation from the boundary window) is unchanged. Would fail if S1 returned nil for a\n    loaded in-repo module, collapsing the record's file. Seams: none.\n  - unhooked_degrade: covered by formatter_test.exs:448ff. Transits S1 only for the\n    remainder's file identities. Would fail if S1 mis-resolved a remainder module's source.\n    Seams: none.\n  - per_test_exclusive_attribution: covered by the integration leg manifested at\n    spec_cover_test_test.exs:18. Transits S1 only for record `file`; the requirement's\n    subject is `lines_hit` disjointness, untouched. Would fail if S1 merged two distinct\n    modules onto one file identity. Seams: none (child-BEAM fixture run).\n\n  ON-SEAM (specled.triangulation):\n  - v1_file_level_path_identity (S4, the requirement the rewritten return shapes underwrite):\n    covered by coverage_closure_test.exs:254 and :300, and spec_triangle_test.exs:468 and :544\n    — all `@tag spec:`-tagged, all green here. Would fail if a producer emitted an absolute\n    closure file, or if an out-of-repo compile source produced a closure file instead of\n    none, or if `[]`-never-nil collapsed to nil and broke the `binding_present?` join.\n    Seams: none — real `CoverageClosure.build_v2/2` and real `mix spec.triangle` over real\n    module compile sources.\n  - per_test_path_identity (S2+S3): covered by coverage_closure_test.exs:527. Would fail if\n    record `:file` and module compile source stopped normalizing to the same identity —\n    i.e. if S3 or S2 diverged. Seams: none.\n  - per_test_unresolvable_source_partition (S2): covered by coverage_closure_test.exs:555,\n    :566, :580, :597. Would fail if `Paths.module_source(mod, false)` returned a path for a\n    module absent from the line index, emptying the partition. Seams: none.\n  - per_test_requirement_reach (S2+S3): covered by the module manifest at\n    coverage_triangulation_test.exs:33 and by coverage_closure_test.exs:506ff. Would fail if\n    S3 mis-normalized record files, breaking the line-set intersection. Seams: none.\n  - pure_function (S3): covered by coverage_triangulation_test.exs:42, :53. Would fail if the\n    rewrite introduced filesystem access into `findings/3` — note `Paths.repo_relative/1`\n    calls `File.cwd!()`, exactly as the deleted `repo_relative_source_path/1` did, so this\n    is unchanged; `File.cwd!()` is not a filesystem *read* of project content and was\n    already present at base. Seams: none.\n  - untested_realization (S3): covered by coverage_triangulation_test.exs:102. Would fail if\n    S3 stopped normalizing record files and closure-file joins ceased to match. Seams: none.\n  - untethered_test (S3): covered by coverage_triangulation_test.exs:160 and the module\n    manifest at :25. Would fail as above. Seams: none.\n  - untethered_test_opt_out (S3): covered by the module manifest at coverage_triangulation_test.exs:26.\n    Transits S3 only through the shared `group_records/2`; the opt-out tag logic is untouched.\n    Would fail if S3 dropped records with resolvable files. Seams: none.\n  - underspecified_realization (S3): covered by coverage_triangulation_test.exs:237. Would\n    fail as above. Seams: none.\n  - envelope_async_contaminated (S3, partial): covered by the module manifest at\n    coverage_triangulation_test.exs:31. Only its non-degraded clause (:838, delegating\n    `:payload` to `findings/3`) transits S3; the degraded clause at :808 returns before\n    any record grouping. Would fail if S3 broke `findings/3`'s delegation. Seams: none.\n\n  NOT REACHABLE BY THIS DIFF (specled.coverage_capture) — each realized by a function that\n  is not S1-S4 and does not call one; the diff changes zero lines in any of them, so their\n  coverage is byte-identical to base:\n  - serialized_run: `Mix.Tasks.Spec.Cover.Test` arming/async config; no path resolution.\n  - per_test_async_contamination: same task's pre-run `async: true` file scan.\n  - per_test_allow_async_degrade: same task's `--allow-async` branch.\n  - default_aggregate_run: same task's default branch + `Aggregate.ingest/2` + `Store.write_v2/2`.\n  - default_aggregate_red_suite_passthrough: same task's exit-code propagation.\n  - default_aggregate_empty_refusal: same task's refusal branch + `Store.read_status/1`.\n  - formatter_snapshot_fn_di: `Formatter.init/1` + `Arming.resolve/1` option plumbing.\n  - formatter_arming_seam: `Coverage.Arming.resolve/1` and `Formatter.init/1` inertness.\n  - formatter_no_fabrication: `Snapshot.diff/2` strict-increase rule; operates on line counts.\n  - snapshot_runtime_mode: `Snapshot.runtime_mode/0`, `native_snapshot/1`, `classic_snapshot/1`.\n  - snapshot_diff_strictly_increased: `Snapshot.diff/2`.\n  - snapshot_negative_delta_diagnostic: `Snapshot.diff/2` diagnostic branch.\n  - per_test_artifact_freshness: the task's `generated_at` staleness check.\n  - cumulative_parity: `.coverdata` export equality; no source-path resolution.\n  - keyed_by_test_pid: `Formatter.extract_key/1` inventory keying, not `file_map`.\n  - anonymous_ets: `:ets.new/2` call shape in `Formatter.init/1`.\n  - artifact_path: `Store` v1 schema + path constant.\n  - store_split: `Store` module boundary.\n  - integration_case: `SpecLedEx.IntegrationCase.run_fixture_mix_test/2` child-BEAM spawn.\n  - store_v2_envelope: `Store.build_envelope/1`, `write_v2/2`, `read_v2/1`, `read_status/1`.\n  - store_v2_legacy_rejection: `Store.read_v2/1` three-way decode.\n  - aggregate_ingest: `SpecLedEx.Coverage.Aggregate.ingest/2` — resolves sources via its OWN\n    `source_for/2` + `relative_under_root/2` (aggregate.ex:181-203), which this diff does not\n    touch and did not consolidate (see Note 1). Not S1-S4.\n  - aggregate_empty_coverage: `Aggregate.ingest/2` empty branch.\n  - aggregate_unmapped_degraded: `Aggregate.analyse_modules/2` + its own `source_for/2`; not S1-S4.\n  - mfa_key_round_trip: `Coverage.MfaKey.format/1` / `parse/1`; pure string codec.\n  - mfa_lines_index: `Coverage.MfaLines.index/1` via `:beam_lib.chunks/2`; reads beams, not sources.\n  - boundary_hook_sync: `Coverage.Boundary.head/1` / `tail/2` ETS + snapshot chaining.\n  - boundary_noop_unarmed: `Coverage.per_test_boundary/1` / `Boundary.head/1` unarmed guards.\n  - case_template: `SpecLedEx.Case` CaseTemplate macro expansion.\n  - write_v2_argument_error_contract: `Store.write_v2/2` exception typing.\n  - degraded_reasons: `Store.degraded_reasons/1` + the formatter's reason fold; no paths.\n  - never_ran_not_inventoried: `Formatter.handle_cast` `test_finished` state filter.\n  - unhooked_remediation_notice: `Formatter`'s `suite_finished` stderr notice; module names, not paths.\n\n  NOT REACHABLE BY THIS DIFF (specled.triangulation) — same basis:\n  - detector_unavailable_on_missing_coverage: `findings(:no_coverage_artifact, _, _)` at\n    coverage_triangulation.ex:98 returns before `group_records/2` (S3) is ever called.\n  - envelope_legacy_and_invalid_distinct: `envelope_findings/3` clauses at :786 and :795,\n    both early-return before record grouping.\n  - envelope_aggregate_untested_realization: `envelope_findings/3` `:aggregate` clause (:843),\n    reads each MFA's `:covered` boolean from the envelope; never calls S1-S4.\n  - envelope_per_test_only_detectors_unavailable: same `:aggregate` clause + `aggregate_requirement_reach/2` (:871).\n  - envelope_aggregate_underspecified_realization: same `:aggregate` clause, derived from the\n    tag index alone.\n  - aggregate_requirement_reach_mfa_intersection: `aggregate_requirement_reach/2` (:871) reads\n    envelope `:mfas` / `:files` that are already repo-relative on disk; does not call S1-S4.\n  (`execution_reach_metric` and `spec_triangle_task` are `should`, not `must`, and are excluded\n   from this enumeration by the step-10 scope.)\n\n- Intent-level scope concerns: none. The `:code.is_loaded/1` vs `Code.ensure_loaded/1`\n  semantics remain DISTINCT (two `ensure_module/2` clauses, paths.ex:54-60, now\n  mutation-proven load-bearing). No new deps (`mix.exs`/`mix.lock` empty diff). No\n  CHANGELOG edit. `.spec/specs/branch_guard.spec.md` untouched — its line-11 false claim was\n  correctly left alone. No spec prose weakened, no false `decisions:` link added.\n- Targeted verification: PASS (all four contract steps, plus the CLAUDE.md format gate).\n  - `mix compile --warnings-as-errors` -\u003e exit 0, no output\n  - `mix test \u003cthe 6 targeted files\u003e` -\u003e 120 tests, 0 failures (seed 516684)\n  - `mix format --check-formatted` -\u003e exit 0\n  - `mix spec.check --no-run-commands` -\u003e `validate status=pass errors=0 warnings=0`;\n    `findings=10 (error=0 warning=0 info=10)`; verdict `spec.check result=pass`\n  - `git diff --stat 2023373d..HEAD -- .spec/realization_hashes.json` -\u003e EMPTY\n  No tagged_tests leg went red, so no serial re-run was needed.\n- No-behavior-change re-derivation (step 7), traced clause-by-clause against the deleted bodies:\n  - formatter.ex `source_file/1` -\u003e `Paths.module_source(module, false)`: old\n    `with {_kind,_} \u003c- :code.is_loaded(module)` is exactly `ensure_module(module, false)`'s\n    `match?`; the old `source when is_list or is_binary` guard survives as the `case` clause\n    at paths.ex:41; old `else _ -\u003e nil` and `rescue _ -\u003e nil` both survive. Returns\n    `String.t() | nil`. PRESERVED — y.\n  - coverage_triangulation.ex `module_source_file/1` -\u003e `Paths.module_source(mod, false)`,\n    with the pre-existing `when is_atom(mod)` guard retained on the wrapper (so no guard\n    delta here at all). Returns `String.t() | nil`. PRESERVED — y.\n  - coverage_triangulation.ex:388 direct call -\u003e `Paths.repo_relative(r.file)`: old\n    `repo_relative_source_path/1` was the same body with the same `is_binary` guard and\n    catch-all nil, and likewise carried no `rescue`. Returns `String.t() | nil`. PRESERVED — y.\n  - review/coverage_closure.ex + mix/tasks/spec.triangle.ex `mfa_source_file/1` -\u003e\n    `Paths.repo_relative_list(Paths.module_source(mod, true))`. Traced every old exit:\n    ensure_loaded fail -\u003e `[]`; source neither list nor binary -\u003e `[]`; out-of-repo absolute\n    -\u003e `[]`; in-repo -\u003e `[rel]`; raise inside -\u003e old `rescue _ -\u003e []`, new `module_source`'s\n    own `rescue _ -\u003e nil` then list-wrapped -\u003e `[]`. Never nil, never an absolute path.\n    The double application of `repo_relative` (once inside `module_source`, once via\n    `repo_relative_list`) is idempotent: the first application already returned a\n    repo-relative path, and `Path.expand(rel, root) |\u003e Path.relative_to(root)` returns it\n    unchanged — that identity case is itself pinned at paths_test.exs:33-37. PRESERVED — y.\n  - All four duplicate copies really are gone: repo-wide grep for\n    `String.starts_with?(absolute, root` returns exactly ONE hit (paths.ex:20), and there\n    are zero dangling references anywhere (code, specs, hashes) to `repo_relative_path`,\n    `repo_relative_source_path`, `repo_relative_source`, or `source_to_binary`.\n- Item-4 case coverage (step 9): all seven enumerated cases covered — y for each.\n  - charlist -\u003e to_binary/1: y, paths_test.exs:9\n  - binary -\u003e to_binary/1: y, paths_test.exs:13\n  - absolute in-repo -\u003e repo-relative: y, paths_test.exs:21-22\n  - out-of-repo absolute -\u003e nil / []: y, paths_test.exs:28-29\n  - already-relative unchanged: y, paths_test.exs:35-36\n  - non-binary -\u003e nil / []: y, paths_test.exs:40-43\n  - unloadable module -\u003e nil / [] under BOTH load? modes: y, paths_test.exs:57-67\n    (self-guarded at :61-62, which proves the fixture really is unloadable)\n  Beyond the enumeration: loaded-module case at :49-55 and the new discriminating case at :71-101.\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope: test/specled_ex/coverage/paths_test.exs only, per\n  `AUDIT: BOUNCE -\u003e implementer` at 2026-07-27T10:35:42Z. Iteration 2's commit 5069e23e\n  touches exactly that one file — the audit's constraint (3) was respected.\n\nVerdict: FAIL\n\nGaps:\n- GAP 1 — the new test's purge silently destroys coverage accounting for a production\n  module under the repo's own CI invocation. `test/specled_ex/coverage/paths_test.exs:79-81`\n  calls `:code.purge/1` + `:code.delete/1` + `:code.purge/1` on\n  `SpecLedEx.Review.CoverageClosure`, a cover-compiled `lib/` module. Executed evidence:\n    $ mix test --cover test/specled_ex/review/coverage_closure_test.exs\n      -\u003e \"83.69% | SpecLedEx.Review.CoverageClosure\"\n    $ mix test --cover test/specled_ex/review/coverage_closure_test.exs test/specled_ex/coverage/paths_test.exs\n      -\u003e \"[warning] Module SpecLedEx.Review.CoverageClosure: Failed to collect coverage\n          information. Has it been reloaded or unloaded?\"\n         and the module is ABSENT from the coverage table entirely.\n  Reproduced on seeds 0, 1, 2, 3, 7 and 42 — seed-independent, so test ordering does not\n  rescue it (the purge de-instruments the module for the remainder of the run regardless of\n  when it happens). The `on_exit` restore is incomplete by construction:\n  `Code.ensure_loaded/1` reloads the ORDINARY beam, not the cover-compiled one, which is\n  precisely why the warning fires.\n  Consequence, source-verified rather than assumed: Elixir builds its summary from\n  `:cover.modules()` (mix/tasks/test.coverage.ex:271) and keeps only those in `gather_coverage/2`'s\n  `keep_set`, so the module is dropped from BOTH the numerator and the denominator — it is\n  excluded, not zeroed. CI runs `mix test --include integration --cover`\n  (.github/workflows/specs.yml:131) on both matrix legs, arming `test_coverage: [summary:\n  [threshold: 83]]` (mix.exs:10) against a recorded 83.05% — 0.05 points of headroom.\n  Magnitude: coverage_closure.ex is 581 of 30,732 lib lines (~1.9%) at 83.69% vs a ~83.05%\n  total, so the arithmetic drop is roughly 0.01 points. That alone does NOT breach the\n  threshold, and I did not run the full suite to confirm the current total. Why it is still\n  a Gap rather than a Note:\n    (a) It permanently removes a production module from the gate's denominator, so any\n        FUTURE coverage regression in coverage_closure.ex is invisible to the threshold\n        gate — a durable hole in the repo's own quality gate, introduced by this diff.\n    (b) It emits a warning naming a production module as uncollectable on every CI run of a\n        project whose entire subject matter is coverage attribution.\n    (c) It contradicts the audit's explicit fix-requirement (2), \"pick a purge target that\n        is safe to unload mid-suite and restore it\". This target is not safe to unload\n        mid-suite under this repo's own test invocation, and the restore does not restore\n        what was lost.\n    (d) It is new to this diff. Every pre-existing purge in the suite targets a test FIXTURE\n        (SpecLedEx.BindingFixtures.*, SpecLedEx.ApiBoundaryFixtures.*, runtime-compiled\n        modules) — `git grep :code.purge 2023373d -- test/ lib/` shows zero production-module\n        purges at base. paths_test.exs is the first.\n    (e) The repo has already been bitten by this exact class and deliberately quarantined\n        it: specs.yml:127-130 documents moving snapshot_test.exs's cover-compiled fixtures\n        into a child BEAM so they \"can no longer leak spurious 100% rows into the tally\".\n        This reintroduces tally contamination from the opposite direction.\n  Suggested fix, inside the already-Allowed file and matching the repo's own established\n  pattern: purge a runtime-compiled fixture instead of a `lib/` module — the\n  `Code.compile_file` + write-beam + `:code.purge`/`:code.delete`/`:code.load_binary`\n  recipe already used at test/specled_ex/coverage/mfa_lines_test.exs:125-138. I verified\n  that pattern is clean: `mix test --cover test/specled_ex/coverage/mfa_lines_test.exs`\n  emits no \"Failed to collect\" warning and corrupts no tally, because a runtime-compiled\n  module was never in `:cover.modules()`. The fixture's source must be written under\n  `File.cwd!()` so `Paths.module_source(mod, true)` still yields a repo-relative path and\n  the discrimination assertion keeps its meaning. (Re-instrumenting via `:cover.compile_beam`\n  in `on_exit` is NOT a good alternative — it would zero the module's counters, trading a\n  denominator hole for a numerator hole.)\n  Whatever fix is chosen must keep both collapse mutants RED; that property is currently\n  satisfied and must not regress.\n\nNotes: advisory only, none affect the verdict.\n1. A FIFTH copy of the repo-relative resolver survives, in `SpecLedEx.Coverage.Aggregate`\n   (`source_for/2` + `relative_under_root/2`, aggregate.ex:181-203) — same\n   `String.starts_with?(abs, root \u003c\u003e \"/\")` + `Path.relative_to` shape. Correctly out of\n   scope: the ticket's mandate enumerated exactly four copies, and Aggregate's variant is\n   parameterized by an `:root` ingest option (aggregate.ex:56) whereas `Paths.repo_relative/1`\n   hardcodes `File.cwd!()`, so folding it in would require widening the `Paths` signature or\n   changing Aggregate's contract. Flagging it only because the ticket's stated motive is\n   \"all copies must stay in lockstep\", and after this change the lockstep set is 2 (Paths +\n   Aggregate), not 1.\n2. Guard hardening at the new boundary, unreachable in practice. `Paths.module_source/2`\n   carries `when is_atom(module) and is_boolean(load?)`; formatter's old `source_file/1` had\n   NO guard and returned nil for a non-atom (badarg swallowed by its `rescue`), and the old\n   `mfa_source_file/1` returned `[]` (likewise swallowed). The new head raises\n   FunctionClauseError instead, since a `rescue` in a `def` body does not catch a head-clause\n   mismatch. I re-walked all four feeders rather than assuming: formatter.ex:233\n   `build_file_map` maps over module atoms; coverage_triangulation.ex:428 iterates\n   `line_index` keys and its wrapper already guarded `is_atom`; coverage_closure.ex:118 and\n   spec.triangle.ex:406 destructure `{mod, _fun, _arity}` from closure MFAs. No non-atom\n   reaches any of them. Same disposition for the narrower point that the old\n   `rescue _ -\u003e []` at the closure/triangle sites is gone, so a raise from `File.cwd!()`\n   inside `repo_relative_list/1` would now escape — but `module_source/2` keeps its own\n   rescue, and a nil result short-circuits before the second `File.cwd!()`.\n3. The `root \u003c\u003e \"/\"` boundary guard at paths.ex:20 is still untested, as the iteration-1\n   test-critic reported under (b). The out-of-repo case at paths_test.exs:26 uses\n   `/tmp/not-in-this-repo/...`, far outside the root, so it exercises no boundary; a mutant\n   dropping the `\u003c\u003e \"/\"` survives every shipped assertion. The audit did not require this\n   and it is not a Gap, but it is a one-line add:\n   `assert Paths.repo_relative(root \u003c\u003e \"-sibling/lib/foo.ex\") == nil`.\n4. Carried forward from iteration 1 and still accurate: the ADMISSION comment's \"arming.ex\n   precedent\" is wrong as stated — arming.ex IS listed at\n   .spec/specs/coverage_capture.spec.md:94, so only the \"no realization binding\" half holds.\n   It did not affect this diff (the SCOPE CONTRACT independently directed the no-surface\n   posture and the implementer complied), but a later ticket citing the precedent could be\n   misled.\n5. Process observation, not a finding against the implementer: while I was preparing my\n   first mutant, `lib/specled_ex/coverage/paths.ex` changed on disk between my Read and my\n   Edit, into a state that was neither the committed content nor my intended edit (a\n   single-clause `ensure_module(module, _load?)` with the `:code.is_loaded` body). Something\n   other than this session wrote to the stage worktree mid-verification. It did not affect\n   my results — I hash-checked the file immediately before and after every mutant run, ran\n   both collapses explicitly, and confirmed `git status --porcelain` clean with both clauses\n   restored at the end — but concurrent writers in a shared stage worktree are a real hazard\n   for this pipeline, alongside the shared-scratchpad one already known.",
      "timestamp": "2026-07-27T12:43:54Z"
    },
    {
      "text": "AUDIT: BOUNCE → implementer\n\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\n(Graded against the TEST_CRITIC: FAIL at 2026-07-27T12:33:00Z. The VERIFY: FAIL at\n2026-07-27T10:27:33Z is void — specled_-ehv's diff, mis-posted, retracted.)\n\nReasoning:\n\nQ1 — THE DEFECT IS REAL AND BLOCKING. The critic did not over-read; I traced the\nmechanism to OTP source rather than re-running the measurement.\n\n- The purge targets a cover-tracked lib/ module. paths_test.exs:75-81 purges\n  `SpecLedEx.Review.CoverageClosure`. Elixir's cover tool cover-compiles every beam in\n  `Mix.Project.compile_path()` — `Mix.Tasks.Test.Coverage.cover_compile/1` calls\n  `:cover.compile_beam(beams(compile_path))` with no filter. `Elixir.SpecLedEx.Review.CoverageClosure.beam`\n  is in `_build/test/lib/spec_led_ex/ebin`. It is tracked.\n\n- The counters are destroyed, and I can name the exact code path. Active toolchain here\n  is OTP 27.2 / tools-4.1.1, `code:coverage_support() = true` (verified), so cover takes\n  the native path. `cover.erl:2366 native_move(Mod)` calls `code:get_coverage(cover_id_line, Mod)`;\n  on a module that was unloaded it raises `badarg`, which is caught at :2371-2373 →\n  `log_native_move_error/1` (:2394-2396, the exact \"Failed to collect coverage information.\n  Has it been reloaded or unloaded?\" string the critic saw) → `Coverage0 = []` → every bump\n  index maps to 0 at :2385-2391. Separately, `cover.erl:2002 get_compiled_still_loaded/2`\n  filters the compiled set by `code:which(Module) =:= cover_compiled`; after\n  `:code.delete` + `Code.ensure_loaded` reloads the plain beam, `code:which/1` returns the\n  beam path, so the module is classed unloaded, `do_clear/1` wipes its DB entry, and it\n  leaves `:cover.modules()`. That is why the critic observed no row rather than a 0% row —\n  both halves of the mechanism check out, and both are destructive.\n\n- The tally really does move. `Mix.Tasks.Test.Coverage.gather_coverage/2` builds its ETS\n  table with `module in keep_set` where `keep = :cover.modules() -- ignored`, and computes\n  `totals` from that same table. A module dropped from `keep` leaves BOTH numerator and\n  denominator. Since its own coverage (83.69%) is above the corpus figure (83.05%),\n  removing it moves the total DOWN. I did not run the full suite, so I cannot say by how\n  much — which is precisely the objection: `.github/workflows/specs.yml:131` runs\n  `mix test --include integration --cover` against `test_coverage: [summary: [threshold: 83]]`\n  (mix.exs:10), the repo records true coverage at 83.05% (specs.yml:124-126), and\n  `summary/3` hard-fails via `System.at_exit(exit {:shutdown, 3})` on `totals \u003c threshold`.\n  Shipping a test that moves a 0.05-point-headroom CI gate by an unmeasured amount in the\n  wrong direction is not acceptable on a merge-gated branch.\n\n- The product-data harm is real too. `Coverage.Snapshot.native_lines/1` reads\n  `:code.get_coverage(:line, mod)` (lib/specled_ex/coverage/snapshot.ex:143-144) from those\n  same counters. And the requirement one entry above the one this ticket underwrites —\n  `.spec/specs/triangulation.spec.md:239-241` — says out-of-repo modules \"shall not be\n  removed from the coverage-percentage denominator.\" A test in the coverage subsystem that\n  removes a module from the coverage-percentage denominator is the wrong shape on its face.\n\n- What is NOT a defect, for the record: `async: false` and the `on_exit` restoration are\n  correct and sufficient against sibling test files (ExUnit drains async modules before any\n  sync module). The critic is right that this is the wrong axis — `:cover` is a BEAM-global\n  observer ExUnit does not schedule, so no ExUnit isolation flag can contain it.\n\nQ2 — REMEDY SHAPE: (b), an ephemeral module compiled to a temp beam dir at runtime.\nI ruled out the alternatives on source, not preference:\n\n- (a) is NOT viable in either form, and the framing behind it is false for this repo.\n  A module defined inline in a `.exs` has no beam on disk, so after purge\n  `Code.ensure_loaded/1` returns `{:error, :nofile}` — it becomes unloadable, not\n  loadable-but-unloaded, and both `load?` modes agree again. And `test/` IS cover-tracked\n  here: `mix.exs:26` sets `elixirc_paths(:test) = lib_paths() ++ [\"test/test_support\"]`, so\n  test_support beams land in the same app ebin — `Elixir.SpecLedEx.EvidenceHelpers.beam` and\n  `Elixir.SpecLedEx.StateJsonFixture.beam` sit next to `Elixir.SpecLedEx.Review.CoverageClosure.beam`\n  in `_build/test/lib/spec_led_ex/ebin`, which is exactly the directory `cover_compile/1`\n  walks. A test_support fixture reproduces this ticket's defect verbatim.\n\n- (c) is not vacuous but is fragile. Dep ebins are outside `compile_path()`, so deps are\n  cover-safe, and a dep's `module_info(:compile)[:source]` is `\u003crepo\u003e/deps/\u003cx\u003e/lib/...`,\n  which `repo_relative/1` DOES resolve. But the expected literal is a third party's internal\n  file path (breaks on any dep bump), and if deps were ever compiled outside the checkout\n  the source goes out-of-repo, `repo_relative/1` returns nil, and the discriminating\n  assertion silently degrades to `nil == nil`. Rejected on robustness.\n\n- (b) is coverage-safe by construction: a module compiled at runtime was never in\n  `compile_path()`, so `:cover.compile_beam/1` never saw it, it can never enter\n  `:cover.modules()`, and purging it cannot touch any tracked module's counters. The trick\n  that makes it work is that the beam's DIRECTORY and its recorded `:source` are\n  independent: put the beam in a throwaway dir on the code path, but pass an in-repo path\n  as the compile `:file` so `repo_relative/1` resolves. The `:file` need not exist on disk;\n  nothing reads it. The critic ran this exact shape against unmutated `Paths` and got\n  nil / \"test/test_support/paths_test_purge_target.ex\" — the same two-mutant kill.\n\nEverything else in the ticket re-derived clean, independently of the VERIFY counts:\n\n- DEFINING CONSTRAINT — clean. Diffed the `def `/`defmacro ` head set of all five\n  api_boundary-baselined modules at 2023373d vs HEAD: coverage.ex (14), coverage/formatter.ex (6),\n  coverage_triangulation.ex (13), review/coverage_closure.ex (1), mix/tasks/spec.triangle.ex (1)\n  — byte-identical in every case. Only `defp` heads removed.\n  `git diff 2023373d..HEAD -- .spec/realization_hashes.json` is a zero-line diff, as is the\n  whole of `.spec/`, CHANGELOG.md, VERSION, mix.exs, mix.lock. paths.ex:2 carries\n  `@moduledoc false` with `@doc false` on every export; `grep -rn \"Coverage.Paths\\|coverage/paths\" .spec/`\n  returns nothing — no `realized_by:`, no `surface:`, no hash baseline. CHANGELOG.md is NOT\n  touched: the orchestrator override was respected.\n\n- NO-BEHAVIOUR-CHANGE — holds, clause by clause against the deleted bodies.\n  formatter `source_file/1`: old `with {_kind,_} \u003c- :code.is_loaded(module)` is exactly\n  `ensure_module(module, false)`; the `is_list or is_binary` source guard survives as the\n  `case` clause at paths.ex:41; every other exit is nil; the outer `rescue _ -\u003e nil` moved\n  intact to paths.ex:50-51. Returns `String.t() | nil`.\n  coverage_triangulation `module_source_file/1` keeps its own `when is_atom(mod)` wrapper\n  guard; the direct call at :388 swaps `repo_relative_source_path/1` for `Paths.repo_relative/1`\n  — same body, same `is_binary` guard, same catch-all nil. Returns `String.t() | nil`.\n  closure/triangle `mfa_source_file/1` → `repo_relative_list(module_source(mod, true))`.\n  Every old exit traced: ensure_loaded fail → nil → []; source neither list nor binary →\n  nil → []; out-of-repo absolute → nil → []; in-repo → [rel]; raise inside → rescued to nil\n  → []. Never nil, never absolute. The double `repo_relative` application is idempotent\n  because the first one only returns strict descendants of root (paths.ex:20 `root \u003c\u003e \"/\"`),\n  so `Path.expand(rel, root)` round-trips — pinned at paths_test.exs:34-39.\n  The two adjudicated notes still stand and are still Notes: the new `when is_atom(module)`\n  head converts a previously-rescued badarg into an escaping FunctionClauseError, and the\n  closure/triangle sites lost their own `rescue _ -\u003e []` so a `File.cwd!()` raise now\n  escapes. I re-walked all four feeders (formatter.ex:233 boundary-table atoms;\n  coverage_triangulation.ex:436 already `is_atom`-guarded; coverage_closure.ex:118 and\n  spec.triangle.ex:406 destructure `{mod,_,_}` from closure MFAs) — no non-atom reaches\n  any of them.\n\n- ITERATION-1 BLOCKER IS GENUINELY CLOSED. The critic's two-mutant evidence is sound and\n  the shipped test's structure supports it: paths_test.exs:90 kills always-`Code.ensure_loaded`\n  and :95 kills always-`:code.is_loaded`. Keep both assertions and the interleaved\n  `refute match?(...)` at :92 — that is the load-bearing half. Only the purge target changes.\n\n- `mix format --check-formatted` — exit 0. Both commits carry the `Spec-Drift:` trailers\n  with Reason lines.\n\nBounce reason:\n\nReplace the purge target in test/specled_ex/coverage/paths_test.exs. Do not purge any\nmodule that lives in `_build/\u003cenv\u003e/lib/spec_led_ex/ebin` — that set includes every lib/\nmodule AND every test/test_support/ module, and it is exactly what `mix test --cover`\ncover-compiles.\n\nREQUIRED SHAPE — an ephemeral module compiled at runtime into a throwaway beam directory:\n1. Mint a unique module name (`System.unique_integer([:positive])` in the atom) so a stale\n   beam from an aborted run can never shadow it.\n2. Compile it with an IN-REPO `:file`, e.g.\n   `src = Path.join(File.cwd!(), \"test/fixtures/paths_purge_target.ex\")` then\n   `[{^mod, bin}] = Code.compile_string(\"defmodule ... do def noop, do: :ok end\", src)`.\n   The path need not exist on disk — it is only recorded in the beam's CInf `source`, which\n   is what `Paths.module_source/2` reads.\n3. Write `bin` to a throwaway dir and `Code.prepend_path/1` it. Prefer `@tag :tmp_dir` so\n   ExUnit owns the dir; `tmp/` at the repo root is already gitignored (.gitignore:17), so it\n   cannot dirty the tree or trip a scope check.\n4. Purge/delete/purge, then keep the iteration-2 assertion trio verbatim against the new\n   target: `module_source(mod, false) == nil`, `refute match?({_k,_p}, :code.is_loaded(mod))`\n   AFTER that call, and `module_source(mod, true) == expected`.\n5. `on_exit`: `Code.delete_path/1`, `File.rm_rf!/1`, and purge/delete the ephemeral module\n   so it does not linger loaded against a deleted beam. Keep `async: false`.\n\nNAMED HAZARDS, all of which silently produce a green-but-vacuous test:\n- H1 (the one that caused this bounce): putting the beam anywhere under\n  `Mix.Project.compile_path()` or adding a source file under `test/test_support/`. Both land\n  in the cover-compiled ebin and reproduce the identical coverage corruption. The temp dir\n  must be outside the build path.\n- H2: an out-of-repo `:file`. If the recorded `:source` is under `System.tmp_dir!()` instead\n  of under `File.cwd!()`, `repo_relative/1` returns nil and the `load?: true` assertion\n  becomes `nil == nil` — it stops discriminating and the always-`:code.is_loaded` mutant\n  survives. The beam dir and the recorded `:source` are deliberately different paths.\n- H3: hardcoding the expected string separately from the `:file` you passed. Derive it —\n  `expected = Path.relative_to(src, File.cwd!())` — so a typo cannot desynchronise them into\n  the H2 failure mode.\n- H4: leaking `Code.prepend_path/1` past the test. It is VM-global; an un-deleted path plus a\n  deleted beam dir is a landmine for later modules.\n- H5: check `Code.get_compiler_option(:tracers)` is empty in the test VM before calling\n  `Code.compile_string/2`. mix.exs:13 wires `SpecLedEx.Compiler.Tracer` as an elixirc tracer;\n  if it is somehow live at runtime, a runtime compile would emit xref events into the\n  `_build/\u003cenv\u003e/.spec` manifest that `test/specled_ex/compiler/tracer_test.exs` asserts over.\n  If it is non-empty, scope it with `Code.put_compiler_option(:tracers, [])` + restore in\n  `on_exit`.\n\nAlso do this: delete or rename the \"loaded in-repo module resolves under both load? modes\"\ntest at paths_test.exs:50-58. It survives BOTH collapse mutants and reads as `load?`\ncoverage while pinning nothing about the fork; \"resolves for an already-loaded module\" is\nthe honest name.\n\nKeep the fix inside Allowed touches — test/specled_ex/coverage/paths_test.exs only (plus\n`test/fixtures/` ONLY if you choose to make the `:file` a real on-disk file, which is\noptional and not required). No production change: paths.ex is correct as written. Do not\ntouch CHANGELOG.md, .spec/realization_hashes.json, or any spec file. In the completion\nrecord, state the two-mutant falsifiability explicitly (which mutant fails which line) AND\nstate affirmatively that the new purge target is not in `Mix.Project.compile_path()` — do\nnot assert coverage, show it. Re-run the SCOPE CONTRACT's four steps plus\n`mix format --check-formatted`.",
      "timestamp": "2026-07-27T12:45:24Z"
    },
    {
      "text": "IMPLEMENTED\nSession: k6s-dn4.12-impl-3\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-dn4.12/grok-k6s-dn4.12-impl-3/log\n\nCommit: bba184e23975085bda8dd78e3c4dd7f6c806a6ce\nFiles touched: test/specled_ex/coverage/paths_test.exs\nTests added: SpecLedEx.Coverage.PathsTest / \"loadable-but-not-loaded module discriminates load?: false from load?: true\" (rewritten fixture only; same test name)\nVerification stubs flipped: none\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/coverage/paths_test.exs test/specled_ex/coverage/formatter_test.exs test/specled_ex/coverage_triangulation_test.exs test/specled_ex/review/coverage_closure_test.exs test/mix/tasks/spec_triangle_test.exs test/integration/guard_ergonomics_attestation_test.exs (120 tests, 0 failures); git diff --stat -- .spec/realization_hashes.json empty; mix spec.check --no-run-commands result=pass; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\nAudit-expanded scope: none (remedy stayed inside already-allowed test/specled_ex/coverage/paths_test.exs; AUDIT: BOUNCE at 2026-07-27T12:47:41Z prescribed the fixture shape only)\nDeferred must requirements: none\n\nSummary: Iteration-3 closes the only remaining defect from iteration 2: the load?-discrimination test no longer purges SpecLedEx.Review.CoverageClosure (a cover-compiled lib/ module). Per the auditor remedy, it now runtime-compiles an ephemeral module into a throwaway directory on the code path, recording an in-repo :file so Paths.repo_relative/1 still resolves, then purges only that untracked beam. Production Paths + four call sites are unchanged; both ensure_module/2 collapse mutants still go RED; --cover keeps CoverageClosure's row and emits no collect-failure warning.\n\n=== PROOF 1: both collapse mutants of ensure_module/2 still RED ===\n\nMutant A (collapse to always Code.ensure_loaded, ignore load?):\n  1) test module_source/2 loadable-but-not-loaded module discriminates load?: false from load?: true\n     Assertion with == failed\n     code:  assert Paths.module_source(mod, false) == nil\n     left:  \"lib/specled_ex/coverage/paths_test_ephemeral.ex\"\n     right: nil\n  9 tests, 1 failure\n\nMutant B (collapse to always :code.is_loaded, ignore load?):\n  1) test module_source/2 loadable-but-not-loaded module discriminates load?: false from load?: true\n     Assertion with == failed\n     code:  assert Paths.module_source(mod, true) == expected\n     left:  nil\n     right: \"lib/specled_ex/coverage/paths_test_ephemeral.ex\"\n  9 tests, 1 failure\n\nRestored both ensure_module/2 clauses; paths_test.exs 9 tests, 0 failures.\n\n=== PROOF 2: --cover keeps CoverageClosure row, no collect-failure warning ===\n\nCommand: mix test --cover test/specled_ex/review/coverage_closure_test.exs test/specled_ex/coverage/paths_test.exs\nResult: 39 tests, 0 failures\n  - No line matching 'Failed to collect coverage information' in output\n  - Coverage table includes:  83.69% | SpecLedEx.Review.CoverageClosure\n  - Also:  88.89% | SpecLedEx.Coverage.Paths\n  (exit 3 is only suite-wide threshold 83% unmet on this narrow file set — Total 3.67%; not a collect failure)\n\n=== PROOF 3: mix format --check-formatted ===\n\nmix format --check-formatted → exit 0 (green)\n\n=== Epic lessons self-check ===\n- [untested-clause] load? split still uses an input where branches disagree (ephemeral loadable-but-unloaded)\n- [other] format gate run green before completion\n- [other] completion posted on specled_-dn4.12\n- [other] no :code.purge of Mix.Project.compile_path() modules; ephemeral throwaway dir + in-repo :file\n- [other] code-server mutation proved under --cover, not only change-scoped unit green\n- remaining lessons N/A (no ADR/spec edit, no dual-path string fix, no new forbidden-spelling guard)",
      "timestamp": "2026-07-27T12:51:06Z"
    }
  ],
  "priority": 2,
  "status": "in_progress",
  "title": "Follow up: consolidate coverage source resolver without public API drift",
  "type": "task",
  "updated_at": "2026-07-27T12:51:35Z"
}
