{
  "assignee": "Manuel Zubieta",
  "blocked_by": [
    "specled_-dn4.7"
  ],
  "blocks": [],
  "closed_at": "2026-07-26T01:03:57Z",
  "created": "2026-07-24T19:18:44Z",
  "description": "Batch of Low-severity cleanups from the consolidated report's Tier 3: one module→source resolver; fold group_records_with_lines/1; one attribution_copy/1 in Review.Html; delete dead diagnostic_count/baseline||%{}/disarmed_notice; collapse collect_lines/2 ladder; MapSet.disjoint?/2; ets.info(tid,:size); read/write_concurrency; persistent_term modules cache; rescue-based boundary insert + head/1 tolerance; boundary_key/1 setup_all fallback; document declare-the-hook-FIRST; publish one measured per-test overhead figure; meta shape validation at classify_v2; non-loading source resolution; fixture mkdir hardening. See report Tier 3 for the full list.\n\n---\n\n## Orchestrator scoping (synthesized 2026-07-25 by /orchestrate-epic from verified code analysis)\n\n## specled_-dn4.9 — Tier-3 seam cleanups, cheap perf, robustness batch\n\n**Advances:** `specled.coverage_capture`, `specled.triangulation`, `specled.spec_review`\n\nEvery sub-item enumerated to its file and function. All symbols verified present at the cited lines.\n\n| # | Sub-item (ticket wording) | Verified location |\n|---|---|---|\n| 1 | one module→source resolver | Two duplicates in the coverage path: `lib/specled_ex/coverage/formatter.ex:268-282` `source_file/1` and `lib/specled_ex/coverage_triangulation.ex:407-421` `module_source_file/1`. **Eight** total `module_info(:compile)[:source]` readers repo-wide: also `realization/binding.ex:188`, `:380`; `realization/closure.ex:257`; `review/coverage_closure.ex:135`; `coverage/aggregate.ex:184`; `mix/tasks/spec.triangle.ex:450`. Scope the unification to the coverage pair unless deliberately widened. |\n| 2 | fold `group_records_with_lines/1` | `lib/specled_ex/coverage_triangulation.ex:367-391`, whose own comment at :365-366 says *\"Like `group_records_by_test/1` but keeps per-file line sets\"*; the sibling is at :433. Callers: `group_records_by_test/1` at :117, :158, :200; `group_records_with_lines/1` at :281. |\n| 3 | one `attribution_copy/1` in `Review.Html` | Five near-identical `case Map.get(…, :attribution, :exact)` blocks: `lib/specled_ex/review/html.ex:2032-2043` `rollup_qualifier_suffix/1`, :2047-2067 `rollup_qualifier_title_suffix/1`, :3063-3078 `render_attribution_qualifier/2`, :3122-3135 `reached_by_tests_title/1`, :3151-3156 `self_verified_qualifier_label/1`. The shared long string is already extracted as `@exact_up_to_escaped_title` at :3101. |\n| 4 | delete dead `diagnostic_count` / `baseline \\|\\| %{}` / `disarmed_notice` | `formatter.ex:157` `diagnostic_count: 0` in `run_init/1` — **never incremented anywhere**; read once at :304. `formatter.ex:287` `state.baseline \\|\\| %{}` — `:baseline` is always set (`run_init/1` :158, `suite_started` :181, `record_inventory/2` :219). Also `state.modules \\|\\| state.modules_fn.()` at :285. `disarmed_notice/0` at :162-167, single caller at :123. |\n| 5 | collapse `collect_lines/2` ladder | `lib/specled_ex/coverage/mfa_lines.ex:64-83` — an 8-clause ladder including a hand-written tuple-arity cascade at :74-79 (`{a,b,c,d,e}` / `{a,b,c,d}` / `{a,b,c}` / `{a,b}` / `other -\u003e Tuple.to_list`). Entry point at :50. |\n| 6 | `MapSet.disjoint?/2` | `lib/specled_ex/coverage_triangulation.ex:357` — `if MapSet.size(MapSet.intersection(hit_lines, mfa_lines)) \u003e 0 do` inside `tests_hitting_mfa/3`. |\n| 7 | `:ets.info(tid, :size)` | Two full-`:ets.info/1` calls used only as liveness probes: `lib/specled_ex/coverage/boundary.ex:123` (`table_alive?/1`) and `lib/specled_ex/coverage/formatter.ex:418` (`load_boundary_index/0`). `:ets.info/1` builds the whole info list; `:ets.info/2` does not. |\n| 8 | read/write_concurrency | `lib/specled_ex/coverage.ex:138` `:ets.new(:anon, [:public, :set])` (docstring at :133) and `lib/mix/tasks/spec.cover.test.ex:197` — same options, no concurrency hints. Prior art for the option list: `lib/specled_ex/compiler/tracer.ex:82`. **Constraint:** `specled.coverage_capture.anonymous_ets` (`.spec/specs/coverage_capture.spec.md:330-337`) quotes the option list literally as `:ets.new(:anon, [:public, :set])`, so adding options is a spec co-change. |\n| 9 | `persistent_term` modules cache | `lib/specled_ex/coverage/boundary.ex:110-120` `modules_for/1` — one `:ets.lookup/2` per head *and* per tail, i.e. twice per test. **Conflicts with dn4.4:** this would delete `modules_cache_key/0` (:86-88), the leak dn4.4 is chartered to close, and its test at `boundary_test.exs:97`. Resolve ownership before either starts. |\n| 10 | rescue-based boundary insert + `head/1` tolerance | `boundary.ex:68-80` — the `if table_alive?(config.boundary_table) do` pre-check is a TOCTOU window; replace with a `rescue ArgumentError` around `:ets.insert/2` at :79. `head/1` at :39-48 has **no** tolerance: `modules_for/1`'s `:ets.insert` at :117 raises if the table died. |\n| 11 | `boundary_key/1` `setup_all` fallback | `lib/specled_ex/coverage.ex:69-71` — a single clause `defp boundary_key(%{module: mod, test: name}) when is_atom(mod) and is_atom(name)`. An ExUnit `setup_all` context has `:module` but **no** `:test`, so wiring the hook via `setup_all` raises `FunctionClauseError`. Reached from `per_test_boundary/1:58`, which is the documented public API (`lib/specled_ex/case.ex:18`). |\n| 12 | document declare-the-hook-FIRST | `docs/coverage.md:185-217` (\"Wiring the per-test boundary hook\") — shows the setup line but never says it must be declared before other `setup` callbacks. Same content is duplicated in `lib/specled_ex/coverage.ex:5-15` (moduledoc) and `lib/specled_ex/case.ex:2-14`. |\n| 13 | publish one measured per-test overhead figure | `docs/coverage.md:171-177` (\"**Cost model.**\") — qualitative only, no number. **Same paragraph dn4.6/F14 rewrites.** |\n| 14 | meta shape validation at `classify_v2` | `lib/specled_ex/coverage/store.ex:329-337` — `classify_v2/1` does `Map.put_new(envelope, :meta, %{})` with no check that an existing `:meta` is a map, while the sibling validator at :320-326 does assert `is_list` on `:files`/`:mfas`. A non-map `:meta` flows through to `Store.degraded_reasons/1` and `CoverageClosure.unhooked_modules/1` (`review/coverage_closure.ex:407-414`, which guards `when is_map(meta)` and silently returns `[]` otherwise). |\n| 15 | non-loading source resolution | `coverage_triangulation.ex:407-421` `module_source_file/1` calls `Code.ensure_loaded(mod)` — a **side effect** inside a function whose own docstring at :265 says *\"Pure: no filesystem access.\"* Same pattern at `formatter.ex:269`. |\n| 16 | fixture mkdir hardening | **Could not pin to a unique site — the consolidated report is authoritative.** Candidates, in order of likelihood: `test/specled_ex/coverage/mfa_lines_test.exs:9-35` (`File.mkdir_p!` then an `on_exit` that hand-walks `File.ls!` and calls `File.rmdir` rather than `File.rm_rf`, so any unexpected file leaks the dir); `test/mix/tasks/spec_cover_test_test.exs` scaffolds at :406-407, :434-435, :476-477, :589-590 (create only `lib/` and `test/`, never `.spec/`); `test/specled_ex/coverage/snapshot_test.exs:217`. |\n\n**Advances rationale:** `specled.coverage_capture` (items 1,4,7,8,9,10,11,12,13,14,16 — plus the `anonymous_ets` requirement co-change for item 8), `specled.triangulation` (2,6,15 — and `per_test_requirement_reach`'s purity claim at `triangulation.spec.md:205-218`), `specled.spec_review` (3).\n\n**Allowed touches (globs):**\n\n- `lib/specled_ex/coverage/boundary.ex`\n- `lib/specled_ex/coverage.ex`\n- `lib/specled_ex/coverage/formatter.ex`\n- `lib/specled_ex/coverage/mfa_lines.ex`\n- `lib/specled_ex/coverage/store.ex`\n- `lib/specled_ex/coverage_triangulation.ex`\n- `lib/specled_ex/review/html.ex`\n- `lib/mix/tasks/spec.cover.test.ex`\n- `docs/coverage.md`\n- `test/specled_ex/coverage/**`\n- `test/specled_ex/coverage_triangulation_test.exs`\n- `test/specled_ex/review/html_test.exs`\n- `test/mix/tasks/spec_cover_test_test.exs`\n- `.spec/specs/coverage_capture.spec.md`\n- `.spec/specs/triangulation.spec.md`\n- `.spec/realization_hashes.json`\n- `CHANGELOG.md`\n\n**Out of Scope (files):** `lib/specled_ex/realization/**`, `lib/specled_ex/compiler/**`, `lib/specled_ex/coverage/aggregate.ex`, `lib/mix/tasks/spec.triangle.ex` (all hold `module_info(:compile)[:source]` duplicates that are *out of scope* for item 1 — resist the pull), `lib/specled_ex/review/coverage_closure.ex`, `.spec/specs/spec_review.spec.md`, `.spec/specs/package.spec.md`, `docs/adoption.md`, `docs/concepts.md`.\n\n**Out of Scope (intent):**\n\n- **This is the only ticket with no correctness mandate.** No sub-item may change observable behavior except items 10, 11, and 14, which are explicitly robustness fixes. If a cleanup changes output, stop and file it.\n- Item 8 requires a spec co-change (`anonymous_ets` quotes the option list) — do not add ETS options and leave the spec asserting the old list.\n- Item 9 must be reconciled with dn4.4 first; do not delete `modules_cache_key/0` unilaterally.\n- Item 13 must not contradict dn4.6's corrected cost model — coordinate or defer to dn4.6.\n- No new deps. No new public functions.\n- Given the sub-item count, this is a natural candidate for splitting into per-file commits; keep them separable so a single regression does not revert sixteen cleanups.\n\n**Verification:**\n\n- `mix test` (full suite — it touches eight lib files across three subsystems)\n- `mix compile --warnings-as-errors`, `mix format --check-formatted`, `mix deps.unlock --check-unused`\n- `mix spec.check --no-run-commands`, then `mix spec.check`\n- Stubs (all **`execute: true`**): `coverage_capture.spec.md:891-897` (`anonymous_ets` — item 8), :929-936 (`mfa_lines_index` — item 5), :937-942 (`store_v2_envelope`/legacy — item 14, via :908-912), :943-947 (`boundary_noop_unarmed` — items 10, 11); `triangulation.spec.md:359-368` (`per_test_requirement_reach` — items 2, 6, 15); `spec_review.spec.md:806-820` (`html_test.exs` — item 3).\n\n**Overlap risk:** **Overlaps every other ticket in the epic.** `boundary.ex` (dn4.1/4.4/4.6), `coverage.ex` (dn4.1/4.6), `formatter.ex` (dn4.2/4.4/4.5), `coverage_triangulation.ex` (dn4.5), `review/html.ex` (dn4.5/4.8), `store.ex` (dn4.5), `spec.cover.test.ex` (dn4.6), `docs/coverage.md` (dn4.6), `coverage_capture.spec.md` (dn4.1/4.3/4.4/4.5/4.6/4.8), `triangulation.spec.md` (dn4.5 + cz0), `formatter_test.exs` (dn4.1/4.2/4.4/4.5/4.7), `boundary_test.exs` (dn4.1/4.4/4.6/4.7). Item 4 (`diagnostic_count`) collides with dn4.2's `flush/1` rewrite; item 9 collides with dn4.4's charter; item 13 collides with dn4.6's F14.\n\n---\n\n\n**Orchestrator ruling (renegotiated items):** You run LAST in the epic; your base includes all eight siblings' landed work. Item 9 (`persistent_term` modules cache): dn4.4 has already closed the `modules_cache_key/0` leak by keeping ETS and privatizing the key inside its arming resolver — only apply `persistent_term` if it demonstrably improves on that landed design WITHOUT reintroducing any cross-module key leak; otherwise skip item 9 and say so in your completion record. Item 13 (measured overhead figure): dn4.6 has already rewritten the `docs/coverage.md` cost-model paragraph — add the measured figure to dn4.6's landed text; it must not contradict the corrected model. Item 16 (fixture mkdir hardening): the scoping analysis could not pin a unique site; the authoritative list is the consolidated review report at ~/vaults/rmbrs-vault/code_review/critical/specled_ex/2026-07-24/specled_-pzd-per-test-sync-boundary/20260724-141032-consolidated.md (Tier 3) — read it before implementing that item.",
  "id": "specled_-dn4.9",
  "labels": [],
  "parent": "specled_-dn4",
  "comments": [
    {
      "text": "ORCHESTRATOR DIRECTIVE (added 2026-07-25, from the specled_-dn4.6 audit): add one item to your batch — lib/specled_ex/coverage/formatter.ex:12's moduledoc still states the RETIRED per-test attribution claim 'exact up to escaped processes'. dn4.6 widened the real bound to 'exact within disclosed chained windows'; reuse the exact wording landed in .spec/decisions/specled.decision.per_test_sync_boundary.md and docs/coverage.md. You own formatter.ex and merge last, so this site is yours; the other four retired-claim sites (README.md:59, docs/adoption.md:26/:387, .spec/specs/package.spec.md:29) belong to sibling ticket specled_-dn4.11 — do not touch them.",
      "timestamp": "2026-07-25T15:49:19Z"
    },
    {
      "text": "ORCHESTRATOR DIRECTIVE #2 (from the dn4.6 audit, 2026-07-25T16:19:49Z) — add one test-hardening item to your batch. You already own test/specled_ex/review/html_test.exs and you merge LAST, which makes you the right closer for this.\n\ndn4.6 left three corrected renderer strings rendered correctly but pinned by no test — each is individually revertible to the retired \"exact up to escaped processes\" claim with html_test.exs green:\n  - html.ex:3075 — render_attribution_qualifier/2's degraded title\n  - html.ex:3135 — reached_by_tests_title/1's degraded branch\n  - html.ex:3078 — render_attribution_qualifier/2's non-degraded title/label\nCause: the existing assertions use text that is byte-identical at base (an unchanged prefix) or a bare substring three sites emit, so neither can fail on a single-site revert.\n\nFIRST verify the three sites still emit the chained-window wording (they may have passed through dn4.5's rebase): grep -c \"hooked tests remain exact only within disclosed chained windows\" lib/specled_ex/review/html.ex must be 3 and \"exact up to escaped processes\" must be 0 in that file. If not, STOP and report — do not pin a reverted string.\n\nTHEN close it, test-file only (html.ex stays frozen for this item): widen the two prefix assertions in the degraded test into WHOLE-TITLE assertions carrying the corrected clause, e.g. \"Per-test run degraded: unhooked modules (UnhookedCase, OtherUnhooked) fold into the aggregate remainder; hooked tests remain exact only within disclosed chained windows.\" and the \"Per-test run is degraded — …\" variant; and add one whole-string assertion for the non-degraded title. Both prefix assertions pass today, so the existing fixture already renders both sites — no new fixture needed.\n\nACCEPTANCE: reverting each of the three strings individually to its ca7ef09 text must fail at least one named test. Report the per-string mutation result. (A prior iteration reported a mutation table it had not actually run — run these.)",
      "timestamp": "2026-07-25T16:20:31Z"
    },
    {
      "text": "ORCHESTRATOR NOTE #3 (from dn4.7's verification, 2026-07-25T18:47:29Z — informational, not a directive): two defensive branches in lib/specled_ex/coverage/formatter.ex remain untested and dn4.7 could not reach them because it was test-only. They are source_file/1's unrecognized-:source-shape clause (~:264) and its rescue (~:270-271). Both require a module whose module_info(:compile)[:source] has a non-list, non-binary shape, which cannot be constructed without touching lib/**. You DO have formatter.ex in your Allowed touches. If one of your cleanups makes them reachable or removable, handle it; if not, leave them and say so — do not contort the code to make them testable. The third branch originally listed with them (the non-loadable clause) IS covered, via UnloadableCoverageModule in formatter_test.exs:400.",
      "timestamp": "2026-07-25T18:47:56Z"
    },
    {
      "text": "IMPLEMENTED\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nCommit: ddda892\nFiles touched: .spec/realization_hashes.json, .spec/specs/coverage_capture.spec.md, docs/coverage.md, lib/mix/tasks/spec.cover.test.ex, lib/specled_ex/coverage.ex, lib/specled_ex/coverage/boundary.ex, lib/specled_ex/coverage/formatter.ex, lib/specled_ex/coverage/mfa_lines.ex, lib/specled_ex/coverage/store.ex, lib/specled_ex/coverage_triangulation.ex, lib/specled_ex/review/html.ex, test/mix/tasks/spec_cover_test_test.exs, test/specled_ex/coverage/boundary_test.exs, test/specled_ex/coverage/formatter_test.exs, test/specled_ex/coverage/mfa_lines_test.exs, test/specled_ex/coverage/store_test.exs, test/specled_ex/review/html_test.exs\nTests added: boundary dead-table head and tail tolerance, setup_all context tolerance, malformed v2 meta rejection, ETS concurrency option assertions, three whole-title HTML assertions, randomized fixture temp roots\nVerification stubs flipped: none\nVerification tier: full suite — mix test; mix compile --warnings-as-errors; mix format --check-formatted; mix deps.unlock --check-unused; mix spec.check --no-run-commands --base HEAD~1; mix spec.check --base HEAD~1; directive greps; three per-site mutation checks\nFinal-tier runs: 2 full mix test runs after implementation (first exposed unaccepted realization-hash attestation drift, second passed after tool-managed accept-drift); 1 post-commit structural spec.check pass; 1 post-commit bare spec.check pass\nSub-item disposition (all 16):\n1 skipped with follow-up specled_-dn4.12 — formatter.ex and coverage_triangulation.ex private resolver copies remain; attempted shared Coverage helper caused API-boundary attestation failure, so final code keeps private non-loading copies.\n2 applies+done — group_records_with_lines and group_records_by_test now share group_records/2 with mode-specific payloads.\n3 applies+done — Review.Html attribution_copy/1 centralizes the five attribution dispatches and preserves missing-key fallback as unqualified.\n4 applies+done — removed diagnostic_count, removed baseline fallback, removed modules fallback, and inlined/deleted disarmed_notice/0 with unchanged emitted text.\n5 applies+done — MfaLines.collect_lines/2 tuple arity ladder collapsed to tuple-to-list head/tail handling with empty tuple tolerance.\n6 applies+done — tests_hitting_mfa/3 uses MapSet.disjoint?/2.\n7 already-done-by-sibling/moot — Boundary and Formatter no longer had the cited full ets.info liveness probes; remaining arming resolver liveness is outside this ticket’s allowed files.\n8 applies+done — anonymous ETS option list now includes read_concurrency and write_concurrency in formatter install and spec.cover.test boundary table; coverage_capture spec updated.\n9 skipped with follow-up specled_-dn4.13 — dn4.4 private ETS arming resolver already closed the modules_cache_key leak; no demonstrated persistent_term improvement without key-leak risk.\n10 applies+done — Boundary.head/1 and tail/2 rescue ArgumentError around dead-table TOCTOU paths and no-op.\n11 applies+done — per_test_boundary/1 now tolerates setup_all contexts via a module-level fallback key.\n12 applies+done — coverage.ex moduledoc and docs/coverage.md now say to declare the boundary setup first and why LIFO on_exit ordering matters.\n13 applies+done — docs/coverage.md adds measured 2026-07-25 boundary tail+next-head micro figure: 264 microseconds per hooked test over an 800-module stub snapshot.\n14 applies+done — Store.classify_v2 rejects present non-map meta as invalid_artifact while still defaulting missing meta to empty map.\n15 applies+done — formatter and triangulation source resolution use code.is_loaded before module_info and no longer call Code.ensure_loaded in those private resolvers.\n16 applies+done — mfa_lines and spec_cover integration fixtures use random suffix roots with File.mkdir! before nested mkdir_p.\nDirective 1 (formatter.ex:12 retired claim): replaced the retired exact up to escaped processes wording with the dn4.6 chained-window wording; repo-wide phrase grep excluding build/deps/git returned 0 matches, aside from the deliberately retained underscored requirement id not matched by phrase grep.\nDirective 2 (pin dn4.6's three strings): precheck html.ex chained-window count was 3 and retired phrase count was 0. Per-site mutation results: reverting render_attribution_qualifier degraded title alone failed test/specled_ex/review/html_test.exs:1319; reverting reached_by_tests_title degraded branch alone failed the same named test; reverting render_attribution_qualifier non-degraded title/label alone failed test/specled_ex/review/html_test.exs:1269.\nDirective 3 note (two untested source_file/1 branches): left defensive unrecognized-source-shape and rescue branches as-is; the resolver was changed to non-loading code.is_loaded lookup, but I did not contort production code to fabricate impossible module_info source shapes.\nBehavior-preservation attestation: no observable behavior changed except the allowed robustness fixes in items 10, 11, and 14; item 8 changes ETS table options only. I stopped the source-resolver consolidation when the public helper altered API-boundary attestation and filed specled_-dn4.12 instead.\nDeferred must requirements: none\n\nSummary: Landed the tier-3 cleanup batch with focused robustness fixes, test hardening for the three dn4.6 renderer strings, ETS option/spec drift updates, and docs for setup ordering plus measured boundary overhead. Source resolver consolidation and persistent_term cache were deliberately deferred with follow-up tickets rather than forcing risky API or cache-design changes.",
      "timestamp": "2026-07-25T19:09:17Z"
    },
    {
      "text": "VERIFY: PASS\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nCold verification of ddda892 against base 1910d7d in /Users/mz/src/specled_ex-tier3-cleanups.\n\n## Mechanical checks\n\n| gate | result |\n|---|---|\n| mix compile --warnings-as-errors | PASS |\n| mix format --check-formatted | PASS |\n| mix deps.unlock --check-unused | PASS |\n| mix test (FULL) | PASS — 3 properties, 1231 tests, 0 failures, seed 94524, FIRST run |\n| mix spec.check --no-run-commands (DEFAULT base) | PASS exit 0 — status=pass errors=0 warnings=0 |\n| mix spec.check (bare, DEFAULT base) | PASS exit 0 — status=pass errors=0 warnings=0 |\n\nNo known flake fired. tracer_test.exs StreamData, verifier_test.exs:1452/:1681 and review_test.exs:135 all passed on the first run; no re-run was needed.\n\n**Methodology flag — answered.** The default base is `origin/main`, not `HEAD~1`. Running both:\n\n- default: `branch base=origin/main changed_files=89 findings=46`, plus one `[WARNING] append_only/same_pr_self_authorization` — ADR `specled.decision.coverage_qualifier_requirement_ids` is new in the diff and its `affects:` list exactly matches the requirement ids removed in the same diff. Marked \"Visible but not blocked; review decides.\"\n- `--base HEAD~1`: `changed_files=16 findings=9`, no warning.\n\nBoth exit 0 with `errors=0 warnings=0`. So `--base HEAD~1` DID mask one finding, but it is **not dn4.9's** — that ADR is a sibling's (dn4.5/dn4.10 line) and the warning is epic-level. It will surface on the epic PR and needs a review decision there. Nothing dn4.9 landed is hidden by the base choice.\n\n**Realization-hash delta — audited, exactly minimal.** 172 attested entries; the diff changes exactly one (`SpecLedEx.Coverage`). Recomputed `Canonical.hash_module_head_union/1` for every module the ticket touched:\n\n- `SpecLedEx.Coverage` — current head-union `b671fda6…` ≠ pre-ticket committed `e80bc2a2…`, = post-ticket committed. The change was **necessary**.\n- `SpecLedEx.Coverage.Formatter` — the only other attested module in the touched set — still MATCHES its **unchanged** committed hash. Correctly not churned.\n- Boundary, MfaLines, Store, CoverageTriangulation, Review.Html, Mix.Tasks.Spec.Cover.Test have no module-level entry.\n\nNo entry was changed that did not need to change, and none that needed changing was missed.\n\n**Directive 1 — SATISFIED.** `lib/specled_ex/coverage/formatter.ex:11-17` no longer carries the retired claim. Repo-wide sweep run independently: `grep -rn \"exact up to escaped processes\" . --exclude-dir=_build --exclude-dir=deps --exclude-dir=.git` returns **zero** matches. The requirement id `coverage_exact_up_to_escaped_processes_qualifier` survives (underscored, so the phrase grep does not match it) at `spec_review.spec.md:323/:359/:751/:864`, `specled.decision.coverage_qualifier_requirement_ids.md:31`, `html.ex:2025`, and four html_test.exs `# covers:` lines — the sanctioned survivor. Carry-forward checks: `grep -c \"exact up to escaped processes\" lib/specled_ex/review/html.ex` → **0**; `grep -c \"hooked tests remain exact only within disclosed chained windows\" lib/specled_ex/review/html.ex` → **3**.\n\nThe new formatter moduledoc is a faithful condensation of dn4.6's landed text, not a paraphrase that weakens the bound. It carries all three of the decision doc's elements — the between-test interval (\"serialized runner / `setup_all` work and any intervening unhooked tests\"), the escaped-process bound (\"increment shared `:cover`/native counters in a later window or the unattributed remainder\"), and the no-runtime-detection disclosure — plus the pointer to `specled.decision.per_test_sync_boundary`. It is condensed rather than copied verbatim from `.spec/decisions/specled.decision.per_test_sync_boundary.md:88-104`; substance matches on every bound.\n\n## Semantic checks\n\n### Item 3 — five-site attribution mutation table (the epic's core bug)\n\nEach site mutated **individually** by restoring the `:exact` default at that site alone, then `mix test test/specled_ex/review/html_test.exs`, then restore + confirm byte-clean tree. Where a site reads `attribution_copy/1` twice (an outer `case` and an inner `if`), both calls were mutated, since a half-mutation is not the bug dn4.5 fixed.\n\n| # | site | mutation | result |\n|---|---|---|---|\n| 1 | `rollup_qualifier_suffix/1` html.ex:2034 | `attribution_copy(reach)` → `Map.get(reach, :attribution, :exact)` | **KILLED** — 1 failure |\n| 2 | `rollup_qualifier_title_suffix/1` html.ex:2044 | same | **KILLED** — 1 failure |\n| 3 | `render_attribution_qualifier/2` html.ex:3062+3072 | both → `Map.get(subject_reach \\|\\| %{}, :attribution, :exact)` | **KILLED** — 1 failure |\n| 4 | `reached_by_tests_title/1` html.ex:3134+3144 | same | **KILLED** — 1 failure |\n| 5 | `self_verified_qualifier_label/1` html.ex:3167 | same | **KILLED** — 1 failure |\n\nEvery mutation was killed by the same named test: *\"render_coverage_tab — v2 envelope bind-closure view keyless per-test reach stays unqualified across closure, reached-by, self-verified, and rollup surfaces\"*. **No site survived.** Tree byte-clean after each.\n\nThe guard-plus-fallback pair dn4.5 landed is preserved — `attribution_copy/1` *is* the pair (head `%{attribution: a} when a in [:exact, :degraded_unhooked]` **plus** fallback `_ -\u003e :unknown`), and all five call sites route `:unknown` to the unqualified branch. A missing `:attribution` key still renders UNQUALIFIED at every one of the five surfaces. Consolidation did not reintroduce the bug.\n\n### Directive 2 — three unpinned strings, each reverted individually to its ca7ef09 text\n\n| site | result |\n|---|---|\n| html.ex:3069 `render_attribution_qualifier/2` degraded title | **KILLED** — *\"degraded unhooked per-test run names unhooked modules on the closure line and banner\"* (html_test.exs:1319) |\n| html.ex:3141 `reached_by_tests_title/1` degraded branch | **KILLED** — same named test |\n| html.ex:3073 `render_attribution_qualifier/2` non-degraded title/label | **KILLED** — *\"per-test closure line renders the exact-within-chained-windows qualifier\"*, html_test.exs:**1269**, assertion at :**1306** |\n\nThe third was re-run a second time with an encoding-clean single-phrase mutation, to rule out the failure being an em-dash artifact of the full-string revert: same named test, same assertion line. Results match the implementer's reported 1319 / 1269. **This three-ticket residual is now closed.**\n\n### Directive 3 — confirmed, no contortion\n\n`source_file/1` keeps both defensive branches: the unrecognized-`:source`-shape path (now the `with` else) and the `rescue _ -\u003e nil`. Nothing was added to production code to make them reachable. One *new* dead clause did appear as a side effect: `repo_relative_path(_), do: nil` (formatter.ex:286) is unreachable because `source_to_binary/1` always returns a binary. Cosmetic.\n\n### Per-sub-item spot-checks (all 16)\n\n1. **skip — honest.** Both private resolvers still present (`formatter.ex:260 source_file/1`, `coverage_triangulation.ex:435 module_source_file/1`); dn4.12 exists and describes exactly that state. Note the duplication actually *grew* by one helper: `source_to_binary/1` is now defined in **both** files.\n2. **applies — DIVERGES, see N1.**\n3. verified above.\n4. stderr notice **byte-identical** (programmatic extract-and-diff of base vs HEAD). `diagnostic_count` gone repo-wide (no lib, test, spec, or json reference). `disarmed_notice/0` gone. `baseline || %{}` removal is safe — `:baseline` is set in `run_init/1`, `suite_started`, and `record_inventory/2`. **But** the `state.modules` fallback removal is a real change — see N2.\n5. verified equivalent for every arity the old ladder handled. `{}` → `acc` (explicit clause; old reached the same via `Enum.reduce([], …)`). 2/3/4/5-tuples identical. Tuples of arity ≥6 now route the head through `maybe_add_anno` instead of `collect_lines` — identical whenever the head is an atom, which is true of every `raw_abstract_v1` function-form node (`{:try, Anno, …}` is the only 6-tuple in the grammar). The one genuine divergence is 1-tuples (old recursed into the element; new treats it as an anno candidate); 1-tuples do not occur in function forms. Not reachable.\n6. verified: `if MapSet.disjoint?(a, b), do: acc, else: [x | acc]` is the exact inverse of the old `if MapSet.size(MapSet.intersection(a, b)) \u003e 0`.\n7. **claim HONEST — verified at the base commit.** `git show 1910d7d:lib/specled_ex/coverage/boundary.ex` and `…/formatter.ex` contain neither `:ets.info` nor `table_alive`, so the two cited probes genuinely no longer existed. The only remaining full `:ets.info/1` liveness probe in lib is `coverage/arming.ex:73`, correctly outside allowed touches.\n8. **spec matches code exactly.** `coverage.ex:29 @boundary_table_options [:public, :set, read_concurrency: true, write_concurrency: true]`; the `anonymous_ets` folded scalar now reads `:ets.new(:anon, [:public, :set, read_concurrency: true, write_concurrency: true])`. `spec.cover.test.ex:198-204` uses the same literal list. Pinned by `formatter_test.exs:66-67`.\n9. **skip — honest.** `modules_for/1` and `modules_cache_key/0` no longer exist in boundary.ex (dn4.4 moved the cache into `Arming.modules/1`); dn4.13 describes exactly that state.\n10. verified; two new boundary_test cases pin dead-table head and tail. The rescue is broader than strictly needed — it also swallows an `ArgumentError` raised by `Arming.modules/1` or `snapshot_fn` — but that is within the sanctioned robustness scope.\n11. verified, **with** the spec co-change (`boundary_noop_unarmed` gained the setup_all sentence). New key is `{mod, :__setup_all__}`.\n12. verified in both `coverage.ex` moduledoc and `docs/coverage.md`. `lib/specled_ex/case.ex` holds the same duplicated wiring text but is **not** in allowed touches and was correctly left alone.\n13. consistent with dn4.6's corrected model — it appends a figure and contradicts nothing. The 264 µs / 800-module / 1,000-cycle number is an **unverifiable claim**: no benchmark artifact is committed, so it cannot be reproduced from the repo.\n14. verified on the read path. `classify_v2/1` still defaults **absent** `:meta` to `%{}`, so `scenario.envelope_meta_tolerant_read` is unaffected; present-non-map now yields `{:error, :invalid_artifact}`. Two notes: the write-path check is scope spillover (N3), and the new test is tagged to `specled.coverage_capture.envelope_meta`, whose statement does not yet state the rejection rule — the requirement under-specifies the behavior its test now asserts.\n15. **verified: no production behavior change.** `Review.CoverageClosure.build_v2/2` builds `subject_reqs` *before* `line_index`, and `requirement_view/2` → `mfa_source_file/1` calls `Code.ensure_loaded(mod)` on every closure MFA's module at `coverage_closure.ex:133`. So every module in the line index is already loaded by the time `module_source_index/1` runs `:code.is_loaded`. Formatter's `source_file/1` only ever sees cover-compiled (hence loaded) modules. dn4.5's identity join and dn4.7's partition test both pass in the full suite. Coverage attribution cannot change for a not-yet-loaded module on any production path.\n16. **partially applied.** Directory *creation* is hardened (8 random bytes + `File.mkdir!`, so a collision raises instead of silently reusing a dir). The `mfa_lines_test.exs:14-33` `on_exit` hand-walk (`File.ls!` / `File.rm` / `File.rmdir`) is unchanged — a nested subdirectory would still leak. Inert now that every run gets a fresh unique root.\n\n## Verdict\n\n**PASS.** All six gates green on the default base. Both binding directives independently reproduced, not taken on report. All five attribution sites and all three dn4.6 strings are individually pinned — eight mutations, eight kills, zero survivors. The disposition table is honest on every claim spot-checked, including the two skips (the code genuinely still holds the un-consolidated state dn4.12 and dn4.13 describe) and item 7's moot claim (verified at the base commit, not taken on faith).\n\n## Notes\n\nThree behavior changes outside the sanctioned items 10/11/14 exist. **None is reachable through any path specled itself produces**, all gates are green, and none warrants blocking — but they make the implementer's blanket attestation (\"no observable behavior changed except items 10, 11, and 14\") overstated. Recording them so the epic PR reviewer sees them.\n\n**N1 — item 2: `:files` is now computed by a different function.** Old `group_records_by_test/1` built it as `recs |\u003e filter(lines_hit != []) |\u003e map(normalize_path(\u00261.file)) |\u003e uniq`. The new `:files_only` mode derives it from the `:with_lines` machinery: `Map.keys(lines_by_file) |\u003e Enum.sort`, keyed by `repo_relative_source_path/1`. Those are different functions — `normalize_path/1` only strips a leading `./`, while `repo_relative_source_path/1` expands against `File.cwd!()` and returns nil outside the root. Measured:\n\n| record `:file` | old `:files` | new `:files` |\n|---|---|---|\n| `lib/x.ex` | `[\"lib/x.ex\"]` | `[\"lib/x.ex\"]` — same |\n| `./lib/x.ex` | `[\"lib/x.ex\"]` | `[\"lib/x.ex\"]` — same |\n| absolute, inside repo | `[\"/Users/…/lib/x.ex\"]` | `[\"lib/x.ex\"]` — **diverges** |\n| absolute, outside repo | `[\"/opt/elsewhere/x.ex\"]` | `[]` — **dropped** |\n| non-binary | `[nil]` | `[]` — **dropped** |\n\nOrdering also flipped from first-seen to sorted; harmless, since every consumer either folds into a MapSet or re-sorts. Not reachable via specled's own writer: `Formatter.records_for_inventory/3` sources `:file` from `compact_hits_to_files/2`, whose keys come from `source_file/1` and are therefore always repo-relative binaries with nil-mapping modules already dropped. It **is** reachable for callers of the public v1 API (`findings/3`, `execution_reach_map/2`, `per_requirement_reach/2`) who hand-build records — for those, `subjects_for_files/2` can now match a subject it previously missed, so `branch_guard_untethered_test` / `branch_guard_underspecified_realization` can fire where they did not.\n\n**N2 — item 4: `state.modules || state.modules_fn.()` → `state.modules` in `flush/1`.** `state.modules` is nil by construction (`run_init/1` sets it), and the adjacent `record_inventory/2` still carries an explicit `%{modules: nil}` clause whose comment reads *\"A `test_finished` arriving with no prior `suite_started` resolves module scope lazily rather than crashing.\"* Probed directly: with `modules: nil`, flush now raises where it previously recovered — the real `Snapshot.take(nil, :classic)` raises `FunctionClauseError`, and a generic enumerating `snapshot_fn` raises `Protocol.UndefinedError`. Unreachable through ExUnit's real lifecycle, which always casts `suite_started` first. But the same defensive posture against a missing `suite_started` is now half-removed, one handler defending and its neighbour not. A reviewer may want the one-line fallback restored.\n\n**N3 — item 14 spillover onto the write path.** `validate_envelope!/1` gained `unless is_map(envelope.meta)`. `:meta` is not in `@v2_required_fields`, so an envelope without `:meta` now raises `KeyError` from a function documented to raise `ArgumentError` on missing required fields. Unreachable via `build_envelope/1` (always sets `:meta`) and contrary to the declared `envelope()` type, but `write_v2/2` is public API. Item 14's charter was `classify_v2/1`, the read path, only.\n\nWorktree left byte-clean; pre-existing `stash@{0}` untouched. Ticket left open.",
      "timestamp": "2026-07-25T19:20:27Z"
    },
    {
      "text": "AUDIT: APPROVE\n\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nCold audit of ddda892 against base 1910d7d in /Users/mz/src/specled_ex-tier3-cleanups. Charter: adjudicate the verifier's three Notes (N1/N2/N3), spot-check two mutation kills, item 15's ordering claim, scope, and the `--base HEAD~1` methodology flag.\n\n## Ruling: N1 — item 2's `:files` fold — genuine Note (advisory)\n\nThe divergence is real. `group_records/2` in `:files_only` mode derives `:files` from `Map.keys(lines_by_file)` keyed by `repo_relative_source_path/1` (`coverage_triangulation.ex:388`, `:416`), where the deleted `group_records_by_test/1` used `normalize_path(\u00261.file)`. Confirmed against `git show 1910d7d:lib/specled_ex/coverage_triangulation.ex`.\n\nBut the framing of it as silent row-dropping toward a stronger claim does not survive tracing, on two independent grounds.\n\n**1. On this branch it is a no-op for every artifact specled writes.** `Formatter.source_file/1` already relativizes (`formatter.ex:260-272` + `repo_relative_path/1` at `:278`) and `compact_hits_to_files/2` (`:229-241`) already drops nil-mapping modules. That relativization landed in dn4.5 (42c0f1b), i.e. it is present at dn4.9's base, so record `:file` was already a repo-relative binary before this ticket. The rewrite only changes the answer for records that did not come from the writer.\n\n**2. Every divergence moves toward WEAKER claims and LOUDER findings, not stronger ones.** I traced all four `:files` consumers:\n\n- `subjects_for_files/2` (`:666`) matches against `owned_files`, which are the subject `surface:` paths — repo-relative. An out-of-repo absolute path could never have matched one, so dropping it changes nothing. A non-binary `:file` produced `[nil]` before, and `Map.get(file_to_subjects, nil)` matched nothing, so `[]` is equivalent.\n- `execution_reach/2` (`:537`), `untested_findings/3` (`:567`) and `per_requirement_reach/2` (`:213`) match against `closure_files`, which are RAW `module_info(:compile)[:source]` values from `spec.triangle.ex:447` and `coverage_closure.ex:132` — both out of scope and unchanged. I verified empirically that this value is ABSOLUTE. `normalize_path/1` only strips a leading `./`, so closure files stay absolute.\n\nConsequence: removing or relativizing a `:files` entry can only remove a match. That LOWERS `execution_reach`, and RAISES `branch_guard_untested_realization`. On the relativizing side it lets `untethered`/`underspecified` fire where they previously could not. Coverage looks worse and the tool gets louder — the direction this epic has been pushing all day. Nothing here makes coverage look better than reality.\n\nReachability for hand-built records is real (`findings/3` `:97`, `execution_reach_map/2` `:154`, `per_requirement_reach/2` `:184` are all `@doc` + `@spec`; `Store.build_records/1` at `store.ex:36` is the documented author helper, and its own docstring scopes it to tests). But given the direction above, that reachability costs a caller findings they should arguably be seeing, not findings they should not.\n\n**Genuine Note.** The blanket attestation is overstated and the epic PR reviewer should see it, but this is not a Gap.\n\n## Ruling: N2 — item 4's `state.modules` fallback removal — genuine Note (and it was CHARTERED)\n\nThe verifier reported this as an unsanctioned removal. It is not. The ticket's own item-4 scoping row names it explicitly: \"`formatter.ex:287` `state.baseline || %{}` ... Also `state.modules || state.modules_fn.()` at :285.\" The implementer deleted exactly what dn4.9 asked to be deleted.\n\nThe deadness classification is also correct. `flush/1` has a single caller, `handle_cast({:suite_finished, _}, state)` at `formatter.ex:184`. `state.modules` is set by `suite_started` (`:165`) and by `record_inventory`'s nil clause (`:204`). ExUnit always emits `suite_started` before `suite_finished`, and every formatter test that casts `suite_finished` calls `suite_started` first (`formatter_test.exs:325`/`:334`, `:763`/`:774`). There is no path, production or test, that reaches `flush/1` with `modules: nil`.\n\nThe asymmetry with the surviving `%{modules: nil}` clause in `record_inventory/2` (`:202-211`) is real, but both clauses guard the same impossible state and only one was on the delete list. Cosmetic. Worth a follow-up to resolve one way or the other; not worth a bounce.\n\n## Ruling: N3 — item 14's write-path spillover — genuine Note (the one real unchartered change)\n\nReproduced directly. A v2 envelope map lacking `:meta` now raises:\n\n    KeyError — key :meta not found\n\nfrom `write_v2/2`, whose `@doc` (`store.ex:193-196`) promises `ArgumentError`. Cause: `:meta` is absent from `@v2_required_fields` (`:306-315`), so the new `unless is_map(envelope.meta)` at `:326` reaches dot access on a missing key. Item 14's charter was `classify_v2/1`, the read path.\n\nWhy it stays advisory: the declared `@type envelope()` (`:116-126`) makes `:meta` required, so the input is out-of-contract; every in-repo caller routes through `build_envelope/1`, which always sets it (`formatter.ex:391`, `spec.cover.ingest.ex:74`/`:83`, `spec.cover.test.ex:140`/`:168`, and six test sites), so it is unreachable in-repo; and it fails LOUDLY rather than silently, which is the safe direction for this epic. It is a one-line fix and cheaper as a follow-up than as a bounce on the last ticket of a twelve-ticket epic.\n\nTwo associated observations, both follow-up material: the write-path check is pinned by no test (`store_test.exs:269-286` only exercises `read_v2`), and the new test is tagged `spec: \"specled.coverage_capture.envelope_meta\"` whose statement (`coverage_capture.spec.md:551-563`) says only that `read_v2/1` shall TOLERATE a missing `:meta` — it never states the non-map rejection rule the test now asserts.\n\n## Spot-checks\n\n**Mutations — 2 of 8 reproduced independently, both KILLED.** Run in an isolated copy of the tree (the audited worktree's `lib/` is write-blocked for this agent), restored to byte-identical afterward:\n\n- Attribution site 5, `self_verified_qualifier_label/1` `html.ex:3167` — `attribution_copy(subject_reach)` restored to `Map.get(subject_reach || %{}, :attribution, :exact)`. KILLED, 1 failure: \"render_coverage_tab — v2 envelope bind-closure view keyless per-test reach stays unqualified across closure, reached-by, self-verified, and rollup surfaces\", assertion at `html_test.exs:1487`. Matches the verifier's row 5.\n- dn4.6 string 2, `reached_by_tests_title/1` degraded branch `html.ex:3141` — reverted to its ca7ef09 text (\"hooked windows remain exact up to escaped processes.\"). KILLED, 1 failure: \"degraded unhooked per-test run names unhooked modules on the closure line and banner\", `html_test.exs:1319`, assertion at `:1353`. Matches the verifier's reported 1319.\n\nBaseline `html_test.exs` was green (97 tests, 0 failures) before each mutation and after each restore.\n\n**Item 15 ordering — holds on the production path.** In `build_v2_ok`, `subject_reqs` is built at `coverage_closure.ex:334-345` BEFORE `line_index` at `:347-363`, and `requirement_view/2` (`:106`) force-loads every closure MFA's module through `mfa_source_file/1` → `Code.ensure_loaded/1` (`:133`). `closure_modules/1` (`:443`) derives the line-index modules from those same `closure_mfas`, so `:code.is_loaded` never sees an unloaded module. Aggregate mode short-circuits to `line_index = %{}`. Formatter's `source_file/1` only ever sees `modules_fn.()` output, defaulting to `Coverage.cover_modules_safe/0` (`arming.ex:66`) — cover-compiled and therefore loaded. The one path where the force-load does NOT precede the resolver is the injected `:line_index` opt at `:348`, documented test-only (\"tests use this to inject a stub\"); the eight injection sites in `coverage_closure_test.exs` all use locally-defined fixture modules, and the suite is green.\n\n**Scope — clean.** All 17 touched files fall inside Allowed touches. None of the Out-of-Scope files appears in the diff: `realization/**`, `compiler/**`, `coverage/aggregate.ex`, `spec.triangle.ex`, `review/coverage_closure.ex`, `spec_review.spec.md`, `package.spec.md`, `docs/adoption.md`, `docs/concepts.md`. The five attribution sites and dn4.6's three strings remain pinned (`grep -c` on `html.ex`: chained-window wording 3, retired phrase 0).\n\n**`--base HEAD~1` methodology flag — the verifier's answer was correct when it ran, and is now STALE. This is the most actionable thing I found.** `origin/main` has since advanced to 134d0e8 (`feat(specled_-hj4): spec.check verdict contract — release 0.8.0`, PR #21), past the epic's merge-base 14f6e72. Re-running both bases now:\n\n- default (`origin/main`): **exit 1**, `changed_files=89 findings=49`, with 4 `[ERROR] append_only/requirement_deleted` — `specled.next.verdict_read_protocol`, `specled.prime.verdict_read_protocol`, `specled.tasks.branch_findings_breakdown`, `specled.tasks.verdict_line`.\n- `--base HEAD~1`: **exit 0**, `changed_files=16 findings=9`, clean.\n\nAll four requirements live in `next.spec.md`, `prime.spec.md` and `mix_tasks.spec.md` on `origin/main` and are absent at HEAD — verified with `git grep` on both refs. Those three spec files were never touched by this epic. This is pure base drift from release 0.8.0 landing on main after the epic branched, not a dn4.9 regression and not any sibling's. It resolves by rebasing the epic branch onto `origin/main` before the PR — but until that rebase happens, the epic PR's CI-equivalent gate is RED, so it must be done before the PR is opened.\n\n## Residual shipping with this approval\n\nThree behavior changes outside the sanctioned items 10/11/14 ship as-is: N1 (`:files` recomputed, no-op for real artifacts, divergences only toward weaker claims), N2 (chartered by the ticket's own item-4 text; unreachable), N3 (`KeyError` instead of the documented `ArgumentError` from `write_v2/2` on a `:meta`-less envelope; unreachable in-repo, fails loudly). The implementer's blanket behavior-preservation attestation is overstated on all three and should be read with this comment attached on the epic PR.\n\nSCOPE_EXPANSION RECOMMENDED: `Store.write_v2/2` raises `KeyError` on an envelope lacking `:meta`, contradicting its documented `ArgumentError` contract — add `:meta` to `@v2_required_fields` or read it with `Map.get/3`, and pin it with a test.\nSCOPE_EXPANSION RECOMMENDED: `specled.coverage_capture.envelope_meta` under-specifies its own test — the statement covers tolerant read of a missing `:meta` but never states the present-but-non-map rejection rule that `store_test.exs` now asserts.\nSCOPE_EXPANSION RECOMMENDED: resolve the formatter's defensive asymmetry — `record_inventory/2` still carries a `%{modules: nil}` recovery clause while `flush/1` no longer does; either restore the fallback, drop the sibling clause, or comment why they differ.\nSCOPE_EXPANSION RECOMMENDED: the v1 file-level detectors join repo-relative `:files`/`owned_files` against absolute `closure_files` from raw `module_info(:compile)[:source]` (`spec.triangle.ex:447`, `coverage_closure.ex:132`), so `execution_reach` and `untested_realization` can never match — extend dn4.5's `per_test_path_identity` normalization to the v1 path.\nSCOPE_EXPANSION RECOMMENDED: `source_to_binary/1` is now duplicated in both `formatter.ex` and `coverage_triangulation.ex`, growing the duplication dn4.12 is chartered to remove — fold it into dn4.12's scope.\n\nWorktree left byte-clean; generated `.spec/state.json` removed to restore the as-found tree; pre-existing `stash@{0}` untouched. Ticket left open, nothing fixed.",
      "timestamp": "2026-07-26T00:25:30Z"
    },
    {
      "text": "APPROVED per AUDIT: APPROVE (2026-07-26T00:25:30Z). Merging into specled_-dn4/fast-follow-per-test.\n\nAll three verifier Notes adjudicated as genuinely advisory (N2 was chartered outright). Five\nfollow-ups recommended by the audit are being carved as STANDALONE tickets, not epic children —\nthe epic's expansion ledger stays at three and none of the five is required for this epic's\ndeliverable.",
      "timestamp": "2026-07-26T00:26:09Z"
    },
    {
      "text": "APPROVED. Merged into specled_-dn4/fast-follow-per-test. Closing per orchestrator sign-off. (Gate initially red on 49 phantom branch findings from a stale base — origin/main moved to 0.8.0 mid-epic; after rebasing the epic onto 134d0e8 the gate is green: spec.check result=pass, exit 0, 1236 tests 0 failures, 0 ERROR findings. Approved with the three advisory behavior-change notes from the 00:25:30Z audit, all filed as standalone follow-ups.)",
      "timestamp": "2026-07-26T01:03:56Z"
    }
  ],
  "priority": 1,
  "status": "closed",
  "title": "Tier-3 seam cleanups, cheap perf, robustness batch (F9/F13/F16/F17/F19/F20/F46, F25, F21/F22/F24, F44/F15, F10/F42/F43)",
  "type": "task",
  "updated_at": "2026-07-26T01:03:57Z"
}
