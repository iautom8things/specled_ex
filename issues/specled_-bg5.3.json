{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-08-06T23:11:01Z",
  "description": "No spec subject (candidates: specled.verification docs surface when picked up). Severity: Medium. From 2026-08-06 critical review of specled_-q0q. spec.check --run-commands executes verification commands defined in repo-resident spec files; in a CI configured to run on fork PRs this executes attacker-authored shell with the workflow token. Keep-reason: docs/policy deliverable (threat-model statement + recommended CI gating for public repos), not a code fix on the shipped branch. Concrete failure: a public repo running armed spec.check on pull_request from forks grants fork authors arbitrary command execution in CI.\n\n## ORCHESTRATOR CONTRACT (specled_-i4x)\n\nAdvances: specled.package\n\n(Orchestrator note: NOT specled.verification — that subject owns command execution\nmechanics; this ticket ships no code. Model the new requirement on the existing\nspecled.package.adoption_guide / .concepts_guide pattern, .spec/specs/package.spec.md:104-111.)\n\nAllowed touches:\n- docs/security.md (new)\n- docs/adoption.md  — the \"CI should always run commands\" line at docs/adoption.md:489\n  must be CORRECTED, not merely supplemented. It is the unsafe advice.\n- README.md\n- .spec/specs/package.spec.md  (new requirement + surface: entry for docs/security.md)\n- .spec/decisions/specled.decision.*.md  (only if the policy is a durable cross-cutting stance)\n- CHANGELOG.md\n\nOut of Scope (files):\n- lib/** (all)\n- test/** except test/docs_identifier_lint_test.exs\n- .spec/realization_hashes.json\n- priv/spec_init/workflows/spec_review.yml.eex  (also specled.spec_review surface; widens the ticket)\n\nOut of Scope (intent):\n- Do NOT flip the --run-commands default or add a sandbox/allowlist. This deliverable is a\n  written policy. Changing the default is a separate breaking change against\n  .spec/specs/mix_tasks.spec.md.\n- Do NOT name any finding code the implementation does not emit. docs/**/*.md is inside the\n  doc-identifier lint corpus (test/docs_identifier_lint_test.exs:223) and an invented code\n  name fails the gate. Show severities in bare YAML token form, not atom form\n  (specled.package.doc_identifier_integrity, .spec/specs/package.spec.md:112).\n- Do NOT write a generic SECURITY.md disclosure process. The ask is one threat statement\n  plus the CI recommendation.\n\nVerification:\n  mix test test/docs_identifier_lint_test.exs\n  mix test test/specled_ex/verifier_test.exs\n  mix compile --warnings-as-errors\n  mix format --check-formatted\n  mix spec.check --no-run-commands\n  bash ./scripts/check_specs.sh\nNo execute:false stub to flip. The doc-identifier lint is the real gate: it pins corpus size\nand fails on any invented code name.\n\nExpected diff: ~130-200 lines across 4-6 files, mostly the new doc.\n",
  "id": "specled_-bg5.3",
  "labels": [
    "phase:implementing"
  ],
  "parent": "specled_-bg5",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: i4x-bg5.3\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-bg5.3/codex-i4x-bg5.3/log\n\nCommit: 5c800179cd41b3c37b1b6b3ec1698b04453ed913\nFiles touched: .spec/specs/package.spec.md, README.md, docs/adoption.md, docs/security.md\nTests added: None; existing SpecLedEx.DocsIdentifierLintTest and SpecLedEx.VerifierTest coverage exercised\nVerification stubs flipped: None\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix format --check-formatted; mix test test/docs_identifier_lint_test.exs; mix test test/specled_ex/verifier_test.exs; mix spec.check --no-run-commands; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\n\nSummary: Added a focused security guide that identifies repository-authored verification commands as a CI trust boundary and documents a least-privilege fork-PR workflow. Corrected the adoption guide's unsafe always-run-commands advice, linked the policy from the README, and bound the new surface to a stable specled.package must requirement. The epic contains no recorded lesson lines to check.",
      "timestamp": "2026-08-12T12:07:57Z"
    },
    {
      "text": "VERIFY: FAIL\nSession: 9a42db78-611e-4632-882a-adaefdaa666f\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/9a42db78-611e-4632-882a-adaefdaa666f.jsonl\n\nMechanical checks:\n- Files touched: 4 (.spec/specs/package.spec.md, README.md, docs/adoption.md, docs/security.md)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0 (git diff --name-only over lib/, test/, priv/, .spec/realization_hashes.json returns 0; priv/spec_init/workflows/spec_review.yml.eex untouched)\n\nCorpus-scoped claim validation (this is a doc diff — the main event):\n- Claims validated: 18 total, 14 by execution\n- CLI commands/flags/defaults checked by running them (fixture repo with a `kind: command` target that appends a sentinel file; sentinel cleared between runs):\n  - `mix spec.check --root FIX` -\u003e sentinel EXECUTED (commands on by default: doc claim TRUE)\n  - `mix spec.check --root FIX --no-run-commands` -\u003e sentinel empty (flag real, suppresses execution)\n  - `mix spec.check --no-run-commands --base origin/main` (the doc's verbatim YAML line, run in-repo) -\u003e exit 0, `spec.check result=pass`\n  - `mix spec.status --root FIX` -\u003e sentinel EXECUTED (commands on BY DEFAULT — see Gap)\n  - `mix spec.status --root FIX --no-run-commands` -\u003e sentinel empty\n  - `mix spec.validate --root FIX` -\u003e sentinel empty (opt-in, correct)\n  - `mix spec.prime --root FIX` -\u003e sentinel empty (opt-in, correct)\n  - `mix spec.review --root FIX` -\u003e sentinel empty (never executes)\n  - `mix spec.review --run-commands` -\u003e `** (Mix) Invalid arguments for spec.review: --run-commands` (task has no such switch)\n  - README/adoption link targets docs/adoption.md, docs/concepts.md, docs/coverage.md, docs/security.md all exist\n- --run-commands default for spec.check vs spec.review: spec.check defaults to TRUE (lib/mix/tasks/spec.check.ex:180-185, `{:ok, false} -\u003e false; _ -\u003e true`); spec.review has no `run_commands` switch (lib/mix/tasks/spec.review.ex:36-45) and forwards no `:run_commands` to `Verifier.verify/3`, whose default is `false` (lib/specled_ex/verifier.ex:20). Both halves of the ticket premise hold, confirmed by execution.\n- Finding codes named in docs vs implementation: docs/security.md names ZERO finding codes and ZERO severity values (no `*/*` code tokens, no `:info`/`:warning`/`:error` atoms), so the bare-YAML-token requirement is satisfied vacuously. `mix test test/docs_identifier_lint_test.exs` -\u003e 19 tests, 0 failures; corpus (skills/**, docs/**, README.md, .spec/**) picks up the new file via the glob at test/docs_identifier_lint_test.exs:222 and stays above the \u003e50 size pin.\n- adoption.md:489 unsafe advice CORRECTED not merely supplemented: YES. New docs/adoption.md:496 reads `| mix spec.check --no-run-commands | One invocation | Local fast loop, and the structural lane for untrusted fork PRs; run commands only after the revision is trusted. |`. `grep -rn \"always run commands\"` over the whole repo (excluding .git/_build/deps) returns 0 hits — the sentence is deleted, not shadowed. docs/adoption.md:180-186 adds the fork-PR warning ahead of the \"Wire CI\" snippet as well.\n- Threat claim traced to the real execution path: ACCURATE and not overstated. `target` is a plain string field parsed out of repo-resident `.spec/specs/*.spec.md` (lib/specled_ex/verifier.ex:1190 `target = string_field(verification, \"target\")`), gated only by `runnable_command_verification?/1` (verifier.ex:2766-2772 — checks kind, `execute: true`, non-empty target, covers is a list of binaries; no syntax inspection, no allowlist), then interpolated verbatim into a `/bin/sh` script (`cd \"#{root}\"` + `#{target}`) and spawned via `Port.open({:spawn_executable, \"/bin/sh\"}, ...)` (verifier.ex:2305-2352). Arbitrary shell with the runner's environment — the doc's \"does not sandbox ... inspect ... or maintain an allowlist\" is literally true.\n- YAML/CI snippets parsed (not eyeballed): docs/security.md's single ```yaml block extracted and parsed with `YamlElixir.read_from_file/1` -\u003e parses clean; top-level keys `[\"jobs\",\"permissions\"]`, jobs `[\"commands\",\"structural\"]`, `permissions =\u003e %{\"contents\" =\u003e \"read\"}`, guard `if: github.event.pull_request.head.repo.full_name == github.repository`. All keys are real GHA keys; both context expressions are real. No `on:` key, so the YAML 1.1 boolean gotcha does not arise (see Notes).\n- Falsehoods found: docs/security.md:11-12 (see Gap).\n\nSemantic checks:\n- Spec addition additive, not weakening: purely additive — new requirement `specled.package.command_execution_trust_policy` (priority must, stability stable), new `surface:` entry `docs/security.md`, new `kind: source_file` verification entry covering it (modelled on the .adoption_guide/.concepts_guide pattern), plus an appended clause on the subject `summary:`. No existing `must` was downgraded, reworded, or deleted; the append-only guard reports 0 findings. Every conjunct of the new requirement is satisfied by docs/security.md (kind: command = repo-authored shell; no sandbox/allowlist; --no-run-commands for the fork lane; commands only for a reviewed trusted revision under least privilege; pull_request_target warning; \"not a general sandbox\" clarification), and docs/security.md:5 carries the `\u003c!-- covers: --\u003e` marker matching the other doc surfaces.\n- Out of Scope held: no lib/** change, no test/** change, no priv/** change, `--run-commands` defaults untouched (verified by execution, not just by diff), no sandbox/allowlist added, no root SECURITY.md and no disclosure-process content in docs/security.md.\n- Spec stubs run: 0 flipped, 0 pending (contract states none exist)\n- Targeted verification: `mix compile --warnings-as-errors` exit 0; `mix format --check-formatted` exit 0; `mix test test/docs_identifier_lint_test.exs` 19 tests 0 failures; `mix test test/specled_ex/verifier_test.exs` 62 tests 0 failures; `mix spec.check --no-run-commands` -\u003e `spec.check result=pass` (validate errors=0 warnings=0, branch findings=0).\n- Full-suite verification: not run by verifier (by design)\n\nVerdict: FAIL\n\nGaps:\n- docs/security.md:11-12 understates the execution surface, in the dangerous direction, in the one sentence that enumerates it. The doc says: \"`mix spec.check` executes enabled command targets by default. Several other tasks accept `--run-commands` to opt into the same behavior.\" Proven by execution: `mix spec.status` also executes command targets BY DEFAULT — `report_run_commands?/1` at lib/mix/tasks/spec.status.ex:77-82 is the same `{:ok, false} -\u003e false; _ -\u003e true` shape as spec.check, and its own moduledoc (spec.status.ex:12-14) says so outright. Only `mix spec.prime` (spec.prime.ex:87-90) and `mix spec.validate` (spec.validate.ex:69) are genuinely opt-in. So exactly two tasks execute by default; the doc names one and files the other under \"opt into\". This falsifies the deliverable's own subject matter: a maintainer who follows the recommended two-lane split verbatim, but whose untrusted fork lane also invokes `mix spec.status` (a maintainer-facing summary command the README lists at README.md:48 and adoption.md:239 recommends for brownfield), executes fork-authored shell with that job's token — the exact failure this ticket exists to prevent. The trap is live in the package's own scaffolding: priv/spec_init/agents/skills/spec-led-development/SKILL.md.eex:120 tells every adopting repo that `mix spec.status` is \"Read-only.\" (that pre-existing line is out of scope to fix here, but it is why the omission is misleading rather than pedantic). Fix is one sentence: name `mix spec.status` as the second default-executing task, and restrict the \"opt into\" clause to `mix spec.prime` and `mix spec.validate`.\n\nNotes (advisory only):\n- docs/security.md:89-92 says `--no-run-commands` \"disables `kind: command` verification targets\". It also disables `kind: tagged_tests` execution, which spawns `mix test` (verifier.ex:1327-1332, tagged_tests.ex:5) and is likewise repository-controlled code. Understated in the SAFE direction (the flag disables more than advertised), but the threat section's scoping to `kind: command` leaves the second specled-owned execution kind unnamed. Same one-line class of fix.\n- The GHA snippet has no `on:` trigger, so it is a fragment rather than a copy-pastable workflow; the `if:` guard on the `commands` job also evaluates false on non-`pull_request` events (`github.event.pull_request` is null), which silently disables the trusted lane on `push`. The surrounding prose does say \"Adjust the setup and base-ref fetch to match your project.\"\n- docs/adoption.md's \"Wire CI\" snippet still shows the armed `mix spec.check --base origin/main` form, but it is trigger-agnostic and now sits directly under the new fork-PR warning, so it does not re-assert the removed advice.\n- CHANGELOG.md was in the Allowed set and was not touched. Not required by the contract; flagging in case the release note is expected at merge.",
      "timestamp": "2026-08-12T12:16:34Z"
    },
    {
      "text": "AUDIT: BOUNCE → implementer\nSession: 9a42db78-611e-4632-882a-adaefdaa666f\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/9a42db78-611e-4632-882a-adaefdaa666f.jsonl\n\nWhich tasks execute repo-authored commands by default (my own fixture, built outside the repo at scratchpad/audit_fixture, one subject with `kind: command` / `execute: true` whose target appends a line to an absolute sentinel path; sentinel deleted before every run; tree left clean):\n- mix spec.validate --root FIX -\u003e sentinel 0 (opt-in, correct)\n- mix spec.prime --root FIX -\u003e sentinel 0 (opt-in, correct)\n- mix spec.status --root FIX -\u003e sentinel 1  ** EXECUTES BY DEFAULT **\n- mix spec.check --root FIX -\u003e sentinel 1  (executes by default, as documented)\n- mix spec.next / spec.index / spec.review / spec.triangle --root FIX -\u003e sentinel 0\n- mix spec.status --no-run-commands -\u003e 0; mix spec.check --no-run-commands -\u003e 0\n- mix spec.prime --run-commands -\u003e 1; mix spec.validate --run-commands -\u003e 1\n- mix spec.next --run-commands / mix spec.review --run-commands -\u003e `** (Mix) Invalid arguments` (no such switch)\nCode agrees: spec.status.ex:77-82 and spec.check.ex:180-185 are both `{:ok, false} -\u003e false; _ -\u003e true`; spec.prime.ex:87-90 is `{:ok, value} -\u003e value; :error -\u003e false`; spec.validate.ex:69 is `opts[:run_commands] || false`. Exactly TWO tasks execute by default; exactly TWO are genuinely opt-in.\n\nVerifier's Gap confirmed/refuted: CONFIRMED, independently, not inherited. docs/security.md:10-12 is false in the dangerous direction. `mix spec.status` accepts `--run-commands` (spec.status.ex:27, `run_commands: :boolean`), so it is literally one of the \"several other tasks [that] accept `--run-commands`\" — and the clause's predicate \"to opt into the same behavior\" is false of it. The sentence does not merely omit spec.status; it actively files it on the safe side of the line.\n\nMateriality: MATERIAL, bounce. Three reasons.\n1. This is the one sentence in the deliverable whose entire job is to enumerate the execution surface. Getting it wrong in the dangerous direction falsifies the document's own subject matter.\n2. Nothing else in docs/security.md closes it. The catch-all at :95-99 names non-specled steps only — \"dependency installation, compilation, tests, custom Mix aliases, and coverage commands\". It never says \"run no other specled task on untrusted input\", and the recommended lane split (:26-38) prescribes only what to run, not what to avoid. A maintainer who wants a coverage summary on fork PRs adds `mix spec.status` to the structural lane and executes fork-authored shell with that job's token — the exact failure this ticket exists to prevent.\n3. The remedy is one sentence inside an already-allowed file.\n\nAggravating context (SKILL.md.eex \"Read-only.\"): VERIFIED. priv/spec_init/agents/skills/spec-led-development/SKILL.md.eex:120 reads \"- `mix spec.status` — coverage summary, weak spots, ADR usage. Read-only.\" That file is scaffolded by `mix spec.init` into every adopting repo, so the package ships the false belief. It does change the call: it moves the omission from pedantic to misleading, because docs/security.md is the one document positioned to correct that line and instead reinforces it. Fixing SKILL.md.eex is out of scope here (priv/** is Out of Scope) — file a follow-up.\n\nSpot-check of the verifier's PASSes — all three hold:\n- adoption.md correction GENUINE, not relocated: `grep -rn \"always run commands\"` over the repo (minus .git/_build/deps) returns 0. The table row at adoption.md:496 is rewritten in place (\"Local fast loop, and the structural lane for untrusted fork PRs; run commands only after the revision is trusted.\"), and adoption.md:180-185 adds the fork-PR warning immediately above the \"Wire CI\" snippet. The snippet still shows the armed `mix spec.check --base origin/main`, but it now sits under that warning and asserts nothing about forks.\n- Threat claim ACCURATE, not overstated: the only gate is `runnable_command_verification?/1` (verifier.ex:2766-2772) — kind == command, execute? true, non-empty target, covers is a list of binaries. No syntax inspection, no allowlist. `target` is then interpolated verbatim into a `/bin/sh` script (`cd \"#{root}\"` + `#{target}`, verifier.ex:2331-2335) spawned via `Port.open({:spawn_executable, \"/bin/sh\"}, ...)` (verifier.ex:2348-2352). \"does not sandbox ... inspect ... or maintain an allowlist\" is literally true.\n- Spec change ADDITIVE and every conjunct satisfied: the only edit to existing content is an append to the subject `summary:`; no `must` downgraded, reworded, or deleted. New `specled.package.command_execution_trust_policy` (must/stable) plus a `kind: source_file` verification covering it. All seven conjuncts land in docs/security.md — repo-authored shell (:8-10), no sandbox/inspect/allowlist (:20-22), `--no-run-commands` for the fork lane (:28-30), commands only for a trusted revision under least privilege (:31-35), `pull_request_target` warning (:76-81), not-a-general-sandbox (:93-99); `\u003c!-- covers: --\u003e` marker at :5. Re-ran the gates myself: `mix format --check-formatted` OK, `mix test test/docs_identifier_lint_test.exs` 19 tests 0 failures, `mix spec.check --no-run-commands` -\u003e `spec.check result=pass`, `git status --porcelain` clean.\n\ntagged_tests Note promoted or not: NOT promoted to blocking on its own, but FOLD INTO THE SAME FIX. Verified: `build_command_results/5` short-circuits to `%{}` when run_commands? is false (verifier.ex:2111), which kills the merged tagged_tests run too (verifier.ex:2138-2165 -\u003e run_merged_command -\u003e the same `/bin/sh` + Port.open path), and TaggedTests base_command is `mix test` (tagged_tests.ex:5). So `kind: tagged_tests` is a second specled-owned execution kind over repository-authored ExUnit code. It stays a Note rather than a bounce because the doc's :95-99 paragraph already names \"tests\" as an untrusted-CI hazard — the reader is warned, just not told specled drives it. One pass is cheaper than two; fix it in the same commit.\n\nBounce reason: docs/security.md:10-12 must be replaced. Exact sentence to write, in place of \"`mix spec.check` executes enabled command targets by default. Several other tasks accept `--run-commands` to opt into the same behavior.\":\n\n  `mix spec.check` and `mix spec.status` both execute enabled command targets by\n  default; pass `--no-run-commands` to either to suppress that. Only\n  `mix spec.prime` and `mix spec.validate` are opt-in, via `--run-commands`.\n\nFold in, replacing the first sentence of \"What `--no-run-commands` does and does not do\" (docs/security.md:89-90, \"`--no-run-commands` disables `kind: command` verification targets.\"):\n\n  `--no-run-commands` disables both executing verification kinds: `kind: command`\n  targets, and `kind: tagged_tests`, which spawns `mix test` over the repository's\n  own test code.\n\nNothing else needs to change. Do not touch priv/** (SKILL.md.eex is a separate ticket). CHANGELOG.md was Allowed and untouched — not required by the contract, flagging for the release cut.\n\nAdvisory (not blocking): the GHA snippet has no `on:` trigger and the `commands` job's `if:` evaluates false on non-`pull_request` events, so the trusted lane silently no-ops on push. The prose does say \"Adjust the setup and base-ref fetch to match your project.\"\n\nMechanical-remedy bounce count this ticket (if any): 1/2",
      "timestamp": "2026-08-12T12:23:04Z"
    }
  ],
  "priority": 2,
  "status": "in_progress",
  "title": "F12: --run-commands executes repo-resident shell — document fork-PR CI trust policy",
  "type": "task",
  "updated_at": "2026-08-12T12:30:40Z"
}
