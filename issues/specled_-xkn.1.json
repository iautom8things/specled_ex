{
  "assignee": "Manuel Zubieta",
  "blocked_by": [
    "specled_-26g",
    "specled_-npo"
  ],
  "blocks": [],
  "closed_at": "2026-07-27T13:01:01Z",
  "created": "2026-07-24T14:22:04Z",
  "description": "Review F3 + CF1 (consensus spec+testing). Store.safe_decode/2 with opts=[] decodes UNSAFE (atom-resurrecting) despite its name — rename or flip the default to fail closed, making unsafe opt-in at the two documented carve-out sites (read_v2 envelope, load_tracer_edges manifest readers). CF1: those non-safe carve-outs have no positive regression-guard test — add tests pinning that a fresh-BEAM decode of a foreign-module artifact still succeeds there (the justification for keeping them unsafe), so a future blanket-[:safe] sweep fails loudly.",
  "id": "specled_-xkn.1",
  "labels": [],
  "parent": "specled_-k6s",
  "comments": [
    {
      "text": "ADMISSION VERIFICATION (specled_-k6s, 2026-07-27)\nCold fresh-context verifier run against the epic base tree (feat/specled_-td7-self-identifying-descriptors @ e81f9f98).\n\nVERDICT: still_applicable   EFFORT: small\nMandate confirmed still applicable as written; no narrowing needed.\n\n== SCOPED REMAINING WORK ==\nBoth halves remain. Full mandate:\n\n1. Fail-closed default (lib/specled_ex/coverage/store.ex):\n   - Line 358: change `defp safe_decode(bin, opts \\\\ []) do` to `defp safe_decode(bin, opts \\\\ [:safe]) do` (or rename the pair to `decode/2` + an explicit `decode_unsafe/1`; flipping the default is the smaller edit).\n   - Line 238 (inside `read_v2/1`): change `{:ok, term} \u003c- safe_decode(bin) do` to an EXPLICIT unsafe opt-in, e.g. `{:ok, term} \u003c- safe_decode(bin, []) do`, keeping the existing justification comment at store.ex:231-237 immediately above it.\n   - Line 295 (inside `read_status/1`): may stay `safe_decode(bin, [:safe])` or drop to `safe_decode(bin)`; keep the `[:safe]` justification comment at store.ex:290-294 intact either way. End state: no call site gets atom-resurrecting behavior implicitly.\n\n2. Positive regression guards (CF1) — a foreign/never-interned atom must still DECODE SUCCESSFULLY at each documented unsafe carve-out, so a future blanket `[:safe]` sweep fails loudly:\n   a. test/specled_ex/coverage/store_test.exs, inside `describe \"v2 envelope round trip\"` (line 150): add a test that builds v2-envelope ETF bytes whose `:files` entry carries a `:module` atom that was never interned in this VM — reuse the existing private helper `hostile_atom_binary/1` (store_test.exs:479-484), which plants the name by byte-patching a same-length placeholder so `String.to_atom/1` is never called on it — write those bytes to the artifact path, then assert `{:ok, envelope} = Store.read_v2(path)` AND that the resurrected atom is now reachable via `String.to_existing_atom(name)`. Tag it with the existing `@describetag spec: [\"specled.coverage_capture.store_v2_envelope\", \"specled.coverage_capture.store_v2_legacy_rejection\"]` or an explicit `@tag spec:` (see item 3).\n   b. Tracer-manifest carve-out: add the mirror guard for the raw readers. Cheapest single site is `SpecLedEx.Realization.Implementation`'s `read_tracer_etf/1` (lib/specled_ex/realization/implementation.ex:519-529), reachable from tests via `load_tracer_edges/2`'s `opts[:tracer_manifest]` override (implementation.ex:495-497, 513-516) — write a manifest file whose caller/callee MFA key names a never-interned module atom (same byte-patch technique; do NOT use `Module.concat`, which interns) and assert the edge map comes back non-empty with that key present. Anchor tag: `specled.compiler_tracer.etf_read_direct` (.spec/specs/compiler_tracer.spec.md:160-165) or `specled.implementation_tier.ghost_edges_filtered`. If the reviewer wants all three raw readers pinned, the same fixture can be driven through `Mix.Tasks.Spec.Triangle` (lib/mix/tasks/spec.triangle.ex:112-130) and `SpecLedEx.Review.CoverageClosure` (lib/specled_ex/review/coverage_closure.ex:38-55), but both are `defp` with no injection seam — the Implementation site is the one with a test hook.\n\n3. Spec ownership: no requirement in .spec/specs/coverage_capture.spec.md states any decode-atom policy (`grep -rn \"binary_to_term\\|:safe\\]\" .spec/` hits only compiler_tracer.spec.md:163 and two ADRs). The new tests need an owning clause or they repeat the specled_-npo \"test ahead of the requirement\" class. Add one additive requirement to the `spec-requirements` block of .spec/specs/coverage_capture.spec.md (e.g. `specled.coverage_capture.decode_atom_policy`, priority: must, stability: evolving) stating: v1 `read/1` and the `last_run.status` sidecar decode with `[:safe]` and shall reject never-interned atoms; `read_v2/1` decodes without `[:safe]` because an ingested/merged aggregate envelope may legitimately name a module absent from this BEAM, and shall return `{:ok, envelope}` in that case. Append after the existing `specled.coverage_capture.store_v2_legacy_rejection` requirement (.spec/specs/coverage_capture.spec.md:419-427). Purely additive → append-only safe.\n\n== EXPECTED TOUCHES ==\nlib/specled_ex/coverage/store.ex, test/specled_ex/coverage/store_test.exs, .spec/specs/coverage_capture.spec.md, test/specled_ex/realization/implementation_test.exs\n\n== RISK NOTES ==\nREALIZATION HASHES — low risk, do not skip the reasoning: `.spec/realization_hashes.json` has a single tier key, `api_boundary`, and it DOES contain `SpecLedEx.Coverage.Store.read_v2/1`, `.read_status/1`, `.read/1`, `.load/1`. But the api_boundary hasher hashes the resolved function HEAD only — lib/specled_ex/realization/api_boundary.ex:5-13 documents it as \"stable under formatting changes, variable renames, and line-number shifts ... but changing under arity, arg pattern shape, or literal default argument changes\". All the edits above are BODY edits to `read_v2/1` plus a change to the private, unbound `safe_decode/2`. `safe_decode/2` has no realization binding (it is `defp`, absent from realization_hashes.json and from .spec/specs/coverage_capture.spec.md's realized_by lists at :131-148). =\u003e no drift expected. If you instead RENAME/re-signature any public Store function, that WILL trip the drift gate and needs a `.spec/realization_hashes.json` baseline update — prefer the flip-the-default option to avoid it.\n\nSPEC-FILE CONTENTION — .spec/specs/coverage_capture.spec.md is edited by at least three tickets in this set: specled_-npo (rewrites the `specled.coverage_capture.envelope_meta` statement at :551-565), specled_-xkn.2 (coverage-provenance documentation, same subject), and this ticket (appends a new requirement near :419-427). All three edit the same `spec-requirements` YAML block. Land them in one pass or expect textual conflicts; ordering does not matter semantically since all three are additive/statement-local.\n\nTEST-FILE CONTENTION — test/specled_ex/coverage/store_test.exs: specled_-26g's mandate (unique-dir the read_status + load sidecar tests) already appears SATISFIED in this tree (setup blocks at store_test.exs:156-168 and :330-344 both build `Path.join(System.tmp_dir!(), \"store_v2_#{unique_integer}\")` / `\"store_status_#{unique_integer}\"` with `on_exit(\u0026File.rm_rf!/1)`), but if 26g is re-opened it edits the same describe-block setups my new test sits inside. test/specled_ex/compiler/tracer_test.exs is specled_-asb's file (merge_edges property at :241-293) — my tracer guard is a separate test, but same file. test/mix/tasks/spec_triangle_test.exs is specled_-b23's file if you choose to pin the spec.triangle reader too; prefer the Implementation seam to avoid that collision entirely.\n\nNO BLOCKING DEPENDENCY — this ticket does not need any other ticket to land first.\n\nGATE — the new tests must carry `@tag spec:`/`@describetag spec:` or the tagged_tests-consistency leg of `mix spec.check` will flag them; that leg is known-flaky under load (per project memory), so re-run before concluding failure. Adding a requirement without at least one `covers:`/tagged test will fire the coverage/verification legs, so land requirement + tests together in the same commit.\n\nCAUTION FOR THE IMPLEMENTER — do not build the never-interned atom with `String.to_atom/1` or `Module.concat/1`; both intern it and silently void the guard. Use the existing byte-patch helper `hostile_atom_binary/1` (test/specled_ex/coverage/store_test.exs:479-484) or copy its technique; if you promote it for reuse across store_test.exs and the tracer/implementation test, put it in test/support rather than duplicating.\n",
      "timestamp": "2026-07-27T08:37:04Z"
    },
    {
      "text": "SCOPE CONTRACT (orchestrator, specled_-k6s — 2026-07-27)\n\nAuthoritative for mechanical scope checks. Supplements, does not replace, the\nADMISSION VERIFICATION comment's mandate (items 1-3), which is what you implement.\n\n## STALE CONTENTION NOTES — the admission predates four merges\nSince that admission was written, specled_-26g, specled_-npo, specled_-asb and\nspecled_-chb have all LANDED on the epic branch. Consequences for you:\n- `test/specled_ex/coverage/store_test.exs`: 26g rewrote the `describe \"load/1\"` setup and\n  it now uses `SpecLedEx.TempName.cross_vm_suffix/0`, NOT `System.unique_integer`. The\n  admission describes the pre-26g shape. Re-read the file.\n- `.spec/specs/coverage_capture.spec.md`: npo appended a `read_v2/1` clause to\n  `specled.coverage_capture.envelope_meta` and extended its scenario. Line numbers moved.\n- Every line number the admission cites has shifted. Anchor on CONTENT, not line numbers.\n- If YOU allocate any new tmp root, use `SpecLedEx.TempName.cross_vm_suffix/0` — that is\n  now the ADR-stated rule for new allocations (specled.decision.cross_vm_temp_names_reach,\n  amended by specled_-ehv).\n\n## Allowed touches\n- `lib/specled_ex/coverage/store.ex`\n- `test/specled_ex/coverage/store_test.exs`\n- `.spec/specs/coverage_capture.spec.md` — item 3's ONE additive requirement, appended\n  after `specled.coverage_capture.store_v2_legacy_rejection`. Purely additive is\n  append-only safe.\n- `test/specled_ex/realization/implementation_test.exs` — item 2b's mirror guard.\n\n## Out of Scope (files)\n- `lib/specled_ex/realization/implementation.ex` — you consume `load_tracer_edges/2`'s\n  `opts[:tracer_manifest]` test hook; do not change the production reader.\n- `test/mix/tasks/spec_triangle_test.exs` — specled_-b23's file. The admission offers\n  pinning the spec.triangle and CoverageClosure readers too; DO NOT. Both are `defp` with\n  no injection seam, and the Implementation site is the one with a test hook. Use it and\n  avoid the collision entirely.\n- `test/specled_ex/compiler/tracer_test.exs` — specled_-asb and specled_-ehv both landed\n  there; your tracer guard belongs in implementation_test.exs per item 2b.\n- `CHANGELOG.md` (orchestrator consolidates once at the end), `VERSION`,\n  `.spec/realization_hashes.json`, `.spec/state.json`.\n- Every file not listed under Allowed touches.\n\n## Out of Scope (intent) — the api_boundary constraint is the one to respect\n- PREFER FLIPPING THE DEFAULT (`safe_decode(bin, opts \\\\ [:safe])`) over renaming the\n  pair. `.spec/realization_hashes.json` DOES carry `SpecLedEx.Coverage.Store.read_v2/1`,\n  `.read_status/1`, `.read/1` and `.load/1`, but api_boundary hashes the resolved function\n  HEAD only — stable under body edits, UNSTABLE under arity, arg-pattern or literal-default\n  changes to a PUBLIC function. `safe_decode/2` is private and unbound, so changing its\n  default is safe. Renaming or re-signaturing any public `Store` function WILL trip the\n  drift gate. `git diff --stat -- .spec/realization_hashes.json` must be EMPTY.\n- End state for item 1: NO call site gets atom-resurrecting behaviour implicitly. The\n  `read_v2/1` site must opt IN explicitly, with the existing justification comment kept\n  immediately above it.\n- Item 3 is not optional. Without an owning requirement the new tests repeat the\n  \"test ahead of the requirement\" class this epic already hit on specled_-npo.\n- No new dependencies.\n\n## FALSIFIABILITY BAR\nThe CF1 guards exist so that a future blanket `[:safe]` sweep FAILS LOUDLY. Prove they do:\n1. After adding each guard, temporarily add `[:safe]` to the carve-out call site it\n   protects (the `read_v2/1` decode, and the tracer-manifest raw read).\n2. Confirm the corresponding guard goes RED. Paste the failure.\n3. Revert. Confirm green.\nDo this for BOTH guards (item 2a and item 2b). A guard that stays green under a blanket\n`[:safe]` sweep is worthless — that sweep is the exact future event it is built to catch.\nRecord both mutations and reverts in the COMMIT MESSAGE.\n\nUse the existing `hostile_atom_binary/1` helper for the never-interned atom. Do NOT use\n`Module.concat` or `String.to_atom` to construct the test atom — both intern it, which\ndestroys the test.\n\n## Verification (change-scoped, run-until-green; host mix, no compose)\n1. `mix compile --warnings-as-errors`\n2. `mix test test/specled_ex/coverage/store_test.exs test/specled_ex/realization/implementation_test.exs`\n3. `mix format --check-formatted`\n4. `mix spec.check --no-run-commands`\n5. `git diff --stat -- .spec/realization_hashes.json` must be EMPTY.\n\nDo NOT run bare `mix spec.check` and do NOT run the full suite. The orchestrator owns\nwhole-system proof at the merge gate and PR CI.",
      "timestamp": "2026-07-27T12:29:22Z"
    },
    {
      "text": "IMPLEMENTED\nSession: k6s-xkn.1-impl-1\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-xkn.1/grok-k6s-xkn.1-impl-1/log\n\nCommit: 53e1c22a541194f09fc998eaeed3849af3162427\nFiles touched: lib/specled_ex/coverage/store.ex, test/specled_ex/coverage/store_test.exs, test/specled_ex/realization/implementation_test.exs, .spec/specs/coverage_capture.spec.md\nTests added: SpecLedEx.Coverage.StoreTest \"read_v2 succeeds on an envelope whose :files name a never-interned module atom\"; SpecLedEx.Realization.ImplementationTest \"hashes_for_seeding loads a tracer_manifest whose keys name a never-interned module\". Also retagged existing v1-read and read_status hostile tests under decode_atom_policy.\nVerification stubs flipped: none (existing tagged_tests block for store_v2_* extended to cover new requirement specled.coverage_capture.decode_atom_policy; execute already true)\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/coverage/store_test.exs test/specled_ex/realization/implementation_test.exs; mix format --check-formatted; mix spec.check --no-run-commands; git diff --stat -- .spec/realization_hashes.json EMPTY; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\nFalsifiability: (1) read_v2 temporarily safe_decode(bin, [:safe]) → store guard RED with {:error, :invalid_artifact}; reverted green. (2) read_tracer_etf temporarily binary_to_term(binary, [:safe]) → tracer guard RED with ArgumentError on String.to_existing_atom; reverted green. Both recorded in commit message.\nEpic lessons checked: [stale-spec] decode_atom_policy describes the shipped default+explicit-opt-in (not a rename); [stale-spec] rule is satisfiable by production paths; [untested-clause] CF1 inputs use never-interned atoms where safe vs unsafe DISAGREE; [other] mix format --check-formatted green; [other] completion posted to specled_-xkn.1.\n\nSummary: Flipped private safe_decode/2 default to [:safe] (fail-closed). read_v2/1 now opts in explicitly with []; read_status uses the default. Added additive requirement decode_atom_policy plus positive CF1 guards for the two documented unsafe carve-outs (v2 envelope foreign module; tracer-manifest foreign module via Implementation's tracer_manifest test hook) so a future blanket [:safe] sweep fails loudly.",
      "timestamp": "2026-07-27T12:37:38Z"
    },
    {
      "text": "TEST_CRITIC: PASS\n\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nFindings:\n\n- (a) Never-interned property HOLDS for both guards. Both use the byte-patch\n  helper: store_test.exs:520 (pre-existing) and implementation_test.exs:628 (new,\n  verbatim copy). The name is built as a STRING \"hostile_never_interned_\u003cN\u003e\" with\n  N = System.unique_integer([:positive, :monotonic]); only the same-length\n  placeholder \"xxx...\" is ever passed to String.to_atom/1, and the real name is\n  planted with :binary.replace/3 into the encoded ETF. No Module.concat, no\n  String.to_atom on the hostile name, no atom literal in the source. The name is\n  lowercase and un-prefixed, so it cannot collide with any Elixir module atom\n  (those are \"Elixir.\"-prefixed); grep over lib/ test/ .spec/ shows the string\n  exists nowhere except these two helpers. N is VM-globally unique, so the two\n  files cannot collide with each other even in the same VM.\n\n- (b) Both guards go RED under the sweep they exist to catch (verified, then\n  reverted; git status clean):\n  * store.ex:239 safe_decode(bin, []) -\u003e safe_decode(bin, [:safe]) =\u003e\n    exactly 1 failure, store_test.exs:355 \"assert {:ok, envelope} = Store.read_v2(path)\"\n    got {:error, :invalid_artifact}.\n  * implementation.ex:522 :erlang.binary_to_term(binary) -\u003e ..., [:safe]) =\u003e\n    exactly 1 failure, implementation_test.exs:670 ArgumentError \"not an already\n    existing atom\" from String.to_existing_atom/1 (the rescue-to-%{} fallback in\n    read_tracer_etf/1 is silent, so this line is the only detector).\n  Both files green as committed (seeds 355568, 347937, 670205, 1).\n\n- (c) Store guard assertion is STRONG: store_test.exs:356-358 asserts\n  {:ok, envelope}, matches out envelope.files' :module, and pins\n  String.to_existing_atom(hostile_name) == mod, i.e. \"decoded AND the atom now\n  exists AND it is the one in the envelope\".\n\n- (c) RECOMMENDATION (not blocking) — tracer guard is weaker than it reads.\n  implementation_test.exs:667 `assert Map.has_key?(hashes, \"A\")` is NOT\n  load-bearing: under the [:safe] mutation the test sailed past it with an empty\n  edge map and only died at line 670. The test never confirms the decoded edge\n  map is non-empty or keyed by the foreign atom — it only proves the decode\n  interned the atom, not that the manifest reached world.tracer_edges. I probed a\n  cheap strengthening and it works: with\n    clean = Implementation.hashes_for_seeding(subjects, nil, tracer_manifest: write_fixture_etf!(root))\n    empty = Implementation.hashes_for_seeding(subjects, nil, tracer_manifest: Path.join(root, \"nope.etf\"))\n  the observed result is {hashes == clean, hashes == empty} = {true, false}.\n  So `assert hashes == clean` + `refute hashes == empty` would prove the foreign\n  manifest was actually consumed, not merely decoded. write_fixture_etf!/1 already\n  exists at implementation_test.exs:606.\n\n- (c) NIT: the store v2 guard has no pre-condition proof that the atom is absent\n  before the read. implementation_test.exs:661 does exactly that\n  (assert_raise ArgumentError, fn -\u003e String.to_existing_atom(hostile_name) end).\n  Adding the same line before Store.read_v2/1 makes the store guard\n  self-evidencing rather than relying on the helper's docstring.\n\n- (d) No isolation hazard. The v2 guard is the only test that interns a hostile\n  atom; the two negative guards (store_test.exs:96, :429) assert absence of THEIR\n  OWN unique name, and monotonic unique_integer makes name reuse impossible. No\n  test anywhere asserts a fixed atom name is absent. Ordering-independent:\n  confirmed green across 4 seeds.\n\n- (e) Temp isolation OK. The tracer guard writes xref_hostile.etf into the\n  per-test `root` from the pre-existing setup at implementation_test.exs:73-80;\n  it allocates NO new root, so TempName.cross_vm_suffix/0 is not owed here. Note\n  (pre-existing, not this ticket): that root uses :erlang.unique_integer, which is\n  VM-local, not cross-VM. Same for the v2 describe's store_v2_ tmp dir\n  (store_test.exs:163). Neither was introduced or worsened here.\n\n- (f) Confirmed retag-only. The v1-read hostile test (store_test.exs:83) and the\n  read_status hostile test (store_test.exs:419) gained exactly one `+ @tag spec:`\n  line each in the diff; zero assertion lines changed, nothing weakened.\n\n- (g) No Process.sleep, no spawned/unmanaged processes, no shared-tmp writes in\n  either new test.\n\n- WARNING — over-tagging widens the requirement's attestation set beyond the\n  guard. store_test.exs:151-155 adds decode_atom_policy to the v2\n  @describetag, and TagScanner expands @describetag spec: lists to every test in\n  the block. That makes ~10 unrelated v2 tests (garbage bytes, non-map :meta,\n  legacy artifact, ...) attest specled.coverage_capture.decode_atom_policy. The\n  addition is also redundant: the new test carries its own @tag spec: at\n  store_test.exs:332, which overrides the describetag at runtime. Net effect —\n  delete the CF1 guard and the requirement still reads as attested by 10 tests\n  that never exercise atom decode policy. Recommend dropping decode_atom_policy\n  from the @describetag and leaving it on the three per-test @tag lines only\n  (:82, :332, :418), which is exactly the set `--only spec:...decode_atom_policy`\n  selects today.\n\n- mix spec.check --no-run-commands: pass (validate errors=0 warnings=0,\n  branch findings 13 info / 0 error / 0 warning). etf_read_direct exists at\n  compiler_tracer.spec.md:160 and is on an execute:true tagged_tests leg\n  (:264); the requirement's own wording (\"via :erlang.binary_to_term/1\") makes\n  the new guard a direct falsifier for the arity-1 clause.\n\n- Working tree left clean (only pre-existing untracked .grok/sandbox.toml).",
      "author": "critic-xkn1",
      "timestamp": "2026-07-27T12:46:42Z"
    },
    {
      "text": "VERIFY: PASS\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\nTicket: specled_-xkn.1\n\nBase used: 378de55a (git merge-base HEAD epic/specled_-k6s-0.11.0-fast-follow). Commit under review: 53e1c22a.\n\nMechanical checks:\n- Files touched: 4 (lib/specled_ex/coverage/store.ex, test/specled_ex/coverage/store_test.exs, test/specled_ex/realization/implementation_test.exs, .spec/specs/coverage_capture.spec.md)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0. lib/specled_ex/realization/implementation.ex, test/mix/tasks/spec_triangle_test.exs and test/specled_ex/compiler/tracer_test.exs are all absent from the diff; the tracer guard goes through hashes_for_seeding/3's opts[:tracer_manifest] hook, production reader untouched. CHANGELOG.md / VERSION / .spec/state.json untouched.\n- realization_hashes.json diff empty: y (`git diff --stat 378de55a..HEAD -- .spec/realization_hashes.json` produced no output)\n- public Store heads unchanged: y — read the heads, did not rely on the empty hash diff. `git diff 378de55a..HEAD -- lib/specled_ex/coverage/store.ex | grep -E \"^[+-]\\s*(def|defp) \"` yields exactly two lines, both the PRIVATE `safe_decode/2`:\n    -  defp safe_decode(bin, opts \\\\ []) do\n    +  defp safe_decode(bin, opts \\\\ [:safe]) do\n  The four bound public heads are byte-identical to base and still `def read(path) when is_binary(path)` (store.ex:63), `def read_v2(path) when is_binary(path)` (:229), `def load(path) when is_binary(path)` (:269), `def read_status(path) when is_binary(path)` (:290) — no arity, arg-pattern or literal-default change. `safe_decode/2` is `defp`, absent from .spec/realization_hashes.json (which carries only build_envelope/1, build_records/1, load/1, read/1, read_status/1, read_v2/1, write/2, write_v2/2 for this module) and absent from the spec's realized_by lists, so the changed default cannot move an api_boundary hash.\n\nSemantic checks:\n\n- Spec stubs run: 2 legs, passed: 2.\n  `mix test --only spec:specled.coverage_capture.store_v2_envelope --only ...store_v2_legacy_rejection --only ...decode_atom_policy` → 0 failures.\n  `mix test --only spec:specled.compiler_tracer.etf_read_direct` → 0 failures.\n  Note on the real gate shape: SpecLedEx.TaggedTests.build_command/2 (lib/specled_ex/tagged_tests.ex:83-107) emits `mix test --include integration --formatter ... \u003cfiles\u003e` — whole FILES, not tag filters. Both affected files were therefore run in full: store_test.exs (31 tests) and implementation_test.exs (24 tests), both green.\n\n- Decode-site inventory (step 6) — every safe_decode/binary_to_term call in lib/, read individually. No site inherits atom-resurrecting decode implicitly:\n  1. lib/specled_ex/coverage/store.ex:69 — v1 `read/1` — `:erlang.binary_to_term(bin, [:safe])` — SAFE, EXPLICIT literal at the site; justification comment retained at :66-68. Unchanged by this diff.\n  2. lib/specled_ex/coverage/store.ex:239 — `read_v2/1` — `safe_decode(bin, [])` — UNSAFE, EXPLICIT opt-in. The justification comment is retained immediately above (:231-238) and now opens \"Explicit unsafe opt-in (`[]`, not the default)\".\n  3. lib/specled_ex/coverage/store.ex:296 — `read_status/1` — `safe_decode(bin)` — SAFE, DEFAULTED, and the default is now the fail-closed `[:safe]`. Justification comment retained at :292-295, relabelled \"[:safe] (default)\". This is the option the SCOPE CONTRACT explicitly permitted.\n  4. lib/specled_ex/coverage/store.ex:362 — `defp safe_decode(bin, opts \\\\ [:safe])` — the definition. Fail-closed default, private, unbound.\n  5. lib/specled_ex/coverage/store.ex:269-279 — `load/1` — performs no decode of its own; it delegates to `read_v2/1` and so inherits that site's EXPLICIT `[]` opt-in. Not an implicit inheritance: the opt-in literal is at read_v2's own call.\n  6. lib/specled_ex/realization/implementation.ex:522 — `read_tracer_etf/1` — bare `:erlang.binary_to_term(binary)` — UNSAFE, explicit at the site (direct BIF call; it never routes through a helper with a default, so no default can silently change it). Out of scope for edits; this diff adds the positive guard for it.\n  7. lib/specled_ex/realization/use.ex:257 — `read_tracer_etf/1` — bare `:erlang.binary_to_term(binary)` — UNSAFE, explicit at the site. Out of scope, unguarded (see Notes).\n  8. lib/specled_ex/compiler/tracer.ex:247 — `read_manifest_file/1` — bare `:erlang.binary_to_term(binary)` — UNSAFE, explicit at the site. Out of scope, unguarded (see Notes).\n  9. lib/specled_ex/review/coverage_closure.ex:48 — bare `:erlang.binary_to_term(binary)` — UNSAFE, explicit at the site, justification comment at :43-47.\n  10. lib/mix/tasks/spec.triangle.ex:123 — bare `:erlang.binary_to_term(binary)` — UNSAFE, explicit at the site, justification comment at :117-122.\n  11. lib/specled_ex/coverage/aggregate.ex:97 — prose only, no call.\n  The ONLY defaulted decode in lib/ is site 3, and its default is `[:safe]`. Item 1's end state holds.\n\n- Falsifiability re-proof (step 7) — executed by me in the stage worktree, not taken from the implementer's paste.\n  (a) Mutated store.ex:239 to `safe_decode(bin, [:safe])`, ran `mix test test/specled_ex/coverage/store_test.exs` → RED:\n        1) test v2 envelope round trip read_v2 succeeds on an envelope whose :files name a never-interned module atom (SpecLedEx.Coverage.StoreTest)\n           test/specled_ex/coverage/store_test.exs:333\n           match (=) failed\n           code:  assert {:ok, envelope} = Store.read_v2(path)\n           left:  {:ok, envelope}\n           right: {:error, :invalid_artifact}\n           stacktrace:\n             test/specled_ex/coverage/store_test.exs:355: (test)\n        31 tests, 1 failure\n  (b) Mutated lib/specled_ex/realization/implementation.ex:522 to `:erlang.binary_to_term(binary, [:safe])`, ran `mix test test/specled_ex/realization/implementation_test.exs` → RED:\n        1) test tracer manifest unsafe decode (foreign module atom) hashes_for_seeding loads a tracer_manifest whose keys name a never-interned module (SpecLedEx.Realization.ImplementationTest)\n           test/specled_ex/realization/implementation_test.exs:643\n           ** (ArgumentError) errors were found at the given arguments:\n             * 1st argument: not an already existing atom\n           code: hostile_atom = String.to_existing_atom(hostile_name)\n           stacktrace:\n             :erlang.binary_to_existing_atom(\"hostile_never_interned_1\")\n             test/specled_ex/realization/implementation_test.exs:670: (test)\n        24 tests, 1 failure\n  (c) Both mutations reverted. `mix test test/specled_ex/coverage/store_test.exs test/specled_ex/realization/implementation_test.exs` → 55 tests, 0 failures. `git status --porcelain` shows only the pre-existing untracked `.grok/sandbox.toml`; `git diff --stat` against HEAD is empty. Tree clean: y.\n  Non-vacuity, checked in the other direction: both guards' atoms are NEVER interned. `hostile_atom_binary/1` (store_test.exs:520-525 and implementation_test.exs:628-633) builds the name as `\"hostile_never_interned_#{System.unique_integer([:positive, :monotonic])}\"`, interns only a same-length `\"xxx…\"` placeholder via `String.to_atom/1`, encodes with the placeholder, then byte-patches the real name into the ETF with `:binary.replace/3`. `String.to_atom/1`, `Module.concat/1` and a source literal are never applied to the hostile name in either file. `System.unique_integer([:positive, :monotonic])` is VM-global, so the two files cannot collide on a name inside one `mix test` VM. Independent mechanical proof: sweep (a) going RED with `{:error, :invalid_artifact}` is itself proof the atom was absent from the atom table at decode time, and implementation_test.exs:661 asserts `assert_raise ArgumentError, fn -\u003e String.to_existing_atom(hostile_name) end` BEFORE the production read. Neither guard is vacuous.\n\n- Owning requirement (step 8): present y; both halves stated y; tests tagged to it y.\n  `specled.coverage_capture.decode_atom_policy` (priority: must, stability: evolving) is appended immediately after `specled.coverage_capture.store_v2_legacy_rejection` in the `spec-requirements` block (.spec/specs/coverage_capture.spec.md:429-439). Half 1: \"v1 `Store.read/1` and the `last_run.status` sidecar (`Store.read_status/1`) shall decode with `[:safe]` and reject never-interned atoms.\" Half 2: \"`Store.read_v2/1` shall decode without `[:safe]` … and shall return `{:ok, envelope}` in that case\", plus the end-state clause \"No Store call site may obtain atom-resurrecting decode behaviour by default; the v2 path opts in explicitly.\" All three claims match the code read at sites 1-5 above. A matching scenario `specled.coverage_capture.scenario.decode_atom_policy` was added with `covers: [decode_atom_policy]`, and the existing `kind: tagged_tests, execute: true` block for store_v2_* gained `decode_atom_policy` in its `covers:` list. Three tests carry `@tag spec: \"specled.coverage_capture.decode_atom_policy\"`: store_test.exs:83 (v1 read/1 rejects), store_test.exs:333 (read_v2/1 accepts — the new CF1 guard), store_test.exs:419 (sidecar rejects); the describe at store_test.exs:151 also lists it. `mix test --only spec:specled.coverage_capture.decode_atom_policy` selects and passes 3 tests. Not a test-ahead-of-requirement repeat.\n\n- Requirements coverage — all 42 `must` requirements in specled.coverage_capture (41 pre-existing + the new one), individually:\n  - serialized_run: covered by test/mix/tasks/spec_cover_test_test.exs:177 (`@moduletag spec:` at :7). Would fail if the task stopped calling `ExUnit.configure(async: false)`/`Application.put_env(:ex_unit, :async, false)` or stopped arming via `:specled_ex, :spec_cover_run` — the child-BEAM fixture run would write no per_test.coverdata. Seams: child-BEAM `IntegrationCase.run_fixture_mix_test/2` (real `System.cmd`, low-seam).\n  - per_test_async_contamination: covered by spec_cover_test_test.exs:212. Would fail if the pre-run `async: true` scan were dropped — the task would exit 0 instead of non-zero naming the file. Seams: fixture source text only.\n  - per_test_allow_async_degrade: covered by spec_cover_test_test.exs:227. Would fail if `--allow-async` still exited non-zero, or emitted no stderr warning naming the contaminated file. Seams: same fixture.\n  - default_aggregate_run: covered by spec_cover_test_test.exs:110. Would fail if the default path registered a formatter, mutated async config, or skipped `Aggregate.ingest/2` → `Store.write_v2/2`; `read_status/1` would not report `{:ok, stats}`. Seams: child-BEAM fixture.\n  - default_aggregate_red_suite_passthrough: covered by spec_cover_test_test.exs:135. Would fail if a successful ingest reset the exit code to 0 after a red suite. Seams: child-BEAM fixture.\n  - default_aggregate_empty_refusal: covered by spec_cover_test_test.exs:156. Would fail if a zero-module export exited 0 or left no `{:refused, _}` sidecar. Seams: child-BEAM fixture.\n  - formatter_snapshot_fn_di: covered by test/specled_ex/coverage/formatter_test.exs:33 and :39 (`@moduletag` at :7). Would fail if `Coverage.init/2` defaulted `:snapshot_fn` instead of raising, or resolved it before arming. Seams: explicit opts map (unit-level; the production default is separately exercised at formatter_test.exs:118).\n  - formatter_arming_seam: covered by formatter_test.exs:86, :99, :118, :134, :149 and boundary_test.exs:50/:79. Would fail if `init/1` read config from its own argument (:149), or if a dead/invalid ETS boundary table still resolved as armed (boundary_test.exs:50, :64). Seams: `Application.put_env(:specled_ex, :spec_cover_run, …)` keyword arming.\n  - formatter_no_fabrication: covered by formatter_test.exs:667 (with snapshot_test.exs:135/:148 for the diff half). Would fail if an unchanged count became a placeholder hit, a decrease became a negative hit, or the run-total stopped marking `degraded: true` with a stderr notice. Seams: injected `snapshot_fn` stub.\n  - snapshot_runtime_mode: covered by snapshot_test.exs:23, :38, :62, :88, :104. Would fail if `runtime_mode/0` hard-gated on an OTP release, if a never-cover-compiled module aborted the snapshot instead of yielding `[]` (:62, :104), or if classic used one whole-table `:cover.analyse(:_, …)` call. Seams: real `:cover`/`:code` BIFs, no stub.\n  - snapshot_diff_strictly_increased: covered by snapshot_test.exs:110, :119, :126. Would fail if an equal count produced an entry, or an absent-in-prev module did not default its baseline to 0. Seams: none (pure function over literal maps).\n  - snapshot_negative_delta_diagnostic: covered by snapshot_test.exs:135 and :148 (plus formatter_test.exs:667). Would fail if a decrease produced a negative/garbage hit rather than a `%{reason: :counters_externally_harvested, …}` diagnostic. Seams: none (pure function).\n  - per_test_v2_envelope: covered by spec_cover_test_test.exs:177 (envelope shape) and formatter_test.exs:484/:650. Would fail if flush wrote a bare v1 list, if `:files` were not the sorted union of payload + remainder, if a zero-payload/non-empty-files run refused the write (:484), or if an empty-`:files` run wrote an artifact instead of one stderr notice (:650). Seams: injected snapshot/modules stubs at unit level; child BEAM end-to-end.\n  - per_test_artifact_freshness: covered by spec_cover_test_test.exs:41, :255, :285. Would fail if a stale (older `generated_at`) or refused artifact were accepted (:255), or if a red suite gained a spurious stale-artifact failure (:285). Seams: freshness predicate is called directly at :41 with synthesized statuses.\n  - cumulative_parity: covered by spec_cover_test_test.exs:414. Would fail if arming the per-test formatter perturbed the `.coverdata` totals `mix test --cover` exports — the two decoded per-module/per-line count maps would differ. Seams: two real child-BEAM runs, decoded via `:erlang.binary_to_term` at :508.\n  - keyed_by_test_pid: covered by formatter_test.exs:197 and :237 (`@moduletag` at :7). Would fail if the inventory key stopped defaulting to `{module, name}` when tags omit `:test_pid` (:237), or if boundary rows were keyed by anything other than `{module, name}`. Seams: hand-built ExUnit test structs.\n  - anonymous_ets: covered by formatter_test.exs:59 and :72. Would fail if `install` used a named table — :72 asserts two installs return distinct tables, which a named table cannot satisfy. Seams: none meaningful.\n  - artifact_path: covered by store_test.exs:12 (`default_path/0` matches `.spec/_coverage/per_test.coverdata`) plus :18/:58 for the v1 five-field schema and the v2 `:payload` shape at :172. Would fail if `default_path/0` moved, or if `build_records/1` stopped producing the five-field record. Seams: none (pure).\n  - store_split: covered by store_test.exs:18, :43, :51, :58 (`@moduletag` at :4). Would fail if `build_records/1` were folded back into the formatter or stopped constructing records from plain Elixir data without a formatter instance. Seams: none.\n  - integration_case: covered by spec_cover_test_test.exs:110 et al., all of which route through `SpecLedEx.IntegrationCase.run_fixture_mix_test/2`. Would fail if the helper ran in-process instead of `System.cmd/3` — the outer `:cover` state would be contaminated and the parity/exclusivity assertions would break. Seams: this requirement IS the anti-seam.\n  - store_v2_envelope: covered by store_test.exs:113, :172, :187, :381, :400. Would fail if `write_v2/2` accepted an empty `:files` (:187), skipped the `last_run.status` sidecar on either success (:381) or refusal (:400), or if v1 `write/2`/`read/1` changed. Seams: temp artifact path from the describe setup.\n  - store_v2_legacy_rejection: covered by store_test.exs:259, :268, :275, :284, :456. Would fail if a v1 bare list returned anything other than `{:error, :legacy_artifact, msg}` naming `mix spec.cover.test`, or if garbage/non-envelope terms did not return `{:error, :invalid_artifact}`. Seams: bytes written directly to disk (bypasses `write_v2/2`) — deliberate, that is the point.\n  - decode_atom_policy (NEW): covered by store_test.exs:83 (v1 `read/1` raises and does not intern), store_test.exs:333 (the new CF1 positive guard: `read_v2/1` returns `{:ok, envelope}` and the foreign atom becomes reachable), store_test.exs:419 (sidecar refuses without interning). Would fail if `read_v2/1`'s decode were swept to `[:safe]` (proved RED above, sweep (a)), if `safe_decode/2`'s default reverted to `[]` (store_test.exs:419 would find the atom interned and its `assert_raise` would fail), or if `read/1` dropped its explicit `[:safe]` (store_test.exs:83's `assert_raise ArgumentError, fn -\u003e Store.read(path) end` would fail). Seams: hostile ETF written directly to disk; the atom-interning seam is closed by the byte-patch helper (verified above).\n  - aggregate_ingest: covered by test/specled_ex/coverage/aggregate_test.exs:22. Would fail if `ingest/2` called `:cover.reset/0`, skipped either analyse pass, or returned a non-`:aggregate` envelope with empty `:files`/`:mfas`. Seams: real `.coverdata` produced by a child-BEAM fixture.\n  - aggregate_empty_coverage: covered by aggregate_test.exs:75. Would fail if an eventless `.coverdata` returned anything but `{:error, :empty_coverage}`, or if `ingest/2` wrote an envelope itself. Seams: same fixture harness.\n  - aggregate_unmapped_degraded: covered by aggregate_test.exs:104. Would fail if an unmappable module aborted the ingest, or leaked into `:files`/`:mfas` without setting `degraded`. Seams: fixture module compiled outside `:root`.\n  - mfa_key_round_trip: covered by test/specled_ex/coverage/mfa_key_test.exs:50 (property-style round trip) plus :9/:14/:21-:43 for the error arms (`@moduletag` at :4). Would fail if `format/1` and `parse/1` disagreed on any `{m,f,a}` triple. Seams: none (pure).\n  - mfa_lines_index: covered by test/specled_ex/coverage/mfa_lines_test.exs:49, :78, :88 (`@moduletag` at :6). Would fail if a module without abstract code silently yielded `%{}` instead of `:no_debug_info` (:78), or if one such sibling poisoned the others (:88). Seams: real `:beam_lib.chunks/2` over compiled fixtures.\n  - boundary_hook_sync: covered by test/specled_ex/coverage/boundary_test.exs:104, :137, :185 and spec_cover_test_test.exs:350. Would fail if `tail/2` were not retained as the next head (:185 asserts the chaining and the one-snapshot-per-test budget), or if the modules cache key were recomputed per test (:137). Seams: injected `snapshot_fn`/`modules_fn` at unit level; the child-BEAM seeded run at :350 closes it end-to-end.\n  - boundary_noop_unarmed: covered by test/specled_ex/case_test.exs:79 and boundary_test.exs:36, :40, :45, :64, :219, :223. Would fail if `head/1` wrote to ETS or called the snapshot when the seam is unset/`false`/`true`/a keyword lacking a live `:boundary_table`, or if `per_test_boundary/1` raised on a `setup_all` context without `:test` (boundary_test.exs:92). Seams: `Application.put_env` arming — exercised across all four disarmed shapes, so the seam is the subject rather than an escape.\n  - case_template: covered by case_test.exs:17 (forwards opts and runs the injected boundary setup), :79, :91, and spec_cover_test_test.exs:350. Would fail if `use SpecLedEx.Case` stopped forwarding `ExUnit.Case` opts or stopped injecting `setup {SpecLedEx.Coverage, :per_test_boundary}`. Seams: the adopter module is defined inline in the test.\n  - boundary_row_exclusive: covered by formatter_test.exs:347 and spec_cover_test_test.exs:350. Would fail if a hooked test's record were derived from anything but its boundary window, or if a lazy-capture fallback produced payload records for unhooked tests. Seams: injected boundary rows at unit level; three explicit seeds in the child BEAM.\n  - per_test_exclusive_attribution: covered by spec_cover_test_test.exs:350 (three explicit seeds, disjointness asserted, not a statistical claim). Would fail if two hooked tests exercising disjoint fixture functions produced overlapping `lines_hit`, or if `tail(N) != head(N+1)`. Seams: child-BEAM fixture; seeds are explicit constants.\n  - envelope_meta: covered by store_test.exs:113, :130, :219, :289, :310 and formatter_test.exs:347, :401. Would fail if a present-but-non-map `:meta` were defaulted to `%{}` rather than classified `{:error, :invalid_artifact}` (store_test.exs:310), or if `meta.boundary`/`unhooked_modules`/`unattributed`/`unmapped_modules` were omitted when their conditions held. Seams: envelopes hand-built in the test.\n  - write_v2_argument_error_contract: covered by store_test.exs:205 (asserts EVERY malformed field raises `ArgumentError`, never a substitute type). Would fail if any optional field were read with dot access, raising `KeyError`. Seams: none (direct calls over malformed maps).\n  - degraded_reasons: covered by formatter_test.exs:517 (async + unhooked records BOTH causes) and :545 (harvest-only records `[:counters_harvested]`). Would fail if `meta.degraded_reasons` were absent on a degraded envelope, dropped a cause that fired, or if `Store.degraded_reasons/1` stopped returning `[]` for a non-degraded envelope / stopped inferring the legacy value. Seams: injected snapshot stub drives the harvest case.\n  - formatter_auditor: covered by formatter_test.exs:197, :347, :401, :691, :748. Would fail if `test_finished` took a per-test snapshot — :748 asserts `snapshot_fn` is called only at `suite_started` and `suite_finished` — or if the remainder were not a line-level set subtraction of attributed from run-total. Seams: counting stub for `snapshot_fn`.\n  - unhooked_degrade: covered by formatter_test.exs:452, :484, :517, :626, :691. Would fail if an unhooked test failed the run, produced a payload record, was omitted from `meta.unhooked_modules`, or if a zero-hooked run with a non-empty remainder refused to write. Seams: inventory rows injected without matching boundary rows.\n  - never_ran_not_inventoried: covered by formatter_test.exs:575 (excluded/skipped/invalid are never inventoried and cannot degrade a fully hooked run) and :626 (`{:failed, _}` stays inventoried). Would fail if a `@tag :skip` test marked its module unhooked or fed the `degraded_async?` fold. Seams: `test.state` values set directly on the event struct — appropriate, ExUnit is the producer under test.\n  - unhooked_remediation_notice: covered by formatter_test.exs:452, :691 and spec_cover_test_test.exs:307. Would fail if the notice were printed per test rather than once per module at `suite_finished`, or if it stopped naming the literal `setup {SpecLedEx.Coverage, :per_test_boundary}` line and the `use SpecLedEx.Case` alternative. Seams: stderr captured; the child-BEAM run at :307 confirms the real notice.\n  - path_identity: covered by formatter_test.exs:401. Would fail if an absolute compile-time source path reached payload records, `meta.unattributed` or `envelope.files`, or if the module-to-source map were rebuilt rather than derived once at suite start and reused for both payload and remainder. Seams: `modules_fn` stub supplies the scope.\n  - unmapped_modules_meta: covered by formatter_test.exs:401 (same test asserts hit modules absent from the single file map surface in `meta.unmapped_modules`). Would fail if such a module's lines were folded into another file's percentage or silently dropped instead of retained as a sorted unique list. Seams: same stub file map, deliberately made incomplete.\n  None of the 42 was dismissed as unreachable; every one has at least one tagged test in-tree.\n\n- Intent-level scope concerns: none. No dependency change (mix.exs / mix.lock absent from the diff). The spec edit is purely additive — `git diff 378de55a..HEAD -- .spec/specs/coverage_capture.spec.md` is 27 insertions, 0 deletions (one requirement appended after store_v2_legacy_rejection, one scenario, one `covers:` line added to an existing tagged_tests block); no existing statement was rewritten, so it is append-only safe against the sibling spec-file tickets. No new tmp root was allocated: the store guard reuses the describe setup's `%{path: path}` and the tracer guard reuses implementation_test's existing `%{root: root}` fixture, so `SpecLedEx.TempName.cross_vm_suffix/0` was not required.\n\n- Targeted verification: pass. Commands run in /Users/mz/src/specled_ex-safe-decode-default (host mix, no compose):\n  - `mix compile --warnings-as-errors` → clean\n  - `mix test test/specled_ex/coverage/store_test.exs test/specled_ex/realization/implementation_test.exs` → 55 tests, 0 failures\n  - `mix format --check-formatted` → clean\n  - `mix spec.check --no-run-commands` → `validate status=pass errors=0 warnings=0`; `branch findings=13 (error=0 warning=0 info=13)`; `spec.check result=pass`\n  - `git diff --stat 378de55a..HEAD -- .spec/realization_hashes.json` → empty\n  - tagged legs: `mix test --only spec:specled.coverage_capture.decode_atom_policy` (3 tests, 0 failures); the store_v2_*+decode_atom_policy leg (0 failures); `mix test --only spec:specled.compiler_tracer.etf_read_direct` (0 failures)\n\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope, if any: none\n\nVerdict: PASS\n\nNotes (advisory only, no action required for this ticket):\n1. `hostile_atom_binary/1` is now duplicated verbatim in test/specled_ex/coverage/store_test.exs:520-525 and test/specled_ex/realization/implementation_test.exs:628-633. The ADMISSION suggested promoting it to test/support if reused; the SCOPE CONTRACT's Allowed-touches list did not include test/support, so duplicating was the in-scope choice. Worth a follow-up if a third copy appears — the helper is the thing that keeps both guards non-vacuous, and drift between copies would silently void one.\n2. In the new tracer guard, `assert Map.has_key?(hashes, \"A\")` is not load-bearing: under sweep (b) it still passed (the reader falls back to `%{}` and seeding still emits \"A\"). The load-bearing assertion is the trailing `String.to_existing_atom(hostile_name)`. The guard fires correctly, but the earlier assertion should not be read as protecting anything.\n3. Three raw tracer-manifest readers in lib/ carry no justification comment for their unsafe decode: implementation.ex:522, use.ex:257, tracer.ex:247 — unlike coverage_closure.ex:43-47 and spec.triangle.ex:117-122, which do. Only implementation.ex:522 is now positively guarded. All three are outside this ticket's Allowed touches. If the epic wants the \"blanket [:safe] sweep fails loudly\" property to hold for the whole tracer-manifest carve-out, use.ex and tracer.ex are the remaining unguarded sites.\n4. The tracer half of the decode policy has no requirement stating an atom policy; the guard is anchored to `specled.compiler_tracer.etf_read_direct`, whose statement says the manifest \"shall be read directly from disk via `:erlang.binary_to_term/1`\" — the bare /1 form does imply the unsafe mode, so the anchor is defensible, but it is weaker ownership than the new Store requirement. Item 3 only mandated the Store halves, so this is not a gap against the contract.\n5. Mechanical note for future verifiers: ExUnit's `--only spec:\u003cid\u003e` does NOT match list-valued tags, so `mix test --only spec:specled.compiler_tracer.etf_read_direct` selects only the string-tagged test in implementation_test.exs and excludes all of tracer_test.exs (whose `@moduletag spec:` is a list). This does not affect the real gate — SpecLedEx.TaggedTests.build_command/2 runs whole files, not tag filters — but ad-hoc tag filtering under-selects and should not be used alone to conclude a leg is covered.",
      "timestamp": "2026-07-27T12:53:00Z"
    },
    {
      "text": "AUDIT: APPROVE (auto — selective audit)\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nReasoning: VERIFY reported PASS with full stub and requirement coverage, zero\nscope violations, passing targeted verification, complete seam disclosure, and\nno seam substituting production wiring. TEST_CRITIC: PASS. No auditor was\nspawned per policy.\n\nGate detail: all 42 `must` requirements on specled.coverage_capture (41 pre-existing plus\nthe new `decode_atom_policy`) accounted for individually. `realization_hashes.json` diff\nempty and no public `Store` head re-signatured — the change is confined to the private\n`safe_decode/2` default, which is what kept the api_boundary bindings stable.\n\nTwo things carried the weight here and both were done properly:\n\n1. The decode-site inventory. Every `safe_decode`/`binary_to_term` call in `lib/` was read\n   individually and no site inherits atom-resurrecting decode implicitly — `read_v2/1`\n   opts in explicitly with its justification comment retained, and the v1 and sidecar paths\n   are fail-closed. That is the actual security property the ticket exists to establish,\n   and it was checked per-site rather than asserted.\n\n2. The never-interned property, which decides whether the guards prove anything. The critic\n   confirmed both guards build the hostile name as a STRING and plant it via\n   `:binary.replace/3` into the encoded ETF, passing only a same-length placeholder to\n   `String.to_atom/1` — no `Module.concat`, no atom literal in source, lowercase and\n   un-prefixed so it cannot collide with any Elixir module atom, and VM-globally unique\n   between the two files. Had the atom been interned, both guards would have passed\n   vacuously while looking green.\n\nThe verifier re-ran both falsifiability sweeps itself rather than accepting the record:\napplying a blanket `[:safe]` to each carve-out turns its guard RED, which is precisely the\nfuture event these guards exist to catch. The new requirement states both halves and the\ntests are tagged to it, so this does not repeat the \"test ahead of the requirement\" class.",
      "timestamp": "2026-07-27T12:54:13Z"
    },
    {
      "text": "APPROVED. Merged into epic/specled_-k6s-0.11.0-fast-follow at 423ab1a1 (confirmed present on the epic branch). Realization-hash baseline diff EMPTY, re-checked at the epic level. Epic smoke green: 1281 tests, 0 failures. Closing per orchestrator sign-off.",
      "timestamp": "2026-07-27T13:01:00Z"
    }
  ],
  "priority": 3,
  "status": "closed",
  "title": "safe_decode/2 defaults to unsafe mode; add regression guards for non-safe carve-outs",
  "type": "task",
  "updated_at": "2026-07-27T13:01:01Z"
}
