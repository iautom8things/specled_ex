{
  "assignee": "Manuel Zubieta",
  "blocked_by": [
    "specled_-ehv"
  ],
  "blocks": [],
  "closed_at": "2026-07-27T12:12:38Z",
  "created": "2026-07-26T00:48:03Z",
  "description": "Advances: specled.compiler_tracer.merge_on_flush\n\nFound by the elixir-test-critic during specled_-zr6 verification (2026-07-25). The merge_edges property (test/specled_ex/compiler/tracer_test.exs:241-293) asserts value provenance only for session-module callers. For a caller present in BOTH previous and session_edges whose module is NOT in session_set (~21% of generated draws), no assertion pins which value wins — mutants reversing Map.merge precedence or concatenating callee lists instead of replacing survived 2000 property cases. The state is production-reachable: during a parallel compile, module B's flush sees module A's fresh edges via session_edges() before A's :on_module fires and marks it a session module (lib/specled_ex/compiler/tracer.ex:183-209). Impact bounded — A's own later flush corrects the manifest — so exposure is transient intermediate manifests. Cheapest fix: one example test asserting the session_edges entry wins over a same-key previous entry when the caller's module is not in session_set. Pre-existing; NOT introduced or worsened by specled_-zr6.",
  "id": "specled_-asb",
  "labels": [],
  "parent": "specled_-k6s",
  "comments": [
    {
      "text": "ADMISSION VERIFICATION (specled_-k6s, 2026-07-27)\nCold fresh-context verifier run against the epic base tree (feat/specled_-td7-self-identifying-descriptors @ e81f9f98).\n\nVERDICT: still_applicable   EFFORT: trivial\nMandate confirmed still applicable as written; no narrowing needed.\n\n== SCOPED REMAINING WORK ==\nAll of it. Add one example test to /Users/mz/src/specled_ex-self-identifying-descriptors/test/specled_ex/compiler/tracer_test.exs inside the existing `describe \"Tracer.merge_edges/3 (specled.compiler_tracer.merge_on_flush)\"` block (starts line 198, ends line 295), tagged `@tag spec: \"specled.compiler_tracer.merge_on_flush\"`.\n\nEnd state — the test must construct a caller whose module is NOT in `session_set` and that is present in BOTH maps with different callee lists, e.g.:\n\n  previous = %{{ModOverlap, :f, 1} =\u003e [{String, :old_callee, 1}]}\n  session_edges = %{{ModOverlap, :f, 1} =\u003e [{String, :upcase, 1}]}\n  merged = Tracer.merge_edges(previous, MapSet.new([]), session_edges)\n  assert merged[{ModOverlap, :f, 1}] == [{String, :upcase, 1}]\n\nThe assertion must be exact list equality (not `Enum.member?`/subset), so it kills BOTH mutants named in the ticket: reversing the `Map.merge(previous_kept, session_edges)` argument order at tracer.ex:299 (would yield `[{String, :old_callee, 1}]`) and concatenating instead of replacing (would yield `[{String, :old_callee, 1}, {String, :upcase, 1}]` after canonicalization). Use module atoms not already bound to other meanings in the file (`ModKeep`, `ModRecompiled`, `ModNew`, `ModA..ModD` are taken).\n\nOptional strengthening (equivalent mandate, higher cost): add a fourth assertion family to the property at tracer_test.exs:241 — for `{caller, callees} \u003c- merged` where the caller's module is NOT in `session_set` and `Map.has_key?(session_edges, caller)`, assert `callees == session_edges |\u003e Map.fetch!(caller) |\u003e Enum.sort() |\u003e Enum.dedup()`. The example test alone satisfies the ticket; do not do both.\n\nNo production-code change is required — the implementation is already correct.\n\n== EXPECTED TOUCHES ==\ntest/specled_ex/compiler/tracer_test.exs\n\n== RISK NOTES ==\n- No realization-hash impact. .spec/realization_hashes.json contains exactly one top-level key, \"api_boundary\", and no entry names SpecLedEx.Compiler.Tracer or any tracer_test binding; the change is test-only and touches no lib/ file.\n- No spec text change required, so no requirement-id collision with other tickets in the set. `specled.compiler_tracer.merge_on_flush` (.spec/specs/compiler_tracer.spec.md:63-73) is not touched by any other ticket in the set (checked all 21 sibling descriptions; none names compiler_tracer, tracer.ex, or tracer_test.exs). Zero file-region conflicts.\n- The new tag `@tag spec: \"specled.compiler_tracer.merge_on_flush\"` is already an existing requirement id and is already covered by an `execute: true` tagged_tests verification entry (.spec/specs/compiler_tracer.spec.md:262-267), so the test-tag-consistency gate in `mix spec.check --no-run-commands` stays green with no spec edit. Adding a NEW tag id would break it; do not invent one.\n- No ordering dependency on any other ticket in the set; can land first or last.\n- Latent trap for the implementer: `Map.merge/2` argument-order mutants are invisible to every existing assertion because `Enum.reject` at tracer.ex:298 pre-removes session-module collisions. If the implementer \"verifies\" the new test by mutating the code, the mutation must be at tracer.ex:299 with a non-session collision fixture, or it will appear to pass.\n- Spec-prose adjacency (do NOT fold in without a separate ticket): the requirement statement at .spec/specs/compiler_tracer.spec.md:65-71 says \"unioned with the session's accumulated edges\" and never states which side wins on key collision. The ticket's mandate is a test, not a spec edit; changing the requirement text would touch a `must`/`evolving` requirement and pull this out of trivial effort.\n",
      "timestamp": "2026-07-27T08:37:06Z"
    },
    {
      "text": "SCOPE CONTRACT (orchestrator, specled_-k6s — 2026-07-27)\n\nAuthoritative for mechanical scope checks. Supplements, does not replace, the\nADMISSION VERIFICATION comment's mandate, which is what you implement.\n\n## PATH CORRECTION AND MOVED LINES\nThe admission comment cites an absolute path under\n`/Users/mz/src/specled_ex-self-identifying-descriptors/`. That worktree is gone. The\nfile is `test/specled_ex/compiler/tracer_test.exs` relative to YOUR stage worktree.\n\nspecled_-ehv has ALREADY LANDED edits to this same file on the epic branch — it\npid-scoped the `litter` filter and the `after` sweep, and collapsed two `Enum.filter`\ncalls onto one line. Your stage is cut from the current epic head, so you have ehv's\nversion, but the admission's cited line numbers (describe block at 198-295, property at\n241) have shifted. Anchor on CONTENT, not line numbers.\n\n## Allowed touches\n- `test/specled_ex/compiler/tracer_test.exs` — one new example test inside the existing\n  `describe \"Tracer.merge_edges/3 (specled.compiler_tracer.merge_on_flush)\"` block,\n  tagged `@tag spec: \"specled.compiler_tracer.merge_on_flush\"`.\n\n## Out of Scope (files)\n- `lib/specled_ex/compiler/tracer.ex` — no production change is required; the\n  implementation is already correct. You will mutate it TEMPORARILY to prove the new\n  assertion red, then revert. The committed diff must not contain it.\n- The regions ehv touched (the litter filter, the `after` sweep) — leave them alone.\n- `CHANGELOG.md` (orchestrator consolidates once at the end), `VERSION`,\n  `.spec/realization_hashes.json`, `.spec/state.json`.\n- Every file not listed under Allowed touches. This should be a ONE-FILE diff.\n\n## Out of Scope (intent)\n- Do the EXAMPLE TEST, not the optional property strengthening. The admission is\n  explicit: \"The example test alone satisfies the ticket; do not do both.\"\n- The assertion must be EXACT LIST EQUALITY — not `Enum.member?`, not a subset check.\n  That is what kills both mutants; a membership check kills neither cleanly.\n- Use module atoms not already bound in the file. `ModKeep`, `ModRecompiled`, `ModNew`,\n  and `ModA`..`ModD` are taken.\n- No new dependencies.\n\n## FALSIFIABILITY BAR — this ticket is one of the five the epic gates on\nThe whole point is that the existing property has no value-provenance assertion. Prove\nyour new test kills BOTH named mutants:\n1. Reverse the `Map.merge(previous_kept, session_edges)` argument order in\n   `lib/specled_ex/compiler/tracer.ex` → the test must go RED (it would yield the\n   previous callee list).\n2. Concatenate instead of replacing → the test must go RED (it would yield both callees\n   after canonicalization).\n3. Revert both, confirm green.\nPaste both red outputs into your completion record and record the mutations and reverts\nin the COMMIT MESSAGE.\n\n## Verification (change-scoped, run-until-green; host mix, no compose)\n1. `mix compile --warnings-as-errors`\n2. `mix test test/specled_ex/compiler/tracer_test.exs`\n3. `mix format --check-formatted`\n4. `mix spec.check --no-run-commands`\n5. `git diff --name-only` must list exactly one file, and must NOT list\n   `lib/specled_ex/compiler/tracer.ex`.\n\nDo NOT run bare `mix spec.check` and do NOT run the full suite. Note this file is\n`async: false` and mutates the shared `_build/\u003cenv\u003e/.spec` manifest — do not run its\ntests concurrently with a compile of the same `_build`.",
      "timestamp": "2026-07-27T11:33:34Z"
    },
    {
      "text": "IMPLEMENTED\nSession: k6s-asb-impl-1\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-asb/grok-k6s-asb-impl-1/log\n\nCommit: 375fde89c9f16b9478dfb175b39c1511f6448112\nFiles touched: test/specled_ex/compiler/tracer_test.exs\nTests added: SpecLedEx.Compiler.TracerTest / \"non-session caller collision prefers session_edges value over previous\"\nVerification stubs flipped: none (existing execute:true tagged_tests entry for specled.compiler_tracer.merge_on_flush already covers the new @tag)\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/compiler/tracer_test.exs; mix format --check-formatted; mix spec.check --no-run-commands; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\n\nFalsifiability (both mutants temporary on tracer.ex:299, then reverted; committed diff is test-only):\n1. Reverse Map.merge args → RED:\n   Assertion with == failed\n   left:  [{String, :old_callee, 1}]\n   right: [{String, :upcase, 1}]\n2. Concatenate conflict resolver → RED:\n   Assertion with == failed\n   left:  [{String, :old_callee, 1}, {String, :upcase, 1}]\n   right: [{String, :upcase, 1}]\n3. Revert confirmed green; git diff --name-only lists only tracer_test.exs.\n\nEpic lessons self-check: [stale-spec] N/A no ADR; [untested-clause] fixture is non-session collision where previous and session disagree; [format] green; [completion on correct ticket] this comment is on specled_-asb.\n\nSummary: Added one example test inside the merge_edges describe block that pins value provenance when a non-session caller is present in both previous and session_edges with different callee lists. Exact list equality asserts session_edges wins, killing reverse-Map.merge and concatenate-instead-of-replace mutants that the existing property could not detect.",
      "timestamp": "2026-07-27T11:42:38Z"
    },
    {
      "text": "VERIFY: PASS\nTicket: specled_-asb\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nCold verifier, stage worktree /Users/mz/src/specled_ex-tracer-merge-provenance,\nbase = git merge-base HEAD epic/specled_-k6s-0.11.0-fast-follow = 1ef97ad1.\nBranch head 375fde89 (single commit).\n\nMechanical checks:\n- Files touched: 1 (test/specled_ex/compiler/tracer_test.exs, +14/-0)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0\n  - `lib/specled_ex/compiler/tracer.ex` is ABSENT from `git diff 1ef97ad1..HEAD --name-only`\n    and from the single commit's file list — the temporary mutations were reverted.\n  - `git diff --stat 1ef97ad1..HEAD -- .spec/realization_hashes.json` is EMPTY.\n  - CHANGELOG.md, VERSION, .spec/state.json, mix.exs, mix.lock: not in the diff (no new deps).\n\nSemantic checks:\n- Spec stubs run: 1, passed: 1\n  The `tagged_tests` verification entry covering `specled.compiler_tracer.merge_on_flush`\n  (.spec/specs/compiler_tracer.spec.md:265-271; covers merge_on_flush,\n  session_module_replacement, seed_time_ghost_prune, atomic_tmp_name_includes_pid), run\n  directly as the four `--only spec:\u003cid\u003e` filters:\n    3 properties, 1276 tests, 0 failures, 1266 excluded  (rc=0)\n\n- Requirements coverage (all 10 `must` requirements of subject specled.compiler_tracer):\n  - specled.compiler_tracer.merge_on_flush: covered by\n    test/specled_ex/compiler/tracer_test.exs:245-252 (the new test; exact list equality at\n    :251). Would fail if the flush merge favored the pre-session manifest on a\n    non-session-caller key collision (reverse `Map.merge` args at\n    lib/specled_ex/compiler/tracer.ex:299 -\u003e left `[{String, :old_callee, 1}]`), or if it\n    unioned/concatenated the two callee lists instead of replacing (-\u003e left\n    `[{String, :old_callee, 1}, {String, :upcase, 1}]`). Both re-proved RED below.\n    Seams: exercises the pure `merge_edges/3` helper only. It does NOT reach the\n    `:on_module` flush call site at lib/specled_ex/compiler/tracer.ex:191, so swapping the\n    `previous`/`session_edges` arguments AT THAT CALL SITE would still pass this test; it\n    does not cover the atomic temp+rename write, nor the requirement's\n    \"incremental == forced full compile\" clause (that clause's only coverage is\n    test/integration/tracer_incremental_merge_test.exs, which has no non-session-collision\n    fixture). Also seam-limited to a single-key map with `MapSet.new([])`.\n  - specled.compiler_tracer.captures_remote_calls: not reachable by this diff because the\n    added test calls `Tracer.merge_edges/3` with two literal maps and never invokes\n    `trace/2` or an `:on_module` event; no `remote_function`/`imported_function` code path\n    is entered. Pre-existing coverage (fixture subprocess compile) is byte-unchanged.\n  - specled.compiler_tracer.session_module_replacement: not reachable by this diff because\n    the added test passes an EMPTY session set (`MapSet.new([])`, tracer_test.exs:249), so\n    the `Enum.reject`/`session_caller?` drop path at tracer.ex:297,303 is never taken with\n    a member. Its own coverage at tracer_test.exs:214-227 is unchanged.\n  - specled.compiler_tracer.seed_time_ghost_prune: not reachable by this diff because\n    `prune_edges/2` (tracer.ex:317+) is never called from the added test; its tests live in\n    a different describe block (tracer_test.exs:313+) that the diff does not touch.\n  - specled.compiler_tracer.atomic_tmp_name_includes_pid: not reachable by this diff because\n    `merge_edges/3` returns a map and performs no filesystem work; the added test writes no\n    file and never names a temp sibling.\n  - specled.compiler_tracer.registered_in_mix_exs: not reachable by this diff because\n    neither `mix.exs` nor `test/fixtures/sample_project/mix.exs` appears in\n    `git diff --name-only` (1-file diff).\n  - specled.compiler_tracer.bootstrap_rebuilds_on_content_change: not reachable by this diff\n    because the `mix.exs` bootstrap block is not in the diff and the added test does not\n    compile the tracer source or read/write the digest sidecar.\n  - specled.compiler_tracer.bootstrap_digest_recorded_after_success: not reachable by this\n    diff, same justification — no `mix.exs` change, no bootstrap invocation in the added\n    test.\n  - specled.compiler_tracer.xref_in_process: not reachable by this diff because\n    `SpecLedEx.Compiler.Xref` is not referenced anywhere in the diff; its tests are in\n    test/specled_ex/compiler/xref_test.exs, untouched.\n  - specled.compiler_tracer.etf_read_direct: not reachable by this diff because the added\n    test operates on in-memory maps and performs no `:erlang.binary_to_term/1` read of\n    `_build/\u003cenv\u003e/.spec/xref_mfa.etf`.\n  (The subject's 11th requirement, specled.compiler_tracer.single_file_cap, is `should`,\n  not `must`; it is a `source_file` verification on lib/specled_ex/compiler/tracer.ex,\n  which this diff does not touch.)\n\n- Intent-level scope concerns: none.\n  - Example test ONLY; the optional property strengthening was NOT done. The property body\n    (tracer_test.exs:255-310) is byte-identical to base — the diff is a pure 14-line\n    insertion in one hunk (@@ -237,6 +237,20 @@) before the property.\n  - Module atom `ModOverlap` is unique in the repo (`grep -rn ModOverlap lib/ test/` returns\n    only the 3 new lines); no collision with ModKeep, ModRecompiled, ModNew, ModA..ModD.\n  - The regions specled_-ehv touched are untouched: the litter filter / `after` sweep test\n    is at tracer_test.exs:388-400, far outside the single hunk.\n  - No new dependencies (mix.exs / mix.lock not in the diff).\n\n- Targeted verification: PASS. Commands run (host mix, sequential, no full suite):\n  - `mix compile --warnings-as-errors` -\u003e exit 0, no output\n  - `mix test test/specled_ex/compiler/tracer_test.exs` -\u003e 1 property, 23 tests, 0 failures\n  - `mix format --check-formatted` -\u003e exit 0\n  - `mix spec.check --no-run-commands` -\u003e `validate status=pass errors=0 warnings=0`;\n    `spec.check result=pass`; rc=0\n  - `mix test --only spec:specled.compiler_tracer.merge_on_flush --only ...session_module_replacement --only ...seed_time_ghost_prune --only ...atomic_tmp_name_includes_pid`\n    -\u003e 3 properties, 1276 tests, 0 failures, 1266 excluded; rc=0\n\n- Falsifiability re-proof (step 7.5), performed by the verifier, not accepted from the\n  implementer:\n  MUTANT A — reverse the merge argument order at lib/specled_ex/compiler/tracer.ex:299\n  (`|\u003e then(fn previous_kept -\u003e Map.merge(session_edges, previous_kept) end)`):\n    1) test Tracer.merge_edges/3 (specled.compiler_tracer.merge_on_flush) non-session\n       caller collision prefers session_edges value over previous\n       test/specled_ex/compiler/tracer_test.exs:245\n       Assertion with == failed\n       code:  assert merged[{ModOverlap, :f, 1}] == [{String, :upcase, 1}]\n       left:  [{String, :old_callee, 1}]\n       right: [{String, :upcase, 1}]\n    Result: 1 property, 23 tests, 1 failure (rc=2) — RED.\n  MUTANT B — concatenate instead of replace\n  (`|\u003e Map.merge(session_edges, fn _caller, prev, sess -\u003e prev ++ sess end)`):\n    1) ... same test, tracer_test.exs:245\n       left:  [{String, :old_callee, 1}, {String, :upcase, 1}]\n       right: [{String, :upcase, 1}]\n    Result: 1 property, 23 tests, 1 failure (rc=2) — RED.\n  Exact equality confirmed: YES — the assertion is `assert merged[{ModOverlap, :f, 1}] ==\n  [{String, :upcase, 1}]` (tracer_test.exs:251), full-list `==`, not `Enum.member?` and not\n  a subset check. Mutant B's `left` proves it: a membership/subset assertion would have\n  passed on `[{String, :old_callee, 1}, {String, :upcase, 1}]`.\n  Does the pre-existing property already kill them: NO — proved by execution, not by\n  reading. Under EACH mutant the run reported exactly ONE failure, and it was the new\n  example test; the property\n  \"merge keeps exactly non-session previous entries plus session edges, and is idempotent\"\n  (tracer_test.exs:255-310) PASSED under both. That matches its text: its key-set assertion\n  (:289) is a set union and is invariant under both mutants; its value-provenance assertion\n  (:294-300) is guarded by `MapSet.member?(session_set, mod)`, so for a NON-session caller\n  present in both maps it asserts nothing about which value wins — and session callers are\n  already removed from `previous_kept` by `Enum.reject` (tracer.ex:297), so no collision\n  can reach that branch; the canonical-list assertion (:303-305) holds because both mutant\n  outputs are re-canonicalized at tracer.ex:300; and idempotence (:308) holds for both\n  mutants (reverse-merge is stable, and concat re-merges a superset that dedupes to itself).\n  This ticket therefore adds coverage the property did not have. Not a duplicate.\n  Tree clean: YES — after reverting both mutants, `git status --porcelain` reports only\n  `?? .grok/sandbox.toml` (pre-existing untracked, ignored per instructions), and\n  `git diff 1ef97ad1..HEAD --name-only` lists only test/specled_ex/compiler/tracer_test.exs.\n  Re-ran green afterwards: 1 property, 23 tests, 0 failures (rc=0).\n\n- Full-suite verification: not run by verifier (by design)\n- Audit-expanded scope, if any: none — no prior `AUDIT: BOUNCE` comment on this ticket.\n\nVerdict: PASS\n\nNotes (advisory only, no action required for this ticket):\n1. Spec-prose gap remains open, as the admission predicted. The requirement statement at\n   .spec/specs/compiler_tracer.spec.md:65-71 says the manifest is the pre-session content\n   \"unioned with the session's accumulated edges\" and still never says which side wins on a\n   key collision. Read literally, \"unioned\" is closer to MUTANT B than to the behavior the\n   new test now pins. The test is right and the code is right; the prose is the weak link.\n   Worth a separate ticket (spec text is a `must`/`evolving` requirement, so folding it in\n   here would have blown the trivial-effort budget).\n2. Unpinned adjacent seam: the flush call site at lib/specled_ex/compiler/tracer.ex:191\n   passes `(fetch_merge_base(path), session_modules(), session_edges())`. No test pins that\n   argument order for the non-session-collision case; the new unit test cannot see a swap\n   there, and the integration test at test/integration/tracer_incremental_merge_test.exs\n   does not construct a non-session collision. Low value relative to cost — noting it so the\n   epic does not later mistake this ticket for full coverage of the production scenario\n   described in the ticket body.\n3. Cosmetic only: `MapSet.new([])` at tracer_test.exs:249 could be `MapSet.new()`, matching\n   tracer_test.exs:234. Not worth a commit.",
      "timestamp": "2026-07-27T11:59:27Z"
    },
    {
      "text": "TEST_CRITIC: PASS\n\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nFindings:\n\n- (a) EXACT LIST EQUALITY confirmed at test/specled_ex/compiler/tracer_test.exs:251 -- assert merged[{ModOverlap, :f, 1}] == [{String, :upcase, 1}]. Not membership, not subset. I simulated both named mutants against the real pipeline (lib/specled_ex/compiler/tracer.ex:296-301): the reverse-merge mutant Map.merge(session_edges, previous_kept) returns [{String, :old_callee, 1}] and dies on both == and membership; the concatenate mutant Map.merge(kept, se, fn _,a,b -\u003e a ++ b end) returns [{String, :old_callee, 1}, {String, :upcase, 1}] and dies ONLY under ==. A membership assertion would have PASSED under the concatenate mutant. The exactness is the load-bearing part, and it is present.\n\n- (b) NOT redundant with the property (:255-310). The property has four assertion families: key-set equality (:289-290), session-caller value provenance (:294-300), canonical callee lists (:303-305), idempotence (:308). The value-provenance family is guarded by MapSet.member?(session_set, mod), so for a non-session caller present in BOTH maps the property asserts nothing whatsoever about the value -- only that the key is present and that the list is sorted+deduped. I ran all four assertion families verbatim against both mutants with a non-session collision input: keyset true, session_provenance true, canonical true, idempotent true for BOTH. Neither mutant is killed by the property. Idempotence in particular does not save it -- under concatenate, re-merging canon(prev ++ sess) with sess re-dedupes to the same list, so it is stable.\n\n- (c) No fixture atom collision. ModOverlap appears only at :246, :247, :251; grep over lib/, test/ and .spec/ finds it nowhere else. Distinct from ModKeep / ModRecompiled / ModNew / ModA..ModD, and a different atom from SpecLedEx.Overlap used at :342, :350, :360.\n\n- (d) Correct describe block and exact tag. Placed inside describe \"Tracer.merge_edges/3 (specled.compiler_tracer.merge_on_flush)\" opened at :198, with @tag spec: \"specled.compiler_tracer.merge_on_flush\" at :244 -- character-identical to the subject id at .spec/specs/compiler_tracer.spec.md:63 and to the tagged_tests verification leg covers list. Verified by execution, not by eye: mix test --only spec:specled.compiler_tracer.merge_on_flush now selects 5 (was 4), 0 failures.\n\n- (e) Canonicalization cannot rescue the concatenate mutant. The two callees are distinct MFAs, so Enum.dedup cannot collapse them; the concatenated result has cardinality 2 against an expected cardinality of 1, and no sort order makes a two-element list equal a one-element one. The chosen values discriminate. Worth noting the near miss: had both maps used the SAME callee MFA, sort+dedup would have collapsed to one element and the concatenate mutant would have survived -- the fixture avoids that.\n\n- (f) Pure in-memory, no shared-state contribution. merge_edges/3 (tracer.ex:294) performs no ETS access and no file I/O; the test builds two literal maps and asserts on the return. It inherits the module setup (Tracer.reset plus manifest snapshot/restore, :18-37) but adds no new mutation of the _build/\u003cenv\u003e/.spec manifest, so it does not deepen the async: false requirement.\n\n- (g) No Process.sleep, no spawned or unmanaged processes, no tmp-path or shared-dir usage. Diff from merge-base 1ef97ad1 is +14 lines in one file.\n\n- Suite: mix test test/specled_ex/compiler/tracer_test.exs -- 1 property, 23 tests, 0 failures.\n\n- Style nit, non-blocking: :249 uses MapSet.new([]) while the sibling test at :234 uses MapSet.new(). Cosmetic only, no behavioral difference.",
      "timestamp": "2026-07-27T12:07:03Z"
    },
    {
      "text": "AUDIT: APPROVE (auto — selective audit)\nSession: b19669f0-1a1a-42a6-b6f8-39da90177a9c\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/b19669f0-1a1a-42a6-b6f8-39da90177a9c.jsonl\n\nReasoning: VERIFY reported PASS with full stub and requirement coverage, zero\nscope violations, passing targeted verification, complete seam disclosure, and\nno seam substituting production wiring. TEST_CRITIC: PASS. No auditor was\nspawned per policy.\n\nGate detail: all 10 `must` requirements on specled.compiler_tracer accounted for\nindividually; one-file diff (+14/-0) with `lib/specled_ex/compiler/tracer.ex` absent, so\nthe temporary mutations were reverted. The verifier re-proved BOTH named mutants red\nitself rather than accepting the implementer's paste — reversing the `Map.merge`\nargument order and concatenating instead of replacing — and confirmed the assertion is\nexact list equality, which is what kills them cleanly where a membership check would\nnot. It also checked the converse that mattered here: that the pre-existing property\ndoes NOT already cover a non-session caller present in both maps, so the new test adds\nreal coverage rather than duplicating it.",
      "timestamp": "2026-07-27T12:07:56Z"
    },
    {
      "text": "APPROVED. Merged into epic/specled_-k6s-0.11.0-fast-follow at 847067bc (confirmed present on the epic branch). Epic smoke green: 1276 tests, 0 failures. Closing per orchestrator sign-off.",
      "timestamp": "2026-07-27T12:12:38Z"
    }
  ],
  "priority": 3,
  "status": "closed",
  "title": "tracer_test merge_edges property: non-session caller present in both previous and session_edges has no value-provenance assertion",
  "type": "task",
  "updated_at": "2026-07-27T12:12:38Z"
}
