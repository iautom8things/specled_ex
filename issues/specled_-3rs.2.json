{
  "assignee": "Manuel Zubieta",
  "blocked_by": [
    "specled_-3rs.1"
  ],
  "blocks": [
    "specled_-3rs.3"
  ],
  "closed_at": "2026-05-13T02:25:04Z",
  "created": "2026-05-13T00:52:26Z",
  "description": "Advances: specled.realized_by\n\nDeliverable:\nAdd `SpecLedEx.Realization.Orchestrator.attestations/2` and `run_with_attestations/2`. The former returns a per-(subject, file) attestation map of shape `%{subject_id =\u003e %{normalized_path =\u003e {:attested_clean, [mfa]}}}` covering production-code `realized_by` bindings only (test-file expansion lands in the next ticket). The latter returns `{findings, attestations}` for callers that need both.\n\nInternals: factor a private `do_run/2` so `run/2` and `run_with_attestations/2` share work without re-computing bindings. Use the new `Binding.resolve_with_source/2` for each binding's MFA. An entry is included only when (a) resolve_with_source returns a non-nil path, (b) the binding's MFA does not appear in this run's `branch_guard_realization_drift` or `branch_guard_dangling_binding` findings, and (c) the path normalizes to the same form as `analysis.policy_files` keys (via `Path.relative_to/2`).\n\n`run/2`'s existing return shape (a flat list of findings) is unchanged — three existing callers (BranchCheck, scenario tests, internal references) continue to work.\n\nVerification:\n- Tests to pass: tagged_tests for specled.realized_by.orchestrator_publishes_attestations in test/specled_ex/realization/orchestrator_test.exs. Flip the corresponding verification entry to execute: true (covering only that requirement; the tagged-tests-expansion requirement remains execute: false until the next ticket).\n- Behaviors to demonstrate: scenarios specled.realized_by.scenario.attestation_clean_binding_appears, specled.realized_by.scenario.attestation_drifted_binding_absent.\n\nOut of Scope (files):\n- lib/specled_ex/realization/binding.ex\n- lib/specled_ex/branch_check.ex\n- .spec/specs/branch_guard.spec.md\n- .spec/specs/binding.spec.md\n\nAllowed touches:\n- lib/specled_ex/realization/orchestrator.ex\n- test/specled_ex/realization/orchestrator_test.exs\n- .spec/specs/realized_by.spec.md (only to flip execute: false → execute: true on specled.realized_by.orchestrator_publishes_attestations)\n\nOut of Scope (intent):\n- Do NOT implement tagged-tests expansion in this ticket — that lands separately. Production-code MFAs only.\n- Do NOT change `run/2`'s return shape. Existing callers must continue to work.\n- Do NOT consume the attestation map in branch_check yet. The map is published; consumption is the next ticket after tagged-tests expansion.\n- Do NOT add a hot-path cache. `Code.ensure_loaded/1` is already paid for during tier dispatch.\n\nMerge gate:\n- [ ] Code compiles cleanly\n- [ ] Tests listed above pass\n- [ ] Spec verification entry flipped to execute: true (only the production-code requirement)\n- [ ] Scope check: diff touches only Allowed globs\n- [ ] Auditor APPROVE comment on the ticket",
  "id": "specled_-3rs.2",
  "labels": [],
  "parent": "specled_-3rs",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: b920bd00-78bf-43a9-b891-877e668cc527\nLog: ~/.claude-remembers/projects/-Users-mz-src-specled-ex/b920bd00-78bf-43a9-b891-877e668cc527.jsonl\n\nCommit: 2a337a4\nFiles touched:\n- lib/specled_ex/realization/orchestrator.ex\n- test/specled_ex/realization/orchestrator_test.exs\n- .spec/specs/realized_by.spec.md\n- .spec/state.json (auto-regenerated by mix spec.check after the spec edit)\n\nTests added (test/specled_ex/realization/orchestrator_test.exs, describe \"run_with_attestations/2 + attestations/2 — production bindings\"):\n- \"clean api_boundary binding produces an attestation under its source path\"\n- \"drifted binding does not appear in the attestation map\"\n- \"dangling binding does not appear in the attestation map\"\n- \"multiple clean bindings for the same subject group under their source paths\"\n- \"attestations/2 returns just the map; run/2's flat-list shape is unchanged\"\n- \"tagged-tests expansion is NOT performed yet — only production paths appear\"\n\nEach new test carries @tag spec: \"specled.realized_by.orchestrator_publishes_attestations\".\n\nVerification stubs flipped:\n- specled.realized_by.orchestrator_publishes_attestations → execute: true (split into its own tagged_tests block in realized_by.spec.md; specled.realized_by.attestation_tagged_tests_expansion stays in a separate execute: false block for specled_-3rs.3)\n\nSummary:\nAdded `Orchestrator.run_with_attestations/2` and `Orchestrator.attestations/2`, both backed by a new private `do_run/2` so `run/2` and the new entrypoints share binding collection + tier dispatch + hash commit without duplicating work. The attestation builder walks every collected (subject, mfa) pair, resolves the MFA via `Binding.resolve_with_source/2`, filters out anything whose `(subject_id, mfa)` appears in this run's `branch_guard_realization_drift` or `branch_guard_dangling_binding` findings, normalizes the resolved source path against `opts[:root]` via `Path.relative_to/2` (to match `analysis.policy_files` keys), and accumulates `{:attested_clean, [mfa]}` entries per `(subject_id, normalized_path)`. Production-code only — tagged-tests expansion deferred to specled_-3rs.3 per Out of Scope. `run/2`'s flat-list shape is preserved and verified by the new \"run/2's flat-list shape is unchanged\" test plus the pre-existing 31 orchestrator tests (all green). Full project test suite: 788 tests, 0 failures.",
      "timestamp": "2026-05-13T02:04:33Z"
    },
    {
      "text": "VERIFY: PASS\nSession: b920bd00-78bf-43a9-b891-877e668cc527\nLog: ~/.claude-remembers/projects/-Users-mz-src-specled-ex/b920bd00-78bf-43a9-b891-877e668cc527.jsonl\n\nMechanical checks:\n- Files touched: 4\n- Files outside Allowed globs: 1 (.spec/state.json — auto-regenerated by mix spec.check; also touched in every prior commit on this branch; not in Out of Scope either)\n- Files matching Out-of-Scope globs: 0\n\nSemantic checks:\n- Spec stubs run: 1 (the orchestrator_publishes_attestations tagged_tests block, flipped to execute: true), 6 tests under that tag, all passed. The companion attestation_tagged_tests_expansion block remains execute: false (correct per ticket — next ticket).\n- Requirements with passing tests: 1/1 for this ticket's new requirement (specled.realized_by.orchestrator_publishes_attestations). Other 27 must requirements in specled.realized_by were advanced by prior tickets and were not in this ticket's scope.\n- Intent-level scope concerns: none. No new external deps (mix.exs / mix.lock unchanged). No hot-path cache for Code.ensure_loaded/1 added. run/2's flat-list shape preserved — BranchCheck.realization_findings/4 still consumes Orchestrator.run/2 unchanged. Tagged-tests expansion correctly NOT implemented (test 6 of the new set asserts the test-file path is absent).\n- Project verification: mix test 788/0; mix compile --warnings-as-errors clean; mix format --check-formatted of TOUCHED files clean (the project-wide mix format fails on ~50 files NOT touched by this commit — pre-existing across the branch's history, not introduced here); mix spec.check fails with 6 errors / 6 warnings, but ALL of them belong to requirements NOT advanced by this ticket (5 errors on specled.branch_guard.file_touch_* — parent epic scope, and 1 error on specled.realized_by.attestation_tagged_tests_expansion — explicitly the NEXT ticket's work per Out of Scope intent).\n\nSource-claim trace:\n- Binding.resolve_with_source/2 is defined at lib/specled_ex/realization/binding.ex:144 (specled_-3rs.1) AND consumed at lib/specled_ex/realization/orchestrator.ex:219 in build_attestations/3. Provenance chain checks out: per-(subject, mfa) pair walks both flat-tier bindings (api_boundary/expanded_behavior/typespecs/use) AND implementation-tier impl_bindings; drift/dangling pairs are filtered via MapSet against this run's findings; nil paths short-circuit; non-nil paths are normalized through Path.expand(root) + Path.relative_to(root) so they match analysis.policy_files key form.\n\nVerdict: PASS\n\nNotes:\n- .spec/state.json is a tracked auto-regenerated artifact (.spec/state.json shows up in every prior commit on this branch including 678d878, a8b2383, 921fb2e, etc.). Not declared in Allowed touches OR Out of Scope (files) — treating as project convention rather than a violation.\n- mix spec.check non-zero exit comes entirely from out-of-scope requirements (branch_guard's file-touch family and the next ticket's tagged-tests expansion). It would pass the CLAUDE.md gate as worded (\"All targets for the subjects you advanced must pass\") for the specific requirement this ticket advances: orchestrator_publishes_attestations is no longer in the requirement_without_test_tag list (was in baseline state.json, gone in HEAD).\n- Project-wide mix format --check-formatted is pre-existing red across the branch's history; the implementer did not introduce it, and the touched files in this commit are formatted.",
      "timestamp": "2026-05-13T02:16:11Z"
    },
    {
      "text": "AUDIT: APPROVE\nSession: a6d5d643a1ee8d971\nLog: ~/.claude-remembers/projects/-Users-mz-src-specled-ex/b920bd00-78bf-43a9-b891-877e668cc527/subagents/agent-a6d5d643a1ee8d971.jsonl\n\nReasoning: All four must-clauses of specled.realized_by.orchestrator_publishes_attestations are covered by tests that would actually fail on violation. (1) Exposes function returning {findings, attestations} — test/specled_ex/realization/orchestrator_test.exs:911-934 (\"attestations/2 returns just the map; run/2's flat-list shape is unchanged\") destructures the tuple and equality-checks both halves. (2) Map shape `%{subject_id =\u003e %{normalized_path =\u003e {:attested_clean, [mfa]}}}` — tests at lines ~830 and ~890 assert `get_in(attestations, [\"attest.clean.subject\", \"lib/specled_ex/coverage.ex\"]) == {:attested_clean, [mfa]}`. (3a) Clean binding produces an entry — line ~830. (3b) Drift filter — line ~854 seeds a wrong hash via HashStore.write, asserts the branch_guard_realization_drift finding is present AND `attestations` for the drifted subject == %{}; if the implementation skipped the MapSet filter at orchestrator.ex:215, this test would fail since the binding still resolves to a path. (3c) Dangling filter — line ~882 uses a nonexistent module and asserts both the dangling finding and empty attestation. (4) run/2 shape unchanged — line ~926 asserts is_list(findings) and that lib/specled_ex/branch_check.ex:191 still consumes Orchestrator.run/2 unchanged. The two required scenarios (attestation_clean_binding_appears, attestation_drifted_binding_absent) are demonstrated by tests 1 and 2 of the new describe block. Out-of-Scope intent verified: test 6 (line ~947) actively asserts tagged-tests expansion is NOT performed (passes a kind: tagged_tests verification block + test_tags entry, asserts production path present but test path absent); run/2 shape preserved (788 tests pass); branch_check.ex untouched; no hot-path cache added. Out-of-Scope files clean: lib/specled_ex/realization/binding.ex, lib/specled_ex/branch_check.ex, .spec/specs/branch_guard.spec.md, .spec/specs/binding.spec.md — none in diff. Mechanical counts re-verified against `git diff --stat`: 4 files touched, 1 outside Allowed (.spec/state.json — auto-regenerated, present in every prior branch commit), 0 in Out-of-Scope. Provenance trace verified: Binding.resolve_with_source/2 (binding.ex:144) is consumed at orchestrator.ex:219 inside build_attestations/3. mix spec.check's remaining error on specled.realized_by.attestation_tagged_tests_expansion is explicitly the next ticket per Out-of-Scope intent; the orchestrator_publishes_attestations error is gone (.spec/state.json diff line 484 shows the requirement_without_test_tag entry deleted). mix format failure on lib/specled_ex/review/html.ex pre-exists at base ref 678d878 — not introduced. Minor (non-blocking) gap: no test covers mixed clean+drifted MFAs on the same subject (would directly exercise per-(subject,mfa) filtering vs. per-subject), and no test exercises the implementation-tier path through all_subject_mfa_pairs/1 — but the spec wording is tier-agnostic and the filter logic is exercised by tests 1+2.",
      "timestamp": "2026-05-13T02:24:38Z"
    },
    {
      "text": "APPROVED. Merged into feature/file-touch-yields-to-realization. Closing per orchestrator sign-off.",
      "timestamp": "2026-05-13T02:25:04Z"
    }
  ],
  "priority": 2,
  "status": "closed",
  "title": "Orchestrator attestations map (production-code bindings)",
  "type": "task",
  "updated_at": "2026-05-13T02:25:04Z"
}
