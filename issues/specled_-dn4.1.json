{
  "assignee": "Manuel Zubieta",
  "blocked_by": [
    "specled_-dn4.4",
    "specled_-dn4.8"
  ],
  "blocks": [
    "specled_-dn4.2",
    "specled_-dn4.6"
  ],
  "created": "2026-07-24T19:18:42Z",
  "description": "boundary_tags/1 denylist-copies the whole ExUnit context into every boundary row in a suite-lifetime :public ETS table; nothing in lib/ reads it. Suite-lifetime retention of O(tests × context), extra deep-copy on the per-test hot path, denylist must track ExUnit keys forever. Drop :tags from the row, collapse tail/3→tail/2, update boundary_test.exs:76 assertion + coverage_capture.spec.md api surface. If a consumer appears later, use an allowlist (Map.take/2). Report F4/CF3.\n\n---\n\n## Orchestrator scoping (synthesized 2026-07-25 by /orchestrate-epic from verified code analysis)\n\n## specled_-dn4.1 — Drop write-only `boundary_row.tags`; collapse to `Boundary.tail/2` (F4, 5-reviewer consensus)\n\n**Advances:** `specled.coverage_capture`\n\nMatched obligations: requirement `specled.coverage_capture.boundary_hook_sync` (`.spec/specs/coverage_capture.spec.md:448-459`) names `Boundary.tail/3` in its statement text at line 452; the `realized_by.api_boundary` list carries `\"SpecLedEx.Coverage.Boundary.tail/3\"` at line 110. Scenario `specled.coverage_capture.scenario.boundary_hook_window` (785-794) covers that requirement and its `then` clause asserts only the hits diff, never tags — so no scenario rewrite is forced (and none should be attempted, to stay clear of `append_only/scenario_regression`).\n\n**Primary files** (all symbols verified present today):\n\n- `lib/specled_ex/coverage/boundary.ex` — `@type boundary_row` field `tags: map()` at :30; `@spec tail(test_key(), Snapshot.module_snapshot(), map()) :: :ok` at :54; `def tail(test_key, head_snapshot, tags \\\\ %{})` head at :55; body clause + `is_map(tags)` guard at :57-58; `tags: tags` in the row map at :76.\n- `lib/specled_ex/coverage.ex` — `per_test_boundary/1` at :52-67, with `tags = boundary_tags(context)` at :59 and `Boundary.tail(key, head_snapshot, tags)` at :62; `defp boundary_tags/1` at :73-90 (the 13-key `Map.drop` denylist). Verified: `boundary_tags` has exactly one definition and one caller repo-wide.\n- **Verified claim \"nothing in lib/ reads it\":** the only `.tags` read in the formatter (`lib/specled_ex/coverage/formatter.ex:398`, `tags: row.tags`) is on the **inventory** row (built at :226-231 with `tags: test.tags`), not the boundary row. The boundary row is destructured at :390/:403 for `.hits` and `.diagnostics` only. Confirmed write-only.\n- `test/specled_ex/coverage/boundary_test.exs` — `Boundary.tail/3` calls at :69, :115, :125; the tags assertion at :76 (`assert row.tags.file == \"test/sample_test.exs\"`).\n- `test/specled_ex/coverage/formatter_test.exs` — **four** hand-built boundary-row literals carrying `tags: %{}` that must drop the key: :351, :496, :526, :644. (Note :224 `assert row.tags == tags` is an *inventory*-row assertion — leave it.)\n- `test/specled_ex/case_test.exs:45` — prose comment \"Boundary.tail/3 tolerates a gone table\".\n- `.spec/specs/coverage_capture.spec.md` — :110 (realized_by entry), :452 (requirement prose).\n- `.spec/decisions/specled.decision.per_test_sync_boundary.md:43` — \"(`Boundary.tail/3`)\".\n- `.spec/realization_hashes.json` — key `api_boundary/SpecLedEx.Coverage.Boundary.tail/3` at :211-214 becomes `.../tail/2` with a new hash.\n\n**Allowed touches (globs):**\n\n- `lib/specled_ex/coverage/boundary.ex`\n- `lib/specled_ex/coverage.ex`\n- `test/specled_ex/coverage/boundary_test.exs`\n- `test/specled_ex/coverage/formatter_test.exs`\n- `test/specled_ex/case_test.exs`\n- `.spec/specs/coverage_capture.spec.md`\n- `.spec/decisions/specled.decision.per_test_sync_boundary.md`\n- `.spec/realization_hashes.json`\n- `CHANGELOG.md`\n\n**Out of Scope (files):** `lib/specled_ex/coverage/formatter.ex` (dn4.2/dn4.4/dn4.5 own it), `lib/specled_ex/coverage/snapshot.ex`, `lib/specled_ex/coverage/store.ex`, `lib/specled_ex/review/**`, `lib/specled_ex/coverage_triangulation.ex`, `lib/mix/tasks/**`, `.spec/specs/spec_review.spec.md`, `.spec/specs/triangulation.spec.md`, `docs/**`.\n\n**Out of Scope (intent):**\n\n- Do not introduce an allowlist (`Map.take/2`) \"just in case\" — the ticket says allowlist only *if* a consumer appears later. Removing the field is the whole change.\n- Do not change the window/diff semantics or the arming decoder while in `boundary.ex` (dn4.4/dn4.6 own those).\n- No `Boundary.head/1` signature change.\n\n**Verification:**\n\n- `mix test test/specled_ex/coverage/boundary_test.exs test/specled_ex/coverage/formatter_test.exs test/specled_ex/case_test.exs`\n- `mix spec.check --no-run-commands` (catches the dangling `tail/3` binding and the realization-drift on the renamed hash key)\n- Relevant `coverage_capture` verification stubs, all currently **`execute: true`**: the `tagged_tests` stub covering `boundary_hook_sync` + `case_template` + `boundary_row_exclusive` (`.spec/specs/coverage_capture.spec.md:913-928`) and the stub covering `boundary_noop_unarmed` + `envelope_meta` (:943-947).\n- Expect an intentional realization-drift finding on the `tail/3`→`tail/2` key; resolve via reseed/`--accept-drift`, not by reverting.\n\n**Overlap risk:**\n\n- `lib/specled_ex/coverage/boundary.ex` — also edited by **dn4.4** (arming resolver replaces `resolve_config/0`), **dn4.6** (chained windows rewrite `head/1`+`tail`), **dn4.9** (rescue-based insert, `head/1` tolerance, `persistent_term` cache, `:ets.info/2`). Highest-contention file in the epic.\n- `lib/specled_ex/coverage.ex` `per_test_boundary/1` — also edited by **dn4.6** (the closure must stop capturing `head_snapshot`) and **dn4.9** (`boundary_key/1` `setup_all` fallback). dn4.6 rewrites the exact lines :59-63 dn4.1 deletes.\n- `test/specled_ex/coverage/formatter_test.exs` — also **dn4.2, dn4.4, dn4.5, dn4.7, dn4.9**.\n- `.spec/specs/coverage_capture.spec.md` — also **dn4.3, dn4.4, dn4.5, dn4.6, dn4.8**.\n\n---",
  "id": "specled_-dn4.1",
  "labels": [
    "phase:merging"
  ],
  "parent": "specled_-dn4",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nCommit: de0af4a0e125c248d47fec2090fd12f0f400c537\nFiles touched: lib/specled_ex/coverage/boundary.ex; lib/specled_ex/coverage.ex; test/specled_ex/coverage/boundary_test.exs; test/specled_ex/coverage/formatter_test.exs; test/specled_ex/case_test.exs; .spec/specs/coverage_capture.spec.md; .spec/decisions/specled.decision.per_test_sync_boundary.md; .spec/realization_hashes.json\nTests added: none (updated existing boundary/formatter/case tests)\nVerification stubs flipped: none\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/coverage/boundary_test.exs test/specled_ex/coverage/formatter_test.exs test/specled_ex/case_test.exs; mix spec.check --no-run-commands; mix format --check-formatted; stale-name grep for tail/3/boundary_tags; full-system proof deferred to the merge gate\nFinal-tier runs: 2 (first exposed intentional realization drift; second green after --accept-drift and stale tail/3 baseline removal via HashStore)\nGate-forced co-changes outside Allowed touches: none\nDeferred must requirements: none\n\nSummary: Dropped write-only boundary row tags and collapsed SpecLedEx.Coverage.Boundary.tail/3 to tail/2, including the per_test_boundary call path. Updated scoped tests, coverage_capture current truth, the per-test boundary ADR, and the realization baseline so the API boundary now tracks tail/2.",
      "timestamp": "2026-07-25T14:06:35Z"
    },
    {
      "text": "VERIFY: PASS\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nVerified cold at HEAD de0af4a in /Users/mz/src/specled_ex-drop-boundary-tags against base f2efdf0. Line locations re-derived at current HEAD (post-dn4.4 arming refactor); the ticket's stated line numbers are stale but its Allowed/Out-of-Scope globs bind and are respected.\n\n## Mechanical checks\n\n- Diff touches 8 files, every one inside Allowed globs: `lib/specled_ex/coverage/boundary.ex`, `lib/specled_ex/coverage.ex`, `test/specled_ex/coverage/boundary_test.exs`, `test/specled_ex/coverage/formatter_test.exs`, `test/specled_ex/case_test.exs`, `.spec/specs/coverage_capture.spec.md`, `.spec/decisions/specled.decision.per_test_sync_boundary.md`, `.spec/realization_hashes.json`. Net 22 insertions / 50 deletions — a removal, as scoped.\n- `CHANGELOG.md` is Allowed but unmodified. Not a gap: no sibling stage commit in this epic (91831bf, f2efdf0, 91987f7, 6fb515d) touches it either; `git log -- CHANGELOG.md` shows it is edited only in release commits (14f6e72, 7747ef6, ce39e0f). Release-PR concern, not stage.\n- Out-of-Scope clean: `lib/specled_ex/coverage/formatter.ex` has **0** changed lines (`git diff f2efdf0..HEAD -- .../formatter.ex | wc -l` = 0). No `review/**`, no `coverage_triangulation.ex`, no `snapshot.ex`/`store.ex`, no `lib/mix/tasks/**`, no `docs/**`, no other spec files.\n- Deleted-symbol sweep (`grep -rn --include=*.ex --include=*.exs --include=*.md --include=*.json`, excluding `_build`/`deps`): `tail/3` — **zero** hits repo-wide. `boundary_tags` — **zero** hits repo-wide. Every surviving `Boundary.tail` reference names tail/2 (spec :119, :470; ADR :43; case_test :93 comment; boundary_test :93/:144/:154; coverage.ex :61). No historical-mention carve-out was even needed.\n\n## Semantic checks\n\n**Core claim (a) — boundary.ex.** `@type boundary_row` is now `%{hits: ..., diagnostics: ...}` only (:19-22), tags field gone. `@spec tail(test_key(), Snapshot.module_snapshot()) :: :ok`. The `def tail(test_key, head_snapshot, tags \\\\ %{})` default-arg head is deleted outright, so no tail/3 can be generated; the single remaining clause is `def tail(test_key, head_snapshot) when is_tuple(test_key) and is_map(head_snapshot)` — the `is_map(tags)` guard removed with its parameter, nothing weakened on the surviving params. Row construction writes `hits` + `diagnostics` only.\n\n**(b) — coverage.ex.** `defp boundary_tags/1` and its 13-key `Map.drop` denylist are deleted in full. `per_test_boundary/1`'s `on_exit` closure now captures only `key` and `head_snapshot` and calls exactly `Boundary.tail(key, head_snapshot)` (:61). No ExUnit-context copy remains anywhere on the hot path — the only surviving `tags` reference in `coverage.ex` is :150 `Map.get(index, \"test_tags\")`, an unrelated artifact-index read.\n\n**(c) — the two row types are genuinely distinct, and the dropped field was genuinely write-only.** All four hand-built boundary-row literals in `formatter_test.exs` (now :350, :495, :525, :642) dropped their `tags:` key. The inventory-row assertion `assert row.tags == tags` is untouched and passes. In `formatter.ex` (unchanged) the inventory row is still built with `tags: test.tags` (:215-221) and `tags: row.tags` at :387 reads *that* inventory row, while the boundary row is destructured only for `.hits` (:379) and `.diagnostics` (:392). The ticket's \"nothing in lib/ reads it\" claim re-confirmed independently at current line numbers.\n\n**(d)** No `Map.take` anywhere in the diff — removal is the whole change, per the orchestrator ruling.\n\n**(e)** Arming resolver untouched (`Arming.resolve(:boundary)` call sites unchanged); `Boundary.head/1` is not in the diff at all; window/diff semantics via `Snapshot.diff/2` unchanged.\n\n**Spec/baseline co-changes (3a).** `realized_by.api_boundary` now lists `\"SpecLedEx.Coverage.Boundary.tail/2\"` (:119) and the `boundary_hook_sync` statement names `Boundary.tail/2` (:470) — mechanism naming survives the rewrite, so no [stale-spec]. Exactly 2 lines of the spec changed, therefore scenario `boundary_hook_window` (:804-813) is byte-identical: its `then` clause asserts only the hits diff, never tags, so it needed no rewrite and got none. No `append_only/scenario_regression` exposure. The requirement's cost prose (\"two snapshot reads + one `Snapshot.diff/2` + one ETS insert\", \"head snapshot lives only in the `on_exit` closure\") becomes *more* true with the deep copy gone.\n\n**(3b) ADR.** Single line 43 changed, in the current-design \"Decision\" section describing what the code does today — correctly updated rather than framed as history. `grep -n \"tags\"` over the ADR returns **zero** hits, so there is no stale row-shape prose left describing a tags field.\n\n**(3c) realization baseline.** The `tail/3` key is gone; `tail/2` present with hash `c6357aab…`. JSON diff is limited to exactly two entries: the tail key rename plus the `SpecLedEx.Coverage` module hash, which legitimately moves because `coverage.ex` source changed. Practical proof the accept-drift flow was used rather than a hand-edit: bare `mix spec.check` is green **and** leaves `git status --porcelain` empty, i.e. the committed hashes reproduce byte-for-byte from the hasher.\n\n## Requirements coverage — `specled.coverage_capture.boundary_hook_sync` (must)\n\n- Covering tests: `test/specled_ex/coverage/boundary_test.exs:69-101` (window diff + tags-absence guard), `:129-149` (diagnostics passthrough on negative deltas), `:103-127` (module-scope cached once), `:152-156` (unarmed no-op); plus `test/specled_ex/case_test.exs:92+` for the armed `per_test_boundary` path end to end.\n- Remaining row fields did **not** silently lose coverage: `row.hits` asserted at :98 and :147, `row.diagnostics` at :99 and :148 — non-default values in both cases (`hits == %{Mod =\u003e [10]}` with line 20 deliberately held flat so the \"strictly increased\" claim can fail; `diagnostics == 1`).\n- **Would-fail-if (executed, not merely asserted):** re-added `tags: %{}` to the row map in `boundary.ex` and ran `mix test test/specled_ex/coverage/boundary_test.exs` → 1 failure at `boundary_test.exs:100`, `refute Map.has_key?(row, :tags)`, \"Expected false or nil, got true\", row inspected as `%{diagnostics: 0, hits: %{Mod =\u003e ...}, tags: %{}}`. The new guard is live, not decorative. Mutation reverted via `git checkout --`; `git status --porcelain` empty, `grep tags boundary.ex` zero hits, recompiled clean.\n- Seams disclosed: the Arming seam is injected in-test via `arm(boundary_table: tid, snapshot_fn: ..., modules_fn: ...)`, with an `Agent` sequencing the head/tail snapshot pair; ETS is real (per-test `new_table()`), `Snapshot.diff/2` is real. The unstubbed shipped path is covered separately by the child-BEAM fixtures in `spec_cover_test_test.exs`, which wire `use SpecLedEx.Case` / `setup {SpecLedEx.Coverage, :per_test_boundary}` and contain no direct `tail/3` heredoc call.\n\n## Targeted verification (all green)\n\n- `mix compile --warnings-as-errors` — clean\n- `mix test boundary_test.exs formatter_test.exs case_test.exs` — 37 tests, 0 failures\n- `mix spec.check --no-run-commands` — status=pass errors=0 warnings=0\n- bare `mix spec.check` — status=pass errors=0 warnings=0; git clean afterward\n- `mix format --check-formatted` — clean\n- `mix deps.unlock --check-unused` — clean\n- Blast radius, child-BEAM shipped API: `mix test test/mix/tasks/spec_cover_test_test.exs` — 10 tests, 0 failures\n- Broadened (since `coverage.ex` changed): `mix test test/specled_ex/coverage/ test/specled_ex/case_test.exs` — 92 tests, 0 failures\n- No known flakes hit; zr6 (tracer_test) and ya8 (scenario_refactor_stable_test) were outside the executed scope. Full suite left to the merge gate.\n\n## Verdict\n\nPASS. The change is exactly the scoped removal — no allowlist smuggled in, no adjacent-file drift, no stale symbol anywhere in code, spec, ADR, or baseline — and the field's write-only status was re-confirmed from the formatter rather than taken on the implementer's word.\n\n## Notes\n\n- The single branch-level WARNING from `spec.check` (`append_only/same_pr_self_authorization` on ADR `specled.decision.coverage_qualifier_requirement_ids`) is **pre-existing and not attributable to dn4.1**: that ADR exists at base f2efdf0, introduced by 91987f7 (dn4.8). It is a warning, not an error, and `errors=0` both with and without command execution.\n- Minor cosmetic churn beyond the required rename: the two `describe` names also swapped an em-dash for a plain hyphen (`\"head/1 + tail/2 - window diff semantics\"`, `\"tail/2 - unarmed no-op\"`). Harmless — those strings are referenced nowhere in `.spec/` or anywhere in the repo outside the test file itself, and test-tag consistency passes. Flagging only so it is not mistaken for a functional edit on review.",
      "timestamp": "2026-07-25T14:15:48Z"
    },
    {
      "text": "AUDIT: APPROVE (auto — selective audit)\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nReasoning: VERIFY reported PASS with full stub and requirement coverage, zero\nscope violations, passing targeted verification, complete seam disclosure, and\nno seam substituting production wiring (the unstubbed shipped path is covered by\nthe child-BEAM fixtures; the tags-removal guard was mutation-proven live). No\nauditor was spawned per policy. Posted by the orchestrator applying the\ncanonical Selective Audit Gate.\n\nMerge sequencing note: ff-merge deferred until specled_-ya8 (gate-health) lands —\nthe ya8 flake signature has exhausted its 2 gate-accepts and every gate run\nexecutes the integration suite.",
      "timestamp": "2026-07-25T14:16:59Z"
    },
    {
      "text": "APPROVED. Merged into specled_-dn4/fast-follow-per-test. Closing per orchestrator sign-off. (Gate green first run: 1211/0, spec.check pass.)",
      "timestamp": "2026-07-25T14:30:59Z"
    }
  ],
  "priority": 1,
  "status": "in_progress",
  "title": "Drop write-only boundary_row.tags; collapse to Boundary.tail/2 (F4, 5-reviewer consensus)",
  "type": "task",
  "updated_at": "2026-07-25T14:30:59Z"
}
