{
  "assignee": "Manuel Zubieta",
  "blocked_by": [
    "specled_-dn4.3"
  ],
  "blocks": [
    "specled_-dn4.1",
    "specled_-dn4.6"
  ],
  "created": "2026-07-24T19:18:43Z",
  "description": "Three independent decoders of :specled_ex, :spec_cover_run across Boundary + Formatter already disagree on table validity (is_reference or is_atom vs any non-nil), plus duplicated default_snapshot_fn/1 and the leaked modules_cache_key/0. Extract one arming resolver returning {:armed, config} | :disarmed. Report F5.\n\n---\n\n## Orchestrator scoping (synthesized 2026-07-25 by /orchestrate-epic from verified code analysis)\n\n## specled_-dn4.4 — Unify the arming-seam decoders behind one resolver (F5)\n\n**Advances:** `specled.coverage_capture`\n\nMatched obligations: `specled.coverage_capture.formatter_arming_seam` (:211-226) and `specled.coverage_capture.boundary_noop_unarmed` (:460-468) — the two requirements that *separately* describe the same seam, which is the root of the divergence. Also `specled.coverage_capture.formatter_snapshot_fn_di` (:199-210), which pins the production default.\n\n**Primary files — the three decoders, verified to disagree:**\n\n1. `lib/specled_ex/coverage/boundary.ex:90-108` `defp resolve_config/0` — matches `opts when is_list(opts)` only (so seam value `true` is `:unarmed`), then accepts **any non-nil** `Keyword.get(opts, :boundary_table)` as a valid tid (:93-103).\n2. `lib/specled_ex/coverage/formatter.ex:133-139` `defp armed/0` — matches `is_list(opts)` **and** `true -\u003e []`, and never looks at `:boundary_table`.\n3. `lib/specled_ex/coverage/formatter.ex:413-441` `defp load_boundary_index/0` — matches `is_list(opts)` then requires `tid when is_reference(tid) or is_atom(tid)` (:417). **This is the concrete disagreement with (1):** an armed seam carrying a non-ref/non-atom `:boundary_table` makes `Boundary` write rows that the formatter then silently ignores.\n\nAlso in scope, as named by the ticket:\n\n- Duplicated `defp default_snapshot_fn(modules), do: Snapshot.take(Snapshot.runtime_mode(), modules)` — `boundary.ex:128` and `formatter.ex:145`, byte-identical.\n- The leaked accessor `@doc false def modules_cache_key, do: @modules_key` — `boundary.ex:86-88`, whose sole consumer is `formatter.ex:423` (`SpecLedEx.Coverage.Boundary.modules_cache_key()`), plus the test assertion at `test/specled_ex/coverage/boundary_test.exs:97`.\n- Target shape per the ticket: one resolver returning `{:armed, config} | :disarmed`.\n- Third arming writer to keep consistent: `lib/mix/tasks/spec.cover.test.ex:197-198` (`:ets.new(:anon, [:public, :set])` then `Application.put_env(:specled_ex, :spec_cover_run, boundary_table: boundary_table)`).\n- `@arming_app :specled_ex` / `@arming_key :spec_cover_run` are duplicated at `boundary.ex:18-19` and `formatter.ex:113-114`.\n\nIf the resolver lands as a new module (e.g. `lib/specled_ex/coverage/arming.ex`), it must be added to `coverage_capture.spec.md`'s `surface:` list (:81-102) or the file will be unmapped.\n\n**Allowed touches (globs):**\n\n- `lib/specled_ex/coverage/boundary.ex`\n- `lib/specled_ex/coverage/formatter.ex`\n- `lib/specled_ex/coverage/arming.ex` (new, if extracted as its own module)\n- `lib/mix/tasks/spec.cover.test.ex` (only if the arming write shape changes)\n- `test/specled_ex/coverage/boundary_test.exs`\n- `test/specled_ex/coverage/formatter_test.exs`\n- `test/specled_ex/coverage/arming_test.exs` (new)\n- `.spec/specs/coverage_capture.spec.md`\n- `.spec/realization_hashes.json`\n- `CHANGELOG.md`\n\n**Out of Scope (files):** `lib/specled_ex/coverage.ex`, `lib/specled_ex/coverage/snapshot.ex`, `lib/specled_ex/coverage/store.ex`, `lib/specled_ex/review/**`, `lib/specled_ex/coverage_triangulation.ex`, `docs/**`, `.spec/specs/spec_review.spec.md`, `.spec/specs/triangulation.spec.md`, `.spec/specs/mix_tasks.spec.md`.\n\n**Out of Scope (intent):**\n\n- Pick **one** table-validity predicate and say so in the spec — do not leave both `is_reference or is_atom` and \"any non-nil\" alive behind a new façade.\n- Do not change the observable arming contract: seam `true` must still activate the formatter with production defaults and must still leave `Boundary` unarmed (that asymmetry is deliberate and is asserted at `test/specled_ex/coverage/boundary_test.exs:32-35`).\n- No new deps; no `Application.put_env` from library code.\n\n**Verification:**\n\n- `mix test test/specled_ex/coverage/boundary_test.exs test/specled_ex/coverage/formatter_test.exs test/specled_ex/case_test.exs test/mix/tasks/spec_cover_test_test.exs`\n- `mix spec.check --no-run-commands` (surface-list completeness for a new module, plus realization drift on `Boundary.head/1`, `Boundary.tail`, `Formatter`, `Formatter.init/1`)\n- Stubs, both **`execute: true`**: `.spec/specs/coverage_capture.spec.md:898-902` (`formatter_arming_seam`, `formatter_no_fabrication`), :943-947 (`boundary_noop_unarmed`, `envelope_meta`), and :891-897 (`formatter_snapshot_fn_di`).\n\n**Overlap risk:**\n\n- `lib/specled_ex/coverage/boundary.ex` — **dn4.1** (`tail/3`→`tail/2` changes the very clause `resolve_config/0` feeds), **dn4.6** (chained windows), **dn4.9** (`table_alive?/1` at :122-126 becomes `:ets.info(tid, :size)`; `modules_for/1` at :110-120 becomes a `persistent_term` cache; rescue-based insert replaces the `table_alive?` pre-check at :68).\n- `lib/specled_ex/coverage/formatter.ex` — **dn4.2** (`flush/1`, which calls `load_boundary_index/0`), **dn4.5**, **dn4.9** (`disarmed_notice/0` inlining).\n- `test/specled_ex/coverage/boundary_test.exs` — **dn4.1**, **dn4.6**, **dn4.9**.\n- dn4.9's \"`persistent_term` modules cache\" would **delete** `modules_cache_key/0` outright, which is exactly the leak dn4.4 is asked to close. Decide ownership once: recommend dn4.4 closes the leak by keeping ETS and making the key private to the resolver, and dn4.9 drops its `persistent_term` item or rebases onto it.\n\n---\n\n\n**Orchestrator ruling (ownership of the modules cache):** dn4.4 owns closing the `modules_cache_key/0` leak. Keep the ETS-based modules lookup and make the key private to the new resolver. Do NOT convert to `persistent_term` here — that evaluation belongs to dn4.9, which runs after you and will build on your landed design.",
  "id": "specled_-dn4.4",
  "labels": [
    "phase:verifying"
  ],
  "parent": "specled_-dn4",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nCommit: 462d6c4\nFiles touched: .spec/realization_hashes.json, .spec/specs/coverage_capture.spec.md, lib/mix/tasks/spec.cover.test.ex, lib/specled_ex/coverage/arming.ex, lib/specled_ex/coverage/boundary.ex, lib/specled_ex/coverage/formatter.ex, test/specled_ex/coverage/boundary_test.exs\nTests added: SpecLedEx.Coverage.BoundaryTest — shared resolver requires a live ETS table for boundary arming; SpecLedEx.Coverage.BoundaryTest — shared resolver keeps true armed for formatter and disarmed for boundary\nVerification stubs flipped: none — already execute: true\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/coverage/boundary_test.exs test/specled_ex/coverage/formatter_test.exs test/specled_ex/case_test.exs test/mix/tasks/spec_cover_test_test.exs (47 tests); mix spec.check --no-run-commands; mix format --check-formatted; full-system proof deferred to the merge gate\nFinal-tier runs: 2\nGate-forced co-changes outside Allowed touches: none\nDeferred must requirements: none\n\nSummary: Added SpecLedEx.Coverage.Arming as the sole seam decoder, using live ETS status as the shared table-validity predicate while preserving formatter true/default arming and boundary true/unarmed behavior. Centralized production defaults and ETS module caching behind the resolver, removed Boundary.modules_cache_key/0, and preloaded the resolver for child-BEAM production runs.",
      "timestamp": "2026-07-25T13:06:04Z"
    },
    {
      "text": "VERIFY: PASS\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nCold re-verification of the CUMULATIVE diff `14f6e72..6fb515d` (2 commits), iteration 2.\n\nMechanical checks:\n- Files touched: 56 changed (57 paths counting both rename endpoints), +121 / -73.\n- Files outside Allowed globs: 1 — `.spec/specs/mix_tasks.spec.md`.\n- Files matching Out-of-Scope globs: 1 — same file (`every .spec/specs/* except coverage_capture.spec.md`).\n- Audit-expanded scope (AUDIT: BOUNCE → implementer, 2026-07-25T11:59:01Z): (a) `test/specled_ex/policy_files_test.exs` — explicitly authorized by bounce item 4; it is also inside the Allowed glob `test/**/*_test.exs` independently. (b) `.spec/specs/mix_tasks.spec.md` — gate-forced surface-path follow, explicitly ordered to STAY by bounce item 5 (proved by revert-and-rerun in the prior VERIFY). Neither is re-flagged as a violation.\n- Rename arithmetic matches the ticket exactly: 54 `use SpecLedEx.FixtureCase` occurrences across 51 test files. The non-`case_test`/non-`policy_files` portion of the diff is provably nothing but `use`-line substitutions — 44 bare + 8 `, async: false` + 1 `, async: true` = 53, plus 1 in `policy_files_test.exs` = 54. Zero incidental edits.\n- Invariants re-confirmed independently: 0 files under `lib/**`; `mix.exs` untouched (so `elixirc_paths(:test)` still excludes root `test_support/`); `test_support/specled_ex_integration_case.ex` and `test_support/append_only_fixtures.ex` untouched; `docs/**` untouched; `.spec/realization_hashes.json` untouched; `test/mix/tasks/spec_cover_test_test.exs` untouched with :445 and :512 still `use SpecLedEx.Case, async: false`; the 5 `use SpecLedEx.Case` doc mentions under `lib/` all preserved; `Code.put_compiler_option(:ignore_module_conflict, true)` present at `test/test_helper.exs:18`; the fixture rename is 98% similarity with exactly 2 changed lines (`defmodule` + `import`), zero helper behavior change.\n- `CHANGELOG.md` is Allowed but untouched — not required by the ticket; the release PR owns it.\n- Base sanity: `origin/main` == `14f6e7253841c67ad9097c606dedfbe1550a04bd` (fetched). Working tree clean before and after all probes.\n\nSemantic checks:\n\nAudit contract execution — items 1, 3, 4 executed; items 2, 5 obeyed.\n\n- Item 1 (G1) — DONE, and independently mutation-verified. `test/specled_ex/case_test.exs:24` is now `use SpecLedEx.Case, async: true`; `:61` is `assert context.async == true`; the load-bearing `assert Agent.get(context.snapshot_agent, \u0026length/1) == 1` is KEPT at `:63`.\n  - **The audit's stated mutation recipe is unimplementable, and this matters.** The bounce asked for \"a child-BEAM template whose `using` block drops opts forwarding\". I built exactly that (an `ExUnit.CaseTemplate` with `using _opts do quote do use ExUnit.Case end end` plus the boundary `setup`) and it exits **0** — opts are still forwarded. Cause: `ExUnit.CaseTemplate.using/2` (Elixir 1.18.1, `lib/ex_unit/lib/ex_unit/case_template.ex:149-157`) compiles to `{:__block__, [], [parent, result]}` where `parent = __proxy__(__MODULE__, opts)`, and `__proxy__/2` (:82-94) unconditionally emits `use ExUnit.Case, ExUnit.Case.__keys__(opts)`. A `using` block cannot suppress forwarding.\n  - The only realizable mutation is overriding `__using__/1` (`use ExUnit.CaseTemplate` marks it `defoverridable __using__: 1`), which is what the implementer reported. I reproduced it from scratch:\n    - Mutant A — overrides `__using__/1` to call bare `use ExUnit.Case`, KEEPS the injected `setup {SpecLedEx.Coverage, :per_test_boundary}`, test body verbatim: child exits **2**, `assert context.async == true` fails `left: false / right: true`. G1 is closed.\n    - Mutant B — KEEPS opts forwarding, drops the injected setup line: child exits **2**, `assert Agent.get(context.snapshot_agent, \u0026length/1) == 1` fails `left: 2 / right: 1`. Confirms :63 is still load-bearing and was not traded away.\n    - Un-mutated control, extracted verbatim from `case_test.exs:20-66` against the shipped `SpecLedEx.Case`: exits **0**, \"1 test, 0 failures\".\n  - Advisory for the auditor (not a gap): because `lib/specled_ex/case.ex` has no `using` block at all, the \"forwards opts\" clause is *inherited* `ExUnit.CaseTemplate` behavior. The assertion is genuinely falsifiable, but the failure mode it actually guards is \"someone adds a `__using__/1` override\", not \"someone breaks forwarding logic\" — there is no such logic in the subject to break. That is the correct and only available proof, so the clause is covered.\n- Item 2 — OBEYED. Still a `System.cmd(\"elixir\", [\"-e\", script], env: [{\"ERL_LIBS\", parent_lib}], stderr_to_stdout: true)` subprocess at `case_test.exs:68-72`. `SpecLedEx.CaseTest` is still `use ExUnit.Case, async: false` (:3) with the serialization rationale comment intact at :2. No host-suite module was converted to `use SpecLedEx.Case`, so nothing new races the process-global `:specled_ex, :spec_cover_run` seam.\n- Item 3 — DONE. `.spec/specs/coverage_capture.spec.md:811` now reads \"the adopter module is an `ExUnit.Case` that receives forwarded options (`async: true`) and runs the injected `setup {SpecLedEx.Coverage, :per_test_boundary}` before the test body\" — it names both the forwarded non-default option and the injected MFA, verbatim the wording the bounce proposed. (It names `ExUnit.Case` rather than `ExUnit.CaseTemplate`, exactly as the audit's own example text did, so it is compliant as written.) `given` at :806 also moved off the source-file framing to \"the production `SpecLedEx.Case` template is loaded\".\n- Item 4 — DONE. `test/specled_ex/policy_files_test.exs:68` now asserts `PolicyFiles.classify(\"test_support/specled_ex_fixture_case.ex\") == :test`. Repo-wide grep for `specled_ex_case\\.ex` over `*.ex`/`*.exs`/`*.md`/`*.eex`/`*.json`/`*.yml`/`*.yaml`, excluding `_build`/`deps`: **zero hits**. The renamed file exists as `test_support/specled_ex_fixture_case.ex` and is required at `test/test_helper.exs:23`; the old path is gone from `test_support/`.\n- Item 5 — OBEYED. `.spec/specs/mix_tasks.spec.md:45` left exactly as the implementer had it.\n\nRequirements coverage (the two `must`s this diff bears on):\n- `specled.coverage_capture.case_template` (must, `.spec/specs/coverage_capture.spec.md:469-477`) — a three-clause conjunction, now ALL THREE covered by `test/specled_ex/case_test.exs:17-76`.\n  - \"shall be an `ExUnit.CaseTemplate`\": covered mechanically at `:62`/`:63` — only a case template can inject a `setup` into an adopter module. Failure mode: replace `use ExUnit.CaseTemplate` with a hand-rolled `__using__` that only does `use ExUnit.Case`, and `:63` fails `left: 2 / right: 1` (mutant B, exit 2).\n  - \"forwards opts to `use ExUnit.Case`\": covered at `:61` via the non-default `async: true` at `:24`. Failure mode: a `__using__/1` override calling bare `use ExUnit.Case` yields `async: false` → `left: false / right: true` (mutant A, exit 2).\n  - \"injects `setup {SpecLedEx.Coverage, :per_test_boundary}`\": covered at `:63`. Failure mode: template with no setup line never consumes the queued snapshot → `left: 2 / right: 1` (mutant B, exit 2).\n  - Seams: the injected production arming seam `Application.put_env(:specled_ex, :spec_cover_run, boundary_table:, snapshot_fn:, modules_fn:)` (identical key/shape to `lib/mix/tasks/spec.cover.test.ex:198`), with an `Agent`-backed fake `snapshot_fn` and a fake `modules_fn` substituting only the coverage *data source*, not the wiring; a real ETS table via `:ets.new/2`; child BEAM via `System.cmd` + `ERL_LIBS`. No `Mox`, no `Memory*` doubles. Confirmed the child can only resolve the shipped module: `_build/test/lib/spec_led_ex/ebin` contains `Elixir.SpecLedEx.Case.beam` and no `FixtureCase` beam.\n- `specled.coverage_capture.boundary_noop_unarmed` (must, `.spec/specs/coverage_capture.spec.md:460-467`) — covered at `test/specled_ex/case_test.exs:78-89` (line numbers shifted only; assertion untouched by iteration 2). Failure mode: `per_test_boundary/1` raising or returning non-`:ok` with `:spec_cover_run` deleted. Seams: none — direct call on the shipped module.\n- The subject's other requirements are untouched: the rest of the diff is provably only `use`-line substitutions plus one surface path and one scenario `then`.\n\nHeadline deliverable — independently re-proved, with evidence the prior VERIFY did not have:\n- A full tagged run at base SHA `14f6e72` emits **133** `SpecLedEx.Case.\u003cfn\u003e is undefined or private` compile warnings (every fixture-helper call resolving against the shipped module while the fixture shadowed it at runtime). The same run at stage HEAD emits **0** — and 0 for `FixtureCase` too. The shadow is genuinely gone and it cleaned up 133 warnings nobody had reported.\n- Host-test-VM probe (temporary test file, run then removed, tree verified clean): `SpecLedEx.Case.module_info(:compile)[:source]` = `\u003cworktree\u003e/lib/specled_ex/case.ex`; `SpecLedEx.FixtureCase` loaded and carrying the helpers; `function_exported?(SpecLedEx.Case, :write_files, 2)` is `false`.\n- `SpecLedEx.Realization.Binding.resolve_with_source(\"SpecLedEx.Case\", nil)` → `{:ok, {:module, SpecLedEx.Case}, \"lib/specled_ex/case.ex\"}`. `.spec/realization_hashes.json` correctly untouched; the stored `api_boundary/SpecLedEx.Case` hash never moved (`b6f29877…`, hasher_version 1), because the resolver reads `module_info(:compile)[:source]` in the Mix VM, which the test-VM-only shadow never reached. `Code.ensure_loaded?(SpecLedEx.FixtureCase)` is `false` in the Mix VM — the helpers stay inert outside test runs, as `test/test_helper.exs:20-22` intends.\n\nCorpus-scoped claims on the touched spec hunks: 5 validated, all 5 by execution, 0 falsehoods.\n1. `coverage_capture.spec.md:806` given \"the production `SpecLedEx.Case` template is loaded\" — TRUE by execution (ebin contents; child control run exits 0).\n2. `coverage_capture.spec.md:811` then \"receives forwarded options (`async: true`)\" — TRUE and now falsifiable (mutant A exits 2). This was the prior VERIFY's one falsehood; it is repaired.\n3. `coverage_capture.spec.md:811` then \"runs the injected `setup {SpecLedEx.Coverage, :per_test_boundary}` before the test body\" — TRUE by execution (mutant B exits 2; `function_exported?(SpecLedEx.Coverage, :per_test_boundary, 1)` holds).\n4. `coverage_capture.spec.md:811` then \"the adopter module is an `ExUnit.Case`\" — TRUE by execution (`function_exported?(__MODULE__, :__ex_unit__, 0)` at :62).\n5. `mix_tasks.spec.md:45` surface `test_support/specled_ex_fixture_case.ex` — TRUE (file present; `mix spec.check --no-run-commands` green confirms no `surface_target_missing`).\n\nTargeted verification (all in the stage worktree):\n- `mix compile --warnings-as-errors` — exit 0.\n- `mix test test/specled_ex/case_test.exs test/specled_ex/policy_files_test.exs` — 19 tests, 0 failures.\n- `mix format --check-formatted` — exit 0. `mix deps.unlock --check-unused` — exit 0.\n- `mix spec.check --no-run-commands` — `status=pass errors=0 warnings=0`, exit 0.\n- bare `mix spec.check` — final run exit 0, `subjects=34 requirements=416 status=pass errors=0 warnings=0`. It failed once earlier on a pre-existing flake; see Notes.\n- `mix spec.next` — exit 0, `change_type=cross_cutting`, 27 impacted subjects (same classifier artifact the prior VERIFY noted; bare `spec.check` is green).\n- Full `mix test` not re-earned per bounce item 6; the iteration-2 delta is 3 files. I did nonetheless execute 8 heavy tagged-suite runs (944 tests each) across stage and base while chasing the flake below.\n\nVerdict: PASS\n\nNotes (advisory only — none change the verdict):\n- **Pre-existing flake, NOT caused by this ticket.** My first bare `mix spec.check` failed with one failing test: `test/integration/scenario_refactor_stable_test.exs:150` (\"cosmetic refactor inside closure produces no drift finding\") reporting a spurious `branch_guard_realization_drift` for subject A, exunit seed 194143. I chased it to ground rather than re-running past it:\n  - The file is **not in the cumulative diff**. It is `use ExUnit.Case, async: true` and mutates the *global* code server — `Kernel.ParallelCompiler.compile_to_path`, `:code.add_patha`, `:code.del_path`, `:code.purge`, `:code.delete` — on the globally-named modules `SpecLedEx.ScenarioFixture.A/B`. Two other `async: true` files do the same globally and are likewise not in the diff: `test/specled_ex/realization/api_boundary_test.exs` and `test/specled_ex/coverage/mfa_lines_test.exs` (the latter also calls `Code.put_compiler_option`). That is the race.\n  - Not order-dependent: seed 194143 passed on 6 subsequent stage runs, including `--max-cases 64`.\n  - **Reproduced at base.** A 12-iteration targeted stress of the five code-server-mutating files at base SHA `14f6e72` failed once (iteration 11) on exactly `scenario_refactor_stable_test.exs:150`; the identical 12-iteration stress at stage HEAD failed 0/12. Tally: stage 6/8 heavy runs green, base 6/6 heavy runs green but 1/12 on targeted stress. The hazard predates this branch.\n  - Worth its own ticket: flip `scenario_refactor_stable_test.exs` to `async: false`, or serialize the three code-server-mutating async files against each other. Not this ticket's scope, and it does not gate the merge — bare `mix spec.check` finishes green.\n  - Aside: the seed echo added in 0.6.0 is what made this tractable in one pass. It worked exactly as designed.\n- The `use SpecLedEx.FixtureCase` adopters get `use ExUnit.Case` twice (once from `__proxy__` with `__keys__(opts)`, once explicitly from the fixture's `using` block with `async: Keyword.get(opts, :async, true)`). Harmless and pre-existing — the rename did not introduce it, and the effective `async` value is unchanged for all 54 sites. Mentioned only so a future reader does not mistake it for rename damage.\n- `mix spec.next` still reports `reconciliation=needs subject updates` over 27 subjects. Artifact of the 51-file rename tripping the cross-cutting classifier, not spec debt.",
      "timestamp": "2026-07-25T13:22:52Z"
    },
    {
      "text": "VERIFY: FAIL\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\n**RETRACTION — please disregard the immediately preceding comment on this ticket.** That\n`VERIFY: PASS` text is dn4.3's verification report (fixture-case rename, `SpecLedEx.FixtureCase`,\n`scenario_refactor_stable_test.exs` flake analysis) and was posted here by mistake: my heredoc\nwrite was blocked by zsh `noclobber` because a sibling verifier in this same session had already\nwritten the scratchpad filename I chose, so `bw comment` uploaded that stale file's contents. `bw`\nhas no comment edit or delete, so I am correcting the record here rather than mutating the tracker\nstore by hand. THIS comment is dn4.4's verification. Its verdict is FAIL, not PASS.\n\nVerified cold at 462d6c4 in /Users/mz/src/specled_ex-arming-resolver, diffed against 6fb515d.\nThe core refactor is correct and complete. One narrow test-quality regression introduced by\nthis diff trips the standing [untested-clause] lesson; the fix is one line.\n\n## Mechanical checks\n\nTouched files: 7. Allowed-glob violations: 0. Out-of-Scope violations: 0.\n\n- `.spec/realization_hashes.json` — Allowed\n- `.spec/specs/coverage_capture.spec.md` — Allowed\n- `lib/specled_ex/coverage/arming.ex` (new) — Allowed\n- `lib/specled_ex/coverage/boundary.ex` — Allowed\n- `lib/specled_ex/coverage/formatter.ex` — Allowed\n- `test/specled_ex/coverage/boundary_test.exs` — Allowed\n- `lib/mix/tasks/spec.cover.test.ex` — conditionally Allowed (\"only if the arming write shape\n  changes\"). JUDGED AUTHORIZED. The write shape did NOT change (`:198` put_env is byte-identical),\n  so the literal condition is not met. But the ticket body separately names this file as the\n  \"third arming writer to keep consistent\", and the single added line\n  (`{:module, _} = Code.ensure_loaded(SpecLedEx.Coverage.Arming)`) joins the pre-existing\n  Formatter/Store/Snapshot residency block that exists for exactly this reason: in a child BEAM\n  the parent ebin must be on the code path before ExUnit boots formatter GenServers. Extracting\n  Arming as a new Formatter dependency forces this co-change. It is within the file's glob and\n  the conditional's intent. Flagging only that the completion record's \"Gate-forced co-changes\n  outside Allowed touches: none\" understates it.\n\nNot touched though Allowed: `CHANGELOG.md` (not required per-ticket; /open-pr owns release notes)\nand `test/specled_ex/coverage/arming_test.exs` (resolver tests were placed in boundary_test.exs\ninstead — permitted, but see Notes on tag attribution).\n\nOut-of-Scope files confirmed untouched: coverage.ex, snapshot.ex, store.ex, review/**,\ncoverage_triangulation.ex, docs/**, spec_review.spec.md, triangulation.spec.md, mix_tasks.spec.md.\nOut-of-Scope intent honored: one predicate (below), no new deps, no `Application.put_env` from\nlibrary code (the only put_env in lib/ is the Mix task writer at spec.cover.test.ex:198,\npre-existing), and `persistent_term` appears nowhere in lib/ or test/ (orchestrator ruling honored).\n\nGates, all run in the stage worktree:\n\n- `mix compile --warnings-as-errors` — pass\n- `mix test \u003c4 targeted files\u003e` — 47 tests, 0 failures (seed 352096)\n- `mix test` (full suite, run on my own initiative given shared-arming blast radius) — 3 properties,\n  1210 tests, 0 failures (seed 240225). Neither known flake (ya8, zr6) fired.\n- `mix spec.check --no-run-commands` — status=pass errors=0 warnings=0\n- `mix spec.check` (bare, executes verification commands; 1m58s wall vs seconds for structural,\n  confirming the tagged_tests commands really ran) — status=pass errors=0 warnings=0\n- `mix format --check-formatted` — pass\n- `mix deps.unlock --check-unused` — pass\n- Worktree clean after all runs (`.spec/state.json` is gitignored, so its absence from the diff\n  is correct, not an omission).\n\nThe `branch changed_files=61 findings=49` line in spec.check output is dn4.3's global fixture-case\nrename fan-out (commits 0e07235, 6fb515d), not dn4.4. dn4.4's own footprint is the 7 files above.\n`origin/main` (14f6e72) is a verified ancestor of HEAD, so no stale-base phantom findings.\n\n## Semantic checks\n\n**5a. Exactly one decode path — CONFIRMED.** `Application.get_env(@arming_app, @arming_key)` now\nappears exactly once in lib/: `arming.ex:21`. Zero survivors. All three pre-change decoders are\ngone: `Boundary.resolve_config/0`, `Formatter.armed/0`, and the seam-decoding half of\n`Formatter.load_boundary_index/0` (which now delegates to `Arming.resolve(:boundary)`).\nEvery other `spec_cover_run` hit in lib/ is either moduledoc prose or the Mix task's put_env writer.\n`@arming_app`/`@arming_key` exist only in arming.ex (5f — single-sourced).\n\n**5b. One table-validity predicate, stated in the spec — CONFIRMED.** `Arming.valid_table/1`\n(arming.ex:72-76) is the sole predicate: `:ets.info(tid) == :undefined -\u003e nil`, with\n`rescue ArgumentError -\u003e nil`. Spec text at coverage_capture.spec.md:218-220 states it verbatim:\n\"A boundary table is valid only when it is a live ETS table, defined as `:ets.info(tid) !=\n:undefined` (invalid identifiers and deleted tables are disarmed).\" `boundary_noop_unarmed`\n(:472-476) was updated to reference \"the arming resolver's `:ets.info/1` predicate\" and to add\n`true` to its no-op list. Both the old `is_reference/is_atom` test and the old \"any non-nil\"\nacceptance are gone. Per [stale-spec], both rewritten requirement texts name the mechanism\n(`SpecLedEx.Coverage.Arming.resolve/1`), and the rewritten scenario\n`boundary_noop_under_plain_mix_test` still names the seam it proves.\n\nI confirmed both predicate branches are reachable and distinct by execution (`mix run` probe):\n`:ets.info(make_ref())` RAISES ArgumentError; `:ets.info(\u003cdeleted tid\u003e)` returns `:undefined`;\n`:ets.info(nil)` and `:ets.info(:unknown_atom)` return `:undefined`; strings/integers/pids/tuples\nraise. So `boundary_test.exs:45` (make_ref) exercises the rescue branch and `:49-53` (deleted tid)\nexercises the `:undefined` branch. Neither clause is dead.\n\n**5c. The F5 disagreement — RESOLVED, and the pre-change divergence was worse than the ticket\nframed.** Reading the deleted code: for a bogus or stale `:boundary_table` the three call sites did\nthree different things. (i) `Boundary.head/1` had NO liveness gate at all — `resolve_config/0`\naccepted any non-nil tid, then `modules_for/1` called `:ets.lookup(tid, @modules_key)` with no\nrescue, so a stale tid raised ArgumentError out of the test's setup. (ii) `Boundary.tail/3` had the\n`table_alive?/1` rescue, so it silently no-oped. (iii) `Formatter.load_boundary_index/0` gated on\n`is_reference(tid) or is_atom(tid)` and then called `:ets.info(tid)` with no rescue — since\n`:ets.info(make_ref())` raises (proved above), a stale ref crashed the flush too; it returned `%{}`\nonly for non-ref/non-atom values or dead named tables. So the real divergence was\ncrash / silence / crash, not merely \"writes rows the formatter ignores.\"\nPost-change all three route through the same `Arming.resolve(:boundary)` call with the same\nargument, so they cannot disagree by construction: a bogus `:boundary_table` disarms both sides\n(Boundary writes nothing, formatter reads `%{}`), and `head/1` no longer raises. Strictly safer,\nand `boundary_test.exs:42-54` locks the new `head/1` behavior in.\nThe deliberate asymmetry is preserved: `decode(true, :formatter)` → `{:armed, config([])}`,\n`decode(true, :boundary)` → `:disarmed`.\n\n**5d. `default_snapshot_fn/1` — single-sourced.** One definition repo-wide, `arming.ex:78`, still\n`Snapshot.take(Snapshot.runtime_mode(), modules)`.\n\n**5e. `modules_cache_key/0` — deleted, zero live references.** Repo-wide grep (excluding .git,\n_build, deps) returns exactly one hit: `boundary_test.exs:122`, a `refute function_exported?`\nregression guard. Zero live callers; the old positive assertion at the former :97 is gone. The\ncache is still ETS (`Arming.modules/1`, arming.ex:26-36) with `@modules_key` private to Arming and\nno accessor. No `persistent_term` anywhere. Caching-once is still proven by\n`assert_received :modules_called` + `refute_received :modules_called` at boundary_test.exs:118-119.\n\n**Narrowing of the formatter's config plumbing — checked, safe.** `Formatter.init/1` used to pass\nthe ENTIRE seam keyword list into `Coverage.init/1`; it now passes exactly\n`snapshot_fn`/`modules_fn`/`artifact_path`. I read `Coverage.init/2` (coverage.ex:109-113): those\nare the only three keys it reads, and its defaults (`\u0026cover_modules_safe/0`,\n`@default_artifact_path`) are identical to the ones `Arming.config/1` now supplies, with the same\nseam-wins precedence the old `Keyword.merge(production_defaults(), seam_opts)` had. Nothing\nobservable is dropped.\n\n**Boundary.tail/3 race window — unchanged.** Old: `resolve_config` then `if table_alive?` then\ninsert. New: `resolve` (liveness inside) then insert. Same check-to-insert window.\n\n### Requirements coverage\n\n`specled.coverage_capture.formatter_arming_seam` (:211-238) — covered by\n`test/specled_ex/coverage/formatter_test.exs:114` (\"arming with `true` activates production\ndefaults\"), `:131` (keyword-list injection), `:146` (init/1 arg untrusted), `:82` (disarmed notice),\n`:95` (no-op events); resolver-specific clauses at\n`test/specled_ex/coverage/boundary_test.exs:56-64`.\nWould-fail-if: if `Arming.config/1` ignored the seam's `snapshot_fn` and used the production\ndefault, formatter_test.exs:138 `assert state.snapshot_fn == stub_fn` fails on function identity —\na NON-DEFAULT value, per [untested-clause]. If `decode(true, :formatter)` returned `:disarmed`,\nformatter_test.exs:116-118 fails. Seams used: `Application.put_env(:specled_ex, :spec_cover_run,\ntrue | keyword)`, plus `Formatter.init/1`'s own argument as the negative seam at :148.\n\n`specled.coverage_capture.boundary_noop_unarmed` (:470-478) — covered by\n`test/specled_ex/coverage/boundary_test.exs:28` (unset), `:32` (seam `true`), `:37` (keyword\nlacking :boundary_table), `:42` (NEW — invalid and deleted table), `:149` (tail no-op), and\n`test/specled_ex/case_test.exs:78`.\nWould-fail-if: if `valid_table/1` accepted any non-nil tid (the old Boundary behavior),\nboundary_test.exs:46 `assert Arming.resolve(:boundary) == :disarmed` fails AND the\n`flunk`-trap snapshot_fn at :43 fires. Both predicate branches (raise, `:undefined`) are\nindependently asserted. Seams used: the arming seam with a NON-DEFAULT bogus `:boundary_table`\n(`make_ref()`) and with a deleted real tid.\n\n`specled.coverage_capture.formatter_snapshot_fn_di` (:201-210) — covered by\n`test/specled_ex/coverage/formatter_test.exs` @moduletag (:8) plus `:131-143` specifically.\nWould-fail-if: `assert state.snapshot_fn == stub_fn` (:138) is an identity assertion on an injected\nnon-default closure; any resolver bug that substituted the default fails it. Non-default\n`artifact_path` is also genuinely load-bearing: `flush_suite/1` (:291-333) arms\n`artifact_path: tmp_path` and the tests at :374/:415/:448/:479/:510/:560/:588/:676 then call\n`Store.read_v2(tmp_path)` — if the resolver dropped `artifact_path`, the artifact would land at the\ndefault `.spec/_coverage/per_test.coverdata` and those reads would fail. `boundary_table`\npassthrough is likewise proven with a real table plus asserted boundary rows in the envelope.\nSeams used: arming seam only.\n\nApplying [untested-clause] to the two NEW resolver tests: boundary_test.exs:42 is strong (flunk\ntrap + both branches). boundary_test.exs:56 asserts only defaults (`is_function/2`, default\nartifact_path) — but its subject IS the default path, and non-default passthrough for all three\nkeys is asserted elsewhere as shown above, so no clause is left unfalsifiable. Both names match\nwhat their assertions can fail on.\n\n**9. Realization hashes — exact, no more and no less.** The only change is one ADDED key,\n`SpecLedEx.Coverage.Arming.resolve/1`. I confirmed all five relevant entries\n(`Arming.resolve/1`, `Boundary.head/1`, `Boundary.tail/3`, `Formatter`, `Formatter.init/1`) live in\nthe `api_boundary` tier, which hashes the public signature/spec surface rather than the body. The\nbodies of `Boundary.head/1`, `Boundary.tail/3` and `Formatter.init/1` changed heavily but their\narities and @specs did not, so their hashes correctly did not move — and bare `mix spec.check`\n(which runs the realization-drift check) passing is the authoritative proof that the committed\nhashes match what the hasher computes from current code. The deleted `modules_cache_key/0` never\nhad an entry, correctly, because it was `@doc false`. The `surface:` list gained\n`lib/specled_ex/coverage/arming.ex` (:83) and `realized_by.implementation` gained\n`SpecLedEx.Coverage.Arming.resolve/1` (:133), so the new file is mapped.\n\n## Verdict\n\nFAIL — on one narrow, one-line-fix Gap. Everything else in the ticket lands correctly: the three\ndecoders really are one, the single predicate is real and stated in the spec, the leak is closed\nwith ETS (not persistent_term), the arming asymmetry is preserved, scope is clean, and all eight\ngates including the full suite are green.\n\n## Gaps\n\n**G1 — [untested-clause] regression: `boundary_test.exs:103` now overclaims in its name.**\nThe test is named \"caches modules_fn result once under the reserved key\". Pre-change it asserted\n`assert [{_, [ModA, ModB]}] = :ets.lookup(tid, Boundary.modules_cache_key())` — a lookup BY the\nreserved key that also pinned the cached value. This diff replaced that with\n`assert :ets.info(tid, :size) == 1` + `refute function_exported?(Boundary, :modules_cache_key, 0)`.\nNeither assertion can fail if the cache row is stored under a DIFFERENT key, and neither can fail\nif the cached VALUE is wrong (store `[]` instead of `[ModA, ModB]` and the row is still present,\nsize is still 1, and `modules_fn` is still called exactly once because `:ets.lookup` returns a\nnon-empty result on the second `head/1`). So the \"under the reserved key\" clause in the test name\nis no longer falsifiable, and the modules-value plumbing is now unasserted anywhere: every\n`snapshot_fn` stub in boundary_test.exs and case_test.exs binds its argument as `_modules`/`_`, so\nno test observes which modules reach the snapshot.\nThis was avoidable collateral of deleting the accessor — a strictly stronger replacement needing no\naccessor was available, e.g. `assert [{key, [ModA, ModB]}] = :ets.tab2list(tid); assert is_atom(key)`.\nSuggested remediation (one line, plus optionally moving the `refute function_exported?` guard to\nsit with the leak-closure assertions at :42-64 where it belongs rather than inside a caching test).\nNo requirement clause is left unproven by this, and no gate fails — it is a test-strength\nregression, not a coverage hole.\n\n## Notes\n\n- The completion record's \"Gate-forced co-changes outside Allowed touches: none\" is inaccurate for\n  `lib/mix/tasks/spec.cover.test.ex`; I judged the edit authorized anyway (reasoning above), but the\n  record should have declared it.\n- Tag attribution is slightly imprecise: clauses ADDED to `formatter_arming_seam`'s text (Arming is\n  the sole decoder; it owns the private cache key and the shared defaults) are proven by tests in\n  `boundary_test.exs`, whose @moduletag carries only `boundary_hook_sync` and\n  `boundary_noop_unarmed`. The `:ets.info` predicate clause is correctly attributed, since\n  `boundary_noop_unarmed`'s own text now states it. Nothing is unproven and spec.check's tag\n  consistency passes; noted because an `arming_test.exs` (which was Allowed and not created) would\n  have made attribution clean.\n- `formatter_arming_seam`'s statement is now doing two jobs — the resolver contract and the\n  formatter-inertness contract — in one long requirement. If a future ticket splits the resolver\n  contract into its own requirement id, the tag attribution note above resolves itself.\n- Pre-existing and NOT introduced here: \"production default dispatches to\n  `Snapshot.take(Snapshot.runtime_mode(), modules)`\" is asserted only as `is_function(_, 1)` at the\n  unit level; its real behavior is covered indirectly by the child-BEAM run in\n  `spec_cover_test_test.exs`.\n- `mix spec.check`'s `branch findings=49` is dn4.3 fan-out noise, not a dn4.4 signal — worth\n  remembering for the next stage worktree on this branch.\n- Process note for the orchestrator: the shared per-session scratchpad directory is a collision\n  hazard when several verifiers run concurrently under one session id, and zsh `noclobber` turns\n  the collision into a silently wrong `bw comment` payload rather than a loud failure. Worth having\n  workers namespace scratchpad filenames by ticket id.",
      "timestamp": "2026-07-25T13:24:59Z"
    },
    {
      "text": "AUDIT: BOUNCE → implementer\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nAudited cold at 462d6c4 in /Users/mz/src/specled_ex-arming-resolver against base 6fb515d. Read-only:\nno gates re-run, no files changed, worktree clean throughout. The stray `VERIFY: PASS` immediately\npreceding the FAIL is dn4.3's report and was disregarded per its own retraction.\n\n## Reasoning\n\nG1 is real and I confirmed both halves independently. (a) `boundary_test.exs:121-122` replaced a\nlookup **by** `Boundary.modules_cache_key()` that also pinned `[ModA, ModB]` with\n`assert :ets.info(tid, :size) == 1` plus a `refute function_exported?` guard — after two `head/1`\ncalls and no `tail/3`, the table holds exactly one row no matter what key it sits under and no\nmatter what value it holds, so the test's own name (\"under the reserved key\") now claims a clause\nits assertions cannot fail on, and storing `[]` instead of `[ModA, ModB]` still leaves size 1 with\n`modules_fn` called once. That is precisely the standing [untested-clause] lesson, and this diff\nintroduced it while deleting the stronger assertion. (b) I traced every `snapshot_fn` stub that can\nreach `Arming.modules/1`: `boundary_test.exs:38, 43, 79, 109, 133` and `case_test.exs:36, 104` all\nbind `_modules`/`_`, so no unit test observes which modules reach the boundary snapshot. The one\nstub that does observe its argument — `formatter_test.exs:169`, `assert_received {:snapshot_called,\n[Fixture]}` at `:181` — is on the **formatter** path (`Coverage.install` → `suite_started`), which\nnever calls `Arming.modules/1`. One correction to the verifier's wording, disclosed and not\nverdict-changing: the boundary modules value is *indirectly* proven by the child-BEAM integration\ntest `spec_cover_test_test.exs:229-293`, which arms with production defaults and asserts\n`MapSet.size(lines_a) \u003e 0` / `lines_b \u003e 0` at `:281-282` — a wrong or empty modules list there\nyields zero hits and fails. So the plumbing is not literally unproven repo-wide; it is unproven at\nthe unit level, and the reserved-key clause in the test name remains unfalsifiable. FAIL stands and\nroutes to the implementer, not the verifier: the epic lesson is a check, not an advisory, and the\nremediation is one line.\n\nIndependently re-checked and CONFIRMED, everything else in the verifier's report:\n\n- **Scope — 7 files, zero violations.** `git diff --stat 6fb515d..HEAD` = the 7 files named, +155/-127.\n  Out-of-Scope files confirmed untouched. Out-of-Scope intent honored: one predicate only, no new\n  deps, `persistent_term` absent from `lib/` and `test/`, and the sole `Application.put_env` in\n  `lib/` is the pre-existing Mix-task writer at `spec.cover.test.ex:198`.\n- **`lib/mix/tasks/spec.cover.test.ex` AUTHORIZATION JUDGMENT — I agree, and the edit is minimal.**\n  I read `:185-215`. The `put_env` at `:198` is byte-identical, so the literal condition (\"only if\n  the arming write shape changes\") is indeed unmet — but the added line is one entry appended to the\n  head of an existing six-line residency block (`Formatter`, `Store`, `Snapshot`, `Coverage`,\n  `Boundary`, `Case`) whose stated purpose is exactly this: parent-app modules must be resident\n  before ExUnit boots formatter GenServers in a child BEAM. `Formatter.init/1` now calls\n  `Arming.resolve(:formatter)` at `formatter.ex:118`, making `Arming` a new first-hop dependency of\n  the formatter, so the block's invariant is incomplete without it. `Code.ensure_loaded/1` changes no\n  behavior beyond load timing and asserts on `{:module, _}` like its five siblings. Nothing wider\n  than disclosed, and no second edit hid in the file. The verifier's note that the completion\n  record's \"Gate-forced co-changes outside Allowed touches: none\" understates this is fair.\n- **One decode path.** `Application.get_env(@arming_app, @arming_key)` appears exactly once in\n  `lib/`: `arming.ex:21`. Every other `spec_cover_run` hit in `lib/` is moduledoc prose\n  (`coverage.ex:14,45`, `case.ex:10`, `boundary.ex:8`, `formatter.ex:47,51,64,68,154`,\n  `spec.cover.test.ex:36`) or the Mix task writer at `:198`. `@arming_app`/`@arming_key` live only in\n  `arming.ex:7-8`.\n- **Spec states the predicate and names the mechanism.** `coverage_capture.spec.md:215` names\n  `SpecLedEx.Coverage.Arming.resolve/1`; `:220-222` states `:ets.info(tid) != :undefined` verbatim;\n  `boundary_noop_unarmed` at `:476` says \"according to the arming resolver's `:ets.info/1`\n  predicate\" and `:475` adds `true` to the no-op list. [stale-spec] satisfied — both rewritten texts\n  name the mechanism rather than describing it generically.\n- **`modules_cache_key/0` — zero live references.** Repo-wide grep over `*.ex`/`*.exs`/`*.md`/`*.json`\n  excluding `_build`/`deps`/`.git`: exactly one hit, `boundary_test.exs:122`, the permitted\n  refute-guard. Definition gone from `boundary.ex`; the former positive assertion is gone.\n- **`realization_hashes.json` delta is exactly one added key** — a single 4-line hunk adding\n  `SpecLedEx.Coverage.Arming.resolve/1`. No other entry moved.\n- **Arming asymmetry still asserted, both directions.** `formatter_test.exs:114-129` (seam `true` →\n  formatter armed on production defaults, incl. `artifact_path == \".spec/_coverage/per_test.coverdata\"`)\n  and `boundary_test.exs:32-35` plus the new `:56-64` (same seam value → `Arming.resolve(:boundary)\n  == :disarmed`). `arming.ex:50-51` is the two-clause source of that asymmetry.\n- **Seam trace — no production path bypasses the resolver.** `Arming.resolve/1` has exactly four\n  consumers in `lib/`, all real production call sites: `boundary.ex:32` (`head/1`), `boundary.ex:51`\n  (`tail/3`), `formatter.ex:118` (`init/1`), `formatter.ex:403` (`load_boundary_index/0`, feeding\n  `flush/1`). No leftover direct decode survives anywhere — the three pre-change decoders\n  (`Boundary.resolve_config/0`, `Formatter.armed/0`, the seam half of\n  `Formatter.load_boundary_index/0`) are all deleted in the diff. Not INTEGRATION_REGRESSION_LATENT.\n  The disclosed second seam, `Formatter.init/1`'s own argument, is still proven untrusted at\n  `formatter_test.exs:146-155` (`refute state.snapshot_fn == smuggled_fn`).\n\n## Bounce reason\n\nOne fix, in `test/specled_ex/coverage/boundary_test.exs` only. Do not touch `lib/`, the spec, or\n`realization_hashes.json` — the refactor itself is correct and complete, and re-opening it would\nchurn hashes for nothing.\n\n1. In the test at `:103` (\"caches modules_fn result once under the reserved key\"), replace\n   `assert :ets.info(tid, :size) == 1` with an assertion that can fail on both the clause the name\n   claims and the cached value:\n\n       assert [{key, [ModA, ModB]}] = :ets.tab2list(tid)\n       assert is_atom(key)\n\n   `tab2list/1` needs no accessor, so the leak stays closed. This restores the pinned\n   `[ModA, ModB]` the diff dropped and makes \"under the reserved key\" falsifiable (a tuple-shaped or\n   test-key-shaped cache key now fails `is_atom/1`). Keep `assert_received :modules_called` /\n   `refute_received :modules_called` at `:118-119` unchanged — they are the caching-once proof and\n   must not be traded away.\n2. Move `refute function_exported?(Boundary, :modules_cache_key, 0)` out of the caching test and\n   into the leak-closure group at `:42-64` (its own one-line test, or appended to the test at `:56`).\n   It is a leak-closure regression guard, not a caching assertion; leaving it at `:122` is what made\n   the caching test look stronger than it is. This is required, not optional — the misplacement is\n   half of how G1 arose.\n3. Optional but preferred: also assert the modules value reaching the boundary snapshot, by binding\n   the argument in one existing stub instead of `_` — e.g. at `:109` use\n   `fn modules -\u003e send(parent, {:snapshot_modules, modules}); %{} end` and\n   `assert_received {:snapshot_modules, [ModA, ModB]}`. That closes the unit-level hole in the\n   `Arming.modules/1` → `snapshot_fn` plumbing directly rather than leaning on the child-BEAM\n   integration test at `spec_cover_test_test.exs:281-282`.\n4. Re-run and report: `mix compile --warnings-as-errors`, `mix test\n   test/specled_ex/coverage/boundary_test.exs test/specled_ex/coverage/formatter_test.exs\n   test/specled_ex/case_test.exs test/mix/tasks/spec_cover_test_test.exs`,\n   `mix spec.check --no-run-commands`, `mix format --check-formatted`. A full `mix test` is not\n   re-earned for a single test-file delta. Bare `mix spec.check` is not re-earned either — no spec or\n   `lib/` text changes.\n5. In the completion record, correct \"Gate-forced co-changes outside Allowed touches: none\" to\n   declare `lib/mix/tasks/spec.cover.test.ex` (conditionally-allowed file entered on the block's\n   intent, not on a changed write shape). The edit itself is authorized and stays as-is; only the\n   disclosure was wrong.",
      "timestamp": "2026-07-25T13:30:08Z"
    },
    {
      "text": "IMPLEMENTED\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nCommit: 945d91e\nFiles touched: test/specled_ex/coverage/boundary_test.exs\nTests added: strengthened cache row/key/value assertion, relocated modules_cache_key leak guard, asserted boundary snapshot receives [ModA, ModB]\nVerification stubs flipped: none\nVerification tier: change-scoped per audit — mix test test/specled_ex/coverage/boundary_test.exs; mix compile --warnings-as-errors; mix test test/specled_ex/coverage/boundary_test.exs test/specled_ex/coverage/formatter_test.exs test/specled_ex/case_test.exs test/mix/tasks/spec_cover_test_test.exs; mix spec.check --no-run-commands; mix format --check-formatted\nFinal-tier runs: 1\nAudit-expanded scope: none (single Allowed test file)\nGate-forced co-changes outside Allowed touches: lib/mix/tasks/spec.cover.test.ex (conditionally-allowed file entered on the block's intent, not on a changed write shape)\n\nSummary: Strengthened the boundary modules cache test so it now pins the cached modules value, validates the private reserved key shape without re-opening the accessor leak, and observes the modules passed to the snapshot function. Moved the deleted accessor regression guard into the leak-closure resolver group.",
      "timestamp": "2026-07-25T13:32:59Z"
    },
    {
      "text": "VERIFY: PASS\nSession: eb717ea1-b20d-491a-a5f9-322b223bfe82\nLog: /Users/mz/.claude-remembers/projects/-Users-mz-src-specled-ex/eb717ea1-b20d-491a-a5f9-322b223bfe82.jsonl\n\nCold re-verification of iteration 2 at 945d91e in /Users/mz/src/specled_ex-arming-resolver, cumulative\ndiff against base 6fb515d. Worked fresh: I re-derived the audit contract from the bounce text, read the\ndelta and the resolver source myself, and mutation-proved every new assertion rather than reasoning about\nit. The retracted stray `VERIFY: PASS` (dn4.3's report) was disregarded per its own retraction.\n\n## Mechanical checks\n\n**Iteration-2 delta scope — CLEAN, exactly one file.** `git diff --stat 462d6c4..945d91e` =\n`test/specled_ex/coverage/boundary_test.exs | 11 ++++++++---`, 8 insertions / 3 deletions, one file.\nZero new files, zero `lib/` changes, zero `.spec/` changes, `.spec/realization_hashes.json` untouched.\nThe audit's file scope (\"in `test/specled_ex/coverage/boundary_test.exs` only. Do not touch `lib/`, the\nspec, or `realization_hashes.json`\") was **obeyed exactly**.\n\n**Cumulative file set — IDENTICAL to iteration 1.** `diff \u003c(git diff --name-only 6fb515d..462d6c4)\n\u003c(git diff --name-only 6fb515d..HEAD)` is empty: the same 7 files, no additions, no removals. Cumulative\nstat: `.spec/realization_hashes.json` +4, `.spec/specs/coverage_capture.spec.md` +17,\n`lib/mix/tasks/spec.cover.test.ex` +1, `lib/specled_ex/coverage/arming.ex` +79 (new),\n`lib/specled_ex/coverage/boundary.ex` 92, `lib/specled_ex/coverage/formatter.ex` 60,\n`test/specled_ex/coverage/boundary_test.exs` +36 (was +25 at iteration 1). Allowed-glob violations: 0.\nOut-of-Scope violations: 0. The iteration-1 scope findings — including the judged-authorized\n`lib/mix/tasks/spec.cover.test.ex` residency line — carry forward unchanged because the bytes are\nunchanged; I re-confirmed the file's diff is still the single `Code.ensure_loaded(...Arming)` line.\n\n**Base freshness.** Remote `main` fetched over HTTPS (SSH agent is broken in this environment):\n`FETCH_HEAD = 14f6e7253841c67ad9097c606dedfbe1550a04bd`, identical to the local `origin/main`, and\n`git merge-base --is-ancestor origin/main HEAD` holds. No stale-base phantom findings. `spec.check`'s\n`branch changed_files=61 findings=49` remains dn4.3's fixture-case rename fan-out (0e07235, 6fb515d),\nnot a dn4.4 signal.\n\n**Working tree clean** at 945d91e before, between, and after every probe including five reverted\nmutations (`git status --porcelain --untracked-files=all` empty each time).\n\n## Semantic checks\n\n### Both audit items executed — YES, both, plus the optional third\n\n**Item 1 (required) — DONE, verbatim as prescribed.** `boundary_test.exs:126-127` now reads:\n\n    assert [{key, [ModA, ModB]}] = :ets.tab2list(tid)\n    assert is_atom(key)\n\n`assert :ets.info(tid, :size) == 1` is gone. This is strictly stronger than the size assertion it\nreplaced: a single-element list pattern asserts \"exactly one row\" *and* pins the cached value *and*\nconstrains the key shape, where `size == 1` asserted only the first. `assert_received :modules_called` /\n`refute_received :modules_called` are KEPT unchanged at `:122-123`, as the bounce required — not traded\naway.\n\n**Item 2 (required) — DONE.** `refute function_exported?(Boundary, :modules_cache_key, 0)` is gone from\nthe caching test and now sits at `:64`, appended to the test at `:56` (\"shared resolver keeps true armed\nfor formatter and disarmed for boundary\") inside the leak-closure group at `:42-64` — exactly the\n\"appended to the test at `:56`\" option the bounce offered.\n\n**Item 3 (optional but preferred) — ALSO DONE.** The stub at `:110-113` now binds its argument:\n`fn modules -\u003e send(parent, {:snapshot_modules, modules}); %{} end`, with\n`assert_received {:snapshot_modules, [ModA, ModB]}` at `:124`. This closes the unit-level hole in the\n`Arming.modules/1` → `snapshot_fn` plumbing directly, so the boundary modules value no longer leans on\nthe child-BEAM integration test at `spec_cover_test_test.exs:281-282`.\n\n### [untested-clause] applied to the NEW assertions — all five mutation-killed\n\nI did not reason about falsifiability; I mutated `lib/` and ran the test. Every mutant was reverted via\n`git checkout --` and the tree re-verified clean. Each mutation killed **exactly** its intended\nassertion line, with no assertion left unfalsifiable:\n\n| Mutation (in `lib/`, reverted) | Killed at | Failure output |\n| --- | --- | --- |\n| A. `Arming.modules/1` caches `[]` instead of `modules` (`arming.ex:33`) | `boundary_test.exs:126` | `match (=) failed / left: [{key, [ModA, ModB]}] / right: [__specled_boundary_modules__: []]` |\n| B. `@modules_key` becomes `{:specled, :modules}` (`arming.ex:9`) | `boundary_test.exs:127` | `code: assert is_atom(key)` |\n| C. caching removed — `modules_fn` called every `head/1` | `boundary_test.exs:123` | `refute_received :modules_called` fires |\n| D. `head/1` passes `[]` to `snapshot_fn` (`boundary.ex:38`) | `boundary_test.exs:124` | `Assertion failed, no matching message after 0ms` |\n| E. `modules_cache_key/0` accessor reintroduced on `Boundary` | `boundary_test.exs:64` | `code: refute function_exported?(Boundary, :modules_cache_key, 0)` |\n\nSo: wrong cached value → A; tuple/non-atom cache key → B; a second `modules_fn` call → C; wrong modules\nreaching the snapshot → D; the leak reopened → E. The un-mutated control is green (9 tests, 0 failures).\nG1 is closed, and the test's name (\"caches modules_fn result once under the reserved key\") is now\nfalsifiable on all three of its clauses — *caches* (C), *once* (C), *under the reserved key* (B) — plus\nthe value it caches (A), which the name implies and the pre-change assertion had pinned.\n\n**Accepted residual, disclosed, not a gap.** `assert is_atom(key)` is a shape assertion, not an identity\nassertion: a *different* atom key would still pass. Identity is unavailable by design — pinning it\nrequires the accessor the ticket was asked to delete, and `:ets.tab2list/1` is precisely the\nno-accessor route the bounce prescribed. I checked whether the residual hides a live hazard and it does\nnot: `@modules_key` (`arming.ex:9`) is private and used symmetrically by `Arming.modules/1` at `:27`\nand `:33`, and `Arming.boundary_index/1` filters the cache row out by its `{@modules_key, _modules}`\nclause at `:43` with the `is_tuple(key) and is_map(row)` guard at `:44` as a second net (a modules list\nis not a map). Any atom key is consistent end-to-end, so no observable behavior rides on which atom it\nis.\n\n### Iteration-1 semantic findings re-spot-checked\n\nThe refactor bytes did not move in iteration 2, so iteration 1's semantic verdict carries forward. I\nindependently re-confirmed the load-bearing structural claims rather than taking them on faith:\n`Application.get_env(@arming_app, @arming_key)` appears exactly once in `lib/` (`arming.ex:21`);\n`Arming.valid_table/1` (`arming.ex:72-76`) is the sole table-validity predicate, `:ets.info(tid) ==\n:undefined` with `rescue ArgumentError`; the asymmetry is two explicit clauses at `arming.ex:49-50`\n(`decode(true, :formatter)` → armed on defaults, `decode(true, :boundary)` → `:disarmed`);\n`default_snapshot_fn/1` has one definition repo-wide (`arming.ex:78`); `Arming.resolve/1` has exactly\nfour production consumers (`boundary.ex:32`, `boundary.ex:51`, `formatter.ex:118`, `formatter.ex:403`);\nand `modules_cache_key/0` is absent from `boundary.ex` with its only repo-wide mention being the\nrefute-guard now at `boundary_test.exs:64`.\n\n### Requirements coverage — the three `must`s\n\nAll three are `priority: must` and all three sit under `execute: true` `tagged_tests` stubs\n(`coverage_capture.spec.md:906`, `:912`, `:957`). Line numbers below are re-read at 945d91e; note the\nformatter_test citations in the iteration-1 report were off by one.\n\n**`specled.coverage_capture.formatter_arming_seam`** (`coverage_capture.spec.md:213-238`) — covered by\n`test/specled_ex/coverage/formatter_test.exs:115` (seam `true` → production defaults, incl.\n`artifact_path == \".spec/_coverage/per_test.coverdata\"` at `:122`), `:132` (keyword-list injection),\n`:147` (`init/1`'s own argument untrusted), `:83` (disarmed notice), `:96` (every event no-ops), plus the\nresolver-specific clauses at `test/specled_ex/coverage/boundary_test.exs:56-64`.\n*Would-fail-if:* if `Arming.config/1` ignored the seam's `snapshot_fn`, `formatter_test.exs:140`\n`assert state.snapshot_fn == stub_fn` fails on function identity against a NON-DEFAULT injected closure;\nif `init/1`'s argument were trusted, `:153` `refute state.snapshot_fn == smuggled_fn` fails; if\n`decode(true, :formatter)` returned `:disarmed`, `:118` fails. The newly-added \"owns the private ETS\nmodules-cache key\" clause is now falsifiable by mutants B and E above.\n*Seams:* `Application.put_env(:specled_ex, :spec_cover_run, true | keyword)` (production key/shape,\nidentical to `spec.cover.test.ex:198`), plus `Formatter.init/1`'s own argument as the negative seam.\n\n**`specled.coverage_capture.boundary_noop_unarmed`** (`coverage_capture.spec.md:470-478`) — covered by\n`boundary_test.exs:28` (unset), `:32` (seam `true`), `:37` (keyword lacking `:boundary_table`), `:42`\n(invalid `make_ref()` and deleted-table branches), `:154` (`tail/3` no-op), and\n`test/specled_ex/case_test.exs:79` (`per_test_boundary/1` under plain `mix test`).\n*Would-fail-if:* if `valid_table/1` accepted any non-nil tid (the old `Boundary` behavior),\n`boundary_test.exs:46` `assert Arming.resolve(:boundary) == :disarmed` fails AND the `flunk`-trap\n`snapshot_fn` at `:43` fires. Both predicate branches are separately reachable and separately asserted:\n`:ets.info(make_ref())` raises `ArgumentError` (rescue branch, `:45-47`) and `:ets.info(\u003cdeleted tid\u003e)`\nreturns `:undefined` (`:49-53`). Neither clause is dead.\n*Seams:* the arming seam carrying NON-DEFAULT bogus (`make_ref()`) and deleted-real-tid\n`:boundary_table` values.\n\n**`specled.coverage_capture.formatter_snapshot_fn_di`** (`coverage_capture.spec.md:201-212`) — covered by\n`formatter_test.exs` `@moduletag` (`:6`) plus `:132-144` specifically.\n*Would-fail-if:* `:140` `assert state.snapshot_fn == stub_fn` and `:141`\n`assert state.modules_fn == modules_fn` are identity assertions on injected non-default closures — any\nresolver bug substituting the production default fails them. Non-default `artifact_path` is separately\nload-bearing: `flush_suite/1` arms `artifact_path: tmp_path` and the downstream tests read\n`Store.read_v2(tmp_path)`, so dropping `artifact_path` from the resolved config would send the artifact\nto the default path and fail those reads.\n*Seams:* arming seam only.\n\nIteration 2 changed no requirement text and no `@moduletag`/`@tag`, so tag attribution is unchanged from\niteration 1; `mix spec.check --no-run-commands` tag consistency passes. See Notes for the one carried-\nforward attribution nit.\n\n## Verdict\n\n**PASS.** Both required audit items were executed as written, the optional third was executed too, the\niteration-2 delta stayed strictly inside the audit's declared file scope (one test file, zero `lib/`,\nzero spec, zero hash changes), the cumulative file set is byte-identical in membership to iteration 1,\nand every new assertion is mutation-proved to fail on the clause it claims. G1 is closed with no new\n[untested-clause] debt introduced. All gates green.\n\n## Gates (all run in the stage worktree at 945d91e)\n\n- `mix compile --warnings-as-errors` — exit 0 (and again after reverting all five mutations).\n- `mix test test/specled_ex/coverage/boundary_test.exs test/specled_ex/coverage/formatter_test.exs\n  test/specled_ex/case_test.exs test/mix/tasks/spec_cover_test_test.exs` — 47 tests, 0 failures\n  (seed 999587). Added the 4th file beyond the brief's three since it is the ticket's own verification\n  command.\n- `mix spec.check --no-run-commands` — `status=pass errors=0 warnings=0`, exit 0.\n- `mix spec.check` (bare) — exit 0, `subjects=34 requirements=416 status=pass errors=0 warnings=0`. Run\n  on my own initiative because the changed file *is* itself a tagged test, so the bare form is what\n  actually executes the strengthened assertions through the spec's `execute: true` commands. Wall time\n  was minutes against seconds for the structural form, confirming the `tagged_tests` commands really ran.\n- `mix format --check-formatted` — exit 0. `mix deps.unlock --check-unused` — exit 0.\n- Full `mix test` not re-earned (green at iteration 1 with 1210 tests / 0 failures; delta is one test\n  file; the merge gate owns the full suite). Neither known flake (ya8 `scenario_refactor_stable_test.exs:150`,\n  zr6 `tracer_test.exs:255`) fired in anything I ran.\n\n## Notes\n\n- **Carried-forward attribution nit (advisory, unchanged by iteration 2).** The clauses ADDED to\n  `formatter_arming_seam`'s text — Arming is the sole decoder; it owns the private cache key and the\n  shared defaults — are proven by tests in `boundary_test.exs`, whose `@moduletag` carries only\n  `boundary_hook_sync` and `boundary_noop_unarmed`. Item 2's relocation moved the leak guard between two\n  tests in the same module, so attribution is neither better nor worse than iteration 1. Nothing is\n  unproven and `spec.check` tag consistency passes; an `arming_test.exs` (Allowed, not created) would\n  have made this clean, and splitting the resolver contract into its own requirement id would resolve it\n  permanently.\n- The completion record correctly declares `lib/mix/tasks/spec.cover.test.ex` under \"Gate-forced\n  co-changes outside Allowed touches\" this time — bounce item 5 executed.\n- **Process, for the orchestrator.** The zsh `noclobber` scratchpad hazard is real and I hit it myself:\n  my first bare `mix spec.check` invocation never ran because `\u003e` onto an existing scratchpad log was\n  refused, and the surviving stale file made the wrapper report a spurious exit 1. Re-run with a\n  ticket-and-agent-namespaced filename and `\u003e|` was clean. Every worker should namespace scratchpad\n  filenames by ticket id AND agent name; verifying file contents immediately before `bw comment` is the\n  only reliable guard, and it is what I did for this comment.",
      "timestamp": "2026-07-25T13:44:21Z"
    }
  ],
  "priority": 1,
  "status": "in_progress",
  "title": "Unify the arming-seam decoders behind one resolver (F5)",
  "type": "task",
  "updated_at": "2026-07-25T13:44:21Z"
}
