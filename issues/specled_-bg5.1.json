{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [
    "specled_-bg5.4"
  ],
  "created": "2026-08-06T23:10:46Z",
  "description": "No spec subject (candidates: specled.append_only when picked up). Severity: Medium (calibrated down from spec auditor H1). From 2026-08-06 critical review of specled_-q0q. The self_authorized_weakening marker is emitted per (requirement, class) by the class-specific detectors; a requirement weakened via a path only one detector sees can surface differently depending on detector order. Keep-reason: unmade design decision — ticket q0q.2 simultaneously required uniform marker emission and forbade touching authorizing_decision/2, so a uniform-emission design (single choke point) needs its own decision + ADR. Concrete failure: a requirement weakened in class A while suppressed in class B can yield marker sets that depend on detector iteration order rather than a stated contract.\n\n## ORCHESTRATOR CONTRACT (specled_-i4x)\n\nAdvances: specled.append_only\n\nDECISION-CLASS: yes. Deliverable is an ADR + spec amendment + code. Per operator ruling on\nepic specled_-i4x, draft it fully and record completion normally — the orchestrator will NOT\nmerge it without human sign-off.\n\nWHERE THE TICKET IS MISLOCATED (read before designing): analyze/4\n(lib/specled_ex/append_only.ex:100-115) runs detectors in a FIXED order and sort_findings/1\nthe result, so output is not \"detector iteration order\" dependent in the naive sense. The\nticket's claim is directionally right but points at the wrong mechanism. The three REAL\ndefects are:\n\n1. authorizing_decision/2 (lib/specled_ex/append_only.ex:698-722) is Enum.reduce_while over\n   `decisions`, halting on the FIRST ADR whose affects contains the id with a weakening\n   change_type. self_authorized_marker_if_new/6 (:673-686) then tests only THAT ADR against\n   new_adr_ids. With two ADRs authorizing the same requirement — one pre-existing, one\n   new-in-diff — whether a marker is emitted depends purely on the order of `decisions`.\n2. self_auth_adr_by_req_id/1 (:665-669) is a Map.put fold: with two new self-authorizing ADRs\n   affecting the same requirement, the LAST one wins and is named in the marker message.\n3. detect_requirement_deleted has a SECOND emission path (:132-138) that emits the marker\n   straight from self_auth_adr_by_req_id without consulting authorizing_decision/2, while\n   detect_must_downgraded (:172), detect_scenario_regression (:225) and\n   detect_negative_removed (:264) all route through authorizing_decision/2 -\u003e\n   self_authorized_marker_if_new/6. Two authorization contracts, one function apart.\n\nWHY A SPEC AMENDMENT IS UNAVOIDABLE: specled.append_only.self_authorized_weakening\n(.spec/specs/append_only.spec.md:135-146) states NO cardinality and NO tie-break rule. The\n\"one marker per (requirement, weakening class)\" contract exists only as a test comment\n(test/specled_ex/append_only_test.exs:1255-1271). Your ADR must state the cardinality and the\ntie-break, and the spec statement must carry it.\n\nAllowed touches:\n- lib/specled_ex/append_only.ex\n- .spec/specs/append_only.spec.md\n- .spec/decisions/specled.decision.*.md  (new ADR: uniform marker emission / authorization tie-break)\n- test/specled_ex/append_only_test.exs\n- test_support/append_only_fixtures.ex\n- .spec/realization_hashes.json\n- CHANGELOG.md\n\nOut of Scope (files):\n- lib/specled_ex/branch_check.ex\n- lib/specled_ex/branch_check/severity.ex\n- lib/specled_ex/review/html.ex\n- lib/mix/tasks/**\n- docs/**\n- test/test_support/emitter_codes.ex\n\nOut of Scope (intent):\n- Do NOT add an ADR-status check to authorizing_decision/2. That is specled_-bg5.4, which is\n  wired to depend on this ticket. Landing both blind produces two conflicting rewrites of the\n  same function.\n- Do NOT change :info severity or stdout rendering. That is specled_-bg5.5.\n- Do NOT add a new append_only/* code — specled.decision.append_only_finding_budget_v2 governs\n  the budget.\n- Do NOT delete the self_auth_adr_by_req_id fast path without a stated replacement contract;\n  it is what makes the superset-affects deletion scenario pass\n  (.spec/specs/append_only.spec.md:344-354).\n\nVerification:\n  mix test test/specled_ex/append_only_test.exs\n  mix test test/specled_ex/branch_check_test.exs\n  mix compile --warnings-as-errors\n  mix format --check-formatted\n  mix spec.check --no-run-commands\n  bash ./scripts/check_specs.sh\nNo execute:false stub to flip — .spec/specs/append_only.spec.md has zero execute:false entries.\nNew scenarios attach to the existing tagged_tests block (.spec/specs/append_only.spec.md:400-417),\nalready execute:true; add to its covers: only if a new requirement id is minted.\n\nExpected diff: ~120-200 lines across 5-6 files (ADR ~60, append_only.ex ~40-60, spec ~10, tests ~60).\n",
  "id": "specled_-bg5.1",
  "labels": [
    "phase:implementing"
  ],
  "parent": "specled_-bg5",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: i4x-bg5.1\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-bg5.1/codex-i4x-bg5.1/log\n\nCommit: f192c073e7a80be543119f0108de327a5d31bd63\nFiles touched: .spec/decisions/specled.decision.uniform_self_authorization_markers.md, .spec/specs/append_only.spec.md, lib/specled_ex/append_only.ex, test/specled_ex/append_only_test.exs\nTests added: SpecLedEx.AppendOnlyTest \"new authorizer wins over pre-existing authorizer regardless of decision order\"; SpecLedEx.AppendOnlyTest \"lexicographically first new authorizer wins deletion tie regardless of order\"\nVerification stubs flipped: none\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/append_only_test.exs test/specled_ex/branch_check_test.exs (61 tests, 2 properties); mix test --stale (1325 tests, 3 properties); mix format --check-formatted; mix spec.check --no-run-commands; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\n\nSummary: Unified all four weakening detectors on an order-independent authorization selector that prefers new-in-diff ADRs and breaks same-newness ties by lexicographic ADR id. The amended subject contract and new ADR define one marker per requirement/class, while focused regression tests prove pre-existing masking and deletion-path last-write-wins attribution are eliminated.",
      "timestamp": "2026-08-12T11:57:20Z"
    },
    {
      "text": "VERIFY: FAIL\nSession: 9a42db78-611e-4632-882a-adaefdaa666f\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/9a42db78-611e-4632-882a-adaefdaa666f.jsonl\n\nMechanical checks:\n- Files touched: 4 (.spec/decisions/specled.decision.uniform_self_authorization_markers.md, .spec/specs/append_only.spec.md, lib/specled_ex/append_only.ex, test/specled_ex/append_only_test.exs)\n- Files outside Allowed globs: 0\n- Files matching Out-of-Scope globs: 0\n- realization_hashes.json unchanged and correct to be unchanged: baseline key is the MFA-scoped \"SpecLedEx.AppendOnly.analyze/4\" (.spec/realization_hashes.json:98), resolved_via beam, hasher_version 1. The diff changes analyze/4's body (one call-site arity change at :90) but adds no public export and does not alter analyze/4's head. `mix spec.check --no-run-commands` reports findings=0 with realization drift in scope -\u003e no drift, so leaving the baseline untouched is correct.\n\nThe three named defects:\n- (a) order-independent authorizer selection: CLOSED IN BEHAVIOR, UNDER-TESTED. authorizing_decision/3 (append_only.ex:672-700) now filters all matching decisions and picks via Enum.min_by on {not new?, adr_id}; false \u003c true so new-in-diff wins. Test: \"new authorizer wins over pre-existing authorizer regardless of decision order\" (append_only_test.exs:854), asserts both [old, new] and [new, old] orders. Mutant caught (executed): replacing the selector with hd(authorizers) -\u003e test fails with markers == [] under old-first order. Mutant NOT caught -\u003e see Gap 1.\n- (b) deterministic tie-break among new authorizers: CLOSED. Test: \"lexicographically first new authorizer wins deletion tie regardless of order\" (append_only_test.exs:905), both [d-a, d-z] and [d-z, d-a]. Mutant executed: hd(authorizers) -\u003e fails, marker names `d-z` instead of `d-a`. assert_self_auth_marker/4 (:1348-1361) does `assert [marker] = markers`, so it also pins cardinality at exactly one per (requirement, class). self_auth_adr_by_req_id/1 is deleted (0 occurrences remain).\n- (c) deletion-path second emission unified: CLOSED. detect_requirement_deleted (:115-142) now has the same shape as the other three: authorizing_decision/3 -\u003e {:authorized, adr} -\u003e self_authorized_marker_if_new/6. Verified all four call sites read identically (:118, :151, :204, :244) and no detector calls self_authorized_weakening_finding/5 directly any more. The fast path's replacement contract is stated in the ADR (\"The deletion fast path is replaced by this shared selection rule. Superset `affects` deletion remains covered because every authorizing ADR is considered...\"), and the superset scenario test at append_only_test.exs:658 passes on its own (`mix test ...:658` -\u003e 1 test, 0 failures). self_authorizing_adrs/2 is retained and still feeds detect_same_pr_self_authorization/2 (:87, :102), so the per-ADR warning is untouched.\n\nSpec amendment:\n- Cardinality now stated in the must: \"Exactly one marker shall be emitted per (requirement id, weakening class).\"\n- Tie-break now stated in the must: \"When multiple ADRs authorize that pair, a new-in-diff ADR shall take precedence over a pre-existing ADR, then the lexicographically smallest ADR id shall break ties within the same newness class. The chosen ADR is independent of input decision order.\" Plus the corrected \"authorized ONLY by ADRs already present at base shall emit no marker.\"\n- ADR justifies both: yes. specled.decision.uniform_self_authorization_markers Decision section states the one-marker-per-pair rule and the 3-step selection (consider all weakening-set authorizers -\u003e prefer new-in-diff -\u003e lexicographic id), and Consequences calls the lexicographic key \"a policy tie-break, not a chronology signal\". ADR is registered in the subject's decisions: list (append_only.spec.md:39). It mints no finding code and does not collide with specled.decision.append_only_finding_budget_v2 (change_type: clarifies, no `replaces`, budget still 13 codes).\n\nAppend-only self-check:\n- mix spec.check --no-run-commands findings against this diff: NONE. `branch base=origin/main changed_files=4 findings=0 (error=0 warning=0 info=0)`; re-run with --verbose (info shown) also reports 0. validate status=pass, spec.check result=pass. origin/main == bb64e69e, so the guard's base is the contract base.\n- If a weakening finding fired, is it authorized by this diff's own ADR: no finding fired, and that is correct rather than lucky. The must statement is extended, not weakened: ModalClass sees \"shall emit\" on both sides (no downgrade), no requirement is deleted, scenario count goes UP by one (scenario.self_authorization_tie_break added), and no polarity is removed. The one clause edit — \"authorized by\" -\u003e \"authorized ONLY by\" ADRs present at base — narrows the no-emit case, i.e. strengthens emission. Independently: the new ADR carries change_type: clarifies, which is NOT in @weakening_set, so it could not have authorized a weakening even if one existed; had this diff actually weakened the subject, append_only/requirement_deleted or must_downgraded would have fired as an error. So neither same_pr_self_authorization nor self_authorized_weakening is applicable to this diff.\n\nSemantic checks:\n- Out of Scope (intent) held: no ADR-status check in authorizing_decision/3 (the only \"status\" read in the module is the pre-existing adr_affects_widened check at :294, untouched by the diff) -\u003e bg5.4 stays open; no severity or rendering change (marker still :info via self_authorized_weakening_finding/5, and lib/specled_ex/branch_check*, review/html.ex, mix/tasks/** are untouched); no new append_only/* code (diff adds zero `code:` literals); fast path deleted only with the ADR-stated replacement contract and the superset scenario still green.\n- Spec stubs run: 0 executed (--no-run-commands tier; append_only.spec.md has zero execute:false entries, so none needed flipping).\n- Targeted verification (all from the stage worktree, host `mix`): mix compile --warnings-as-errors -\u003e clean; mix format --check-formatted -\u003e clean; mix test append_only_test.exs + branch_check_test.exs -\u003e 61 tests, 2 properties, 0 failures; mix spec.check --no-run-commands -\u003e result=pass. Tree confirmed clean (git status --porcelain empty) after all mutation experiments were reverted.\n- Full-suite verification: not run by verifier (by design)\n\nVerdict: FAIL\n\nGaps (if FAIL):\n- GAP 1 (single blocker, test-net only — production behavior is correct). The ADR's selection rule step 2, \"prefer new-in-diff ADRs over ADRs already present at base\", is NOT discriminated by any test. Executed proof: mutate authorizing_decision/3's min_by key from {not MapSet.member?(new_adr_ids, adr_id), adr_id || \"\"} to {adr_id || \"\"} — dropping the newness preference entirely, leaving a plain lexicographic sort — and the whole append_only suite stays GREEN (41 tests, 0 failures). The reason is fixture-id choice: the test at :854 names the pre-existing ADR \"d-old\" and the new one \"d-new\", and \"d-new\" \u003c \"d-old\" lexicographically, so lexicographic order accidentally agrees with newness order. Renaming to pre-existing \"d-aaa\" / new \"d-zzz\" and re-running the same mutant makes it fail with markers == [] — i.e. the pre-existing ADR masks the same-diff self-authorization and NO marker is emitted, which is exactly defect F1 that this ticket exists to close. With the shipped code and the renamed ids the test passes, confirming production genuinely prefers the new ADR. Fix is two string literals in test/specled_ex/append_only_test.exs:854-897: pick ids where lexicographic order OPPOSES newness order (pre-existing lexicographically first, new-in-diff lexicographically last). Without it, a future simplification of the selector to a plain sort silently reintroduces F1 with CI green, and the spec/ADR rule has no enforcement.\n\nNotes (advisory only, not blocking):\n- The new scenario specled.append_only.scenario.self_authorization_tie_break carries `covers: []`, while its sibling scenario.self_authorized_weakening_marker covers specled.append_only.self_authorized_weakening. Validation passes either way, but the tie-break scenario is the one that now carries the amended must's cardinality + tie-break clauses; listing that requirement in its covers would make the spec-to-scenario link explicit.\n- Minor behavior change not mentioned in the ADR: the :missing_change_type branch changed from last-match-wins (reduce_while overwriting acc) to first-match-wins (Enum.find). Immaterial — the returned decision is discarded at the only consumer (append_only.ex:134 binds it as _adr), and the actual append_only/missing_change_type findings come from the independent detect_missing_change_type/2 pass over all decisions. No test pins it. Worth one ADR sentence if bg5.4 touches this function again.\n- self_authorized_marker_if_new/6 still re-checks new_adr_ids after the selector already ranked by newness. Harmless and correct (the selector may legitimately return a pre-existing ADR when no new one authorizes), just now a second-order guard rather than the primary decision.",
      "timestamp": "2026-08-12T12:06:51Z"
    },
    {
      "text": "TEST_CRITIC: PASS\nSession: 9a42db78-611e-4632-882a-adaefdaa666f\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/9a42db78-611e-4632-882a-adaefdaa666f.jsonl\n\nGraded: HEAD f192c073 on base bb64e69e (epic specled_-i4x).\n\n- Both-permutation coverage: Genuinely both, not a fixed order with an order-independence claim in the name. Test A builds [old_adr, new_adr] and [new_adr, old_adr] at test/specled_ex/append_only_test.exs:893-894, asserted at :897 and :898. Test B builds [adr_a, adr_z] and [adr_z, adr_a] at :932-933, asserted at :936 and :937. The second permutation is load-bearing in both cases: under each mutant only ONE of the two orderings goes red, so a single-permutation test would have been vacuous.\n\n- Mutant results: Mutation run on a scratch `git archive` copy outside the repo; worktree never touched (git status --porcelain empty at exit). Scratch baseline 41 tests + 2 properties, 0 failures. MUTANT A (Defect A, revert selector to first-match-wins: Enum.min_by(...) -\u003e hd(authorizers) at lib/specled_ex/append_only.ex:683-690) =\u003e 2 failures. Test A dies at :897 (old_first, markers == [] because pre-existing d-old won and self_authorized_marker_if_new suppressed emission); Test B dies at :937 (marker named d-z). MUTANT B (Defect B, keep new-in-diff preference but revert intra-new tie-break to List.last, i.e. the old Map.put fold) =\u003e 1 failure: Test B dies at :936 (a_first, marker named d-z, expected d-a); Test A correctly stays green since it does not target Defect B. Clean split: Test A catches Defect A, Test B catches Defect B and also Defect A.\n\n- Tie-break pins the actual winner: Yes, it pins the specific ADR id. assert_self_auth_marker/4 (append_only_test.exs:1348-1361) filters to exactly one marker by (code, entity_id, weakening_class) via `assert [marker] = markers` at :1356, then pins the ADR with `marker.message =~ \"ADR `#{adr_id}`\"` at :1359. Production names exactly one ADR per marker (lib/specled_ex/append_only.ex:514), so the losing ADR cannot satisfy it, and a nondeterministic tie-break would fail one permutation or the other.\n\n- Fixture hygiene: Clean. test_support/append_only_fixtures.ex is pure constructors end to end (requirement/3, scenario/1, adr/1, state_fixture/1) with no ETS, Agent, process state, or disk I/O; nothing leaks across tests, and `use ExUnit.Case, async: true` at :3 is safe. Test B's ids d-a / d-z are unambiguous under both byte and locale collation (ASCII lowercase, differ at byte 3). BUT Test A's ids are not: \"d-new\" \u003c \"d-old\" lexicographically, so the NEW ADR happens to sort first, and Test A cannot discriminate \"prefers new-in-diff\" from \"prefers lexicographically smallest\".\n\n- Sweep: No Process.sleep anywhere in the file. Tests assert on analyze/3 findings output (outcome), not internal state or call sequences. No seam or substitution: AppendOnly.analyze/3 is the same public entry the sole production caller uses (lib/specled_ex/branch_check.ex:421); no behaviour, mock, or injected module in the path. Neither test is a tautology (both go red under their mutants). Test names carry precondition and outcome; @tag spec: present on both.\n\n  ONE GAP WORTH FIXING (not verdict-changing, but real). MUTANT C: delete the new-in-diff component entirely, leaving a pure lexicographic sort -- Enum.min_by(authorizers, fn d -\u003e (d |\u003e meta() |\u003e meta_get(\"id\")) || \"\" end). The ENTIRE file stays GREEN: 41 tests, 2 properties, 0 failures. So the first clause of the new spec sentence at .spec/specs/append_only.spec.md:146 (\"a new-in-diff ADR shall take precedence over a pre-existing ADR\") is unpinned by the suite. Mutant C is a genuine Defect-A regression, not a no-op refactor: with pre-existing d-aaa and new-in-diff d-zzz (both change_type: weakens, both affecting x.req_a) the marker vanishes entirely (markers == []), which is exactly the original bug. I verified that same scenario passes against the shipped implementation (42 tests, 0 failures). Cheapest fix, no new test needed: in Test A rename d-old -\u003e d-aaa and d-new -\u003e d-zzz (append_only_test.exs:857, 884, 891, 897-898). That single rename kills Mutant C while still killing Mutant A, because it forces the assertion to depend on newness rather than on collation.\n\n  Minor, outside test quality: .spec/specs/append_only.spec.md:349-358 -- the new scenario specled.append_only.scenario.self_authorization_tie_break carries `covers: []`, so the tie-break rule it describes is not traced back to specled.append_only.self_authorized_weakening, unlike every sibling scenario in that block. For the verify agent; does not affect the two tests.\n\nVerdict rationale: Both new tests construct both input permutations and both turn RED under a faithful revert of the defect they target -- Test A under first-match-wins, Test B under last-write-wins -- so neither is vacuous and the order-independence claim in their names is backed by actual assertions rather than naming. The tie-break test pins the specific winning ADR id in the marker message, so it would not survive a nondeterministic selector. The one substantive gap is that Test A's ADR ids happen to collate in favour of the new ADR, leaving the new-in-diff precedence clause satisfiable by a pure lexicographic sort; that is a one-line id rename rather than a missing test, so it does not sink the verdict.",
      "timestamp": "2026-08-12T12:06:56Z"
    },
    {
      "text": "AUDIT: BOUNCE → implementer\nSession: 9a42db78-611e-4632-882a-adaefdaa666f\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/9a42db78-611e-4632-882a-adaefdaa666f.jsonl\n\nGap confirmed/refuted: CONFIRMED, both halves established personally on a `git archive HEAD` scratch copy at $SCRATCH/mut-bg51 (worktree never touched; `git status --porcelain` empty at exit). Scratch baseline: 41 tests, 2 properties, 0 failures. (i) Mutant C — replace authorizing_decision/3's min_by key `{not MapSet.member?(new_adr_ids, adr_id), adr_id || \"\"}` with `{adr_id || \"\"}` (pure lexicographic, newness dropped): suite stays GREEN, 41 tests / 2 properties / 0 failures. So the ADR's selection-rule step 2 is unpinned. (ii) Same Mutant C with Test A's fixture ids renamed so collation OPPOSES newness (pre-existing \"d-old\"→\"d-aaa\", new-in-diff \"d-new\"→\"d-zzz\"): RED — append_only_test.exs:854 dies at :897 with `assert [marker] = markers` / right: `[]`, i.e. the pre-existing ADR masks the same-diff self-authorization and no marker is emitted. That is F1 verbatim. (iii) Mutation reverted, SHIPPED selector with the same opposing ids: GREEN, 41 tests / 0 failures. (ii)+(iii) together are the proof: production prefers new-in-diff, the test net does not check that it does.\n\nTest-net-only classification: CONFIRMED — production behavior is correct. Independent of any test: authorizers is non-empty ⇒ `Enum.min_by/2` over `{not MapSet.member?(new_adr_ids, adr_id), adr_id || \"\"}` (append_only.ex:694-698). A new-in-diff ADR yields `not true` = `false`; a pre-existing one yields `not false` = `true`; Elixir orders atoms by name so `false \u003c true`, and the tuple's first element dominates — new-in-diff always wins, order-independently. Empirically corroborated by (iii) above. `new_adr_ids` is genuinely a newness signal: `new_head_adrs = new_in_diff(decisions, decision_ids(prior))` at :66-68, so base-present ADRs are excluded by construction. Semantics of the rewrite are otherwise equivalent to the old reduce_while: `{:authorized, _}` is returned iff at least one weakening-set authorizer exists in either version, and a non-weakening non-empty change_type still authorizes nothing.\n\nShared blind spot check: I re-derived (b) and (c) from scratch rather than accepting either grader's word.\n- Defect (b), intra-new tie-break: PINNED BY ID, not by mere emission. I ran an ISOLATED mutant that keeps the newness component and inverts ONLY the intra-newness collation (`for(\u003c\u003cc \u003c- adr_id || \"\"\u003e\u003e, do: 255 - c)`). Test B (append_only_test.exs:905) goes RED at :936 with the marker naming `d-z` where `d-a` was expected. So the tie-break clause is discriminated independently of the newness clause. assert_self_auth_marker/4 (:1348-1361) does both jobs: `assert [marker] = markers` (:1356) pins cardinality at exactly one per (requirement, class) and `marker.message =~ \"ADR \\`#{adr_id}\\`\"` (:1359) pins the winner. Both of Test B's ADRs are new-in-diff (prior fixture has no decisions), so it really is the same-newness case.\n- Defect (c), deletion-path unification: CONFIRMED. `self_authorized_weakening_finding/5` has exactly one call site repo-wide — append_only.ex:656, inside self_authorized_marker_if_new/6. No detector calls it directly. All four detectors call `authorizing_decision(decisions, id, new_adr_ids)` (:120, :152, :205, :244) and route `{:authorized, adr}` into self_authorized_marker_if_new/6. `self_auth_adr_by_req_id/1` is gone; `self_authorizing_adrs/2` survives only to feed detect_same_pr_self_authorization (:87, :102). Superset-affects deletion (spec :362-372) still green: `mix test test/specled_ex/append_only_test.exs:658` → 1 test, 0 failures. I also checked the deleted fast path could not have been load-bearing: it required `MapSet.subset?(affects, weakened_ids)`, which the superset scenario fails by construction, so that scenario always went through authorizing_decision anyway. The new path is a strict superset of the old fast path's emissions, so no marker is lost.\n\nSpec amendment states cardinality AND tie-break: YES, both in the `must` (append_only.spec.md:143-150): \"Exactly one marker shall be emitted per (requirement id, weakening class). When multiple ADRs authorize that pair, a new-in-diff ADR shall take precedence over a pre-existing ADR, then the lexicographically smallest ADR id shall break ties within the same newness class. The chosen ADR is independent of input decision order. Weakenings authorized only by ADRs already present at base shall emit no marker.\" The contract's stated purpose — move the cardinality rule out of a test comment and into the subject — is met. Multi-class cardinality (\"two classes ⇒ two markers\") is separately covered by the pre-existing test asserting both markers at :846-847.\n\nAdvisory notes promoted to blocking: NONE. All three are Notes, and one of them is partly mistaken.\n- `covers: []` on scenario.self_authorization_tie_break: BOTH GRADERS OVERSTATED THIS. The claim \"unlike every sibling scenario in that block\" is factually wrong — 6 of the 22 scenarios in append_only.spec.md already carry `covers: []`, including the immediately adjacent scenario.superset_affects_deletion_marker_without_warning. Validation passes and the new scenario is not an outlier. Still worth adding while the implementer is in the file, since this scenario is the one carrying the amended must's clauses, but it is not a defect.\n- `:missing_change_type` last-match-wins → first-match-wins: CONSUMER CLAIM VERIFIED. append_only.ex:134 is the ONLY site that pattern-matches `{:missing_change_type, _adr}` and it discards the decision; detect_must_downgraded (:167), detect_scenario_regression (:217) and detect_negative_removed (:255) all use a bare `_ -\u003e` catch-all, so the identity of the named decision is unobservable at every consumer. The user-visible append_only/missing_change_type findings come from the independent detect_missing_change_type/2 pass (:313). Immaterial, as the verifier said.\n- self_authorized_marker_if_new/6 re-checking new_adr_ids: NOT redundant — it is load-bearing. The selector legitimately returns a pre-existing ADR when no new one authorizes, and the re-check is precisely what makes scenario.preexisting_adr_emits_no_self_auth_findings emit nothing. Leave it.\n\nCritic calibration: TEST_CRITIC SHOULD HAVE RETURNED FAIL. The critic executed Mutant C itself, observed the entire file stay green, and correctly characterized it as \"a genuine Defect-A regression, not a no-op refactor\" that makes \"the marker vanish entirely… which is exactly the original bug\" — and then graded PASS on the reasoning that the fix is \"a one-line id rename rather than a missing test\". That conflates the CHEAPNESS OF THE REMEDY with the SEVERITY OF THE HOLE. A surviving mutant that reinstates the exact defect the ticket exists to close is the canonical fail condition for a mutation-graded test review; how many characters the fix costs is irrelevant to the verdict. The verifier's FAIL is the correctly calibrated call. Recording this as a critic calibration miss: leniency triggered by low remedy cost.\n\nBounce reason: close the test-net gap. In test/specled_ex/append_only_test.exs, test \"new authorizer wins over pre-existing authorizer regardless of decision order\" (:854), change the two ADR ids so lexicographic order OPPOSES newness — pre-existing ADR lexicographically FIRST, new-in-diff ADR lexicographically LAST (e.g. \"d-old\"→\"d-aaa\" at :861 and :877, \"d-new\"→\"d-zzz\" at :886 and the two assertions at :897-898). Verified: that rename alone kills Mutant C while the shipped implementation stays green. No production change, no new test, no spec change required. Optional while in the file: set `covers: [specled.append_only.self_authorized_weakening]` on scenario.self_authorization_tie_break (append_only.spec.md:349-358). Re-run `mix test test/specled_ex/append_only_test.exs` and `mix format --check-formatted`; the full ladder does not need re-running for a fixture-id rename.\n\nMechanical-remedy bounce count this ticket (if any): 1/2",
      "timestamp": "2026-08-12T12:15:09Z"
    },
    {
      "text": "IMPLEMENTED\nSession: i4x-bg5.1-b2\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-bg5.1/codex-i4x-bg5.1-b2/log\n\nCommit: a80b49b12fefa60cddad580153ccb174f894ee8a\nFiles touched: test/specled_ex/append_only_test.exs\nTests added: none; tightened SpecLedEx.AppendOnlyTest \\\"new authorizer wins over pre-existing authorizer regardless of decision order\\\"\nVerification stubs flipped: none\nVerification tier: change-scoped — mix test test/specled_ex/append_only_test.exs; mix format --check-formatted; mix spec.check --no-run-commands; full-system\n  proof deferred to the merge gate / PR CI\nFinal-tier runs: 1\n\nSummary: Renamed the pre-existing authorizer to d-aaa and the new-in-diff authorizer to d-zzz so lexicographic ordering opposes newness. The existing test now discriminates the required new-in-diff preference while continuing to verify both decision orders.",
      "timestamp": "2026-08-12T12:20:31Z"
    }
  ],
  "priority": 2,
  "status": "in_progress",
  "title": "F1: self-auth marker emission is weakening-class/order-dependent — design decision needed",
  "type": "task",
  "updated_at": "2026-08-12T12:20:31Z"
}
