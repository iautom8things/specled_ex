{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [],
  "closed_at": "2026-08-12T19:12:00Z",
  "created": "2026-08-12T00:36:59Z",
  "description": "Advances: specled.append_only\n\nReview-bar-override: needs a new finding code (which finding_code_budget requires be justified on its own terms), a statement-digest design, and a migration story for existing specs.\n\nFrom the critical review of specled_-n5q (finding F20, Medium, spec auditor).\n\nAn in-place rewrite of a must statement is invisible to the append-only gate: the requirement id still exists, its priority is still must, its scenarios still cover it, so none of the four weakening classes fire. The repo rule \"Do NOT weaken specs to make failing code pass\" therefore has no mechanical backstop — only review.\n\nThis branch rewrote specled.realized_by.implication_amplification_dedup in place. The spec auditor judged that particular rewrite a legitimate correction (the old text hard-coded its own implementation and guaranteed the very collapse that was the defect; the rewrite is accompanied by two strictly stronger requirements and INCREASES finding volume). But the gate did not make that judgment — a human did.\n\nDesign sketch: digest each must statement, store the digest in the append-only baseline, and emit a warning-level finding when a digest changes without the id changing. Needs a code from the budget, and a first-run migration for every existing spec.\n\n## ORCHESTRATOR CONTRACT (specled_-i4x)\n\nAdvances: specled.append_only\n\nDECISION-CLASS: yes — needs a new finding code from a governed budget. Per operator ruling on\nepic specled_-i4x, draft it fully and record completion normally; the orchestrator will NOT\nmerge without human sign-off on the budget slot.\n\nMAJOR CORRECTION THAT SHRINKS THIS TICKET — read before designing. The ticket calls for \"a\nstatement-digest design and a migration story for every existing spec.\" There is NO PERSISTED\nappend-only baseline to migrate. .spec/specs/append_only.spec.md:9-11 states the baseline is\nRECONSTRUCTED from the base-tree .spec/specs and .spec/decisions with the current parser;\nanalyze/4 (lib/specled_ex/append_only.ex:54-61) is a pure function over two in-memory state\npayloads. detect_must_downgraded/4 (:164-196) ALREADY holds both statement(prior) and\nstatement(current) for every requirement on both sides (:168-169). detect_statement_rewritten/4\nis a near-copy of that loop: whitespace-normalize both, compare, emit on difference. NO digest\nstorage, NO migration, ~50 lines. The genuinely expensive part of this ticket is the BUDGET\nJUSTIFICATION, not the mechanism.\n\nTHE BUDGET — six steps, in this order. Do not improvise around them:\n1. Author a SUPERSEDES ADR on the append-only axis, NOT an amendment.\n   .spec/decisions/specled.decision.append_only_finding_budget_v2.md:108-109 is explicit: \"Any\n   future addition to this axis must justify itself in another supersedes-style ADR.\" So create\n   .spec/decisions/specled.decision.append_only_finding_budget_v3.md with\n   change_type: supersedes, replaces: [specled.decision.append_only_finding_budget_v2], and a\n   NON-EMPTY reverses_what: — all required by specled.decisions.frontmatter_contract and\n   cross_field/supersedes_missing_replaces. Do NOT edit v2 in place: it is status: accepted, and\n   append_only/adr_affects_widened fires at :error on any change to an accepted ADR's\n   affects/change_type/reverses_what (append_only.spec.md:114-122).\n2. Reproduce the FULL ratified table with the new row (v2's table at :41-55 is the format), one\n   default severity and one entity shape. This code is :warning, entity shape = requirement id.\n   State the new count (14) using the stated counting rule.\n3. Justify on its own terms, matching the pattern\n   .spec/decisions/specled.decision.finding_code_budget.md:86-102 set for the tenth code: show\n   it is not a new emission surface but a DISTINCT FAILURE SHAPE with distinct remediation, and\n   that reusing an existing code would violate one-code / one-severity / one-entity-shape. The\n   argument is available and strong here: an in-place rewrite has a different\n   entity-vs-severity pairing than the four weakening classes (warning, not error) and different\n   remediation (review the rewrite / author a clarifies ADR, not restore prior text).\n4. Register the default severity in lib/specled_ex/branch_check.ex @per_code_defaults.\n5. Update the mirror at test/docs_identifier_lint_test.exs @append_only_codes (:76-88). The\n   mirror-drift test at :368-379 asserts set equality in BOTH directions against codes extracted\n   from @implementation_code_files (which already lists append_only.ex and branch_check.ex), so\n   the suite stays RED until this is done. It is not optional. Also confirm the code name\n   contains NO DIGITS — :381-391 rejects digit-bearing guarded codes.\n6. Document it in docs/adoption.md's code table.\n\nTHE COUNTING RULE (.spec/decisions/specled.decision.finding_code_budget.md:69-84 — this is what\n\"justified on its own terms\" refers to): \"'Tenth' counts entries on THIS ADR's Decision list,\nNOT codes emitted by the implementation. The two are not the same set.\" Current count on THIS\naxis is 13 (11 append_only/* + 2 overlap/*, v2 table at :41-55). Your code makes 14.\n\nLIVE INCONSISTENCY YOU WILL TRIP ON: append_only_finding_budget_v2:107 computes a repo-wide\ntotal of 24 using SEVEN branch-guard codes, but finding_code_budget's Decision list now carries\nTEN slots. The two ADRs disagree. Your ADR should either reconcile the total or explicitly\ndecline to restate it. Restating \"24\" unexamined is exactly the stale-total overclaim class\nspecled_-vzd.5 exists to mechanize against.\n\nNOT REQUIRED: an allow-marker. \u003c!-- spec-lint:allow-code=... --\u003e is only for codes named in ADRs\nbut NEVER emitted (docs_identifier_lint_test.exs:165-183). Your code will be emitted, so a\nmarker on it would be flagged stale by the sweep at :457-466.\n\nAllowed touches:\n- lib/specled_ex/append_only.ex\n- lib/specled_ex/branch_check.ex  (@per_code_defaults entry)\n- .spec/specs/append_only.spec.md\n- .spec/decisions/specled.decision.append_only_finding_budget_v3.md  (NEW, supersedes v2)\n- .spec/decisions/specled.decision.finding_code_budget.md  (amendment only, if you reconcile the total)\n- test/specled_ex/append_only_test.exs\n- test_support/append_only_fixtures.ex\n- test/docs_identifier_lint_test.exs  (MANDATORY, step 5)\n- docs/adoption.md  (code table row)\n- .spec/specs/package.spec.md  (if the adoption.md table is its surface claim)\n\nOut of Scope (files):\n- lib/specled_ex/realization/**\n- lib/specled_ex/overlap.ex\n- lib/specled_ex/modal_class.ex\n\nOut of Scope (intent):\n- Do NOT build a persisted statement-digest baseline. See the correction above.\n- Do not make the finding :error. v2 assigns one severity per code; an in-place rewrite is not\n  one of the four weakening classes and must not gate. :warning.\n- Do not extend detection to scenarios, ADR bodies, or summary: prose.\n- Do not touch ModalClass — the modal-downgrade path already covers the strength axis.\n\nVerification:\n  mix compile --warnings-as-errors\n  mix test test/specled_ex/append_only_test.exs test/docs_identifier_lint_test.exs\n  mix test --only spec:specled.append_only.statement_rewritten   # your new requirement id\n  mix test --only spec:specled.package.doc_identifier_integrity\n  mix format --check-formatted\n  make xref\n  bash ./scripts/check_specs.sh\nNO stub flip: both spec-verification blocks in append_only.spec.md (:418-452) are already\nexecute: true. Your new requirement id must be added to BOTH the tagged_tests covers: list AND\nthe source_file block's covers: list.\n\nExpected diff: 8-10 files, ~250 lines.\n",
  "id": "specled_-vzd.3",
  "labels": [],
  "parent": "specled_-vzd",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: i4x-vzd.3\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-vzd.3/codex-i4x-vzd.3/log\n\nCommit: 20f340e14bb8363f89411944c08483e2d51eeadc\nFiles touched: .spec/decisions/specled.decision.append_only_finding_budget_v3.md, .spec/specs/append_only.spec.md, .spec/specs/branch_guard.spec.md, .spec/specs/implementation_tier.spec.md, .spec/specs/package.spec.md, docs/adoption.md, lib/specled_ex/append_only.ex, lib/specled_ex/branch_check.ex, test/docs_identifier_lint_test.exs, test/specled_ex/append_only_test.exs\nTests added: SpecLedEx.AppendOnlyTest — \"semantic rewrite of a must-priority statement emits a warning\"; \"whitespace-only statement reflow is ignored\"; \"a statement rewrite is ignored when either side is not must priority\"\nVerification stubs flipped: none (both append-only verification blocks were already execute: true)\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/append_only_test.exs test/docs_identifier_lint_test.exs; mix test --only spec:specled.append_only.statement_rewritten; mix test --only spec:specled.package.doc_identifier_integrity; mix format --check-formatted; make xref; mix spec.check --no-run-commands; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 2\n\nSummary: Added whitespace-normalized detection for in-place must-priority statement rewrites, emitting the governed warning-level append_only/statement_rewritten finding while leaving reflow and non-must changes silent. Added behavioral and identifier-integrity proof, documented the code, and superseded the append-only finding budget with a fourteen-entry ADR that explicitly requires no persisted digest migration.",
      "timestamp": "2026-08-12T12:49:15Z"
    },
    {
      "text": "VERIFY: PASS\nSession: 9a42db78-611e-4632-882a-adaefdaa666f\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/9a42db78-611e-4632-882a-adaefdaa666f.jsonl\n\nMechanical checks:\n- Files touched: 10 (+259/-18), single commit 20f340e1 on base bb64e69e (== origin/main, verified)\n- Outside Allowed globs: .spec/specs/branch_guard.spec.md, .spec/specs/implementation_tier.spec.md\n  - branch_guard.spec.md — LEGITIMATE gate-forced co-change. lib/specled_ex/branch_check.ex is on branch_guard's surface: list (branch_guard.spec.md surface line 2). BranchCheck.run's file-touch loop (branch_check.ex:71-101) emits branch_guard_missing_subject_update at :error (per_code_defaults:17) for every impacted subject whose spec file is not also changed. Touching branch_check.ex therefore FORCES a branch_guard.spec.md edit. The edit is confined to the \"Branch reconciliation note:\" prose in ## Verification, replacing a stale note left by a prior branch. No requirement, statement, priority, or spec-verification block touched.\n  - implementation_tier.spec.md — LEGITIMATE, same mechanism. branch_check.ex is line 1 of implementation_tier's surface: list. Edit is the same reconciliation-note prose, \"eleventh\" -\u003e \"twelfth\". No must changed. The count is accurate: grep -c '\"append_only/' lib/specled_ex/branch_check.ex == 12.\n  - Corroboration: spec.check reports branch impacted_subjects = specled.append_only, specled.branch_guard, specled.implementation_tier, specled.package — exactly the four spec files edited (package.spec.md is in Allowed). Neither out-of-Allowed edit changes a must; both are surface/bookkeeping. Not scope creep.\n- Out-of-Scope globs matched: 0 (lib/specled_ex/realization/**, lib/specled_ex/overlap.ex, lib/specled_ex/modal_class.ex all untouched)\n\nBudget procedure (six steps):\n- (a) supersedes ADR with replaces + non-empty reverses_what: CONFIRMED. New file .spec/decisions/specled.decision.append_only_finding_budget_v3.md carries change_type: supersedes, replaces: [specled.decision.append_only_finding_budget_v2], and a 4-line non-empty reverses_what. affects: [specled.append_only, specled.overlap] matches v2's exactly (no widening). Not an amendment.\n- (b) v2 untouched: CONFIRMED. `git diff bb64e69e..HEAD --name-only | grep -c budget_v2` == 0. Only one file under .spec/decisions/ in the diff (+102/-0, the new v3). No adr_affects_widened exposure.\n- (c) full table reproduced, count 14 by the stated counting rule: CONFIRMED. v3's table is v2's 13 rows verbatim — every severity and entity shape byte-identical — with `append_only/statement_rewritten | :warning | requirement id` inserted after must_downgraded. Title \"Fourteen Ratified Codes\"; section header \"Ratified codes (12 append_only + 2 overlap)\". Derived by the stated rule, not by counting emissions: \"The counting rule from specled.decision.finding_code_budget applies here: 'fourteenth' counts entries in this ADR's ratified table, not codes emitted across the repository.\" (Minor wording drift from finding_code_budget:69 \"Decision list\" -\u003e \"ratified table\"; the table sits under ## Decision, so equivalent.)\n- (d) distinct-failure-shape justification argued, not just asserted: ARGUED, adequately but thinner than the finding_code_budget:86-102 exemplar. It names the remediation contrast concretely (four weakening codes: restore prior strength or authorize; this code: review the rewrite / record clarification rationale) and states the severity rationale (prose may clarify or strengthen, so must not gate). It also makes the required reuse argument explicitly. One weak leg: it lists \"one-entity-shape\" among the disciplines reuse would violate, but the entity shape is IDENTICAL to must_downgraded (requirement id) — its own table and its own sentence (\"keyed to the requirement id\") concede this. Only one-severity and remediation actually distinguish. Small overclaim, advisory.\n- (e) @per_code_defaults registers :warning (not :error): CONFIRMED. branch_check.ex:31 `\"append_only/statement_rewritten\" =\u003e :warning`, and the block comment updated 11 -\u003e 12 ratified codes. Executed finding shape confirms severity: :warning at runtime.\n- (f) mirror updated, no digits, mirror-drift green: CONFIRMED and FALSIFIED-AGAINST. Code name \"append_only/statement_rewritten\" contains no digits (the :381-391 guard). Added to @append_only_codes at docs_identifier_lint_test.exs:79. Falsification: deleting that one line reddens 2 tests — \"the hand-maintained known-code mirror matches implementation anchors both ways\" (:368) and \"every finding-code-shaped token in docs and specs references a real implementation code\" (:234). Restored; tree clean. The check is live, not vacuous.\n\nThe shrink:\n- No persisted digest store or migration introduced: CONFIRMED. detect_statement_rewritten/2 (append_only.ex:208-224) is a 17-line pure near-copy of the detect_must_downgraded loop: Enum.flat_map over prior_reqs, Map.fetch on current_reqs, compare in memory. No new state key, no baseline file, no first-run migration path, no writes. v3 states it directly: \"No digest or migration state is persisted.\" Total lib delta is +45/-2 across two files.\n\nDetector behaviour (by execution — probed via a scratch script outside the repo driving AppendOnly.analyze/3 on hand-built state payloads; tree left clean):\n- semantic must rewrite -\u003e one warning: CONFIRMED. \"The system stores audit events.\" -\u003e \"The system discards audit events.\" yields exactly [{\"append_only/statement_rewritten\", :warning}], entity_id x.req_a, subject_id x, fix-block present.\n- whitespace reflow -\u003e silent, and normalizer is whitespace-only: CONFIRMED, and probed for over-eagerness. \"  The system\\n\\tstores   audit\\n  events.  \" -\u003e []. But punctuation-only change (trailing \".\" dropped) FIRES; case-only change (\"The System Stores Audit Events.\") FIRES; same-words-reordered FIRES; U+00A0 and U+200B substitutions FIRE (String.split/1 does not split them — verified directly). The normalizer is String.split/1 |\u003e Enum.join(\" \") and collapses nothing but standard whitespace runs. It errs toward firing, never toward silencing. No over-eager-normalizer failure mode.\n- non-must either side -\u003e silent: CONFIRMED for must-\u003eshould, should-\u003emust, and should-\u003eshould (all []). Note: priority is read as a literal field (must_priority?/1 == Map.get(\"priority\") == \"must\"), so a requirement with NO priority key is also silent — not a live evasion here, all 408 corpus requirements carry an explicit priority.\n- no fire on add/delete: CONFIRMED. New id in current only -\u003e []. Id removed -\u003e [{\"append_only/requirement_deleted\", :error}] only.\n- no double-report with must_downgraded: NOT CONFIRMED — see Gaps. It does double-report when the statement's modal verb weakens while the priority: field stays must.\n\nSelf-check:\n- append_only findings against this diff: NONE. `mix spec.check --no-run-commands` -\u003e validate status=pass errors=0 warnings=0; branch changed_files=10 findings=0 (error=0 warning=0 info=0); spec.check result=pass. The new detector does not fire on its own spec edits because it did not rewrite any existing must statement: the append_only.spec.md diff is additive only (one new requirement, one new scenario, two covers:-list appends) plus non-requirement bookkeeping (header prose, summary:, and decisions: v2 -\u003e v3). Neither an alternate edit nor an authorizing ADR was needed — the ticket simply did not rewrite anything.\n\nStale-total trap:\n- v3 DECLINED TO RESTATE, explicitly and with the reason named. It carries no \"Cap revision\" section and no repo-wide arithmetic. Verbatim: \"This ADR deliberately does not restate a repo-wide total. The v2 total used seven branch-guard codes while the branch-guard ADR's Decision list now carries ten slots, so repeating or arithmetically extending that total would preserve a known inconsistency rather than reconcile it.\" That is the contract's second option, taken cleanly. No stale-total overclaim introduced. Not self-refuting against sibling specled_-vzd.5.\n\nTargeted verification (all from the stage worktree, host, no Docker):\n- mix compile --warnings-as-errors -\u003e exit 0\n- mix test test/specled_ex/append_only_test.exs test/docs_identifier_lint_test.exs -\u003e 2 properties, 61 tests, 0 failures\n- mix test --only spec:specled.append_only.statement_rewritten -\u003e 1 test, 0 failures (tag resolves; requirement id is live in both the tagged_tests and source_file covers: lists)\n- mix format --check-formatted -\u003e clean\n- make xref -\u003e exit 0 (MIX_ENV=test compile-connected --fail-above 0)\n- mix spec.check --no-run-commands -\u003e spec.check result=pass, findings=0\n- git status --porcelain -\u003e empty after every probe\nFull-suite verification: not run by verifier (by design)\n\nVerdict: PASS\n\nGaps (non-blocking; nothing the ticket CLAIMS is false, but the team lead should rule on the first):\n- DOUBLE-REPORT WITH must_downgraded, confirmed by execution. must_downgraded keys off ModalClass.classify(statement), NOT off the priority: field. So a statement edited \"MUST reject\" -\u003e \"SHOULD reject\" while priority: stays must emits BOTH [{\"append_only/must_downgraded\", :error}, {\"append_only/statement_rewritten\", :warning}] on the same entity. Only when the priority: field is ALSO downgraded does the rewrite code fall silent (then just must_downgraded). The visible cost is already in the diff: the pre-existing must_downgraded test at append_only_test.exs:130 had to be loosened from `assert [finding] = AppendOnly.analyze(...)` to an Enum.find over the list, so that test no longer pins the exact finding set for a modal downgrade. This is redundant noise on the highest-signal path and sits outside v3's own framing of the code (\"none [of the existing detectors] distinguishes\" the stable-id rewrite case). The suppression plumbing already exists and was not used: detect_must_downgraded/4 returns {findings, consulted_ids} and detect_statement_rewritten/2 is called without that set (append_only.ex:72-75). A one-line MapSet.member? guard would fix it. NOT a spec violation — the new requirement statement, the ADR, and the implementer's report all stay silent on overlap, so nothing is overclaimed.\n- statement present -\u003e statement key absent fires statement_rewritten (statement/1 coerces missing to \"\"). Not reachable through validate, which requires a statement. Cosmetic.\n\nNotes (advisory only):\n- v3's \"one-entity-shape\" claim in the reuse argument does not hold on its own terms; entity shape is requirement id for both this code and must_downgraded. Severity and remediation carry the argument. Worth a one-word softening if v3 is revised for other reasons, not worth a revision by itself.\n- The two out-of-Allowed spec edits are the branch-guard surface-owner mechanism working as designed. If the orchestrator contracts for this epic keep listing lib/specled_ex/branch_check.ex as an Allowed touch, they should also pre-list branch_guard.spec.md and implementation_tier.spec.md, or every such ticket will land \"outside Allowed\" for a gate-mandated edit.\n- DECISION-CLASS ticket: v3 occupies a governed budget slot. Per the contract this still needs human sign-off before merge; this verification does not substitute for it.",
      "timestamp": "2026-08-12T12:57:18Z"
    },
    {
      "text": "TEST_CRITIC: PASS\nSession: 9a42db78-611e-4632-882a-adaefdaa666f\nLog: /Users/mz/.claude-personal/projects/-Users-mz-src-specled-ex/9a42db78-611e-4632-882a-adaefdaa666f.jsonl\n\nGraded: HEAD 20f340e1 on base bb64e69e (epic specled_-i4x). All mutants run on a scratch rsync copy outside the repo; worktree `git status --porcelain` clean at exit.\n\n- Each test kills its own mutant (baseline 42 tests + 2 properties green):\n  detector deleted (statement_rewrite_findings = []): RED, and only \"semantic rewrite of a\n  must-priority statement emits a warning\" failed.\n  normalization skipped (normalized_statement/1 -\u003e statement/1 raw): RED, and only\n  \"whitespace-only statement reflow is ignored\" failed.\n  must-guard dropped (both must_priority?/1 clauses removed): RED, and only \"a statement rewrite\n  is ignored when either side is not must priority\" failed.\n  All three are load-bearing; each mutant killed exactly one test, no collateral.\n\n- Normalizer over-eagerness probe (24 statement pairs driven through AppendOnly.analyze/3, all\n  must-priority on both sides). DETECTED: word change; period-\u003eexclamation; trailing period\n  removed; comma added; initial-letter case; word case; added trailing clause; negation added\n  (\"does not store\"); markdown emphasis added (**audit**); emphasis style swap (*x* -\u003e _x_);\n  code span added (`audit`); digit change (30 -\u003e 3 days); NBSP U+00A0 substituted for a space;\n  ZWSP U+200B inserted; WORD JOINER U+2060 inserted; MONGOLIAN VOWEL SEPARATOR U+180E;\n  NFC vs NFD accented \"café\"; ASCII hyphen -\u003e non-breaking hyphen U+2011; space deleted so two\n  words merge. SWALLOWED: repeated ASCII spaces; newline reflow; tab; leading/trailing padding;\n  EN SPACE U+2002 substituted for a space.\n  THE NORMALIZER SWALLOWS NOTHING THAT IS NOT WHITESPACE. String.split/1 + Enum.join(\" \")\n  collapses only runs of the Unicode whitespace String.split/1 recognises. Every semantic,\n  punctuation, case, markdown, and encoding change probed is still detected. No hole.\n  (The residual risk is the opposite direction — NFC/NFD and NBSP-for-space are visually\n  identical yet DETECTED, so an editor that rewrites those emits an advisory warning. That is\n  noise on a :warning code, not a silencing hole, and I am not blocking on it.)\n\n- Exactly one warning, no double-report with must_downgraded: one plain rewrite yields exactly\n  one finding; two rewritten requirements yield exactly two, correctly sorted. BUT a MUST-\u003eSHOULD\n  prose edit whose `priority:` field stays \"must\" emits BOTH append_only/must_downgraded and\n  append_only/statement_rewritten. That is spec-consistent (must_downgraded keys off the modal in\n  the statement text via ModalClass; statement_rewritten keys off the priority field, so a modal\n  prose edit is genuinely both), and the ADR's comparison rule permits it. It is not pinned by\n  any test — worse, this diff RELAXED the pre-existing pin: the must_downgraded test at\n  append_only_test.exs:113 changed from `assert [finding] = AppendOnly.analyze(...)` (which\n  asserted must_downgraded was the ONLY finding) to an Enum.find on the code, and nothing\n  replaced the total-count assertion. Non-blocking, but the file lost a real constraint.\n\n- Silent on requirement add/delete, pinned by a test: silent on both, verified by probe —\n  addition yields [] (the detector folds over prior_reqs only); deletion yields exactly\n  [\"append_only/requirement_deleted\"]; authorized deletion yields []. NO TEST PINS EITHER. Gap.\n\n- Fixture hygiene: clean. All three new tests build independent state_fixture/1 maps via the\n  shared SpecLedEx.AppendOnlyFixtures builders; no module attributes, ETS, Application env, or\n  processes anywhere in the file, so async: true is sound and the reused \"x\" / \"x.req_a\" ids\n  cannot collide across the 44 tests. The new code is also swept by the existing\n  fix_block_discipline test (its combined fixture rewrites x.req_a, so statement_rewritten now\n  flows through that all-findings fix-block loop) and by the findings-sorted test.\n  Two design nits: (a) the third test asserts `[] == analyze(...)` rather than \"no\n  statement_rewritten finding\" — it pins GLOBAL silence on a must-\u003eshould priority-field\n  downgrade that no detector currently catches, so it will break for the wrong reason if\n  priority-field downgrade detection is ever added; (b) only the first of the three new tests\n  carries `@tag spec:`, though the @moduletag covers the subject so tagged_tests verification\n  still runs all three.\n\n- Sweep: no Process.sleep, no timing, no processes, no I/O — pure function under test. No vacuous\n  assertions; the positive test pins code, severity, subject_id, entity_id, and fix-block\n  presence. Tests assert outcomes (finding shape) not internals. One message finding: the fix\n  block says \"document the rationale in a clarifies ADR affecting `x.req_a`\", but\n  detect_statement_rewritten/2 never consults decisions — I confirmed by execution that adding a\n  matching clarifies ADR to both state and head decisions leaves the warning in place. The ADR's\n  Consequences do say resolution is by reviewer judgment, so this is remediation wording that\n  overpromises, not a behaviour bug. Worth a follow-up.\n\nVerdict rationale: the headline question resolves cleanly in the deliverable's favour — across\n24 probed pairs the normalizer swallows only genuine whitespace runs and detects every\npunctuation, case, wording, clause, markdown, digit, and encoding change, so there is no silenced\nreal rewrite. All three shipped tests are load-bearing under their own killing mutants, with no\ncollateral kills. The remaining items are unpinned-behaviour gaps (add/delete silence, total\nfinding count) and one overpromising fix-block string, none of which lets a must-priority rewrite\npass unreported.",
      "timestamp": "2026-08-12T13:06:35Z"
    },
    {
      "text": "GATE MET — HELD FOR OPERATOR SIGN-OFF. Not merged.\n\nNo AUDIT: APPROVE marker posted, for the same protocol reason recorded on specled_-bg5.1: that\ntoken instructs a fast-forward merge, and this ticket needs a human decision first. The trail ends\nat VERIFY: PASS + TEST_CRITIC: PASS, which the resume table maps back to the Selective Audit Gate.\n\nSelective Audit Gate conditions, all met: VERIFY: PASS with no Gaps, zero Out-of-Scope, stubs run\n== passed, TEST_CRITIC: PASS, complete seam disclosure, no seam substituting production wiring.\n\nWHAT THE OPERATOR IS SIGNING OFF ON: a new finding code. specled.decision.append_only_finding_budget_v3\nsupersedes v2 and takes the append-only axis from 13 ratified codes to 14. v2 is explicit that any\naddition must justify itself in a supersedes-style ADR, and that procedure was followed in full:\nsupersedes change_type with replaces + non-empty reverses_what, v2 left untouched (it is\nstatus: accepted, and editing an accepted ADR's frontmatter fires adr_affects_widened at :error),\nthe full ratified table reproduced with the new row, one severity and one entity shape, and the\ncount stated by the ADR's own counting rule (entries on the Decision list, not emissions).\n\nTHE STALE-TOTAL TRAP WAS HANDLED CORRECTLY, which matters because getting it wrong would have been\nself-refuting. v2:107 computes a repo-wide total of 24 using SEVEN branch-guard codes while\nfinding_code_budget's Decision list now carries TEN slots — the two ADRs disagree. v3 explicitly\nDECLINED to restate the total and said why: \"repeating or arithmetically extending that total would\npreserve a known inconsistency rather than reconcile it.\" Restating \"24\" unexamined is exactly the\noverclaim class its sibling specled_-vzd.5 exists to mechanize against.\n\nTHE TICKET ALSO SHRANK BY AN ORDER OF MAGNITUDE, correctly. Its original body demanded \"a\nstatement-digest design and a migration story for every existing spec\". There is no persisted\nappend-only baseline to migrate — it is reconstructed per run from the base tree — and\ndetect_must_downgraded/4 already holds both prior and current statement text. The detector is ~45\nlines with no digest store and no migration.\n\nNORMALIZER PROBE, the finding I most wanted: the detector whitespace-normalizes before comparing,\nand an OVER-EAGER normalizer would be worse than the original hole — the gate would report clean\nwhile a must was quietly reworded. The critic drove 24 statement pairs through analyze/3.\nDETECTED: word change, period-\u003eexclamation, trailing period removed, comma added, initial-letter\ncase, word case, added clause, negation added, markdown emphasis added, emphasis style swap, code\nspan added, digit change, NBSP U+00A0, ZWSP U+200B, WORD JOINER U+2060, MONGOLIAN VOWEL SEPARATOR,\nNFC vs NFD, ASCII hyphen -\u003e U+2011, and a deleted space merging two words.\nSWALLOWED: only repeated ASCII spaces, newline reflow, tab, leading/trailing padding, EN SPACE.\nNothing that is not whitespace is swallowed. The residual runs the SAFE direction — visually\nidentical NBSP/NFD changes are detected and emit advisory noise on a :warning code.\n\nTWO SCOPE ADJUDICATIONS the operator should know about: .spec/specs/branch_guard.spec.md and\n.spec/specs/implementation_tier.spec.md are outside this ticket's Allowed list but were ruled\nLEGITIMATE gate-forced co-change. lib/specled_ex/branch_check.ex sits on both subjects' surface\nlists, and touching it makes branch_guard_missing_subject_update fire at :error unless the spec\nfiles change too. Both edits are confined to reconciliation-note prose; no requirement, statement,\npriority, or verification block was touched.",
      "timestamp": "2026-08-12T13:10:34Z"
    },
    {
      "text": "MERGE_CONFLICT\nRebase onto the epic branch (9f45f3d0) aborted. Branch untouched at 20f340e1, worktree clean.\n\nThis is the cross-branch interaction recorded on the epic before either ticket was implemented, and\nit landed exactly as predicted. specled_-bg5.6 merged first and REWIRED the mechanism this ticket\nextends. Six files conflict, but the textual conflicts are not the important part.\n\nTHE SUBSTANTIVE GAP — this is what makes it a bounce rather than a rebase.\n\nvzd.3's new emitter is a PLAIN MAP LITERAL, not a `finding/2` macro call\n(lib/specled_ex/append_only.ex:436-448):\n\n    defp statement_rewritten_finding(id, prior_req) do\n      %{code: \"append_only/statement_rewritten\", severity: :warning, ...}\n    end\n\nbg5.6's `finding_codes/0` derives its catalog exclusively from codes registered via\n`Module.put_attribute` inside `defmacrop finding(code, fields)`. A raw map never registers. So even\nif every textual conflict were resolved perfectly, `append_only/statement_rewritten` would be\nABSENT from the derived catalog — the fourteenth code would not actually join the thing this ticket\nspent an entire supersedes ADR budgeting for.\n\nIt fails closed, which is the design working: bg5.6's restored source-text witness scans\nappend_only.ex for literal code strings, so the code appears on the implementation side and is\nmissing from the derived side, and the mirror-drift test goes RED. Loud, not silent.\n\nWHAT MUST CHANGE (implementer, not rebase worker):\n\n1. Convert `statement_rewritten_finding/2` to call `finding(\"append_only/statement_rewritten\", %{...})`\n   rather than building a raw map. This is the load-bearing fix. Note bg5.6 also added an\n   `is_binary(code)` compile-time guard, so the code must be a literal.\n2. DELETE the `@append_only_codes` literal-list edit in test/docs_identifier_lint_test.exs entirely.\n   That list is now `SpecLedEx.AppendOnly.finding_codes() |\u003e Enum.sort()`. Your addition to it is\n   obsolete — once (1) is done the code arrives automatically. Do not re-add a hand-written list.\n3. Merge the `# covers:` comment lists in append_only.ex:2-6 and append_only_test.exs:2-6 — keep\n   BOTH sides' subjects (bg5.6's `accepted_adr_authorization` + `finding_codes`, and your\n   `statement_rewritten`). Bodies already merge clean; only these comments conflict.\n4. Merge .spec/specs/append_only.spec.md: the `summary:` field (both descriptions are true) and the\n   `decisions:` list. On the decisions list, verify the budget chain deliberately: your\n   append_only_finding_budget_v3 declares `replaces: [v2]`, which is still correct — the other two\n   entries HEAD carries (uniform_self_authorization_markers, accepted_adr_authorization) are\n   `clarifies` ADRs on unrelated axes and do not participate in the budget lineage. State that\n   reasoning in your completion record rather than assuming it.\n5. Merge the \"Branch reconciliation note\" paragraphs in branch_guard.spec.md and package.spec.md —\n   KEEP BOTH, consecutively. This is now the fourth note-slot collision in this epic; the\n   established resolution is restore-alongside, never choose.\n6. CORRECT your package.spec.md note. It currently claims this ticket \"adds\n   `append_only/statement_rewritten` to the adoption guide's code table and the identifier-lint\n   implementation mirror\". The second half is now FALSE — bg5.6 replaced that mirror with\n   derivation. A reconciliation note asserting something untrue is the exact defect class\n   specled_-vzd.6 exists to eliminate.\n\nClean and untouched by the conflict: docs/adoption.md, lib/specled_ex/branch_check.ex,\n.spec/specs/implementation_tier.spec.md, and .spec/realization_hashes.json (neither side edits it).\n\nBounce budget: this ticket has used 0 bounces. This consumes the first.",
      "timestamp": "2026-08-12T18:54:22Z"
    },
    {
      "text": "IMPLEMENTED\nSession: i4x-vzd.3-b2\nLog: /Users/mz/.local/state/agentic-runs/specled_ex/specled_-vzd.3/codex-i4x-vzd.3-b2/log\n\nCommit: 444e088122c19dec957db7f134e9e5a83c1973fe\nFiles touched: .spec/decisions/specled.decision.append_only_finding_budget_v3.md, .spec/specs/append_only.spec.md, .spec/specs/branch_guard.spec.md, .spec/specs/implementation_tier.spec.md, .spec/specs/package.spec.md, docs/adoption.md, lib/specled_ex/append_only.ex, lib/specled_ex/branch_check.ex, lib/specled_ex/review/html.ex, test/specled_ex/append_only_test.exs, test/specled_ex/branch_check/severity_integration_test.exs, test/specled_ex/review/html_test.exs\nTests added: SpecLedEx.AppendOnlyTest — \"semantic rewrite of a must-priority statement emits a warning\"; \"whitespace-only statement reflow is ignored\"; \"a statement rewrite is ignored when either side is not must priority\"\nVerification stubs flipped: none (both append-only verification blocks were already execute: true)\nVerification tier: change-scoped — mix compile --warnings-as-errors; mix test test/specled_ex/append_only_test.exs test/docs_identifier_lint_test.exs test/specled_ex/review/html_test.exs test/specled_ex/branch_check/severity_integration_test.exs; MIX_ENV=test make xref; mix spec.check --no-run-commands; full-system proof deferred to the merge gate / PR CI\nFinal-tier runs: 2\n\nCatalog proof: SpecLedEx.AppendOnly.finding_codes/0 returned base_count=11 current_count=12 delta=1 contains_statement_rewritten=true, with append_only/statement_rewritten present in the printed MapSet.\nDerived mirror: test/docs_identifier_lint_test.exs keeps `@append_only_codes SpecLedEx.AppendOnly.finding_codes() |\u003e Enum.sort()` and carries no hand-written append-only list.\nBudget chain: append_only_finding_budget_v3 remains `replaces: [specled.decision.append_only_finding_budget_v2]`. Both HEAD additions, uniform_self_authorization_markers and accepted_adr_authorization, declare `change_type: clarifies`, affect only specled.append_only, and do not participate in the supersedes finding-budget lineage.\nReconciliation notes: both HEAD's and this ticket's Branch reconciliation note paragraphs survive consecutively, HEAD first, in both .spec/specs/branch_guard.spec.md and .spec/specs/package.spec.md. The package note now accurately says the emitter reaches the compiler-exported catalog through SpecLedEx.AppendOnly.finding_codes/0.\n\nSummary: Rebased the statement-rewrite detector onto 9f45f3d0, converted its finding emitter to the literal finding/2 macro form, and reconciled bg5.6's derived catalog and independent downstream mirrors without altering its derivation machinery. The new warning is present in the 12-member catalog, documented, severity-governed, and covered by the targeted detector, identifier-lint, HTML review, and severity-integration tests.",
      "timestamp": "2026-08-12T19:02:20Z"
    },
    {
      "text": "APPROVED. Merged into specled_-i4x/fast-follow-backlog. Closing per operator sign-off.\n\nMerged as 80efa9bd. Full merge gate GREEN: compile clean, format clean, deps.unlock clean,\nMIX_ENV=test make xref at zero compile-connected edges, mix test 1350 tests / 3 properties /\n0 failures, bash ./scripts/check_specs.sh -\u003e validate status=pass errors=0 warnings=0;\nspec.check result=pass.\n\nTHIS TICKET WOULD HAVE SHIPPED A NO-OP WITHOUT THE ORDERING. It required one bounce and two\nrebases, and the reason is worth recording.\n\nspecled_-bg5.6 merged first and replaced the hand-maintained append-only catalog with\nSpecLedEx.AppendOnly.finding_codes/0, derived at compile time from codes registered by\n`defmacrop finding/2`. This ticket built its new finding as a PLAIN MAP LITERAL:\n\n    %{code: \"append_only/statement_rewritten\", severity: :warning, ...}\n\nA raw map never registers with the macro's accumulator. So even after a flawless textual merge, the\nfourteenth code — the one this ticket spent an entire supersedes ADR budgeting for — would have been\nABSENT from the catalog it exists to join. The rebase worker was asked specifically to report which\nform the emitter took, and that single fact is what turned a six-file textual merge into a real fix.\n\nIt failed closed rather than silently, which is bg5.6's design working: the restored source-text\nwitness scans append_only.ex for literal code strings, so the code appeared on the implementation\nside and was missing from the derived side, and the mirror-drift test would have gone RED.\n\nResolution verified independently after rebase:\n  finding_codes/0 -\u003e count=12 has=true\n  @append_only_codes -\u003e still `SpecLedEx.AppendOnly.finding_codes() |\u003e Enum.sort()`, no\n    hand-written list crept back\n  both Branch reconciliation notes survive consecutively in branch_guard.spec.md and package.spec.md\n  the package note no longer claims this ticket extends an \"implementation mirror\" — that clause\n    became FALSE when bg5.6 replaced the mirror with derivation, and shipping it would have been the\n    exact defect its sibling specled_-vzd.6 exists to eliminate\n\nTHE DIFF GREW FROM 10 FILES TO 12, and that is the gate working rather than scope creep.\nbg5.6 deliberately kept two catalogs hand-maintained and fail-loudly — branch_check.ex's\n@per_code_defaults and review/html.ex's governance-leg codes. A fourteenth code makes both go red\nuntil updated, so this ticket had to update them. bg5.6's design caught a real omission one ticket\nlater, which is precisely what it was sharpened to do.\n\nBudget chain verified rather than assumed: append_only_finding_budget_v3 keeps\n`replaces: [append_only_finding_budget_v2]`. The two entries HEAD added to the subject's decisions\nlist — uniform_self_authorization_markers and accepted_adr_authorization — both carry\n`change_type: clarifies` and do not participate in the supersedes budget lineage.\n\nNOTE FOR THE PR: spec.check now reports 8 `append_only/statement_rewritten` WARNING findings against\norigin/main. Those are this new detector firing on THIS EPIC's own in-place must-statement rewrites\n— bg5.1, bg5.4, bg5.5, bg5.6, vbo, vzd.1, vzd.2 and this ticket each amended a must in place. Every\none is ADR-backed and was reviewed by the operator. The detector is warning-severity by design and\ndoes not gate; error=0. It found eight real instances of its target class in the very branch that\nintroduces it.",
      "timestamp": "2026-08-12T19:11:53Z"
    }
  ],
  "priority": 3,
  "status": "closed",
  "title": "No append-only detector for in-place must statement rewrites",
  "type": "task",
  "updated_at": "2026-08-12T19:12:00Z"
}
